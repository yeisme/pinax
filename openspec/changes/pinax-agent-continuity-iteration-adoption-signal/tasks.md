## 0. 决策门禁与范围冻结

- [x] 0.1 Owner: product-analysis；Scope: continuity cohort/follow-up redacted evidence；Dependencies: none；Lane: gate；用 CLI 生成 participant-level、task-level、安全、复用、来源、提案与商业信号分析；Verification: `go run ./internal/testkit/continuitydogfooddecision --cohort-run temp/continuity-dogfood-runs/20260728T024122Z-3496487 --followup-run temp/continuity-dogfood-followups/20260810T014149Z-2988511`；Expected: 生成完整 evidence 目录且不含正文、绝对路径、secret 或 provider payload；Failure re-check: 校验 cohort/follow-up sample ID、digest、观察窗口和分母。Evidence: `temp/continuity-dogfood-decisions/20260810T021721Z-3240325/`，task completion/reuse/continuation/proposal acceptance 均 10/10，source resolvability 20/20，silent promotion 0/10，WTP=`not_measured`。
- [x] 0.2 Owner: CEO/product；Scope: decision receipt；Dependencies: 0.1；Lane: gate；作出唯一 Go/Iterate/Stop 决策并限制最多两个失败类别；Verification: 人工审阅 `artifacts/decision.json`；Expected: 不以单一 operator 的任务级成功代替多用户或商业证据；Failure re-check: 缺少分母或偏差时保持 experimental。Evidence: decision=`iterate`，failure classes=`external_validity,commercial_signal`，allowed scope 仅为个人 action-capture canary 与 Pinax correctness/onboarding/retrieval/review/MCP compatibility 修复。

## 1. Pinax MCP 兼容与只读边界

- [x] 1.1 Owner: mcp-compatibility；Scope: `internal/mcpserver/server.go`；Dependencies: 0.2；Lane: A；增加标准 initialize、notification、`result.tools`、`inputSchema`、tool result content/structuredContent、整数错误码与 request ID `0` 支持，同时保留旧顶层 projection；Verification: `go test ./internal/mcpserver -count=1`；Expected: 标准与旧 consumer 均通过，未知写工具返回 `-32001` + `legacy_code=approval_required`；Failure re-check: 分别检查 JSON-RPC frame、standard SDK decode 和 legacy fields。Evidence: 完整 `internal/mcpserver` package tests PASS；新增标准握手测试覆盖 ID 0、initialized notification、camelCase schema、standard tool result 和旧 projection。
- [x] 1.2 Owner: mcp-compatibility；Scope: Agent MCP 参数默认值；Dependencies: 1.1；Lane: A；为三个 Agent 只读工具公开最小 schema，并把缺省 workspace 解析为 `default`；Verification: Python MCP SDK 调用 `pinax.agent.context` 空 arguments；Expected: `isError=false` 且返回 bounded structured content；Failure re-check: 检查 schema default 与 service scope validation。Evidence: Hermes 安装环境内的 Python MCP SDK 返回 `context_is_error=False`、一个 content item 和 bounded structured keys；缺省 workspace 成功解析为 `default`。
- [x] 1.3 Owner: compatibility；Scope: 真实 Hermes MCP client；Dependencies: 1.1-1.2；Lane: A；验证 Hermes 可连接并发现工具，写工具仍被 Pinax 拒绝；Verification: `hermes mcp test pinax` 加 Python MCP SDK 写工具拒绝 smoke；Expected: 连接成功、发现 20 个工具、写拒绝为标准可解析错误；Failure re-check: 区分 transport、Skill discovery、LLM inference 和外部 writer。Evidence: `hermes mcp test pinax` 在约 111ms 内连接并发现 20 个工具；Python MCP SDK 对 `pinax.repair.apply` 得到 code=`-32001`、legacy=`approval_required`。

## 2. Hermes 个人 Canary 运行面

- [x] 2.1 Owner: local-ops；Scope: 用户级 Hermes `personal` profile；Dependencies: 1.3；Lane: B；注册 `pinax mcp serve` 并用工具过滤器仅暴露 `pinax.agent.context`、`pinax.agent.memory_recall`、`pinax.agent.handoff_read`；Verification: `hermes tools list --platform cli` 与 `hermes tools list --platform feishu`；Expected: 仅三个 Pinax 只读工具可用；Failure re-check: 检查 active profile、global/platform filter precedence 和 MCP server command。Evidence: active `personal` profile 已注册 `pinax` stdio MCP；CLI/Feishu tool list 均排除其余 17 个 Pinax tools，保留三个 Agent 只读入口。
- [x] 2.2 Owner: conversation-workflow；Scope: 用户级 `pinax-action-capture` Hermes Skill；Dependencies: 2.1；Lane: B；固化任务预览、Pinax unavailable fallback、明确确认、外部工具委派和完成后 proposal/review 规则；Verification: Hermes Skill validator 与 `hermes skills list`；Expected: Skill 可发现，任何修改请求不等于写入确认；Failure re-check: 检查 Skill 触发描述、工具名和 safety boundaries。Evidence: Skill validator PASS，`hermes skills list` 显示 local/enabled；`lark-cli task +create --dry-run --as user` 返回 ok=true、dry_run=true、POST `/open-apis/task/v2/tasks`，未创建真实任务。
- [x] 2.3 Owner: memory-governance；Scope: Hermes memory config；Dependencies: 2.2；Lane: B；启用 memory write approval，并把 Hermes memory 限于偏好、交互习惯和工具路由；Verification: `hermes config get memory.write_approval`；Expected: 返回 `true`，项目决定和执行经验仍由 Pinax proposal/review 管理；Failure re-check: 检查 active profile 与配置键支持情况。Evidence: active `personal` profile 已设置 `memory.write_approval=true`，Skill 明确将项目决定与执行经验留在 Pinax proposal/review。
- [x] 2.4 Owner: canary-evidence；Scope: 用户级 Skill `scripts/`；Dependencies: 2.2；Lane: B；实现 CLI-authored canary recorder，支持 init、begin、preview、revise、create、completion、review 和 report，只写脱敏离散指标与 digest；Verification: recorder unit/self-test、空 canary report 和完整样例 report；Expected: 不手写 JSON/JSONL，不保存标题、正文、完整 task ID、raw prompt、会话或 provider payload；Failure re-check: 扫描输出敏感类别并验证事件状态机。Evidence: `pinax-action-canary self-test` PASS，6 个 unittest PASS；14-day canary 已初始化为 2026-08-10 至 2026-08-24，`report --week 1 --json` 在空分母下保持 metrics=`not_measured`。

## 3. 无外部写入 Smoke 与回滚

- [x] 3.1 Owner: local-qa；Scope: Hermes action-capture preview；Dependencies: 2.3-2.4；Lane: C；执行一次明确禁止外部写入的预览 smoke，验证 Pinax 来源、缺失信息和 `awaiting confirmation`；Verification: `hermes chat -t pinax` 的受限交互或等价本地 inference smoke；Expected: 能看到预览且飞书/外部任务写入为 0；Failure re-check: 分离模型超时、MCP、Skill 和 provider 问题，不扩大 Pinax scope。Evidence: Hermes session `20260810_023908_b433c8` 实际调用 `pinax.agent.context` 与 `pinax.agent.memory_recall`，输出 source-backed preview、缺失信息和 `Write status: awaiting confirmation`；因会话未暴露 terminal，recorder 明确标记 metrics 未记录；外部写工具调用=0、任务创建=0。
- [x] 3.2 Owner: rollback；Scope: 用户级 Hermes profile；Dependencies: 3.1；Lane: C；验证移除 Pinax MCP 或禁用 Skill 后 Hermes 可继续普通任务草案，并明确上下文不可用；Verification: 在隔离 profile 执行 `hermes mcp remove pinax` 后恢复配置的 smoke；Expected: 回滚不迁移或删除 Pinax/外部任务数据；Failure re-check: 检查 profile 隔离和 tool cache。Evidence: 2026-08-23 在隔离 profile `pinaxrollbacksmoke`（`--clone-from personal --no-alias`，配置/凭据隔离）完成：注册 pinax MCP（20/20 tools，`hermes mcp test pinax` 连接并列出含 `pinax.agent.context|memory_recall|handoff_read`）→ `hermes mcp remove pinax` → 普通任务草案 smoke 继续（"整理本周周报" 草案 + 一句话描述，0 外部写入），Agent 明确报告无 Pinax notes/agent memory 上下文（`PINAX_CONTEXT_AVAILABLE: no`，会话内无任何 pinax 工具）→ 恢复 smoke（re-add → test 20 tools）→ 清理隔离 profile 并切回 `personal`。数据完整性：yeisme-notes vault 全量 checksum 前后一致（`156979959c4ae0f1`，931 files），回滚未迁移/删除 Pinax 或外部任务数据。

## 4. 两周真实使用与指标决策

Status 2026-08-23: 4.1-4.4 依赖两周真实使用 canary。原 08-10 窗口的环境与数据已被重置清除；**canary 环境已于 2026-08-23 重建**（lane-2 状态恢复）：personal profile 的 pinax MCP 已注册且两平台工具过滤仅暴露 `pinax.agent.context|memory_recall|handoff_read`（`hermes tools list --platform cli|feishu` 验证），`pinax-action-capture` Skill（productivity/local/enabled，会话内可见），重写的 `pinax-action-canary` recorder（init/begin/preview/revise/create/completion/review/report + self-test PASS，脱敏 digest-only，空分母 not_measured），`memory.write_approval=true`，新 14 天窗口 **2026-08-23→2026-09-05**。剩余阻塞仅为真实使用：≥10 次行动项捕获、跨 ≥4 个不同日期（recorder 分母自动累计）；4.2-4.4 依次依赖。本会话不伪造 dogfood 指标，也未用测试事件污染新窗口。3.2（rollback）已于 2026-08-23 在隔离 profile 完成并勾选。

- [ ] 4.1 Owner: personal-dogfood；Scope: 两周真实工作；Dependencies: 3.1；Lane: D；完成至少 10 次行动项捕获，覆盖至少 4 个不同日期，并通过 recorder 记录预览修改、来源、延迟、确认与创建结果；Verification: `pinax-action-canary report --week 1` 与 `--week 2`；Expected: 每个指标有明确分母，未确认写入为 0；Failure re-check: 区分入口不可发现、上下文质量、模型延迟和外部 writer 失败。
- [ ] 4.2 Owner: memory-dogfood；Scope: 已完成重要任务；Dependencies: 4.1；Lane: D；对重要完成任务提出结果摘要并通过现有 Pinax proposal/review 判断是否沉淀；Verification: recorder report + Pinax review receipt；Expected: 一次性日志不沉淀，长期决定/经验/约束只有审阅通过后进入 Pinax；Failure re-check: 检查 proposal source、修改/拒绝路径和 silent promotion。
- [ ] 4.3 Owner: product-analysis；Scope: canary report；Dependencies: 4.1-4.2；Lane: decision；计算预览零/一次修改率、来源可打开率、未确认写入、重复/错误率、预览延迟中位数、重要完成任务提案率和每周主动使用天数；Verification: recorder `report --json` 与人工抽样；Expected: 报告 known bias、异常值、分母和缺失指标；Failure re-check: 禁止用任务总数或命令量代替行动闭环。
- [ ] 4.4 Owner: CEO/product；Scope: Go/Iterate/Stop receipt；Dependencies: 4.3；Lane: final；按 spec 预登记门槛决定是否评估 `intent=action_capture`、只优化 retrieval/review 或停止集成扩张；Verification: 新 decision receipt + `openspec validate pinax-agent-continuity-iteration-adoption-signal --strict`；Expected: 只有全部门槛通过才创建接口固化 change，任何后续接口保持 provider-neutral；Failure re-check: 缺少证据则保持 experimental，不默认 Go。
