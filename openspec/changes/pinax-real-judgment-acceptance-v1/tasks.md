# 实施与验收任务

由 scripts/openspec-tasks.py 维护状态。

- [x] 1.1 盘点真实 source 与现有 consumer 缺口，保留 typed gap。 | evidence: 真实 `yeisme-notes` vault 盘点完成：`pinax inbox list` 12 条真实 inbox source + `pinax note list --limit 100` 候选池（产品 CLI projection，只读）；`scripts/judgment-acceptance.py inventory --json` 冻结 12 个真实案例（available=12、input_count=12、excluded=0、gaps=[]、mode=shadow、target_count=20、status=needs_connection），敏感源扫描 0 命中（同一 bounded/SENSITIVE 代码路径的 typed gap `sensitive_source_excluded` 由 1.2 离线测试覆盖）。现有 consumer 缺口确认：internal/inboxjudgment（默认 off）只有建议 evidence，无真实案例验收与人工反馈持久化；本 change 的 owner sidecar store（`~/.local/share/pinax/judgment-acceptance/jev-real-20260921`，inventory.json 0600 / cases 0700）补齐该缺口且不改 canonical 业务状态。脱敏 evidence：temp/integration-test-runs/judgment-acceptance-real-inventory-20260921T184628Z/（标题/正文全部 REDACTED，完整冻结案例只存用户级 owner store）。
- [x] 1.2 实现 owner-bound 采样、SDK 调用和反馈持久化并完成离线验证。 | evidence: `scripts/judgment-acceptance.py`（owner=pinax，opt-in）实现 tag-overlap 候选采样、经 Aigora 共享 SDK wire 的 run 调用（requested_model=typesafe/jev-1.13、limit 限 3/20 累计、单 attempt 不重提、unknown submission_state sticky barrier）与 review 回执持久化（source_digest+request_id 绑定、幂等重放、冲突/过期拒绝、business_adoption=false）。离线验证 8/8 PASS（`python3 scripts/judgment_acceptance_offline_test.py`，隔离 HOME + fake `pinax` CLI + 记录型 fake evaluator）：show/inventory 零模型调用、敏感源→typed gap、重复 digest 去重（input 3→available 2）、candidate 按 tag 交集选择、run 重放零新调用、limit 校验 fail closed、unknown 屏障阻断后续提交、review 幂等/冲突/stale 拒绝、隔离 store 与真实 owner store 指纹不变、文件权限 0600/0700。共享 runtime 以字节相同快照提供（scripts/judgment-acceptance-runtime-snapshot/，sha256 2f3b6750b0d294496aee4a11e10ac193bb14e2602b0fe599a364e37b606a5a2b；本机共享安装尚未执行，inventory 经 PYTHONPATH 使用同字节快照）。evidence：temp/integration-test-runs/judgment-acceptance-offline-20260921T184628Z/。
- [ ] 1.3 执行首批真实案例；来源不足或待用户复核时不宣称效果通过。
  Status 2026-09-22: [external-gate skipped] — 首批真实案例执行需要 Aigora `launch.py` 提供的局部 adapter 环境（OpenRouter `openrouter/personal-default` 正式授权、5 美元/200 次预算、人工批准）加用户 DSH 复核；本编码会话无外部凭据、不付费、不代填用户反馈。来源已就绪（12 个冻结真实案例待 `run --limit 3`），效果未验证、不宣称通过。
