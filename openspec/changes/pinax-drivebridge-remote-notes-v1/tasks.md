## 0. OpenSpec 冻结（节点 PX-3-00，本切片）

- [x] 0.1 用 `openspec new change` 创建 `pinax-drivebridge-remote-notes-v1`。Owner: implementer；Scope: `openspec/changes/pinax-drivebridge-remote-notes-v1/`；Dependencies: 根包 `drivebridge-consumer-binding-v1`；Lane: spec；Verification: 目录由 OpenSpec CLI 生成且含 `.openspec.yaml`；Expected: 无手写 machine metadata。 Evidence: `openspec new change pinax-drivebridge-remote-notes-v1 --schema spec-driven --description "Attach existing Pinax local/S3 storage to DriveBridge; hydrate; keep Capsa and storage set commands."` → `Created change ... at openspec/changes/pinax-drivebridge-remote-notes-v1/`；`.openspec.yaml` schema=spec-driven, created=2026-09-19。
- [x] 0.2 填写 proposal、design（含 mermaid）、四份 delta/new specs 与本任务清单，冻结命令名、facts、错误码与 DB-2-01 依赖。Owner: implementer；Scope: 本 change 文档；Dependencies: 0.1；Lane: spec；Verification: `openspec status --change pinax-drivebridge-remote-notes-v1`；Expected: proposal/design/specs/tasks 均 complete；人读中文、命令与字段英文。 Evidence: `openspec status --change pinax-drivebridge-remote-notes-v1` → 4/4 artifacts complete；design 含三种模式/attach-hydrate/依赖 mermaid；命令冻结 `storage attach-drivebridge|detach-drivebridge|bind-working-copy|hydrate`。
- [x] 0.3 严格校验本 change。Owner: implementer；Scope: 本 change；Dependencies: 0.2；Lane: spec；Verification: `openspec validate pinax-drivebridge-remote-notes-v1 --strict --no-interactive`；Expected: valid。本切片到 0.3 为止，不改 `cmd/**` 或 `internal/**`。 Evidence: `openspec validate pinax-drivebridge-remote-notes-v1 --strict --no-interactive` → `Change 'pinax-drivebridge-remote-notes-v1' is valid`。

## 1. Attach 已有 local / S3（等待 DB-2-01）

实现任务硬依赖 DriveBridge adopt 零拷贝（节点 DB-2-01 / `drivebridge-owner-storage-adopt-v1`）。adopt 合同可测前不得勾选本节。

- [x] 1.1 实现 `pinax storage attach-drivebridge --space <space>`：读取当前 `pinax.storage.v1` profile，请求 DriveBridge adopt 同一 local root 或同一 S3 bucket/prefix，零拷贝写入 `.pinax/drivebridge-attach.yaml`（`pinax.drivebridge_attach.v1`）；并将 `storage` 与 `capsa`/`sync` 一样保留为 Remote API 下的本地控制面。Owner: implementer；Scope: `internal/cli/storage_cmd.go`、`internal/cli/root.go`、`internal/app`；Dependencies: 0.3、DB-2-01；Lane: impl；Verification: `go test ./internal/app ./cmd/pinax -run 'AttachDrivebridge|RemoteModeLocal' -count=1`；Expected: 不新开桶、不复制笔记字节；配置 `PINAX_API_URL` 时 attach 仍走本地且不返回 `remote_command_unsupported`。 Evidence: 实现于 internal/app/service_drivebridge.go（AttachDrivebridge）与 internal/cli/storage_cmd.go；internal/cli/root.go 将 storage 并入 capsa/sync 同级本地控制面（remoteModeLocalCommand/classifyRemoteCommand）。Evidence: `go test ./internal/app ./cmd/pinax -run 'AttachDrivebridge|RemoteModeLocal' -count=1` → 全绿；`PINAX_API_URL` 下 attach 走本地且 facts `copied=false`（TestStorageAttachDrivebridgeRemoteModeLocal）；零拷贝断言 storage.json 逐字节不变。
- [x] 1.2 location 与当前 storage profile 不一致时返回 `drivebridge_location_mismatch`，不写部分 attach 记录。Owner: implementer；Scope: app storage attach；Dependencies: 1.1；Lane: impl；Verification: 夹具测试 bucket/prefix/root mismatch；Expected: `.pinax/storage.json` 不变，无 attach 文件。 Evidence: Evidence: TestAttachDrivebridgeS3AndLocationMismatch（bucket/prefix 不一致）与 TestAttachDrivebridgeLocalRootMismatch（root 不一致）均返回 `drivebridge_location_mismatch`，`.pinax/drivebridge-attach.yaml` 不存在、storage profile 不变；adopt 返回 location 与 profile 不一致同样拒绝。
- [x] 1.3 DriveBridge 未安装时 `attach-drivebridge` 失败为 `drivebridge_not_installed`；`storage set local|s3`、`status`、`doctor`（未 attach）、`capsa backend set`、`backend add` 仍成功。Owner: implementer；Scope: CLI/app；Dependencies: 1.1；Lane: impl；Verification: PATH 不含 drivebridge 的测试；Expected: 未 attach 时 doctor 不因缺二进制失败。 Evidence: Evidence: TestAttachDrivebridgeNotInstalledKeepsOwnerCommands 与 cmd 层 TestStorageDrivebridgeNotInstalledContract：PATH 无 drivebridge 时 attach 失败 `drivebridge_not_installed`，而 `storage set s3`、`status`、`doctor`（未 attach）、`capsa backend set`、`backend add` 全部成功。
- [x] 1.4 实现 `pinax storage detach-drivebridge`：删除 attach 记录并请求解除 adopt，不改 storage profile、Capsa 配置与笔记字节。Owner: implementer；Scope: CLI/app；Dependencies: 1.1；Lane: impl；Verification: detach 后 `storage status` 仍显示原 backend；Expected: owner 读写不受影响。 Evidence: Evidence: TestDetachDrivebridgeKeepsOwnerStorage：detach 删除 `.pinax/drivebridge-attach.yaml` 并 best-effort 请求 `drivebridge storage unadopt`（DB 本波无该命令 → 降级 warning），`storage status` 仍显示原 backend；重复 detach 返回 `drivebridge_not_attached`。
- [x] 1.5 实现 `pinax storage bind-working-copy --provider onedrive|gdrive` 显式 opt-in；无该命令时 attach 到网盘空间返回 `drivebridge_working_copy_opt_in_required`。Owner: implementer；Scope: CLI/app；Dependencies: 1.1；Lane: impl；Verification: 负向测试 init/note add 不上传网盘；Expected: 日常 `note add` 只写本地。 Evidence: Evidence: TestBindWorkingCopyProviderPlaintext（显式 opt-in 写 content_mode=provider-plaintext；note add 后 fake DriveBridge 调用日志零增长、只写本地）与 TestAttachDrivebridgeWorkingCopyOptInRequired（attach 网盘空间未 opt-in → `drivebridge_working_copy_opt_in_required`，不写记录）。

## 2. Doctor / status 诚实声明

- [x] 2.1 扩展 `storage status`、`storage doctor`、`vault doctor` 加法 facts：`drivebridge_attached`、`drivebridge_space`、`drivebridge_kind`、`drivebridge_content_mode`、`drivebridge_location_match`、`capsa_sync_configured`、`remote_api_configured`。Owner: implementer；Scope: `internal/app` doctor/status、output contract tests；Dependencies: 1.1；Lane: impl；Verification: `--json`/`--agent` 合同测试；Expected: 枚举为英文；human chrome 中文；未 attach 时 `content_mode=none`。 Evidence: Evidence: drivebridgeAttachmentFacts 为 `storage status`/`storage doctor`/`vault doctor` 加法输出 drivebridge_attached/space/kind/content_mode/location_match/capsa_sync_configured/remote_api_configured；TestStorageDoctorFactsSeparateModes 断言未 attach 时 `content_mode=none` 且不要求安装 DriveBridge；cmd 层 --json facts 英文合同测试通过。
- [x] 2.2 `bind-working-copy` 后 doctor 报告 `provider-plaintext`，不得报 Capsa encrypted / `opaque_encrypted`。Owner: implementer；Scope: doctor；Dependencies: 1.5、2.1；Lane: impl；Verification: 明文网盘 fixture；Expected: 与 Capsa rclone OneDrive 状态分开展示。 Evidence: Evidence: TestBindWorkingCopyProviderPlaintext + TestStorageDoctorFactsSeparateModes：bind 后 doctor 报 `provider-plaintext`，永不报 Capsa encrypted/opaque_encrypted；Capsa 状态由独立 fact `capsa_sync_configured` 分开展示。
- [x] 2.3 若 adopt 前缀实际是 Capsa 密文对象，doctor 报告 `opaque_encrypted`，不把对象当笔记。Owner: implementer；Scope: doctor；Dependencies: 2.1；Lane: impl；Verification: Capsa prefix fixture；Expected: 无 note projection，不解密。 Evidence: Evidence: TestStorageDoctorOpaqueEncryptedPrefix：attach 前缀与 Capsa s3-direct 同 bucket/prefix 时 doctor 报 `opaque_encrypted` 并出 `drivebridge_opaque_encrypted` issue，不投影笔记、不解密。
- [x] 2.4 同时配置 Capsa 与 DriveBridge 时两条 status 分开；任一成功不得把另一条标成已同步，且不得发出 Capsa `remote_write=true`。Owner: implementer；Scope: storage/sync status；Dependencies: 2.1；Lane: impl；Verification: 双配置 fixture；Expected: sync-state 不因 DriveBridge 传输前进。 Evidence: Evidence: TestStorageDoctorFactsSeparateModes（双配置 facts 分开、无 remote_write=true）与 TestStorageHydrateS3SecondDevice（hydrate 前后 `.pinax/sync-state.json` 逐字节不变）。

## 3. 换机 hydrate

- [x] 3.1 实现 `pinax storage hydrate --space <space>`：钉住 file id/version/sha256，传输工作副本，然后 `index refresh` 可成功。Owner: implementer；Scope: CLI/app hydrate；Dependencies: 1.1、DB-2-01；Lane: impl；Verification: 双设备夹具（同一 S3 prefix）；Expected: `vault validate` 可通过；笔记 id 仍由 Pinax 管理；无 Capsa `remote_write=true`。 Evidence: Evidence: TestStorageHydrateS3SecondDevice：ls/download 经 fake DriveBridge 文件面钉住 ref/version/sha256，`.pinax/**` 跳过保护，落地后 `pinax index refresh` 与 `pinax vault validate` 通过，facts `remote_write=false`，sync-state 不变。
- [x] 3.2 远端相对钉住版本变化时该文件失败为 `file_version_changed`，不覆盖未提交本地编辑。Owner: implementer；Scope: hydrate；Dependencies: 3.1；Lane: impl；Verification: 中途替换源对象的测试；Expected: 本地未提交字节保留。 Evidence: Evidence: TestStorageHydrateVersionChangedAndConflicts：stat_bump 使远端版本变化 → `file_version_changed`、不留半份副本；本地未提交编辑（LOCAL_EDIT_MARKER）原样保留。
- [x] 3.3 `kind=local` 在目标机器不可见时返回 `drivebridge_local_unreachable`，提示改用 S3 attach 或 `bind-working-copy`，不把本机路径当远端文件。Owner: implementer；Scope: hydrate；Dependencies: 3.1；Lane: impl；Verification: 隐藏目录/错机器夹具；Expected: 不声称 MCP 可读客户端磁盘。 Evidence: Evidence: TestStorageHydrateLocalUnreachable：kind=local 且目录不可见 → `drivebridge_local_unreachable`，hint 指向 S3 attach / bind-working-copy，不声称远端可读本机磁盘。
- [x] 3.4 hydrate 冲突与损坏交给既有 Pinax repair/conflicts，DriveBridge 不自动合并正文。Owner: implementer；Scope: hydrate + repair 交接点；Dependencies: 3.1；Lane: impl；Verification: 双方修改同一 `.md` 的夹具；Expected: 下一步是 Pinax repair 命令，不是 DriveBridge merge。 Evidence: Evidence: TestStorageHydrateVersionChangedAndConflicts：冲突文件不覆盖、状态 partial，Actions 指向 `pinax repair plan` / `pinax sync conflicts`，DriveBridge 不合并正文。

## 4. 附件 DriveBridge 引用

- [x] 4.1 允许 asset/attachment metadata 登记已钉住的 `drivebridge://` 引用（file id、version、sha256），不把 DriveBridge list 当 note 投影。Owner: implementer；Scope: `internal/app` asset/note attach；Dependencies: 1.1；Lane: impl；Verification: asset 合同测试；Expected: 无 payload 字节；`note_id` 仍由 Pinax 管理。 Evidence: Evidence: TestAssetRegisterAndConsumeDrivebridgeRef：`asset add --register --drivebridge-ref drivebridge://<space>/<file-id>@<version>` 只写 manifest metadata（file id/version/sha256），输出无 payload 字节，asset id 仍为 Pinax `asset_*`；TestParseDrivebridgeRefShapes 固化引用形状。
- [x] 4.2 源文件变化后消费引用失败为 `file_version_changed`，已落地 vault 副本不变。Owner: implementer；Scope: asset consume；Dependencies: 4.1；Lane: impl；Verification: 替换远端字节后的消费测试；Expected: 本地附件哈希不变。 Evidence: Evidence: TestAssetRegisterAndConsumeDrivebridgeRef：源版本变化后 consume 返回 `file_version_changed`，已落地 vault 副本哈希不变。
- [x] 4.3 跨项目绑定同一引用不共享 `note_id`、不绕过各自授权；detach/撤销后新读取失败，已落地 Pinax 附件仍可用。Owner: implementer；Scope: asset metadata + 文档交接点；Dependencies: 4.1；Lane: impl；Verification: 引用形状夹具（Scaena 消费在其 owner change 验收）；Expected: Pinax 正文与 proof 仍只在 Pinax。 Evidence: Evidence: detach 后 `consume-drivebridge` 返回 `drivebridge_not_attached`（新跨项目读取拒绝），已落地附件经 `asset show` 仍可用；引用形状夹具 TestParseDrivebridgeRefShapes；Scaena 侧消费留其 owner change 验收。

## 5. 文档与输出合同

- [x] 5.1 更新 `docs/commands/storage.md`：加法命令、零拷贝 attach、opt-in 网盘、未 attach 不依赖 DriveBridge；人读中文、命令英文。Owner: docs；Scope: `docs/commands/storage.md`；Dependencies: 1.5、2.1；Lane: docs；Verification: 命令审计与 `pinax storage --help`；Expected: 不删除 `storage set local|s3`。 Evidence: Evidence: docs/commands/storage.md 新增四个加法命令、零拷贝 attach、opt-in 网盘、未 attach 不依赖 DriveBridge 与错误码表；`storage set local|s3` 未删除，命令与 `pinax storage --help` 一致。
- [x] 5.2 更新 `docs/architecture/cloud-sync-design.md`：增加 Pattern C DriveBridge attach，与 Pattern A Remote API、Pattern B Capsa 并列且不得混写。Owner: docs；Scope: `docs/architecture/cloud-sync-design.md`；Dependencies: 2.4；Lane: docs；Verification: 文档审查；Expected: rclone OneDrive ≠ `bind-working-copy` 明文。 Evidence: Evidence: docs/architecture/cloud-sync-design.md 新增 Pattern C DriveBridge attach，与 Pattern A Remote API、Pattern B Capsa 并列，明确 rclone OneDrive（opaque_encrypted）≠ bind-working-copy 明文，且 Pattern C 不是 Capsa transport。
- [x] 5.3 确认 `--json`/`--agent`/`--events` 无凭据、无 note body 泄漏；human 摘要中文。Owner: implementer；Scope: output contract tests；Dependencies: 2.1、3.1；Lane: docs；Verification: 递归脱敏扫描；Expected: 协议字段英文稳定。 Evidence: Evidence: cmd 层 TestStorageDrivebridgeMachineModesRedaction 对 --json/--agent/--events 递归扫描断言无凭据、无 note body；新命令 human 摘要中文、facts/错误码英文稳定。

## 6. 质量门禁

- [x] 6.1 聚焦测试：attach/mismatch/未安装/doctor 模式/hydrate/version-changed/local-unreachable。Owner: test owner；Scope: 变更包；Dependencies: 1–4；Lane: final；Verification: `go test ./internal/app ./cmd/pinax -run 'AttachDrivebridge|StorageDoctor|Hydrate|WorkingCopy|Drivebridge' -count=1`；Expected: 全绿。 Evidence: Evidence: `go test ./internal/app ./cmd/pinax -run 'AttachDrivebridge|StorageDoctor|Hydrate|WorkingCopy|Drivebridge' -count=1` → ok（internal/app 1.7s，cmd/pinax 0.6s）。
- [x] 6.2 `task check` 与 `openspec validate pinax-drivebridge-remote-notes-v1 --strict --no-interactive`。Owner: test owner；Scope: `cli/pinax`；Dependencies: 6.1、5.2；Lane: final；Verification: 上述命令；Expected: 退出 0；无无关文件改动。 Evidence: Evidence: `task check`（fmt-check/lint/test/build/openspec validate --all）exit 0；`openspec validate pinax-drivebridge-remote-notes-v1 --strict --no-interactive` → valid；改动仅限本 change 涉及路径。
- [x] 6.3 兼容评审：确认未删除 `storage set local|s3`、`capsa backend set`、`backend add`；未 attach 回归与现网一致。Owner: reviewer；Scope: CLI 树与本 change；Dependencies: 6.2；Lane: final；Verification: help/命令树与既有 storage 测试；Expected: 可进入 PX-3-01 closeout，不宣称 DriveBridge 产品已全局落地。 Evidence: Evidence: internal/cli/root.go coverage 将 storage 归为 local_only（storage_and_drivebridge_attach_run_locally）；TestRemoteCommandCoverageClassifiesEveryVisibleRunnableCommand 与既有 storage/asset/backend 测试全绿；`storage set local|s3`、`capsa backend set`、`backend add` 未删除未改名，未 attach 行为与现网一致。可进入 PX-3-01 closeout，未宣称 DriveBridge 产品全局落地。
