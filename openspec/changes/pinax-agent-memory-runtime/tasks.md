## 0. 前置门禁与兼容基线

- [x] 0.1 Owner: release/identity；Scope: `openspec/changes/pinax-identity-first-note-kernel-v2/` 与共享工作树；Dependencies: none；Lane: gate；确认 identity-first change 的 7.3/8.3 已完成或明确允许本 change 只写独立新文件；Verification: `openspec status --change pinax-identity-first-note-kernel-v2 && git status --short`；Expected: 共享 `internal/domain`、`internal/app/service.go`、index/sync 文件没有未协调并行编辑；Failure re-check: 暂停相关 lane，仅执行 contract/spec/新包任务。Evidence: `pinax-identity-first-note-kernel-v2` archived 2026-07-11 (29/29 tasks), no uncoordinated parallel edits on shared files.
- [x] 0.2 Owner: compatibility；Scope: `cmd/pinax`、`internal/cli`、`internal/mcpserver`、`internal/api`、`internal/memory` 当前合同；Dependencies: none；Lane: A；固化现有 `pinax memory`、`pinax brain`、`pinax.brain.*`、Remote API 和 ledger fixture 的兼容测试清单；Verification: `go test ./cmd/pinax ./internal/mcpserver ./internal/api ./internal/memory -run 'Memory|Brain|MCP|RPC' -count=1`；Expected: 当前基线通过并记录现有 schema/tool/command；Failure re-check: 区分既有失败与本 change 影响，不放宽或删除旧测试。Evidence: all 4 packages pass (`ok cmd/pinax`, `ok internal/mcpserver`, `ok internal/api`, `ok internal/memory`).
- [x] 0.3 Owner: contract；Scope: `openspec/changes/pinax-agent-memory-runtime/`；Dependencies: none；Lane: A；完成 stable surface 分类：new CLI/MCP/API/DB/public Go 为 additive experimental，旧面不进入 deprecation；Verification: `rg -n 'additive|experimental|deprecation|rollback|Existing surfaces' proposal.md design.md specs`；Expected: migration、compatibility 和 rollback 明确；Failure re-check: 补 consumer list 与旧入口保留规则。Evidence: design.md (14 hits), proposal.md (3 hits), specs (5 hits) all classify new surfaces as additive/experimental with rollback path; deprecation window explicitly not started in this change.

## 1. Protocol 与领域类型

- [x] 1.1 Owner: agent-protocol；Scope: 新建 `internal/agentprotocol/principal.go`、`scope.go`、`errors.go` 及测试；Dependencies: 0.3；Lane: B；定义 versioned `Principal`、capabilities、trust level、owner/workspace/project/repository/session/task scope 和 stable validation errors；Verification: `go test ./internal/agentprotocol -run 'Principal|Scope|Validation' -count=1`；Expected: invalid scope/capability 返回稳定 English code，unknown optional metadata 可忽略；Failure re-check: 逐个运行最小失败 test 并检查 JSON tag 与 schema version。
- [x] 1.2 Owner: agent-protocol；Scope: 新建 `internal/agentprotocol/memory.go`、`source.go` 及测试；Dependencies: 1.1；Lane: B；定义 `MemoryRecord`、kind、lifecycle、confidence、source ref、supersession、conflict 和 timestamps；Verification: `go test ./internal/agentprotocol -run 'MemoryRecord|SourceRef|Lifecycle' -count=1`；Expected: fact/decision/preference/procedure/event/task/failure 和 proposed/confirmed/rejected/superseded/expired/conflicted 均可验证；Failure re-check: 检查 enum/string forward compatibility 和 required fields。
- [x] 1.3 Owner: agent-protocol；Scope: 新建 `internal/agentprotocol/context.go`、`proposal.go`、`handoff.go`、`feedback.go` 及测试；Dependencies: 1.1、1.2；Lane: B；定义 context request/pack、budget、proposal、handoff、feedback DTO；Verification: `go test ./internal/agentprotocol -run 'Context|Proposal|Handoff|Feedback' -count=1`；Expected: payload bounded、source-backed、无 runtime-specific required field；Failure re-check: 对照根 `general-agent-memory-contract` scenarios 修正字段。
- [x] 1.4 Owner: agent-protocol；Scope: 新建 `internal/agentprotocol/adapter.go` 及测试；Dependencies: 1.1；Lane: B；定义 `AdapterDescriptor`、supported versions/transports/capabilities、max context 和 degraded behavior；Verification: `go test ./internal/agentprotocol -run 'AdapterDescriptor|CapabilityNegotiation' -count=1`；Expected: Codex/Ordo fixture 使用相同 core versions，仅 metadata extension 不同；Failure re-check: 删除进入 core 的 runtime-specific required fields。
- [x] 1.5 Owner: architecture；Scope: `internal/architecture/` guard tests；Dependencies: 1.1-1.4；Lane: B；增加 core packages 禁止 import Codex/Ordo/provider SDK、CLI/output/MCP 的架构守卫；Verification: `go test ./internal/architecture -run 'AgentMemory|ImportBoundary' -count=1`；Expected: provider-neutral boundary 被测试锁定；Failure re-check: 移动 adapter conversion 到 transport/adapter package。

## 2. Additive Memory Store 与迁移

- [x] 2.1 Owner: memory-store；Scope: 新建 `internal/agentmemory/model.go`、`repository.go` 与旧 ledger fixture tests；Dependencies: 0.1、1.2；Lane: C；设计 GORM additive tables/nullable fields，映射现有 `internal/memory.Record` 到 common view；Verification: `go test ./internal/agentmemory -run 'Legacy|Model|Migration' -count=1`；Expected: 旧 fact/decision/event/task ID 和内容不变；Failure re-check: 使用真实旧 `ledger.sqlite` fixture 定位 migration 或 mapping 差异。
- [x] 2.2 Owner: memory-store；Scope: `internal/agentmemory/store.go`、migration tests；Dependencies: 2.1；Lane: C；实现 principal、scope、memory-source、supersession、conflict、proposal、handoff、feedback repository；Verification: `go test ./internal/agentmemory -run 'Store|Transaction|Rollback' -count=1`；Expected: GORM transaction 原子写入，失败不损坏旧 rows；Failure re-check: 检查 migration、unique key、foreign relation 和 transaction boundary。
- [x] 2.3 Owner: memory-store；Scope: `internal/agentmemory/legacy.go`、compat tests；Dependencies: 2.1；Lane: C；实现旧 memory row 的 default local principal/scope read mapping，不静默 backfill；Verification: `go test ./internal/agentmemory ./internal/memory -run 'LegacyView|ExistingMemory' -count=1`；Expected: old/new read path 指向同一 record ID；Failure re-check: 检查 ID normalization 与 source relation。
- [x] 2.4 Owner: memory-store；Scope: FTS/index repository internals 与 benchmarks；Dependencies: 2.2；Lane: C；扩展 kind/scope/source search，raw SQL 仅保留在 virtual-table repository；Verification: `go test ./internal/agentmemory -run 'Recall|FTS|Scope' -count=1 && go test ./internal/agentmemory -run '^$' -bench 'MemoryRecall' -benchmem`；Expected: 10k memory fixture 无每 request 全表 scan；Failure re-check: 分离 scope filter、FTS candidate 和 ranking profile。
- [x] 2.5 Owner: memory-store；Scope: migration receipt application service 与 tests；Dependencies: 2.2、3.1；Lane: C；首次持久化新 state 时写 redacted schema receipt，失败可继续旧 path；Verification: `go test ./internal/app ./internal/agentmemory -run 'AgentMemoryMigration|SchemaReceipt|Rollback' -count=1`；Expected: receipt 无正文/secret，rollback 不 drop additive schema；Failure re-check: 模拟 read-only FS、corrupt DB 和 interrupted migration。

## 3. Lifecycle、Policy 与 Application Service

- [x] 3.1 Owner: agent-memory-domain；Scope: 新建 `internal/agentmemory/service.go`、`lifecycle.go` 及 tests；Dependencies: 1.2、2.1；Lane: D；实现 lifecycle transition validator、scope checks、source requirement、supersession 和 conflict grouping；Verification: `go test ./internal/agentmemory -run 'Lifecycle|Supersede|Expire|Conflict' -count=1`；Expected: 非法 transition 返回稳定 code，conflict 不静默覆盖；Failure re-check: 重放单一 transition table case。
- [x] 3.2 Owner: policy；Scope: 新建 `internal/agentmemory/policy.go` 及 tests；Dependencies: 1.1、3.1；Lane: D；实现 capability、trust、evidence class、risk 和 requested status policy；Verification: `go test ./internal/agentmemory -run 'Policy|Approval|Capability' -count=1`；Expected: adapter 默认只能 propose，unsourced preference 不能自动 confirm；Failure re-check: 检查 principal capability 与 policy precedence。
- [x] 3.3 Owner: proposal-service；Scope: 新建 `internal/app/agent_memory_proposal.go` 及 tests；Dependencies: 2.2、3.1、3.2；Lane: D；实现 propose/list/show/review/approve/reject application services；Verification: `go test ./internal/app -run 'AgentMemoryProposal|Approve|Reject' -count=1`；Expected: validate/dedupe/conflict/policy + redacted receipt；Failure re-check: 保存 proposal/store 摘要并定位事务首个失败步骤。
- [x] 3.4 Owner: lifecycle-service；Scope: 新建 `internal/app/agent_memory_lifecycle.go` 及 tests；Dependencies: 3.1、3.3；Lane: D；实现 supersede/expire/conflict resolve/rollback use cases；Verification: `go test ./internal/app -run 'AgentMemorySupersede|Expire|ConflictResolve|Rollback' -count=1`；Expected: lifecycle history 可审计，旧 confirmed record 不被删除；Failure re-check: 检查 expected version 和 concurrent update guard。
- [x] 3.5 Owner: handoff-feedback；Scope: 新建 `internal/app/agent_handoff.go`、`agent_feedback.go` 及 tests；Dependencies: 2.2、3.2；Lane: D；实现 bounded handoff 与 useful/irrelevant/stale/incorrect/missing/completed/scope-too-wide feedback；Verification: `go test ./internal/app -run 'AgentHandoff|AgentFeedback' -count=1`；Expected: handoff 不自动 confirm，feedback 不静默改 content；Failure re-check: 检查 source refs、principal permission 和 receipt redaction。

## 4. Context Compiler Read Path

- [x] 4.1 Owner: context-compiler；Scope: 新建 `internal/agentcontext/compiler.go`、`filters.go` 及 tests；Dependencies: 1.3、2.3、3.1；Lane: E；实现 permission/scope/lifecycle/source visibility 前置过滤；Verification: `go test ./internal/agentcontext -run 'Permission|Scope|Lifecycle|Visibility' -count=1`；Expected: unauthorized source 不泄漏 title/body/path secret；Failure re-check: 单独重放 principal/scope/source ACL fixture。
- [x] 4.2 Owner: context-compiler；Scope: `internal/agentcontext/ranking.go`、`merge.go` 及 tests；Dependencies: 4.1；Lane: E；融合现有 memory scorer、search、KB、graph、query、project 和 receipt refs；Verification: `go test ./internal/agentcontext -run 'Ranking|Merge|ExistingProjection' -count=1`；Expected: exact scope/entity 优先，ranking reasons 可解释；Failure re-check: 分源打印 bounded score breakdown，禁止 raw body dump。
- [x] 4.3 Owner: context-compiler；Scope: `internal/agentcontext/budget.go` 及 tests；Dependencies: 4.2；Lane: E；实现 deterministic max-items/max-chars truncation、conflict grouping 和 drill-down actions；Verification: `go test ./internal/agentcontext -run 'Budget|Truncate|ConflictGroup|NextAction' -count=1`；Expected: 同输入稳定输出，`truncated=true` 准确；Failure re-check: 固定排序 seed/time 并检查 UTF-8 char budget。
- [x] 4.4 Owner: context-app；Scope: 新建 `internal/app/agent_context_runtime.go` 及 tests；Dependencies: 4.1-4.3；Lane: E；编排 context request → projection，复用 existing projections，不直接访问 transport；Verification: `go test ./internal/app -run 'AgentContextRuntime|BoundedContext' -count=1`；Expected: `pinax.agent_context.pack.v1` 包含 facts/decisions/preferences/procedures/tasks/failures/conflicts/sources；Failure re-check: 对照 compiler input/output 与旧 brain bundle。
- [x] 4.5 Owner: performance；Scope: `internal/agentcontext` benchmarks 与 fixture generator；Dependencies: 4.3、2.4；Lane: E；验证 10k memories/100k source refs 的 bounded context；Verification: `go test ./internal/agentcontext -run '^$' -bench 'ContextCompile' -benchmem`；Expected: 内存有界，无每次全 vault scan，结果写 integration artifact；Failure re-check: 分 profile 到 filter/ranking/merge/budget hot path。

## 5. CLI 与输出合同

- [x] 5.1 Owner: CLI；Scope: 新建 `internal/cli/agent_cmd.go`、`cmd/pinax` wiring 与 help tests；Dependencies: 1.1-1.4、4.4；Lane: F；增加 `pinax agent runtime|context|memory|handoff|feedback|status` additive command tree；Verification: `go test ./cmd/pinax ./internal/cli -run 'AgentCommand|AgentHelp' -count=1`；Expected: English help、无旧 command rename、planned write flags 明确；Failure re-check: 对照 Cobra command tree 和 golden help。
- [x] 5.2 Owner: output；Scope: `internal/output` Agent runtime projections 与 tests；Dependencies: 5.1；Lane: F；为 default/`--agent`/`--json`/`--events`/`--explain` 实现同源 projection；Verification: `go test ./internal/output ./cmd/pinax -run 'Agent.*Output|MachineMode|Explain' -count=1`；Expected: machine output 无 ANSI/human progress，stable English keys；Failure re-check: 比较五种模式 projection source 与 envelope。
- [x] 5.3 Owner: CLI-write；Scope: proposal/lifecycle/handoff/feedback commands tests；Dependencies: 3.3-3.5、5.1；Lane: F；实现 dry-run/plan/`--yes` gate、approval_required 和 receipts；Verification: `go test ./cmd/pinax -run 'AgentMemoryPropose|Approve|Reject|Handoff|Feedback' -count=1`；Expected: 无 `--yes` 不 confirm，失败不写半成品；Failure re-check: 检查 flag validation、service transaction 和 receipt。
- [x] 5.4 Owner: CLI-compat；Scope: existing/new command parity tests；Dependencies: 5.2、5.3；Lane: F；证明 old `memory recall` 与 new `agent memory recall` 指向同一 ID/source；Verification: `go test ./cmd/pinax -run 'MemoryCompatibility|BrainCompatibility|AgentMemoryParity' -count=1`；Expected: 旧命令和新 facade 同时通过；Failure re-check: 不修改旧 golden 来掩盖 breaking difference。

## 6. MCP、REST/RPC 与 Go SDK

- [x] 6.1 Owner: MCP；Scope: `internal/mcpserver` Agent tools/schema/tests；Dependencies: 4.4、5.2；Lane: G；新增 read-only `pinax.agent.context|memory_recall|sources|handoff_read` 与 descriptor metadata；Verification: `go test ./internal/mcpserver -run 'AgentTool|ToolsList|Readonly|BodyExposure' -count=1`；Expected: 默认 read-only、bounded、existing `pinax.brain.*` 保留；Failure re-check: 检查 tools/list compatibility 和 call routing。
- [x] 6.2 Owner: MCP-write；Scope: optional proposal/feedback tools tests；Dependencies: 3.3、3.5、6.1；Lane: G；仅在显式 capability enabled 时公开 proposal/feedback，返回 approval facts；Verification: `go test ./internal/mcpserver -run 'AgentProposalTool|FeedbackTool|CapabilityDisabled|Approval' -count=1`；Expected: 默认不可写，direct confirmed mutation 不存在；Failure re-check: 检查 server config、descriptor 和 service gate。
- [x] 6.3 Owner: API；Scope: `internal/api` route/handler/registry tests；Dependencies: 4.4、3.3-3.5；Lane: G；新增 localhost REST/RPC Agent runtime routes，handler 只调用 application service；Verification: `go test ./internal/api -run 'AgentMemory|AgentContext|Handoff|Feedback|Capability' -count=1`；Expected: read/write/approval/body exposure 在 discovery 可见；Failure re-check: 检查 registry、route method 和 projection parity。
- [x] 6.4 Owner: SDK；Scope: 新建 `pkg/agentmemory` public experimental client/types/tests；Dependencies: 1.1-1.4、4.4；Lane: G；提供 embedded Go consumer 的 context/recall/propose/handoff/feedback facade；Verification: `go test ./pkg/agentmemory -count=1 && go vet ./pkg/agentmemory`；Expected: API 标记 experimental、无 internal store 泄漏；Failure re-check: 检查 exported signature、JSON schema 和 consumer fixture。
- [x] 6.5 Owner: parity；Scope: `tests/e2e` transport parity harness；Dependencies: 5.2、6.1、6.3、6.4；Lane: G；同一 fixture 通过 CLI/MCP/REST-RPC/SDK 返回语义一致 context；Verification: `go test ./tests/e2e -run 'AgentMemoryTransportParity' -count=1`；Expected: core fields 一致，仅 wrapper 不同；Failure re-check: 保存四种 normalized projection 并定位首个差异字段。

## 7. Reference Adapter Harness 与跨 Agent Handoff

- [x] 7.1 Owner: adapter-core；Scope: 新建 `internal/agentadapter/descriptor.go`、`harness.go` 及 tests；Dependencies: 1.4、6.4；Lane: H；实现 capability negotiation、request/render conversion 和 degraded status harness；Verification: `go test ./internal/agentadapter -run 'Descriptor|Negotiation|Degraded' -count=1`；Expected: adapter failure 不改 ledger；Failure re-check: 模拟 unsupported version、timeout 和 missing executable。
- [x] 7.2 Owner: Codex reference；Scope: `internal/agentadapter/testdata/codex/` 与 conversion tests；Dependencies: 7.1；Lane: H；建立 Codex descriptor/context/proposal/handoff fixture，不实现 plugin/hook installer；Verification: `go test ./internal/agentadapter -run 'CodexReference' -count=1`；Expected: 只使用 common schema + optional metadata；Failure re-check: 删除进入 core DTO 的 Codex-specific required field。
- [x] 7.3 Owner: Ordo reference；Scope: `internal/agentadapter/testdata/ordo/` 与 conversion tests；Dependencies: 7.1；Lane: H；建立 Ordo team/role/trace/handoff fixture，不 import Ordo runtime code；Verification: `go test ./internal/agentadapter -run 'OrdoReference' -count=1`；Expected: 与 Codex 共用 100% core schema；Failure re-check: 将 team-specific state 放入 adapter metadata。
- [x] 7.4 Owner: cross-adapter e2e；Scope: `tests/e2e/agent_memory_handoff_test.go` 与 fixtures；Dependencies: 7.2、7.3、6.5；Lane: H；验证 Ordo producer → Codex consumer 和 Codex producer → Ordo consumer handoff；Verification: `go test ./tests/e2e -run 'CrossAgentMemoryHandoff' -count=1`；Expected: objective/state/decisions/blockers/verification/follow-ups/source refs 可继续；Failure re-check: 比较 normalized handoff 与 principal permission。
- [x] 7.5 Owner: consumer handoff；Scope: root/Ordo future OpenSpec note；Dependencies: 7.4；Lane: H；输出 Ordo adapter implementation handoff，不在 Pinax 实现 runtime integration；Verification: `rg -n 'ordo-pinax-memory-adapter|reference adapter|not owned' docs openspec`；Expected: owner 和触发条件清楚；Failure re-check: 删除 Pinax 中的 Ordo runtime code 计划。

## 8. 安全、证据、文档与发布门禁

Status 2026-08-23（重申）: 8.5 最早四周观察完成日期为 2026-08-25，且需真实 Ordo/Codex 两端运行；8.6 依赖 8.5。两者保持产品/发布 owner 任务，不由编码会话完成或勾选。
- [x] 8.1 Owner: redaction/security；Scope: protocol、output、receipt、adapter recursive scan tests；Dependencies: 3.3-3.5、5.2、6.1-6.4；Lane: I；扫描 secret、Authorization/Cookie、raw prompt、provider payload、private tool arguments、hidden system prompt 和 chain-of-thought sentinel；Verification: `go test ./internal/agentprotocol ./internal/agentmemory ./internal/agentcontext ./internal/output ./internal/mcpserver ./internal/api ./pkg/agentmemory -run 'Redaction|Secret|Forbidden' -count=1`；Expected: 所有 surface 脱敏；Failure re-check: 定位首次进入 projection/receipt 的字段并在 central redaction 修复。
- [x] 8.2 Owner: integration-evidence；Scope: `internal/testkit/integrationevidence`、Taskfile、CI artifact wiring；Dependencies: 6.5、7.4、8.1；Lane: I；增加 `task integration:agent-memory`，成功失败均写标准 evidence 并保留原退出码；Verification: `task integration:agent-memory`；Expected: `temp/integration-test-runs/<run-id>/` 含 summary/command/stdout/stderr/env/artifacts；Failure re-check: 人工触发失败验证 evidence、redaction 和 exit code。
- [x] 8.3 Owner: docs；Scope: `README.md`、`README.zh-CN.md`、`docs/product/general-agent-memory-prd.md`、`docs/commands`、`docs/architecture`、`docs/interfaces`；Dependencies: 5.1、6.1-6.4；Lane: I；更新当前/experimental/planned 区分、真实命令、兼容和 owner 边界；Verification: `rg -n 'pinax agent|pinax.agent.|experimental|proposal|handoff|feedback' README.md README.zh-CN.md docs`；Expected: 未实现命令不写成当前可用，Codex/Ordo 只写 adapter；Failure re-check: 对照 `pinax --help`、MCP tools/list 和 API routes。
- [x] 8.4 Owner: quality；Scope: 全 Pinax；Dependencies: 0.1-8.3；Lane: final；运行格式、lint、unit、integration、race-sensitive、build 和 OpenSpec；Verification: `task check && task ci && CGO_ENABLED=0 go build ./cmd/pinax && openspec validate pinax-agent-memory-runtime --strict`；Expected: 全部通过且旧合同测试仍在；Failure re-check: 从最小失败 package/command 修复本 change 相关问题后回到全量 gate。Evidence: `task check` PASS (exit 0, 58 items passed), `task ci` PASS (exit 0), `CGO_ENABLED=0 go build ./cmd/pinax` PASS, `openspec validate pinax-agent-memory-runtime --strict` → "Change is valid"; all old contract tests pass.
- [ ] 8.5 Owner: product dogfood；Scope: 真实 Pinax/Ordo/Codex 任务与脱敏 evidence；Dependencies: 8.2、8.4；Lane: final；连续四周记录 recall precision、source resolvability、proposal approval、handoff continue 和 adapter failures；Verification: `pinax agent status --json`、`pinax agent memory proposals --json`、`task integration:agent-memory`；Expected: recall/context ≥80%、source ≥95%、跨 Agent handoff ≥80%、0 silent promotion；Failure re-check: 保留 evidence，回到 context/policy/adapter 对应 task，不提前声明 stable。Progress evidence: 2026-07-28 week-0 真实 vault 10-task cohort 已记录 10/10 continuation、10/10 source resolution、proposal approval 10/10、silent promotion=0，并修复完整 handoff/source semantics；`task integration:agent-continuity:followup` 已为 day 7-14 与后续周观察提供时间门禁 receipt。该证据仍是单一 operator + isolated real-vault tasks，且尚未覆盖连续四周及真实 Ordo/Codex 两端运行，因此本 task 保持未勾选；最早四周观察完成日期为 2026-08-25。
  Status 2026-09-03: [external-gate skipped] — 需连续四周真实 Pinax/Ordo/Codex 双端运行 dogfood。只读取证（2026-09-03）: `pinax agent status --json` 正常返回（当前 data 为空）、`pinax agent memory proposals --json` 返回 `count=0`。指标只能来自真实使用，编码会话不生成。
  Status 2026-09-07: [external-gate skipped] — 只读取证：`pinax agent status --json` 正常返回（data 空）、`pinax agent memory proposals --json` count=0，与 2026-09-03 一致；真实 Pinax/Ordo/Codex 四周 dogfood 分母仍为 0，不可由编码会话产生。
  Status 2026-09-08: [external-gate skipped] — 只读取证与 09-07 一致：`pinax agent status --json` data 空、`pinax agent memory proposals --json` count=0；自 week-0（2026-07-28）以来无新增真实 dogfood，连续四周分母不可用。
- [ ] 8.6 Owner: release/evolution；Scope: capability maturity 和 closeout；Dependencies: 8.5；Lane: final；决定 new runtime 是否 stable-from，明确旧 memory/brain 不进入本 release deprecation；Verification: `openspec status --change pinax-agent-memory-runtime && openspec validate pinax-agent-memory-runtime --strict`；Expected: stable-from 或继续 experimental 的证据明确；Failure re-check: 缺少两个 adapter 或四周 evidence 时保持 experimental，不 archive 为 GA。
  Status 2026-09-03: [external-gate skipped] — stable-from 发布决策依赖 8.5 四周 evidence；按本 task 自身规则，缺证据时保持 experimental、不 archive 为 GA。capability 维持 experimental，旧 memory/brain 不进入 deprecation。

## Closeout 说明（不可通过编码完成）

8.5 与 8.6 是产品验证任务，不是工程任务：
- 8.5 要求**连续四周**真实 Pinax/Ordo/Codex dogfood evidence（recall precision ≥80%、source resolvability ≥95%、跨 Agent handoff ≥80%、0 silent promotion）。这些指标无法通过编写代码或测试生成。
- 8.6 是 stable-from release 决策，明确依赖 8.5 的四周 evidence；任务自身要求"缺少...四周 evidence 时保持 experimental，不 archive 为 GA"。

当前状态：8.1-8.4（含全质量门禁）已完成；code-complete 部分已实现并通过 `task check`/`task ci`/`openspec validate --strict`。capability 保持 **experimental**，旧 memory/brain 不进入 deprecation。8.5/8.6 保持未勾选，待真实 dogfood evidence 产生后由产品/发布 owner 完成。本会话不伪造 evidence。
