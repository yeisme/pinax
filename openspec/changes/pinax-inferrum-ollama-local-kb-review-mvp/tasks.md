## 1. 合同基线与写入边界

- [x] 1.1 冻结 `pinax kb rebuild/search/context/provider doctor` 的 human、JSON、agent、events、error code、v1 request 与 projection metadata 基线；Owner: implementer；Scope: `cmd/pinax/kb_command_test.go`、`internal/semantic/**_test.go`、本 change；Dependencies: none；Verification: focused Go tests先通过现有行为；Expected: 迁移测试能区分兼容输出与内部协议变化；Evidence: `go test ./internal/semantic -run 'Test(KBDomain|Provider|Build)' -count=1` 与 `go test ./cmd/pinax -run 'TestKB' -count=1` 均 exit 0；fixtures 为 `cmd/pinax/kb_command_test.go`、`internal/semantic/*_test.go`。
- [x] 1.2 建立非重叠 path lease，并记录当前 dirty worktree 中本 change 禁止触碰的文件；Owner: root；Scope: Pinax git status、本 change `tasks.md`；Dependencies: none；Verification: `git status --short` 人工对照；Expected: 只有 `internal/semantic`、KB app/CLI/API、相关 tests/docs/specs 和必要 module files 可进入 writer scope；Evidence: lease 摘要见下方；现有非 KB 修改保持 untouched。
- [x] 1.3 先增加 Inferrum v1 migration RED tests，覆盖 `schema_version`、`domain=kb`、table、Record shape、safe metadata allowlist 和禁止历史 Pinax v1 新写入；Owner: implementer；Scope: `internal/semantic` tests；Dependencies: 1.1、1.2；Verification: `go test ./internal/semantic -run 'Test.*(Inferrum|Sidecar|Record)' -count=1`；Expected: 因当前仍写旧 schema/chunks 而确定性失败；Evidence: RED test `TestInferrumV1RebuildUsesCanonicalRecordsAndSafeMetadata` first failed on old schema and logged legacy `chunks/collection`, then passed after the Inferrum v1 adapter.
- [x] 1.4 先增加空 allowed IDs fail-closed RED tests，证明当前搜索若收到空集合不能调用 sidecar；Owner: implementer；Scope: `internal/semantic`、`internal/app` tests；Dependencies: 1.1、1.2；Verification: focused test 使用 fake sidecar 调用计数；Expected: 测试因当前缺少短路或状态区分而失败；Evidence: `TestSearchWithExplicitEmptyAllowedIDsDoesNotInvokeSidecar` first failed on missing `SearchWithAllowedIDs`, then passed with zero sidecar calls.

### Path lease for this change

Owned paths: `internal/semantic/**`; KB-owned files under `internal/app/` and `internal/cli/kb_cmd.go`; bounded KB API files and KB-specific tests/fixtures; `cmd/pinax/kb_command_test.go`; `internal/semantic/*_test.go`; KB-specific `docs/`; `go.mod`/`go.sum` and only the minimum Inferrum dependency entries; this OpenSpec change directory.

Forbidden paths for this change: all existing non-KB dirty files listed by the 2026-08-01 baseline `git status --short`, including agent continuity/memory/sync/backup/research code and tests, shared output/profile/root files unless a KB contract test proves the exact additive change is required, unrelated OpenSpec changes, vendor credentialctl edits, Workbench React source, root repository files, and consumer projects other than Pinax. No reset, checkout, broad formatter, dependency upgrade, service publication, credential access, or production mutation is authorized by this lease.

## 2. Pinax 消费 Inferrum SDK

- [x] 2.1 在 Pinax module 中加入 `github.com/yeisme/inferrum` 依赖和本地开发 replace，确认不引入 CGO、AWS/S3 或 LanceDB Go binding；Owner: implementer；Scope: `go.mod`、`go.sum`/vendor 所需最小路径；Dependencies: 1.3；Verification: `go list -deps ./cmd/pinax`、`CGO_ENABLED=0 go test ./internal/semantic -count=1`；Expected: Inferrum 由 Go import 消费，Python 仍在外部 sidecar；Evidence: `go list -mod=mod -deps ./internal/semantic | grep github.com/yeisme/inferrum` and `CGO_ENABLED=0 go test -mod=mod ./internal/semantic -count=1` passed; local replace is `../inferrum`.
- [x] 2.2 实现 Pinax `KBDomain` 的 `inferrum.Domain` adapter，并将 metadata 从 denylist 改为 safe-field allowlist；Owner: implementer；Scope: `internal/semantic/kbdomain.go` 及 tests；Dependencies: 2.1、1.4；Verification: RED→GREEN focused tests 覆盖未知字段、全文、绝对路径、secret sentinel；Expected: 只保留 spec 定义字段且权限规则仍由 Pinax 拥有；Evidence: `TestKBDomain_Redact` now transforms relative `vault_path` to `source_ref`, drops unknown fields and forbidden payloads; focused semantic suite passed.
- [x] 2.3 实现 Chunk→`inferrum.Record` 映射、批量 embedding 与 model/dimension 一致性检查；Owner: implementer；Scope: `internal/semantic` provider/build mapping；Dependencies: 2.1、2.2；Verification: table-driven tests 先 RED 后 GREEN；Expected: ID/vector/model/dim/indexed_at/safe metadata 完整，混合维度失败；Evidence: `TestNewInferrumProviderUsesSharedRegistry`、`TestBuildChunksRejectsMixedEmbeddingDimensions`、`TestBuildChunksUsesUniqueStableIDsForRepeatedText`、`TestBuildChunksCarriesImportedSourceLineage` 与 `TestInferrumV1RebuildUsesCanonicalRecordsAndSafeMetadata` 通过；不等维向量在 build/record 两层拒绝，重复正文生成稳定唯一 chunk ID，导入笔记的 `source_type/source_version/source_digest` 贯穿 Chunk 到 Inferrum v1 opaque metadata。
- [x] 2.4 将 sidecar doctor/rebuild/search 内部路径切到 `inferrum.SidecarClient`/`VectorStore` 和 `inferrum.sidecar.v1`，保留现有 Pinax stable errors 的 adapter 映射；Owner: implementer；Scope: `internal/semantic/backend.go`、`sidecar.go`、相关 tests；Dependencies: 2.3；Verification: fake Inferrum v1 executable tests；Expected: 新写入不调用历史 Pinax v1，错误不泄露 stderr secret；Evidence: `TestInferrumSidecarRequestUsesV1Protocol`、`TestInferrumV1RebuildUsesCanonicalRecordsAndSafeMetadata` 与 existing Inferrum sidecar adapter passed; writes use `inferrum.VectorStore`/v1 and safe error translation。
- [x] 2.5 在 Pinax search/context 前实现 unresolved/non-empty/explicit-empty 三态 allowed-ID 解析和 fail-closed short-circuit；Owner: implementer；Scope: `internal/semantic`、`internal/app/kb.go` 及 tests；Dependencies: 2.2、2.4；Verification: `CGO_ENABLED=0 go test ./internal/semantic ./internal/app -run 'Test.*Permission' -count=1`；Expected: explicit-empty sidecar 调用数为 0，解析失败不宽搜；Evidence: `TestSearchWithExplicitEmptyAllowedIDsDoesNotInvokeSidecar`、`TestSearchWithResolvedAllowedIDsForwardsOnlyThoseIDs`、`TestSearchWithResolvedAllowedIDsFiltersFakeBackend`、`TestKBSearchExplicitEmptyPermissionIsFailClosed`、`TestKBSearchRejectsUnresolvedPermission` and `TestKBSearchResolvesEmptyPersonalVaultBeforeSidecar` passed; default P0 personal searches now derive stable chunk allow-lists from Pinax notes, while explicit unresolved state remains a stable error.
- [x] 2.6 运行 Pinax KB CLI focused compatibility tests并修复仅由迁移引入的输出差异；Owner: implementer；Scope: `cmd/pinax/kb_command_test.go`、必要 output projection；Dependencies: 2.4、2.5；Verification: `CGO_ENABLED=0 go test ./cmd/pinax -run 'TestKB' -count=1`；Expected: 既有 command/flags/envelope/agent facts 兼容并新增 additive protocol/generation facts；Evidence: focused KB command suite passed after the Inferrum v1 adapter; empty-vault sidecar-unavailable behavior remains stable.

## 3. Generation、激活与回滚

- [x] 3.1 先定义并测试 immutable generation manifest、包含 active+previous+sequence 的单一 activation descriptor、状态枚举和 hash/redaction schema；Owner: implementer；Scope: KB-owned `internal/app/kb_generation.go` 与 tests；Dependencies: 2.3；Verification: `CGO_ENABLED=0 go test ./internal/app -run 'TestKB(GenerationManifest|ActivationDescriptor)' -count=1`；Expected: 不存在第二个权威 previous pointer，manifest/descriptor 不含正文、向量、绝对路径或 secret；Evidence: `TestKBGenerationManifestValidatesSafeImmutableIdentity`、`TestKBActivationDescriptorUsesOneAuthoritativePreviousRef`、`TestKBActivationDescriptorRejectsPreviousWithoutActive` passed; validation rejects unsafe snapshot/dimension/counts and JSON sentinel scan finds no body/vector/secret/second previous pointer.
- [ ] 3.2 实现 staging generation 目录、source snapshot/digest 检查、row-count/dimension/citation validation 和失败保留；Owner: implementer；Scope: `internal/app/kb.go`、KB repository/adapter tests；Dependencies: 3.1、2.4；Verification: fake provider+sidecar component tests覆盖成功、partial embedding、source drift、dimension mismatch；Expected: 失败不触碰 active；Evidence: `TestKBRebuildStagesGenerationWithoutReplacingActivation`、`TestKBRebuildSourceDriftKeepsExistingActivation`、`TestKBRebuildSidecarFailureKeepsExistingActivation`、`TestValidateStagedChunksRejectsUnsafeCitationPaths`、`TestValidateStagedChunksRejectsNonFiniteVectors` passed; full partial-embedding/fault matrix remains in 8.2.
- [ ] 3.3 实现 vault-scoped mutation lock、expected-sequence CAS、同目录临时文件+flush/fsync+atomic rename 的 activation descriptor transition 和 previous rollback，不使用 symlink；Owner: implementer；Scope: KB generation repository/service及 tests；Dependencies: 3.2；Verification: 每个提交边界 interruption、stale sequence、并发 activate/rollback/prune、descriptor snapshot tests先 RED 后 GREEN；Expected: 崩溃只留下完整旧/新状态，并发命令不 last-writer-wins；Evidence: `TestKBActivationCommitUsesSequenceCAS`、`TestKBActivationConcurrentCASAllowsOnlyOneStaleWriter`、`TestKBActivationMixedRollbackCASAllowsOnlyOneStaleWriter`、`TestKBActivationRecoversStaleLockWithoutOverwritingFreshLock`、`TestKBActivationAtomicWriteFailuresPreservePreviousDescriptor`、`TestKBRollbackAtomicWriteFailuresPreservePreviousDescriptor`、`TestKBActivateApplicationAtomicWriteFailuresPreserveDescriptorAndReceipt`、`TestKBRollbackApplicationAtomicWriteFailuresPreserveDescriptorAndReceipt`、`TestRollbackKBActivationSwapsActiveAndPreviousAtomically` passed; broader prune/concurrent-fault matrix remains in 8.2.
- [x] 3.4 增加 disk admission、generation/evidence/model-cache 预算和 previous 有界保留策略；Owner: implementer；Scope: KB service/config/docs/tests；Dependencies: 3.2；Verification: fake disk-stat tests覆盖 high-water、override、generation/evidence/model-cache budget、显式 prune；Expected: 根盘高水位或任一配置预算超限时不开始不可安全完成的 rebuild，prune 不删除 active/previous；Evidence: `TestKBCheckDiskAdmissionRejectsHighWaterWithoutOverride`、`TestKBCheckDiskAdmissionRejectsGenerationBudget`、`TestKBCheckDiskAdmissionRejectsEvidenceBudget`、`TestKBCheckDiskAdmissionRejectsConfiguredModelCacheBudget`、`TestPlanKBGenerationPrunePreservesActivePreviousAndNewest`、`TestKBPruneGenerationsRequiresExplicitConfirmation`、`TestKBPruneGenerationsDeletesOnlyUnprotectedCandidates` 和 `TestKBGenerationPruneCLIIsDryRunByDefault` 通过；`kb generations prune` 默认 dry-run，`--dry-run=false --yes` 才执行删除，损坏 manifest 保留。
- [x] 3.5 扩展 doctor/status/search/context projection，报告 generation id、protocol、provider/exact tag/base digest/resolved model-manifest digest/profile hash/dim、source snapshot、fresh/stale/failed 和 rollback availability；Owner: implementer；Scope: `internal/app`、`internal/output`、CLI contract tests；Dependencies: 3.3；Verification: `CGO_ENABLED=0 go test ./internal/app -run 'TestKBDoctor|TestKBReview|TestKBRebuild|TestKBEvaluate' -count=1` 与 `CGO_ENABLED=0 go test ./cmd/pinax -run 'TestKB' -count=1`；Expected: 历史 Pinax v1 不被标为 Inferrum v1，端口可达不被标为 retrieval ready；Evidence: `fillKBDoctorGenerationFacts` adds redacted active-generation identity/freshness/rollback facts on both success and sidecar failure, `TestKBDoctorProjectsActiveGenerationIdentityAndFreshness` and `TestKBDoctorKeepsGenerationFactsWhenSidecarIsUnavailable` pass, existing search/review/evaluate contracts continue to cover generation metadata, and no `retrieval_ready` fact is inferred.

## 4. Ollama 最小资源真实服务

- [x] 4.1 增加当前服务器开发 runbook 和可重复 canary，固定 Ollama 0.20.5、loopback、单模型/单并发/queue=4/keep_alive=0、CPU affinity/nice，并分别记录 base tag/digest、derived exact tag的resolved model-manifest digest、normalized profile hash和daemon version；Owner: implementer；Scope: Pinax `docs/`、本 change、非 tracked `temp/` runner；Dependencies: 1.2；Verification: `docs/commands/kb-local-mvp.md` runbook commands plus `OLLAMA_HOST=http://127.0.0.1:11434 PINAX_KB_SIDECAR=... CGO_ENABLED=0 go run ./internal/testkit/kblocalevidence --allow-disk-high-water`；Expected: 不用base digest替代derived identity，也不声称 `num_thread` 是跨版本稳定参数；Evidence: latest `temp/integration-test-runs/20260801T202930Z-3712299/summary.json` and `artifacts/canary.json` passed with daemon `0.20.5`, loopback scope, exact derived tag/model-manifest/profile identity, 1024 dimension, real configured-sidecar shadow artifact with missing-projection classification, and 1024-dimension real Ollama path; docs explicitly keep `num_thread` version-gated and distinguish daemon policy from port reachability.
- [x] 4.2 先增加 provider doctor 分层 RED tests，分别覆盖 daemon down、model missing、embed failure、empty vector、dimension mismatch 和 success；Owner: implementer；Scope: KB provider/app/CLI tests；Dependencies: 2.1；Verification: fake Ollama HTTP server；Expected: 现有仅 `/api/tags` 的 readiness 测试失败；Evidence: `TestDoctorProviderForBackendRunsOllamaEmbedCanary` proves `/api/tags` plus a two-input real `/api/embed` canary; daemon-down/model-missing/HTTP failure/empty-vector/mixed-dimension cases classify as `provider_unavailable`/`provider_request_failed`/`embedding_dimension_invalid`/`embedding_dimension_mismatch`; focused semantic and fake-provider CLI doctor tests pass.
- [x] 4.3 实现 exact-model Ollama embed canary 和 active-generation exact tag/resolved model-manifest digest/profile hash/dimension mismatch gate，不持久化 vector 值；Owner: implementer；Scope: Inferrum consumer adapter、KB app/CLI projection；Dependencies: 4.2、3.1；Verification: focused provider/CLI tests覆盖base与derived digest不同；Expected: readiness 至少区分 process/daemon/model/embed/projection/retrieval；Evidence: `internal/semantic/provider_doctor_test.go` identity/dimension tests、`internal/app/kb_permission_test.go` pinned-generation drift gate、最新真实 canary artifact `temp/integration-test-runs/20260801T202930Z-3712299/artifacts/canary.json` 均证明 exact tag、base/resolved/profile、daemon、1024 dimension；vector values未持久化。
- [x] 4.4 建立 Python 3.11/3.12 隔离 venv，安装固定 `lancedb>=0.33,<0.34` 和 workspace `inferrum-lancedb-sidecar`，验证 sidecar doctor/rebuild/search；Owner: root/operator；Scope: user-local runtime或 Pinax temp evidence，不修改系统 Python；Dependencies: 2.4；Verification: sidecar protocol tests与真实 doctor；Expected: sidecar 无端口、每次命令退出；Evidence: `/home/yeshugen/workplace/yeisme-agent/cli/inferrum/temp/local-kb-mvp/venv/bin/python` 为 Python 3.12.13，`lancedb==0.33.0`，`inferrum-lancedb-sidecar` executable 可用；该 venv 运行 sidecar 33 tests（8 skipped）通过，且真实 canary `20260801T183856Z-1449742` 使用同一 executable 完成 doctor/rebuild/search。
- [x] 4.5 用非敏感 canary 在当前服务器跑真实 Ollama embed，分别记录 exact tag、base digest、resolved model-manifest digest、profile hash、1024 dimension、cold/warm duration、CPU/RSS/VRAM、unload 状态和失败分类；Owner: test-engineer；Scope: `temp/integration-test-runs/<run-id>`；Dependencies: 4.1、4.3；Verification: project integration entrypoint；Expected: 证据由 runner 生成且不保存 vector/raw query；Evidence: latest `temp/integration-test-runs/20260801T202930Z-3712299/summary.json` 与 `artifacts/canary.json`；最新真实 Ollama/Inferrum canary 通过真实 rebuild/evaluation/activation/search，并额外记录 legacy Pinax v1 / Inferrum v1 shadow compare、脱敏、empty-permission 及 missing-projection 归一化语义；`quality_verdict=not_proven_without_real_corpus` 仍明确表示合成 canary 不是真实语料质量结论。

## 5. 评测集、指标与 receipts

- [x] 5.1 定义 versioned evaluation suite parser/validator 和安全 expected citation ref，先写 duplicate ID、invalid ref、insufficient dataset RED tests；Owner: implementer；Scope: KB-owned `internal/app/kb_evaluation.go` 及 tests；Dependencies: 3.1；Verification: `CGO_ENABLED=0 go test ./internal/app -run 'TestKBEvaluationSuiteValidatesQuestionIDsAndCitationRefs' -count=1`；Expected: suite invalid时不启动 provider/sidecar；Evidence: `ParseKBEvaluationSuite`、duplicate question-id、absolute citation-ref and small-dataset (`insufficient_dataset`) assertions passed; query text is suite data, not receipt/evidence data.
- [x] 5.2 用 TDD 实现 Recall@K、MRR@K、citation coverage、status counts 和 unavailable latency 语义；Owner: implementer；Scope: KB-owned evaluation metrics；Dependencies: 5.1；Verification: `CGO_ENABLED=0 go test ./internal/app -run 'TestKBEvaluationMetricsComputeRecallMRRAndCitationCoverage' -count=1`；Expected: denominator/K/suite/generation 明确，缺失延迟不写 0；Evidence: deterministic metric test passed with Recall@2=2/3, MRR@2=0.5, citation coverage=0.5 and status counts; latency fields remain absent until stage timing is wired.
- [ ] 5.3 实现 `pinax kb evaluate` CLI/application workflow、stable run id、默认active与显式`--generation <candidate-id>`、unknown-timeout 不自动重跑和 shadow-generation compare；Owner: implementer；Scope: `internal/app`、`internal/cli`/`cmd/pinax`、tests；Dependencies: 5.2、3.2、4.3；Verification: testscript/CLI JSON/agent/events/explain tests先 RED后 GREEN；Expected: candidate评测不改activation descriptor，machine keys保持 English，human 默认中文；Evidence: contract runs。
- 5.3 incremental evidence: `TestKBEvaluateTimeoutPersistsReceiptWithoutRetryingQuestions` injects a 100ms sidecar timeout for 20 questions, persists one failed receipt with 20 timeout statuses/failures, leaves activation unchanged, and proves exactly one sidecar call per question; `TestKBEvaluateCLIOutputModesKeepContract` covers JSON/agent/events/explain projection boundaries; `TestKBEvaluatedCandidateCanActivateAndBecomeDefaultSearchTarget` also evaluates the active generation when `--generation` is omitted; the latest canary now persists a bounded shadow-generation artifact, while broader contract coverage remains open.
- [x] 5.4 实现 Pinax-owned allowlist `PersistedRetrievalReceipt`、candidate identity/gate binding、sanitized manifest refs、redaction和比较投影；Inferrum raw query/permission manifest仅留内存；Owner: implementer；Scope: KB-owned `KBEvaluationReceipt`/receipt repository/tests；Dependencies: 5.3；Verification: `CGO_ENABLED=0 go test ./internal/app -run 'TestKBEvaluation(Receipt|ReceiptStore)|TestActivateKBCandidate' -count=1`；Expected: receipt在服务停止后可读且无全文/secret/vector/raw query/permission list；Evidence: `ValidateKBEvaluationReceiptForCandidate` binds generation/source/model/profile/dim/suite/gate/manifest hashes, `WriteKBEvaluationReceipt` is immutable and readable, and recursive sentinel assertions cover query/permission/vector/secret fields. Full integration-run recursive scan remains part of 5.6/8.2.
- [x] 5.5 将candidate activation gate接到3.3 transition engine，只接受精确匹配 generation/source/model/profile/dimension/suite/gate/manifest 的`passed` receipt；Owner: implementer；Scope: KB generation/evaluation service及 tests；Dependencies: 3.3、5.4；Verification: `CGO_ENABLED=0 go test ./internal/app -run 'TestKBEvaluationGateRejectsIdentityMismatches|TestActivateKBCandidateRequiresMatchingReceiptAndPreservesPrevious' -count=1`；Expected: 任一不匹配不更新sequence/active；Evidence: `ActivateKBCandidate` calls the CAS transition only after exact generation/source/model/derived digest/profile/dimension/suite/gate/manifest checks; table-driven mismatch and wrong-receipt tests pass, and failed activation leaves sequence/active unchanged. CLI evaluate admission and broader interruption/concurrency fault evidence remain in 5.3/8.2.
- [x] 5.6 实现 Pinax project integration evidence runner，失败也写完整 `summary.json`/logs/env/artifacts并保留原 exit code；Owner: implementer；Scope: Pinax现有 Taskfile/test harness、runner tests；Dependencies: 5.5、4.4；Verification: success/failure self-tests；Expected: evidence位于 Pinax `temp/`、`project=cli/pinax`，真实走candidate→Inferrum v1→evaluation→activation decision且不进入Git；Evidence: latest `temp/integration-test-runs/20260801T202930Z-3712299/` 通过合成 3 文档/20 问题真实 Ollama canary，artifact 记录 Inferrum v1、1024 维、exact/base/resolved/profile/daemon/source identity、Recall/MRR/citation、activation/search，并新增真实配置 sidecar 执行的脱敏 `shadow-compare.json`（含 permission-empty 与 missing-projection 归一化语义）；`internal/testkit/evidence` failure self-test 验证 exit code 透传和完整脱敏目录。

## 6. KB Review 服务端 projection 与 Workbench handoff

- [x] 6.1 先增加 Local API/capability RED contract tests，覆盖 overview、sources、suites、question summary、run detail、GET-only 和 bounded payload；Owner: implementer；Scope: Pinax Local API tests/fixtures；Dependencies: 3.5、5.4；Verification: focused HTTP injection tests；Expected: 当前缺少 routes/capabilities 而失败；Evidence: `TestKBReviewEvaluationRoutesAreBoundedGETOnly` first failed with route 404, then passed after the three review route families and capability entries were added; response checks reject absolute vault paths and body fields.
- [x] 6.2 实现 bounded GET projections和capability metadata，不让 handler直接读 vault/LanceDB或复制业务规则；Owner: implementer；Scope: Local API handler、app service projection、tests；Dependencies: 6.1；Verification: API contract tests；Expected: CLI/API facts一致，非 GET 返回稳定 method-not-allowed；Evidence: `CGO_ENABLED=0 go test ./internal/api -run 'TestKBReview|TestHTTPRoutes' -count=1` passed; suites/questions/run projections read Pinax-owned suite/receipt files only, use relative refs, cap questions at 50, and keep P0 routes GET-only.
- [ ] 6.3 增加 truth-state fixtures/tests，覆盖 empty_sources、index_missing/stale、ollama_unreachable、model_missing、sidecar_unavailable、unsupported_source、no_hits、partial、not_generated、passthrough、offline snapshot；Owner: implementer；Scope: API fixtures/output tests；Dependencies: 6.2；Verification: table-driven state projection tests和recursive redaction scan；Expected: 状态不互相替代且禁用原因精确；Evidence: `kb_review_truth_state_test.go` 已覆盖 empty_sources、candidate_only、present_unversioned、stale/failed、sidecar unavailable、unsupported/not_configured、partial/no_hits/insufficient_dataset、not_generated 和 passthrough；provider down/model missing 与 offline cached snapshot 仍由 Workbench connector/browser 证据门闭合，故本项保持未勾选。
- [x] 6.4 创建 `client/yeisme-workbench` 独立实现 handoff，冻结 Pinax module route、projection refs、desktop/mobile wire、keyboard/a11y、Playwright/axe验收和not-owned-here；Owner: root；Scope: Workbench owner OpenSpec，不在本 Pinax writer中实现 React；Dependencies: 6.2、6.3；Verification: Workbench strict OpenSpec；Expected: 无第二套 Pinax Web 壳；Evidence: `/home/yeshugen/workplace/yeisme-agent/client/yeisme-workbench/openspec/changes/workbench-pinax-kb-review-v1/`，`openspec validate workbench-pinax-kb-review-v1 --strict` 通过。

## 7. Source、v1 迁移与运行文档

- [x] 7.1 为 Markdown/纯文本 import 增加 source type/ref/digest/version/acquired_at lineage，并测试 dry-run/confirm 不变；Owner: implementer；Scope: KB import/app/receipt/tests；Dependencies: 3.1；Verification: focused import tests；Expected: canonical Markdown仍是真源，source lineage不泄露绝对路径；Evidence: `internal/app/kb_source_lineage.go`、`kb_import_lineage_test.go`；dry-run 保持源文件不变，confirm 写入 `source_type/source_ref/source_digest/source_version/acquired_at/importer_version`，projection/receipt 不回显外部绝对路径。
- [x] 7.2 为 PDF、网页、Git、飞书、OCR 返回稳定 `unsupported`/`not_configured` source状态和真实下一步，不新增假 upload；Owner: implementer；Scope: source inventory projection/docs/tests；Dependencies: 6.2、7.1；Verification: `CGO_ENABLED=0 go test ./internal/app -run 'TestKBReviewSourceCapabilities|TestKBReviewOverview' -count=1`；Expected: 未实现格式不会进入 ready/indexed/uploaded/synchronized 计数；Evidence: `kbReviewSourceCapabilities` adds copyable Markdown/plain-text import fallbacks for every unsupported or unconfigured type, `TestKBReviewSourceCapabilitiesHaveRealNextActionsWithoutFalseReadiness` verifies stable statuses and next actions, and existing overview/API redaction tests remain green.
- [ ] 7.3 实现 `legacy_v1_readonly` detection/profile、N/N+1/N+2兼容事实和 Inferrum v1 migration next action；Owner: implementer；Scope: KB doctor/generation resolver/tests；Dependencies: 3.3；Verification: legacy-only vault fixtures、注入release identifiers、search/context/doctor read tests及import/rebuild/refresh/activate/rollback/prune全部拒绝测试；Expected: normal rebuild只写 Inferrum v1，历史投影输出有protocol/期限标识且旧 sidecar mutation调用数为0；Evidence: `TestDetectLegacyV1Projection`、`TestSearchLegacyV1ReadonlyUsesLocalProjectionWithoutSidecar`、`TestKBDoctorReportsLegacyV1ReadonlyCompatibility`、`TestKBReviewOverviewReportsLegacyV1WithoutCallingSidecar`、`TestKBSearchRequiresExplicitLegacyV1ReadonlyProfile`、`TestKBRebuildTargetsInferrumV1WhenLegacyProjectionExists` passed; remaining full release-window and mutation refusal matrix stays open.
- 7.3 incremental evidence: `TestKBContextRequiresExplicitLegacyV1ReadonlyProfile` proves context follows the same explicit read-only gate as search; `TestKBRefreshTargetsInferrumV1WhenLegacyProjectionExists` proves refresh creates an Inferrum v1-ready candidate instead of mutating the legacy projection; `TestKBPruneRejectsLegacyOnlyProjection` refuses prune for a legacy-only vault with stable `kb_legacy_v1_readonly`; the broader release-window and import/activate/rollback mutation matrix remains open.
- [x] 7.4 用 legacy v1 / Inferrum v1 shadow compare，核对 document/chunk 数、top-K citation、安全 metadata 和错误语义，不要求 score 字节一致；Owner: test-engineer；Scope: component evidence；Dependencies: 7.3、4.4；Verification: integration runner；Expected: 差异可解释且无 citation 泄漏；Evidence: `TestLegacyV1InferrumV1ShadowCompareDeterministicCorpus` compares the same two-note corpus through legacy read-only rows and Inferrum v1, while latest `temp/integration-test-runs/20260801T202930Z-3712299/artifacts/shadow-compare.json` persists document/chunk counts, bounded citation identity, safe metadata facts, explicit-empty semantics and missing-projection class normalization (`kb_legacy_v1_unavailable`/`kb_sidecar_failed` → `unavailable`); score bytes are intentionally not compared.
- [x] 7.5 更新 Pinax KB命令、个人本地quickstart、Inferrum迁移、Ollama低资源、SSH tunnel、disk quota、故障恢复和历史投影 rollback文档；Owner: implementer；Scope: Pinax `docs/`/README相关最小路径；Dependencies: 4.5、7.3；Verification: `docs/commands/kb.md` 与 `docs/commands/kb-local-mvp.md` 的命令/path smoke、`git diff --check`、`openspec validate --all --strict`；Expected: 示例均为真实命令且不暴露公网；Evidence: quickstart/runbook 现在覆盖 Inferrum v1 candidate/evaluation/CAS、Ollama low-resource profile、`keep_alive=0`、loopback-only API、SSH `-L` tunnel、disk budget/prune、故障恢复顺序和 `legacy_v1_readonly`→Inferrum v1 rollback；未接入格式仍给真实 import fallback，不提供假 upload。

## 8. 真实语料、故障注入与最终门禁

- [ ] 8.1 用户提供50–200份真实/脱敏Markdown或纯文本和20–50个问题/期望citation后，跑真实candidate rebuild/evaluation并决定是否激活；Owner: root+test-engineer；Scope: user-local vault和Pinax temp evidence；Dependencies: 4.5、5.6、7.4；Verification: Pinax component/system runner；Expected: 记录Recall@5、MRR@10、citation coverage、latency、disk/RSS并按matching receipt门决策；Evidence: summary和artifact refs，不提交语料。
- [ ] 8.2 注入Ollama down、model missing、sidecar missing、base/derived digest mismatch、dimension mismatch、disk high-water、source drift、staging failure、allowed IDs空、timeout unknown、missing/stale candidate receipt，以及activation每个fsync/rename边界和并发previous rollback；Owner: test-engineer；Scope: real/fake component evidence；Dependencies: 8.1；Verification: fault suite；Expected: 每项fail closed、保留完整旧/新descriptor、写完整失败证据且不自动重试未知run；Evidence: failure classification matrix。
- 8.2 incremental evidence: `TestKBRebuildSidecarFaultsFailClosedWithoutReplacingActivation` covers missing-sidecar and timeout faults, preserves the existing active descriptor, and requires a bounded failure receipt with the stable code; `TestKBRollbackAtomicWriteFailuresPreservePreviousDescriptor` plus `TestKBActivateApplicationAtomicWriteFailuresPreserveDescriptorAndReceipt` and `TestKBRollbackApplicationAtomicWriteFailuresPreserveDescriptorAndReceipt` cover rollback/activation write/sync/close/rename boundaries without mutation receipts, while `TestKBActivationMixedRollbackCASAllowsOnlyOneStaleWriter` covers one mixed stale writer; the persisted shadow artifact also records explicit-empty permission behavior; the broader provider/identity/dimension/prune matrix remains open.
- [ ] 8.3 运行Pinax focused/full tests、build、lint/fmt、sidecar protocol和strict OpenSpec；Owner: test-engineer；Scope: stable diff；Dependencies: 2–7全部实现项；Verification: `task check` 与 `openspec validate --all --strict`；Expected: exit 0，或准确分类pre-existing/concurrent/environment失败；Evidence: focused KB/app/semantic/API/CLI tests、CGO=0 build、`git diff --check`、gofmt and `openspec validate --all --strict` pass; full `CGO_ENABLED=0 go test ./... -count=1` still has only the unrelated existing `TestPublishBuildGitHubPagesRealHugoSmokeWhenAvailable` failure (`title: Tag: pages` YAML mapping error), with all other packages passing.
- [ ] 8.4 在稳定diff上并行进行correctness、security/redaction和performance review，任何permission/body/secret泄漏为发布veto；Owner: reviewer+security-reviewer+performance-reviewer；Scope: read-only stable diff和evidence；Dependencies: 8.2、8.3；Verification: review envelopes；Expected: P0/P1问题修复或明确阻塞，不审查moving diff；Evidence: findings/resolution。
- [ ] 8.5 完成WorkBench独立实现后运行375/768/1024/1440响应式、键盘、axe、network/console和truth-state browser smoke；Owner: browser-debugger；Scope: Workbench local/test target；Dependencies: 6.4和Workbench实现；Verification: Playwright/axe screenshots/traces；Expected: 无serious/critical a11y、无水平溢出、无假成功；Evidence: Workbench项目temp，不写Pinax temp。
- [ ] 8.6 更新本change tasks evidence、兼容表、资源基线、未支持格式和剩余风险；只有Pinax代码、真实链路与必要Web handoff均有证据时才决定归档；Owner: root；Scope: 本change；Dependencies: 8.3、8.4；Verification: `openspec validate pinax-inferrum-ollama-local-kb-review-mvp --strict`；Expected: 未完成项保持未勾选，不把spec/daemon/HTTP 200当实现或检索成功；Evidence: final status。
