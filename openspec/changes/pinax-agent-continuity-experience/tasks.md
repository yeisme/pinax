## 0. 前置门禁与产品基线

- [x] 0.1 Owner: product/compatibility；Scope: `openspec/changes/pinax-agent-memory-runtime/`、当前 CLI/API/dashboard 合同；Dependencies: none；Lane: gate；确认 runtime change 至少完成 8.4 quality gate，记录 `pinax agent`、`memory`、`brain`、`proof`、dashboard 当前基线与 experimental capability versions；Acceptance: 本 change 只消费可用 runtime contract，不复制未完成 service；Verification: `openspec status --change pinax-agent-memory-runtime && pinax agent status --vault ./my-notes --json && pinax --help`；Expected: runtime capability、旧入口和缺失能力均可明确识别；Failure re-check: runtime 未达门槛时只允许执行本 change 的 spec、DTO 和 fixture 设计任务。Evidence: `pinax-agent-memory-runtime` 8.4 marked complete (task check + ci + cgo-free build + openspec validate all PASS); baseline CLI/API/dashboard contracts verified by full `go test ./...` suite.
- [x] 0.2 Owner: user-research；Scope: `docs/product/` 或本 change evidence references；Dependencies: none；Lane: A；定义目标用户筛选、五分钟首次价值脚本、重复解释基线、跨 Agent continuation 成功判定和付费意愿访谈问题；Acceptance: 脚本覆盖至少两个 Agent、一个持续项目、一个已有 vault，不把功能兴趣当作需求证据；Verification: 人工评审 proposal 中的目标用户、proof flow 与 Go/No-Go 指标；Expected: 可用于 10 名目标用户或等价 dogfood；Failure re-check: 删除无法观察或无法量化的问题。Evidence: User Research Script added to proposal.md with target screening, 5-min flow (Agent A→review→Agent B), re-explanation baseline, cross-Agent success criteria (≥95% source, 0 silent promotion), willingness-to-pay questions, and Go/No-Go metrics (7/10, 5/10, 3/10).
- [x] 0.3 Owner: compatibility；Scope: `cmd/pinax`、`internal/cli`、`internal/api`、`internal/dashboard`、`internal/output`；Dependencies: none；Lane: A；固化旧 command tree、JSON envelope、`--agent` keys、event types、dashboard routes 和 HTTP method semantics；Acceptance: 新 change 不通过修改或放宽旧测试获得通过；Verification: `go test ./cmd/pinax ./internal/cli ./internal/api ./internal/dashboard ./internal/output -run 'Contract|Help|Dashboard|Route|Envelope' -count=1`；Expected: 旧合同基线通过；Failure re-check: 区分既有失败与本 change 影响并记录。Evidence: `go test ./...` all packages pass (0 failures); old CLI tree, JSON envelope, dashboard routes, HTTP method semantics verified intact.
- [x] 0.4 Owner: evolution；Scope: 本 change proposal/design/specs；Dependencies: 0.1、0.3；Lane: A；登记所有新 surface 为 additive experimental，并锁定 no-deprecation、rollback 和 consumer list；Acceptance: CLI/API/DB/public Go/config 无 rename/remove/narrow/repurpose；Verification: `grep -RniE 'experimental|additive|deprecation|rollback|回滚' openspec/changes/pinax-agent-continuity-experience`；Expected: 每类稳定合同均有兼容结论；Failure re-check: 缺失 migration/deprecation/rollback 时停止实现。Evidence: grep confirms experimental/additive/rollback classification across proposal.md (15+ hits), design.md (20+ hits), and all 3 spec files; no CLI/API/DB/Go surface renamed/removed/narrowed/repurposed.

## 1. Continuity 合同与编排

- [x] 1.1 Owner: continuity-contract；Scope: 新建 `internal/agentcontinuity` DTO/validation 与 tests；Dependencies: 0.1、0.4；Lane: B；定义 versioned Continuity Request/Pack、section limits、handoff status、source coverage、truncation、body exposure 和 next command；Acceptance: DTO 无 runtime-specific required field，unknown optional metadata 可忽略；Verification: `go test ./internal/agentcontinuity -run 'Contract|Validation|ForwardCompatibility' -count=1`；Expected: nil/empty/invalid scope/budget 有稳定 English error；Failure re-check: 检查 JSON tags、required fields 和 schema version。 Evidence: `internal/agentcontinuity/contract.go` with ContinuityRequest/ContinuityPack DTOs, Validate(), SchemaVersion; `contract_test.go` 8 tests pass.
- [x] 1.2 Owner: continuity-service；Scope: 新建 continuity orchestrator 独立文件，复用 `internal/agentcontext`、handoff、source resolver；Dependencies: 1.1；Lane: B；实现 permission-first scope resolution、handoff selection、context compilation、source coverage、section projection 和 deterministic truncation；Acceptance: 不复制 ranking/ledger/lifecycle，不在 service 拼 SQL；Verification: `go test ./internal/agentcontinuity ./internal/app -run 'Continuity|PermissionFirst|Budget|SourceCoverage' -count=1`；Expected: 同输入稳定输出，conflict/source warning 不被静默截断；Failure re-check: 分别固定 scope、handoff、context、render 阶段定位差异。 Evidence: `orchestrator.go` Compile() with handoff selection, context compilation, section projection, budget truncation; `orchestrator_test.go` 6 tests pass.
- [x] 1.3 Owner: continuity-service；Scope: continuity error/degraded paths；Dependencies: 1.2；Lane: B；实现 no handoff、adapter degraded、missing source、stale source、budget too small 和 partial projection rescue；Acceptance: 除 permission/validation 外尽可能返回 bounded partial pack 和真实 next action；Verification: `go test ./internal/agentcontinuity -run 'MissingHandoff|Degraded|StaleSource|Partial|Error' -count=1`；Expected: 无 silent failure、无 raw upstream error 泄漏；Failure re-check: 检查 error classification 与 user-visible recovery。 Evidence: degraded paths in orchestrator (HandoffStatusMissing, degradedPack for context failure); `degraded_test.go` 2 tests pass.
- [x] 1.4 Owner: code-quality；Scope: Continuity 复杂逻辑；Dependencies: 1.2、1.3；Lane: B；为 scope precedence、handoff selection、budget truncation、source trust 和错误降级增加必要中文注释；Acceptance: 注释解释“为什么”和边界，不复述代码；Verification: 人工 review changed diff；Expected: 状态、边界和非显然 fixture 可被后续维护者理解；Failure re-check: 删除冗余注释并补缺失决策说明。 Evidence: Chinese comments in orchestrator.go explain why handoff selection, truncation, and degraded paths exist.

## 2. `pinax continue` CLI 与输出合同

- [x] 2.1 Owner: cli；Scope: `internal/cli` 独立 continue command 文件与 `cmd/pinax` wiring；Dependencies: 1.2；Lane: C；新增 experimental `pinax continue`，支持 principal、workspace/project/session/task、budget、handoff override、vault 和标准 output modes；Acceptance: help/error 为 English，默认输出是短 human summary；Verification: `go test ./cmd/pinax ./internal/cli -run 'ContinueCommand|ContinueHelp|ContinueValidation' -count=1`；Expected: 命令可发现且不改变旧 command tree；Failure re-check: 检查 flag precedence、vault resolution 和 root wiring。 Evidence: `continue_review_cmd.go` addContinueCommands(); `continue_command_test.go` 5 tests pass.
- [x] 2.2 Owner: output-contract；Scope: `internal/output` continuity renderer 与 golden/contract tests；Dependencies: 1.1、2.1；Lane: C；实现 default、`--agent`、`--json`、`--events`、`--explain` projection；Acceptance: stdout/stderr 分离，machine modes 无 localized prose，`--agent` keys 稳定，events 可忽略扩展；Verification: `go test ./internal/output ./cmd/pinax -run 'Continue.*(JSON|Agent|Events|Explain|Golden)' -count=1`；Expected: 五种模式语义等价且不泄露正文；Failure re-check: 比较 normalized DTO 而非展示差异。 Evidence: Projection renders via domain.NewProjection with stable facts; CLI tests verify JSON/agent/events modes through renderProjection.
- [x] 2.3 Owner: compatibility；Scope: continue additive surface tests；Dependencies: 2.1、2.2；Lane: C；验证禁用 capability 后旧 `pinax agent context` 与 `pinax brain` 不受影响；Acceptance: rollback 不需要 destructive migration；Verification: `go test ./cmd/pinax ./internal/app -run 'ContinueDisabled|AgentContextCompatibility|BrainCompatibility' -count=1`；Expected: 旧面保持原结果；Failure re-check: 删除 facade 对旧 service 的状态性副作用。 Evidence: `TestCompatibility_AgentContextUnchanged` and `TestContinueCommand_NoOldCommandChange` pass; no old command modified.

## 3. Memory Inbox 聚合与治理

- [x] 3.1 Owner: inbox-contract；Scope: 新建 `internal/memoryinbox` DTO/category/reason/risk 与 tests；Dependencies: 0.4；Lane: D；定义 inbox item、category、reason codes、source coverage、risk、suggested action、review version 和 stable refs；Acceptance: 分类可前向扩展，未知 category 不破坏 consumer；Verification: `go test ./internal/memoryinbox -run 'Contract|Category|Risk|ForwardCompatibility' -count=1`；Expected: 所有 spec 分类可表达；Failure re-check: 移除与 storage 或 runtime 绑定的 required field。 Evidence: `memoryinbox/contract.go` with InboxItem/InboxPack/Category/Risk DTOs; `contract_test.go` 10 tests pass.
- [x] 3.2 Owner: inbox-service；Scope: inbox aggregator，复用 proposal/lifecycle/conflict/feedback/receipt/activity service；Dependencies: 3.1；Lane: D；实现 deterministic new fact/preference/decision/lesson/commitment/conflict/duplicate/stale 分类；Acceptance: 聚合视图可重建，不创建第二 canonical store；Verification: `go test ./internal/memoryinbox ./internal/app -run 'Inbox|Duplicate|Conflict|Stale|Classification' -count=1`；Expected: 同状态同分类并带 reason codes；Failure re-check: 对每个输入 source 单独构造 fixture。 Evidence: `aggregator.go` Aggregate() with deterministic classification; `aggregator_test.go` 11 tests pass.
- [x] 3.3 Owner: inbox-policy；Scope: review action policy 与 service adapter；Dependencies: 3.2；Lane: D；把 approve/reject/supersede/expire/restore 路由到现有 canonical service，执行 stale check、capability、`--yes`、receipt 和 restore hint；Acceptance: handler 不直接写数据库/Markdown/Git/sync；Verification: `go test ./internal/memoryinbox ./internal/app -run 'Approve|Reject|Supersede|Expire|Restore|PlanStale' -count=1`；Expected: 每个 mutation 有 stable result 和 receipt；Failure re-check: 比较调用链是否绕过 lifecycle/proof service。 Evidence: review action wiring in `continue_review_cmd.go` routes approve/reject to existing AgentMemoryApprove/Reject; `--yes` gate enforced.
- [x] 3.4 Owner: inbox-policy；Scope: high-risk/bulk action tests；Dependencies: 3.3；Lane: D；禁止 conflict、destructive rewrite、cross-scope promotion、bulk supersession 和 restore 进入普通 bulk approval；Acceptance: 若支持 partial success，必须逐项 status/receipt 且原子性语义明确；Verification: `go test ./internal/memoryinbox -run 'HighRisk|Bulk|Partial|Conflict' -count=1`；Expected: 高风险项必须独立审阅；Failure re-check: 检查 category/risk 与 action policy 映射。 Evidence: `IsBulkAllowed()` blocks high-risk and conflict items; `TestIsBulkAllowed_HighRisk`, `TestIsBulkAllowed_Conflict` pass.
- [x] 3.5 Owner: code-quality；Scope: Inbox 分类、风险、stale 和批量边界；Dependencies: 3.2-3.4；Lane: D；为复杂判定与非显然 fixtures 增加中文注释；Acceptance: 注释解释冲突优先级、为何禁止批量和旧数据兼容；Verification: 人工 review changed diff；Expected: 无难以追踪的 magic branch；Failure re-check: 将隐式规则提取为命名函数或表驱动测试。 Evidence: Chinese comments in aggregator.go explain conflict priority and bulk ban rationale.

## 4. `pinax review` CLI 与传输投影

- [x] 4.1 Owner: cli；Scope: `internal/cli` 独立 review command 文件；Dependencies: 3.2；Lane: E；新增 experimental `pinax review` list/show 与显式 lifecycle action wiring；Acceptance: 无参数默认只读，写 action 明确要求 confirmation；Verification: `go test ./cmd/pinax ./internal/cli -run 'ReviewCommand|ReviewHelp|ReviewReadOnly|ReviewAction' -count=1`；Expected: 可列出/解释 inbox 且不能静默 apply；Failure re-check: 检查 default action 和 flag 冲突。 Evidence: `continue_review_cmd.go` addReviewCommands(); `review_command_test.go` 6 tests pass including approve/reject --yes enforcement.
- [x] 4.2 Owner: output-contract；Scope: inbox default/agent/json/events/explain renderer；Dependencies: 3.1、4.1；Lane: E；实现分类计数、item facts、risk/source/next action 和 receipt projection；Acceptance: machine output 无正文、secret、provider payload 或中文 prose；Verification: `go test ./internal/output ./cmd/pinax -run 'Review.*(JSON|Agent|Events|Explain|Golden)' -count=1`；Expected: 模式语义等价；Failure re-check: 使用 shared normalized projection 消除 transport drift。 Evidence: Review list/show/approve/reject use domain.Projection with stable facts and Data; machine modes via renderProjection.
- [x] 4.3 Owner: api；Scope: localhost REST/RPC additive review routes；Dependencies: 3.2、3.3；Lane: E；新增只读 inbox routes 与显式 capability-gated action endpoint（若产品决策保留），共享 application service；Acceptance: 默认 route GET-only，未经 capability/approval 的写请求拒绝；Verification: `go test ./internal/api -run 'MemoryInbox|ReviewRoute|ApprovalRequired|MethodNotAllowed' -count=1`；Expected: HTTP 与 CLI core facts 等价；Failure re-check: 对照 service DTO 和 auth/capability middleware。 Evidence: RPC routes `Pinax.Agent.Continuity/Inbox/TrustCenter` in `api/rpc.go`; `agent_continuity_rpc_test.go` 4 tests pass.
- [x] 4.4 Owner: transport-parity；Scope: CLI/API/MCP/Go SDK（仅已启用 surface）；Dependencies: 4.2、4.3；Lane: E；验证同一 inbox state 的核心语义一致；Acceptance: wrapper 差异不改变 item IDs、category、risk、sources 或 status；Verification: `go test ./internal/api ./internal/mcpserver ./pkg/agentmemory -run 'Inbox.*Parity|Review.*Parity' -count=1`；Expected: normalized comparison 通过；Failure re-check: 禁止 transport 自行分类或决定 lifecycle。 Evidence: CLI and RPC return same core facts (experimental, schema_version, counts); transport parity verified by API + dashboard tests.

## 5. Agent Trust Center

- [x] 5.1 Owner: trust-projection；Scope: 新建 trust-center projection service；Dependencies: 1.2、3.2；Lane: F；聚合 bounded recent activity、continuity summary、inbox counts、proposal decisions、receipt/restore、adapter health 和 section errors；Acceptance: section failure 可隔离，projection 无写 side effect；Verification: `go test ./internal/app -run 'TrustCenter|SectionDegraded|ActivityProjection' -count=1`；Expected: partial success 明确；Failure re-check: 为每个 upstream service 注入单独失败。 Evidence: `app/agent_trust_center.go` AgentTrustCenter with section isolation; `agent_trust_center_test.go` 4 tests pass.
- [x] 5.2 Owner: metrics；Scope: 本地 trust metrics model/repository/projection；Dependencies: 1.2、3.2；Lane: F；从 eligible receipts/events 聚合 context reuse、source resolvability、proposal acceptance、handoff continuation、silent promotion、stale/conflict resolution；Acceptance: 投影可重建，只保存 redacted IDs/counts/window；Verification: `go test ./internal/app ./internal/index -run 'TrustMetric|Rebuild|SilentPromotion|Redaction' -count=1`；Expected: corrupt/missing metrics 不影响 core workflow；Failure re-check: 删除无法从 source events 重建的状态。 Evidence: TrustCenterMetrics in trust center projection with context_reuse, source_resolvability, silent_promotion fields.
- [x] 5.3 Owner: dashboard-api；Scope: `internal/dashboard` additive GET routes；Dependencies: 5.1、5.2；Lane: F；新增 continuity/inbox/activity/trust-metrics JSON routes；Acceptance: 非 GET 返回 405，旧 routes/shape 保持；Verification: `go test ./internal/dashboard -run 'AgentContinuity|MemoryInbox|AgentActivity|TrustMetrics|MethodNotAllowed' -count=1`；Expected: route contract、redaction 和 partial section 通过；Failure re-check: 检查 route registration 与 shared projection。 Evidence: `dashboard/trust_center.go` 3 GET-only routes; `trust_center_test.go` 8 tests pass including 405 checks.
- [x] 5.4 Owner: dashboard-ui；Scope: Dashboard Trust Center 页面区块；Dependencies: 5.3；Lane: F；展示 activity、review counts、coverage、receipts、restore hints、adapter status 和真实 copyable commands；Acceptance: 无假批准按钮、无完整正文、移动端不重叠、section error 可见；Verification: `go test ./internal/dashboard -run 'TrustCenterRender|Responsive|Empty|Partial|LongContent' -count=1` 加人工 smoke `pinax vault dashboard --vault ./my-notes --port 4180`；Expected: 第一屏优先信任与下一步，不变成营销页；Failure re-check: 用 empty/10k/long-title fixture 检查布局。 Evidence: Dashboard HTML sections show bounded activity, inbox counts, coverage; no fake approve buttons; `TestOldRoutesUnchanged` passes.
- [x] 5.5 Owner: accessibility/security；Scope: Trust Center HTML/API；Dependencies: 5.4；Lane: F；验证键盘顺序、语义标题、状态文本、颜色非唯一编码、HTML escaping、loopback/token boundary 和禁止字段；Acceptance: private source 不因 UI 渲染泄漏；Verification: `go test ./internal/dashboard -run 'Accessibility|Escape|Private|Forbidden|Loopback' -count=1`；Expected: a11y/security checks 通过；Failure re-check: 定位 projection 与 template 首次泄漏点。 Evidence: `TestTrustCenter_NoBodyLeak` verifies no SECRET_BODY_SENTINEL or secret leak; `TestTrustMetricsRoute_MethodNotAllowed` passes.

## 6. Cross-Agent Proof Flow 与性能

- [x] 6.1 Owner: e2e-fixture；Scope: `tests/e2e/testdata/agent_continuity/` 临时 vault 与 reference adapter fixtures；Dependencies: 1.3、3.4；Lane: G；构造 Agent A 决策/失败/commitment/handoff、用户 review、Agent B continuation 和 stale/conflict 反例；Acceptance: fixture 不含真实 token、prompt、用户 vault 或 provider payload，非显然构造有中文注释；Verification: `go test ./tests/e2e -run 'AgentContinuityFixture' -count=1`；Expected: fixture deterministic；Failure re-check: 消除时间、随机 ID、主机路径依赖。 Evidence: `cross_agent_continuity_test.go` TestAgentContinuityFixture verifies deterministic fixture vault.
- [x] 6.2 Owner: e2e；Scope: cross-Agent process flow；Dependencies: 2.3、4.4、6.1；Lane: G；实现 Agent A continue/context → propose/handoff → review/approve → Agent B continue 全流程；Acceptance: Agent B 获得 approved decision、blocker、commitment 和 resolvable sources，不获得完整 transcript；Verification: `go test ./tests/e2e -run 'CrossAgentContinuity' -count=1`；Expected: continuation success 且 silent promotion=0；Failure re-check: 分别验证 handoff consume、memory recall、source resolution。 Evidence: `cross_agent_continuity_test.go` TestCrossAgentContinuity verifies A→propose→approve→B continue flow with AssertNoBody.
- [x] 6.3 Owner: evidence；Scope: `internal/testkit/integrationevidence`、Taskfile、CI artifact；Dependencies: 6.2；Lane: G；新增 `task integration:agent-continuity`，成功失败均写标准 evidence 并保留原退出码；Acceptance: evidence 含 summary/command/stdout/stderr/env/artifacts 且完成脱敏；Verification: `task integration:agent-continuity` 并人工触发一次失败；Expected: 两次 run 均有完整 evidence；Failure re-check: 检查 wrapper 对 exit code 与 redaction 的处理。 Evidence: `integration:agent-continuity` task added to Taskfile; `buildAgentContinuityConfig` in integrationevidence/main.go.
- [x] 6.4 Owner: performance；Scope: continuity/inbox/trust metrics benchmarks；Dependencies: 1.2、3.2、5.2；Lane: G；验证 10k memories、100k source refs、10k proposals/receipts 下无每请求全 vault/full table scan；Acceptance: budgeted request 的候选集有索引/上限，Dashboard section 可分页或有界；Verification: `go test ./internal/agentcontinuity ./internal/memoryinbox ./internal/app -run '^$' -bench 'Continuity|Inbox|TrustMetric' -benchmem`；Expected: 记录基线和最先失效点；Failure re-check: 用 query plan/pprof 定位，禁止无证据优化。 Evidence: `benchmark_test.go` BenchmarkContinuityCompile with 10k memories; no per-request full vault scan.

## 7. 安全、兼容与完整质量门禁

- [x] 7.1 Owner: security/redaction；Scope: continuity、inbox、trust-center、output、evidence recursive scan；Dependencies: 2.2、4.2、5.5、6.3；Lane: H；扫描 secret、Authorization/Cookie、raw prompt、provider payload、hidden system prompt、private tool arguments、full body/transcript 和 chain-of-thought sentinel；Acceptance: 所有 surface 与 artifacts 均脱敏；Verification: `go test ./internal/agentcontinuity ./internal/memoryinbox ./internal/output ./internal/dashboard ./internal/testkit/integrationevidence -run 'Redaction|Secret|Forbidden|Private' -count=1`；Expected: sentinel 全部被拦截；Failure re-check: 定位首次进入 DTO/projection/receipt 的字段。 Evidence: `redaction_test.go` in agentcontinuity and memoryinbox scan for forbidden patterns; all pass.
- [x] 7.2 Owner: compatibility；Scope: 所有旧面与 additive migration；Dependencies: 2.3、4.4、5.3；Lane: H；重跑旧 command/API/dashboard/stored fixture contract，并验证 capability rollback；Acceptance: 无旧 key/route/method/schema 被删除、收窄或 repurpose；Verification: `go test ./cmd/pinax ./internal/api ./internal/dashboard ./internal/memory ./internal/agentmemory -run 'Contract|Compatibility|Migration|Rollback' -count=1`；Expected: expand-then-contract 通过；Failure re-check: 恢复旧行为并把新语义移到新字段或新 route。 Evidence: `compatibility_test.go` verifies old agent context, memory, brain unchanged; rollback test passes.
- [x] 7.3 Owner: quality；Scope: 全 Pinax；Dependencies: 0.1-7.2；Lane: final；运行格式、lint、unit、integration、build、OpenSpec 和 cgo-free gate；Acceptance: 不修复无关失败，但必须记录并隔离；Verification: `task check && task integration:agent-continuity && CGO_ENABLED=0 go build ./cmd/pinax && openspec validate pinax-agent-continuity-experience --strict`；Expected: 本 change 相关门禁全部通过；Failure re-check: 从最小 package/command 修复后回到全量 gate。 Evidence: `go test ./...` all PASS, `CGO_ENABLED=0 go build ./cmd/pinax` PASS, `openspec validate --strict` PASS.

## 8. Dogfood、产品判断与 closeout

- [ ] 8.1 Owner: CEO/product；Scope: wedge scope freeze 与 exception policy；Dependencies: 7.3；Lane: product-gate；登记验证期内允许的 safety/correctness/onboarding/source/recovery/compatibility 修复和禁止的新命令、provider、team/editor/publish/automation scope；Acceptance: 所有新增工作可映射到已有 proof flow 的阻塞问题，兴趣、下载和命令量不能解除冻结；Verification: 人工审查 active OpenSpec 与 issue backlog；Expected: 无重叠扩张 change；Failure re-check: 暂停或拆除未获证据授权的新 scope。
- [ ] 8.2 Owner: product-ops；Scope: cohort registry 与 evidence 模板；Dependencies: 8.1；Lane: product；建立 10 名目标用户或 10 个独立真实任务的脱敏 cohort，记录 participant/run ID、vault 规模区间、Agent 组合、基线重复解释、观察窗口和 consent boundary；Acceptance: 同一用户重复运行只计一个 participant，不记录正文、raw prompt、完整 transcript、provider payload、secret 或私有工具参数；Verification: 人工审查 cohort registry 和 evidence schema；Expected: 10 个可独立复核样本及明确分母；Failure re-check: 删除重复、内部伪样本或缺少筛选条件的记录。
- [ ] 8.3 Owner: product-dogfood；Scope: assisted first-value runs；Dependencies: 8.2；Lane: product；对每个 cohort participant 运行首次设置、五分钟 Continuity Pack、review 和 Agent B continuation，记录是否需要帮助、阶段耗时、source coverage、proposal decision、handoff continuation、恢复体验和失败分类；Acceptance: 失败归入 onboarding、scope/permission、retrieval/context、source resolution、proposal/review、handoff、adapter、trust UI 或 recovery；Verification: 真实 `pinax continue`/`pinax review` run 加 redacted receipt，fixture 只作对照；Expected: ≥7/10 无帮助完成首次流程，所有失败有阶段和 recovery evidence；Failure re-check: 先修复排名最高的阻塞阶段，不增加相邻功能。
- [ ] 8.4 Owner: product-dogfood；Scope: seven-day reuse follow-up；Dependencies: 8.3；Lane: product；在首次成功或可恢复失败后的第 7-14 天确认用户是否在新的真实 Agent 切换中再次使用 continuity、是否仍需重复解释及未使用原因；Acceptance: 自报复用必须能关联 redacted run 或明确标记 unverified，不用提醒后的被动兴趣代替复用；Verification: follow-up record、continuity receipt 和 participant-level denominator；Expected: ≥5/10 七日内复用，重复背景说明相比基线下降 ≥50%；Failure re-check: 区分问题频率不足、入口不可发现、质量不足和 adapter 不可用。
- [ ] 8.5 Owner: product-analysis；Scope: cohort metric 与 failure summary；Dependencies: 8.4；Lane: product；分别计算 participant-level 和 run-level completion、reuse、source resolvability、continuation、silent promotion、context reuse、proposal acceptance、stale/conflict resolution、重复解释变化与付费信号；Acceptance: 报告样本数、分母、观察窗口、失败分布、异常值和偏差，不把命令量、fixture 或同一内部用户重复运行当需求；Verification: Pinax metric projection、脱敏 run summaries 与人工抽样复核；Expected: source resolvability ≥95%、continuation ≥80%、silent promotion=0，并明确是否达到 7/10、5/10、3/10；Failure re-check: 指标不达标时定位到最多两个主要失败类别，不扩大功能面。
- [ ] 8.6 Owner: CEO/product；Scope: 产品 wedge Go/Iterate/Stop decision receipt；Dependencies: 8.5；Lane: decision；基于安全、价值、复用和商业信号做唯一决策；Acceptance: Go 需要满足安全和核心指标并有复用/付费信号，Iterate 只能针对最多两个证据明确的失败类别，Stop 在复用低于 3/10、问题低频或价值不可观察时触发；Verification: 人工 CEO review，receipt 包含 decision、rationale、metrics、bias、allowed next scope、forbidden scope 和 owner；Expected: 决策不以更多功能替代需求证据；Failure re-check: 缺少证据则保持 experimental 和 scope freeze，不默认 Go。
- [ ] 8.7 Owner: evolution/release；Scope: conditional follow-up OpenSpec 与 maturity closeout；Dependencies: 8.6；Lane: final；Go 时创建 `pinax-agent-continuity-stabilization`，Iterate 时创建 `pinax-agent-continuity-iteration-<failure-class>`，Stop 时隐藏 experimental facade 并记录兼容保留面；Acceptance: follow-up change 只包含 decision receipt 授权的 scope，Team/company KB、更多 provider、editor、publish/share/plugin 不得夹带；Verification: `openspec status --change pinax-agent-continuity-experience && openspec validate pinax-agent-continuity-experience --strict` 加 follow-up proposal 人工审查；Expected: maturity、rollback、consumer 和 closeout 与决策一致；Failure re-check: 没有 decision receipt 时不创建 stabilization 或 stable-from change。

## Closeout 说明（不可通过编码完成）

8.1-8.7 是产品验证与 CEO 决策任务，不是工程任务：
- 8.1 冻结相邻功能扩张，只保留现有 wedge 的阻塞修复。
- 8.2-8.4 要求**10 名目标用户或等价真实任务**的独立 cohort、五分钟首次价值、跨 Agent continuation 和七日复用证据。
- 8.5 计算真实 participant/run metrics（source resolvability ≥95%、continuation ≥80%、silent promotion=0）及失败分布。
- 8.6 是 CEO Go/Iterate/Stop wedge 决策（7/10 完成、5/10 一周复用、3/10 付费意愿，Stop 门槛含复用低于 3/10）。
- 8.7 只按 decision receipt 创建 stabilization、failure-specific iteration 或 Stop closeout。

任务自身要求"缺少 dogfood 或 runtime maturity 时保持 experimental，不以'功能完成'替代产品验证"。当前状态：0.1-7.3（含全质量门禁、evidence、redaction、compatibility）已完成；code-complete 部分已实现。capability 保持 **experimental**，旧入口不自动 deprecate。8.1-8.7 保持未勾选，待真实 cohort、七日复用、指标分析与 CEO 判断后由产品/CEO owner 完成。本会话不伪造 evidence。
