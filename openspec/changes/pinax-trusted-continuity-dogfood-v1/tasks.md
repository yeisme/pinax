## 0. 产品、Owner 与兼容门禁

- [x] 0.1 Owner: CEO/product；Scope: continuity wedge、六周 freeze、Pinax-only dogfood；Dependencies: none；Lane: gate；记录目标用户、核心价值时刻、四值 outcome、北极星、三类任务、Codex/Claude coverage、action-capture closeout 和 cross-project unvalidated 偏差；Acceptance: required capabilities 未被 silent downgrade；Validation: 人工审阅 `proposal.md` 与 `design.md` 的 Required Capability Ledger / Scope change log；Expected: deliver-now、retain-next、reject-now 边界完整。Evidence: `proposal.md`、`design.md`。
- [x] 0.2 Owner: architecture/compatibility；Scope: CLI、JSON/agent、config registry、GORM、source kind、Skill handoff；Dependencies: 0.1；Lane: gate；完成 surface-by-surface additive classification、rollback 和 split-owner decision；Acceptance: breaking surface = none，existing `continue` explicit behavior preserved，Pinax core 不嵌入 Codex/Claude plugin；Validation: `openspec validate pinax-trusted-continuity-dogfood-v1 --strict`；Expected: valid。Evidence: `design.md` 的 Compatibility table 与 Migration Plan。
- [x] 0.3 Owner: implementation lead；Scope: existing `continue`/handoff/review/output/store fixtures；Dependencies: 0.2；Lane: gate；在改动前固化旧 explicit command、envelope、DB auto-migrate 和 source coverage behavior；Acceptance: baseline tests 覆盖 `--scope/--vault/--handoff/--task/--intent`、JSON top-level envelope 和 non-recorded read-only；Validation: `go test ./cmd/pinax ./internal/agentcontinuity ./internal/app ./internal/output -count=1`；Expected: baseline PASS；Failure re-check: 将失败分类为 pre-existing/concurrent/environmental/introduced，不修改无关业务逻辑清全局告警。

## 1. User-level Continuity Binding

- [x] 1.1 Owner: binding-domain；Scope: new `internal/continuitybinding/`；Dependencies: 0.3；Lane: A；实现 `pinax.continuity_binding.v1` model、validation、exact canonical worktree key、digest/redaction 和 registry repository；Acceptance: user-level owner-only file、unique enabled exact binding、无 secrets/remote URL/userinfo；Validation: `go test ./internal/continuitybinding -count=1`；Expected: create/update/disable/list/corrupt/0600 tests PASS；Failure re-check: XDG/config path resolution、atomic write、Windows path/case、symlink/canonicalization。复杂路径边界与损坏恢复必须有中文注释。
- [x] 1.2 Owner: binding-app；Scope: `internal/app/continuity_binding.go` 与 existing vault/project services；Dependencies: 1.1；Lane: A；实现 bind/status/resolve application services，验证 registered vault 与 bounded scope，不隐式创建 project；Acceptance: precedence 为 explicit > exact binding > legacy default，missing/invalid/ambiguous 状态稳定；Validation: `go test ./internal/app -run 'ContinuityBinding|ContinuityResolution' -count=1`；Expected: precedence/fail-closed/recovery tests PASS；Failure re-check: explicit flag detection、vault registry lookup、scope existence、duplicate active rows。
- [x] 1.3 Owner: binding-cli；Scope: `internal/cli/continue_binding_cmd.go`、root registration 与 command tests；Dependencies: 1.2；Lane: A；新增 `pinax continue bind` 和 `pinax continue status`，所有 mutation 由 service 写入，默认输出 bounded；Acceptance: JSON/agent facts additive、stable errors、stdout/stderr separation、status read-only；Validation: `go test ./cmd/pinax -run 'ContinueBind|ContinueStatus' -count=1`；Expected: command/help/contract tests PASS；Failure re-check: Cobra leaf+subcommand coexist、global `--vault` precedence、path redaction、partial write。
- [x] 1.4 Owner: continuity-integration；Scope: existing `internal/cli/continue_review_cmd.go` 与 `internal/app/agent_continuity.go`；Dependencies: 1.3；Lane: A；接入 automatic binding resolution，同时保留所有 explicit flags 和 unbound `workspace:default` fallback；Acceptance: bound no-scope uses binding，unbound behavior unchanged plus one warning/action；Validation: `go test ./cmd/pinax ./internal/app -run 'Continue.*Binding|Explicit.*Continue|LegacyDefault' -count=1`；Expected: compatibility PASS；Failure re-check: command flag Changed state、remote mode、non-Git cwd、disabled binding。

## 2. Source Resolution 与真实 Freshness

- [x] 2.1 Owner: source-domain；Scope: `internal/agentprotocol/source.go`、new repository resolver adapter；Dependencies: 1.2；Lane: B；以 additive `repository` SourceRef kind 支持 binding 内 repo-relative path/revision，阻止 `..`、absolute path、symlink escape 和跨 binding access；Acceptance: 不返回 source body，unknown kind 仍按 unresolved 兼容；Validation: `go test ./internal/agentprotocol ./internal/app -run 'RepositorySource|SourceBoundary' -count=1`；Expected: valid/stale/missing/escape tests PASS；Failure re-check: path normalization、Git worktree/common-dir、revision probe 和 Windows separators。边界判断必须有中文注释。
- [x] 2.2 Owner: continuity-source；Scope: `internal/app/agent_continuity.go`、`internal/agentcontinuity/contract.go`；Dependencies: 2.1；Lane: B；扩展 coverage 为 resolved/stale/missing/optional ambiguous，并从 supporting handoff/memory/source evidence 计算 freshness；Acceptance: `generated_at` 与 evidence freshness 分离，`total=0` 为 not-measured downstream；Validation: `go test ./internal/agentcontinuity ./internal/app -run 'SourceCoverage|Freshness|RepositorySource' -count=1`；Expected: note/asset old tests与新 repository drift tests PASS；Failure re-check: timestamp source、revision mismatch、ambiguous denominator、max-source truncation。
- [x] 2.3 Owner: source-e2e；Scope: fixture Git repos/vaults/testscript；Dependencies: 2.2；Lane: B；覆盖 resolvable repository/OpenSpec ref、deleted file、drifted revision、symlink escape、no binding；Acceptance: partial trustworthy output 不因一个 missing source 全失败；Validation: focused process e2e command；Expected: source status/count/next action 与 spec 一致，evidence 进入 `temp/integration-test-runs/<run-id>/`；Failure re-check: fixture revision、working tree dirtiness、absolute-path redaction、original exit code preservation。

## 3. Resume Card 与 Uncertainty UX

- [x] 3.1 Owner: continuity-projection；Scope: `internal/agentcontinuity/` 与 `internal/app/agent_continuity.go`；Dependencies: 1.4 + 2.2；Lane: C；实现一个 recommended next action、partial status、warning categories、review attention count 和 evidence/freshness status；Acceptance: conflict/source warning 不被 budget 丢弃，missing handoff context-only；Validation: `go test ./internal/agentcontinuity ./internal/app -run 'Resume|Partial|Conflict|NextAction' -count=1`；Expected: deterministic pack tests PASS；Failure re-check: ordering、budget truncation、empty sections、conflict precedence。状态与排序逻辑必须有中文注释。
- [x] 3.2 Owner: human-output；Scope: `internal/output/` 与 continue command renderer；Dependencies: 3.1；Lane: C；增加 command-specific Resume Card，固定 objective/state/decisions/blockers/one-next-action/evidence-status 六区块；Acceptance: human compact、无 full body；machine envelope/English keys 不变；Validation: `go test ./internal/output ./cmd/pinax -run 'Continue.*Render|ResumeCard|OutputContract' -count=1`；Expected: human golden + JSON/agent compatibility PASS；Failure re-check: TTY/non-TTY、width/theme、empty/partial/conflict states、stdout/stderr。
- [x] 3.3 Owner: UX-contract；Scope: command fixture/testscript；Dependencies: 3.2；Lane: C；用 bound ready、unbound、no handoff、stale source、conflicting decision、ambiguous binding 六种场景验证“最多一个关键问题/next action”；Acceptance: 不猜事实、不扫描 vault、不因 partial 丢弃可信内容；Validation: process e2e/golden command；Expected: 每种场景的 status、warnings、action count 和 source facts 固定；Failure re-check: generic renderer fallback、action dedupe、locale/English machine contract。

## 4. Checkpoint 与 Proposal-only Closeout

- [x] 4.1 Owner: checkpoint-app；Scope: new `internal/app/agent_continuity_checkpoint.go`、existing handoff/proposal services；Dependencies: 1.2；Lane: D；实现绑定感知的 bounded checkpoint orchestration，复用 canonical handoff，durable candidates 只调用 proposal service；Acceptance: item/char caps、source validation、no auto-confirm、no transcript/raw payload ingress；Validation: `go test ./internal/app -run 'ContinuityCheckpoint|CheckpointProposalGuard' -count=1`；Expected: valid/oversize/source-failure/proposal-only tests PASS；Failure re-check: partial handoff write、proposal failure after handoff、principal/runtime mapping、body leak guard。状态机与补偿边界必须有中文注释。
- [x] 4.2 Owner: checkpoint-cli；Scope: `internal/cli/continue_checkpoint_cmd.go` 与 command tests；Dependencies: 4.1；Lane: D；新增 additive `pinax continue checkpoint`，支持 bounded repeated fields、resolved binding/scope 和 optional run link；Acceptance: help/output English、stable validation、existing `agent handoff create` 不变；Validation: `go test ./cmd/pinax -run 'ContinueCheckpoint|HandoffCompatibility' -count=1`；Expected: command contract PASS；Failure re-check: repeated flag mapping、comma compatibility、Cobra subcommands、no-run path。
- [x] 4.3 Owner: cross-runtime-e2e；Scope: testscript/fixture adapters；Dependencies: 4.2 + 3.3；Lane: D；验证 Codex checkpoint → Claude resume 和反向流程，断言 objective/decision/blocker/verification/follow-up/source semantics；Acceptance: core schema 无 runtime-specific required field，confirmed memory count 不变；Validation: focused cross-runtime process e2e；Expected: 双向 PASS 并写标准 evidence；Failure re-check: principal capability、handoff selection、runtime descriptor、fixture source revisions。

## 5. Run Receipt、Feedback 与 Report

- [x] 5.1 Owner: evidence-domain；Scope: new `internal/continuityevidence/` + GORM repository/migrations；Dependencies: 0.3；Lane: E；实现 additive `continuity_runs` 与 append-only `continuity_feedback_events` models、enums、supersession 和 redaction；Acceptance: 无 hard-coded SQL、旧 DB auto-migrate、无正文/path/prompt/provider fields；Validation: `go test ./internal/continuityevidence ./internal/agentmemory -run 'ContinuityRun|ContinuityFeedback|Migration' -count=1`；Expected: fresh/legacy DB、enum、append-only、redaction tests PASS；Failure re-check: GORM indexes、transaction boundary、migration idempotency、SQLite locking。复杂 migration/state fold 必须有中文注释。
- [x] 5.2 Owner: recorded-continue；Scope: continue app/CLI integration；Dependencies: 5.1 + 1.4 + 2.2；Lane: E；新增 opt-in `--record-run`、`--runtime`、`--task-class`，默认 continue 不写，recorded run 返回 opaque ID 并记录 source/handoff/safety facts；Acceptance: old read-only contract pinned，invalid enums fail before write；Validation: `go test ./cmd/pinax ./internal/app -run 'RecordedContinue|ContinueReadOnly|TaskClass' -count=1`；Expected: default zero writes，recorded one run；Failure re-check: retry/idempotency、partial compile、DB failure fallback、output field omission。
- [x] 5.3 Owner: feedback-cli；Scope: app service + `internal/cli/continue_feedback_cmd.go`；Dependencies: 5.1；Lane: E；实现四值 feedback、optional review seconds 和 superseding event；Acceptance: user-submitted only、missing run/invalid enum no write、latest fold deterministic；Validation: `go test ./internal/continuityevidence ./cmd/pinax -run 'ContinueFeedback|FeedbackSupersede' -count=1`；Expected: contract tests PASS；Failure re-check: duplicate submission、negative/overflow duration、concurrent feedback、audit retention。
- [x] 5.4 Owner: report-app；Scope: evidence aggregation + `internal/cli/continue_report_cmd.go`；Dependencies: 5.2-5.3；Lane: E；实现 time-window report、trusted/source rates、runtime/task coverage、weekly burden、silent writes、not-measured 和 known bias；Acceptance: exact Go gates，Pinax-only 固定 `cross_project_routing=unvalidated`；Validation: `go test ./internal/continuityevidence ./cmd/pinax -run 'ContinuityReport|GoGate|KnownBias' -count=1`；Expected: threshold boundary/zero denominator/missing feedback tests PASS；Failure re-check: denominator definition、superseded feedback、week boundary/timezone、synthetic run exclusion。

## 6. Relevant-inline 与 Weekly Review

- [x] 6.1 Owner: memory-inbox；Scope: `internal/memoryinbox/` 与 app aggregation；Dependencies: 3.1；Lane: F；实现 deterministic relevance reason codes，只有改变 objective/decision/blocker/conflict/next action 的 item 才进入 inline attention；Acceptance: human card ≤1 item，machine projection bounded count/refs，无 body；Validation: `go test ./internal/memoryinbox ./internal/app -run 'ReviewRelevance|InlineAttention' -count=1`；Expected: relevant/unrelated/conflict/stale tests PASS；Failure re-check: scope overlap、false positive、risk precedence、source drift。非显然 relevance fixtures 必须有中文注释。
- [x] 6.2 Owner: review-evidence；Scope: review CLI integration + continuity feedback duration；Dependencies: 6.1 + 5.3；Lane: F；支持 weekly bounded review duration/report linkage，不让 read-only list 隐式写 state；Acceptance: empty inbox 可记录 0 秒，mutation 仍经 canonical service/`--yes`/stale check/receipt；Validation: `go test ./cmd/pinax ./internal/app -run 'WeeklyReview|ReviewCompatibility|StaleReview' -count=1`；Expected: list/show/approve/reject old behavior与新增 evidence PASS；Failure re-check: duration owner、stale item refresh、partial success、receipt redaction。

## 7. Contract、Integration 与 Root Skill Handoff

- [x] 7.1 Owner: compatibility-qa；Scope: CLI output/config/DB/source public surfaces；Dependencies: 1-6；Lane: final-verification；运行 old/new contract suite，记录 `breaking_surfaces=[]`、OpenSpec ID、deprecation=`none`、rollback；Acceptance: 无 rename/remove/repurpose，old DB/vault/explicit scripts work；Validation: focused contract tests + `go test ./...` only after focused suites stable；Expected: PASS；Failure re-check: introduced vs dirty-worktree failures、top-level envelope、agent keys、GORM migration、unknown source kind。
- [x] 7.2 Owner: integration-qa；Scope: three task-class process flows；Dependencies: 7.1；Lane: final-verification；使用 fixture vault/Git repo 完成 implementation_debugging、product_spec_docs、release_operations 的 resume→work fixture→checkpoint→other runtime resume→feedback→report；Acceptance: 每次 run 写标准 redacted evidence 且保留原退出码；Validation: owning integration entrypoint；Expected: 3/3 flows PASS，source coverage ≥95%，silent confirmed writes=0；Failure re-check: fixture pollution、runtime mapping、report denominator、evidence redaction。
- [x] 7.3 Owner: root-handoff；Scope: `../../openspec/changes/pinax-continuity-operator-handoff-v1/` consumer contract；Dependencies: 7.2；Lane: handoff；提供 versioned command/output examples和 fixture evidence，不修改 root generated skills；Acceptance: root Skill owner能仅靠 public Pinax contract完成 Codex/Claude 编排；Validation: root `openspec validate pinax-continuity-operator-handoff-v1 --strict` + child contract smoke；Expected: handoff accepted；Failure re-check: planned/current command drift、missing stable errors、runtime-specific core fields。
- [x] 7.4 Owner: project-quality；Scope: stable completed diff；Dependencies: 7.3；Lane: final-verification；运行 full project gates，仅修复本 change 引入问题；Acceptance: fmt/lint/test/build/OpenSpec 全通过，unrelated dirty failures 已分类；Validation: `task check`；Expected: PASS；Failure re-check: 先用 focused command复现，禁止修改无关业务逻辑或用户已有改动清理全局警告。

## 8. 真实 Personal Dogfood 与决策

Status 2026-09-03（编码会话只读取证）: 8.2-8.4 全部为外部门控，本会话只做只读取证并标注，不勾选、不归档。真实 dogfood 分母（`pinax continue report --since 6w --json`，2026-09-03 于真实 `yeisme-notes` vault 读取，exit 0）: `total_runs=1`（want ≥30）、`runtime_coverage=codex`（want codex+claude）、`class_coverage=release_operations`（want 3 classes）、`source_resolvability=not_measured`、`go_ready=false`、`silent_confirmed_writes=0`；exact binding `bdabffe4d9b88a90` 复核仍 `ready`（`pinax continue status --repo . --json`）。窗口 2026-08-29→2026-10-10 尚未结束。注：本 change 的 3.2（Resume Card 渲染）早已完成勾选；rollback smoke 属 `pinax-agent-continuity-iteration-adoption-signal` 3.2，已于 2026-08-23 在隔离 profile 完成并勾选，无本地遗留。

- [x] 8.1 Owner: local operator；Scope: real `yeisme-notes` 与 Pinax repository；Dependencies: 7.4 + root operator handoff ready；Lane: dogfood-setup；通过现有 CLI 创建/确认 `project:pinax`，用新 CLI 建立 exact worktree binding并验证 status；Acceptance: structured assets 全由 CLI/service 创建，未手写 vault/registry metadata；Validation: `pinax project list --vault yeisme-notes --json`、`pinax continue status --repo . --json`；Expected: project/binding ready；Failure re-check: default vault registry、scope slug、worktree root、path redaction。Evidence: CLI 创建的 project object `01a05363-e266-7b25-b58e-da864a07644e` 可在 registered vault 中读取，且未静默切换既有 `current_project=screenwriting-learning`；exact binding 返回 `binding_status=ready`、`ready=true`、`scope=project:pinax`、`vault_ref=yeisme-notes` 与 bounded digest `bdabffe4d9b88a90`。
- [x] 8.2 Owner: existing action-capture owner；Scope: `pinax-agent-continuity-iteration-adoption-signal` tasks 4.1-4.4；Dependencies: calendar reaches 2026-09-05；Lane: parallel-closeout；完成固定窗口、生成唯一 Go/Iterate/Stop receipt并 archive/closeout，当前 change 不复制其 task state；Acceptance: 不新增 Feishu/task/intent surface；Validation: existing change commands + `openspec validate pinax-agent-continuity-iteration-adoption-signal --strict`；Expected: receipt and closeout recorded；Failure re-check: 不伪造真实使用、不用 continuity loop 代替 action-capture denominator。
  Evidence (2026-09-06): calendar gate 已过（窗口 2026-08-23→09-05 结束）；adoption-signal 侧已收口——4.3 终版两周 report（captures=0/0、not_measured 全显式）+ 4.4 唯一 NOT GO decision receipt（`pinax-agent-continuity-iteration-adoption-signal/decision-receipt-2026-09-06.md`：保持 experimental、不创建接口固化 change、停止集成扩张）；本 change 未新增任何 Feishu/task/intent surface，未复制其 task state；`openspec validate pinax-agent-continuity-iteration-adoption-signal --strict` PASS。4.1/4.2 在该 change 内保持未勾选（真实使用未发生的永久负结果记录），action-capture 支线以负结果 closeout，continuity 主线（8.3/8.4）不受影响。
  Status 2026-09-03: [external-gate skipped] — 双重外部门控：calendar gate `2026-09-05` 未到（今日 2026-09-03），且其依赖的 adoption-signal 4.1-4.4 真实分母为 0（canary week1 已结束 captures=0、week2 至今 captures=0，见该 change 4.x 标注）。需真实用户使用，编码会话不能完成，不得用 continuity loop 代替 action-capture denominator。
- [ ] 8.3 Owner: personal dogfood；Scope: 2026-08-29 至 2026-10-10 real Pinax work；Dependencies: 8.1；Lane: dogfood；完成至少 30 个 substantial continuity loops，Codex/Claude 与三类 task 均有样本，每次由用户提交或明确缺失 outcome；Acceptance: weekly CLI-authored report，不保存正文/prompt/transcript；Validation: `pinax continue report --since 6w --json`；Expected: 明确分母、bias 和 failed gates；Failure re-check: synthetic/test run 排除、missing feedback、source total=0、runtime/task class coverage。
  Status 2026-09-03: [external-gate skipped] — 真实 personal dogfood（≥30 substantial loops、双 runtime、三 task class、每次由用户提交或明确缺失 outcome），窗口至 2026-10-10。当前真实分母 1/30（见节首取证）；只读 report 命令已验证可用，本会话未伪造任何 run/feedback。
  Status 2026-09-07: [external-gate skipped] — 只读取证（真实 `yeisme-notes` vault）：`pinax continue report --since 6w --json` exit 0，total_runs=1（want ≥30）、runtime_coverage=codex、class_coverage=release_operations、source_resolvability=not_measured、go_ready=false、silent_confirmed_writes=0；分母 1/30，窗口至 2026-10-10 未结束，本会话未伪造任何 run/feedback。
- [ ] 8.4 Owner: CEO/product；Scope: final six-week report；Dependencies: 8.2-8.3；Lane: decision；依据预登记门槛记录唯一 Go/Iterate/Stop；Acceptance: Go 仅在 ≥30 loops、trusted ≥80%、sources ≥95%、两 runtime、三 task class、weekly review ≤300s、silent writes=0 时成立，并保留 cross-project unvalidated；Validation: report + `openspec validate pinax-trusted-continuity-dogfood-v1 --strict`；Expected: decision receipt 和一个 bounded next change/stop action；Failure re-check: 不用命令量、工程完成、单仓库 wrong_project=0 或 action-capture结果替代 continuity 证据。
  Status 2026-09-03: [external-gate skipped] — CEO/product 决策依赖 8.2+8.3；当前 `go_ready=false`（sample/runtime/class/source 四 gate 未过），不可提前决策。
  Status 2026-09-07: [external-gate skipped] — 复核同日 report：`go_ready=false`（sample/runtime/class/source 四 gate 未过），不可提前决策；依赖 8.3 窗口结束后的终版分母。
