## 1. 合同与测试基线

- [x] 1.1 冻结 core command 集合、advanced 可见性规则和 `commands.list` 输出字段；Owner: implementer；Scope: `openspec/changes/pinax-personal-local-cli-simplification`；Dependencies: none；Lane: A；Verification: `openspec validate pinax-personal-local-cli-simplification --strict`；Expected: 变更明确不删除命令、不改 flags/JSON/agent/events 合同，并记录回滚；Failure re-check: 对照 `yeisme-evolutionary-change-policy` 检查每个稳定 surface。Evidence: 2026-07-30 strict validation exit 0，兼容与回滚策略已写入 proposal/design/spec。
- [x] 1.2 先增加 root help RED 测试，要求默认仅展示九个 core 入口并提示 `pinax commands`；Owner: implementer；Scope: `cmd/pinax/cli_output_contract_test.go`；Dependencies: 1.1；Lane: B；Verification: `go test ./cmd/pinax -run TestCLITreeHelpSmoke -count=1`；Expected: 测试因当前高级命令仍可见而失败；Failure re-check: 若测试直接通过，收紧断言以确认测试覆盖真实旧行为。Evidence: 2026-07-30 RED exit 1，缺少 `Start here` 且旧 help 展示全部高级命令。
- [x] 1.3 先增加完整目录 RED 测试，覆盖 human、JSON、agent、events 与旧高级命令可调用；Owner: implementer；Scope: `cmd/pinax/cli_output_contract_test.go`；Dependencies: 1.1；Lane: B；Verification: `go test ./cmd/pinax -run 'TestCLI(CommandCatalog|AdvancedCommandsRemainExecutable)' -count=1`；Expected: `pinax commands` 因功能尚未实现而失败；Failure re-check: 区分 unknown command 与 test harness 错误。Evidence: 2026-07-30 RED exit 1，Cobra 返回 unknown command `commands`。

## 2. Personal-first 命令面实现

- [x] 2.1 在 root command annotation 中增加 `core|advanced` 可见性，默认 help 只渲染 core，不设置 Cobra Hidden；Owner: implementer；Scope: `internal/cli/root.go`；Dependencies: 1.2；Lane: C；Verification: `go test ./cmd/pinax -run TestCLITreeHelpSmoke -count=1`；Expected: root help 只展示 core，高级子命令 help 仍可访问；Failure re-check: 检查 grouped template 与 `IsAvailableCommand` 交互。Evidence: 默认 help 仅展示九个 core；sync/capsa/api/agent 子命令 help 通过。
- [x] 2.2 实现 `pinax commands` 的共享 Projection 和目录生成，复杂分组/过滤逻辑补中文注释；Owner: implementer；Scope: `internal/cli/commands_cmd.go`、`internal/cli/root.go`；Dependencies: 1.3、2.1；Lane: C；Verification: `go test ./cmd/pinax -run 'TestCLI(CommandCatalog|AdvancedCommandsRemainExecutable)' -count=1`；Expected: human/JSON/agent/events 均通过统一 renderer，目录覆盖全部 root child；Failure re-check: 检查 command 自包含、排序稳定和输出模式互斥。Evidence: `commands.list` 由同一 Projection 派生 human/JSON/agent/events，目录共 52 个 root commands。
- [x] 2.3 运行 CLI tree、remote coverage、completion 和 output contract 聚焦回归；Owner: verifier；Scope: `internal/cli`、`cmd/pinax`；Dependencies: 2.2；Lane: D；Verification: `go test ./internal/cli ./cmd/pinax -run 'Test.*(CLI|Help|Command|RemoteCommandCoverage|Completion)' -count=1`；Expected: 旧命令执行与机器合同无回归；Failure re-check: 将失败分类为 introduced、pre-existing 或 concurrent，只修复本变更引入问题。Evidence: 2026-07-30 两个 package 均 exit 0；旧 root-help 断言已迁移为完整目录/直接子命令检查。

## 3. 产品定位与文档

- [x] 3.1 更新中英文 README 与 quickstart，把 Pinax 首要定位改为个人本地知识工具，示例只使用当前真实可运行命令；Owner: docs owner；Scope: `README.md`、`README.zh-CN.md`、`docs/quickstart.md`；Dependencies: 2.2；Lane: E；Verification: `rg -n 'pinax (init|note|inbox|journal|search|project|vault|version|commands)' README.md README.zh-CN.md docs/quickstart.md`；Expected: 首屏不要求用户理解 Capsa、daemon、API 或 Agent runtime；Failure re-check: 不展示尚未实现的 `backup`/`restore` 命令。Evidence: 首屏和 5 分钟路径已改为 init/inbox/note/journal/search/version/commands。
- [x] 3.2 更新命令索引，区分 core 与 advanced，并记录旧命令无弃用、无迁移要求；Owner: docs owner；Scope: `docs/commands/README.md`；Dependencies: 2.2；Lane: E；Verification: `go run ./cmd/pinax commands` 与文档目录人工对照；Expected: 所有示例均为真实命令且高级命令仍有入口；Failure re-check: 对照 root command tree 检查遗漏。Evidence: command manual 已说明默认 help 与完整目录的边界，advanced 命令无弃用、无删除。

## 4. 最终验证与收口

- [x] 4.1 运行 focused tests、格式化、构建和 OpenSpec strict validation；Owner: verifier；Scope: changed files；Dependencies: 2.3、3.1、3.2；Lane: final；Verification: `gofmt -w internal/cli/root.go internal/cli/commands_cmd.go cmd/pinax/cli_output_contract_test.go && go test ./internal/cli ./cmd/pinax -count=1 && go build ./cmd/pinax && openspec validate pinax-personal-local-cli-simplification --strict`；Expected: 全部 exit 0；Failure re-check: 只修复本变更引入失败，不改当前同步/Agent memory 工作。Evidence: internal/output、internal/cli、cmd/pinax 测试、build、strict validation 全部 exit 0。
- [x] 4.2 运行 Pinax 完整质量门禁并记录 concurrent/pre-existing 失败；Owner: verifier；Scope: Pinax repo；Dependencies: 4.1；Lane: final；Verification: `task check`；Expected: 全绿，或对非本变更失败提供准确分类和证据；Failure re-check: 不为清理无关 dirty worktree 警告修改其他业务逻辑。Evidence: 2026-07-30 `task check` exit 0，Go 全量测试、e2e、lint、fmt、build、OpenSpec 62/62、sidecar 和 Web renderer 均通过。
- [x] 4.3 更新 tasks evidence、复核兼容表并决定是否归档；Owner: root；Scope: OpenSpec artifacts；Dependencies: 4.2；Lane: final；Verification: `openspec validate --all`；Expected: tasks 与实际证据一致，未验证事项保持未完成；Failure re-check: 不伪造测试、使用频率或 Capsa 拆分完成状态。Evidence: 兼容表复核通过；本 change 的首个 CLI 收敛切片完成。暂不归档，以便下一阶段继续收敛 backup/sync 产品边界，且不宣称 Capsa SDK 已拆分。

## 5. 个人本地备份入口

- [x] 5.1 冻结 `backup.*` additive Projection、local-only 语义、version 兼容与远端非目标；Owner: implementer；Scope: proposal/design/specs；Dependencies: 4.3；Lane: F；Verification: `openspec validate pinax-personal-local-cli-simplification --strict`；Expected: 不新增备份 engine/schema，不删除 version/sync/capsa；Failure re-check: 对照 evolutionary policy 检查命令与机器合同。Evidence: 2026-07-30 strict validation exit 0；backup 为 additive facade，version/sync/capsa 无删除和迁移。
- [x] 5.2 先增加 backup facade RED 测试，覆盖默认 help、status/create/history/restore、human/JSON/agent/events 和 version 兼容；Owner: implementer；Scope: `cmd/pinax/cli_output_contract_test.go`；Dependencies: 5.1；Lane: G；Verification: `go test ./cmd/pinax -run 'TestCLI(PersonalBackup|TreeHelpSmoke)' -count=1`；Expected: 当前因 unknown command `backup` 或 root help 缺少 backup 而失败；Failure re-check: 确认失败来自缺失 facade。Evidence: 2026-07-30 RED exit 1，unknown command `backup`、root help 缺少 backup、version visibility 仍为 core。
- [x] 5.3 实现 `pinax backup` 薄 facade，复用现有 version application service，并把 version 降为 advanced；Owner: implementer；Scope: `internal/cli/backup_cmd.go`、`internal/cli/root.go`；Dependencies: 5.2；Lane: H；Verification: `go test ./cmd/pinax -run 'TestCLI(PersonalBackup|TreeHelpSmoke)' -count=1`；Expected: backup 全模式通过，version 原路径继续执行；Failure re-check: 不修改 sync/Capsa/service/storage。Evidence: backup status/create/history/restore plan/apply、JSON/agent/events 和 version.status 兼容测试 exit 0；未修改 version service、sync/Capsa 或 storage。
- [x] 5.4 更新 README、quickstart 和 command manual，以 local Git snapshot 为默认个人备份，远端同步明确 advanced/experimental；Owner: docs owner；Scope: `README.md`、`README.zh-CN.md`、`docs/quickstart.md`、`docs/commands/README.md`、`docs/commands/backup.md`；Dependencies: 5.3；Lane: I；Verification: `rg -n 'pinax backup|local Git|advanced' README.md README.zh-CN.md docs/quickstart.md docs/commands/README.md docs/commands/backup.md`；Expected: 无远端稳定性夸大；Failure re-check: 对照实际 help 和 projections。Evidence: personal paths 已改用 backup；新增 backup manual 明确 local-only，S3/rclone/Capsa 仍是 advanced 且需各自 durable evidence。
- [x] 5.5 运行 focused tests、build、strict validation 与 `task check`；Owner: verifier；Scope: Pinax repo；Dependencies: 5.4；Lane: final；Verification: `go test ./internal/output ./internal/cli ./cmd/pinax -count=1 && go build ./cmd/pinax && openspec validate pinax-personal-local-cli-simplification --strict && task check`；Expected: 全部 exit 0；Failure re-check: 只修复 introduced failure，保留并行 sync/Agent memory 改动。Evidence: 2026-07-30 focused tests、build、strict validation 与 `task check` 全部 exit 0；OpenSpec 62/62，Go 全量测试/e2e、lint、fmt、sidecar、Web renderer 均通过。

## 6. 死代码清理

- [x] 6.1 证明 `internal/research` 不在生产依赖图中，并核对 briefing 的真实实现；Owner: root；Scope: `internal/research`、`internal/app/service_sync_briefing.go`、`internal/briefing`；Dependencies: 5.5；Lane: J；Verification: `grep -RIn 'internal/research|ResolveAdapter' . --exclude-dir=internal/research` 与 `go list -deps ./cmd/pinax`；Expected: 仅历史 OpenSpec/审查文档引用，生产构建不依赖该包；Failure re-check: 若存在生产 import 或配置读取方则停止删除并改为迁移。Evidence: 2026-07-31 未发现生产 import；`BriefingRun` 直接调用 `internal/briefing.FakeEvidence`。
- [x] 6.2 删除 `internal/research` 生产代码与自包含测试，并从当前模块审查索引移除；Owner: root；Scope: `internal/research/**`、`docs/operations/module-review-tdd.md`；Dependencies: 6.1；Lane: K；Verification: `test ! -d internal/research && go test ./internal/briefing ./internal/app ./cmd/pinax -run 'Briefing|CLI' -count=1`；Expected: 无死包残留且 briefing/CLI 行为不变；Failure re-check: 仅恢复被证明仍有调用方的最小文件。Evidence: 2026-07-31 目录已删除；briefing、app、cmd/pinax focused tests exit 0；主二进制依赖图不含 research 包。
- [x] 6.3 运行全量测试、构建与 OpenSpec strict validation；Owner: verifier；Scope: Pinax repo；Dependencies: 6.2；Lane: final；Verification: `go test ./... && go build ./cmd/pinax && openspec validate pinax-personal-local-cli-simplification --strict`；Expected: 全部 exit 0；Failure re-check: 将失败分类为 introduced、pre-existing、concurrent 或 environment，不修改并行 sync/S3 工作。Evidence: 2026-07-31 `go test ./...`、`go build ./cmd/pinax` 与 change strict validation 均 exit 0；未进入主二进制的剩余 production-file packages 均为 e2e adapter、test/evidence helper、mock server、GORM generator 或 version test helper，保留其明确用途。
