# 实施与验收任务

由 scripts/openspec-tasks.py 维护状态。

- [x] 1.1 owner=Pinax；scope=领域最小投影与问题集；depends=公共 SDK 合同；lane=domain；实现 source/candidate/question/policy 版本绑定和确定性前置规则；验收=覆盖 design 场景且不夹带未授权文本；验证=go test ./...（先限本 slice）；失败先修 projection fixture。 | evidence: go test ./internal/inboxjudgment/ → ok（问题集 digest 稳定、off 绑定必须为空、跨 vault/未授权候选 fail closed 且不回显 ID、secret 形态 fail closed、digest 全等确定性重复、rune 边界截断）；2026-09-22 review fix：inbox 笔记自身也必须位于授权集合（投影阶段拒绝，不留“正文已可外发、采纳门才拒绝”的窗口），空授权集合 = deny-all（与缓存重授权语义一致）；回归 TestBuildProjectionInboxNoteAuthorization（变异验证：还原旧守卫测试即红）
- [x] 1.2 owner=Pinax；scope=可选 SDK consumer；depends=1.1,SDK 包；lane=integration；实现 off/shadow/assist 和注入 transport，默认 off；验收=off/replay 零调用且 unknown outcome 不自动重发；验证=go test ./...；失败复验提交状态与授权。 | evidence: go test ./internal/inboxjudgment/ → ok（off 零 transport 调用、timeout/outcome_unknown 单次调用不自动重发、缓存 replay 零网络）；go test -tags judgment_sdk ./internal/inboxjudgment/ → ok 与 go test -tags judgment_sdk -mod=mod ./internal/inboxjudgment/ → ok（真实 SDK bridge 一致性，vendor 快照已提交）；2026-09-22 review fix：InboxJudgmentCache 加互斥锁（共享 consumer 并发 Store/Get 不再数据竞态，go test -race 验证）、缓存 key 绑定 mode（shadow evidence 不再 replay 给 assist consumer，回归 TestShadowEvidenceNotReplayedAcrossModes 变异验证）、Get 重授权补查 suggestion.InboxNoteID
- [x] 1.2 owner=Pinax；scope=可选 SDK consumer；depends=1.1,SDK 包；lane=integration；实现 off/shadow/assist 和注入 transport，默认 off；验收=off/replay 零调用且 unknown outcome 不自动重发；验证=go test ./...；失败复验提交状态与授权。 | evidence: go test ./internal/inboxjudgment/ → ok（off 零 transport 调用、timeout/outcome_unknown 单次调用不自动重发、缓存 replay 零网络）；go test -tags judgment_sdk ./internal/inboxjudgment/ → ok 与 go test -tags judgment_sdk -mod=mod ./internal/inboxjudgment/ → ok（真实 SDK bridge 一致性，vendor 快照已提交）
- [x] 1.3 owner=Pinax；scope=领域建议/evidence 与原 review handoff；depends=1.2；lane=domain；复用既有应用服务生成证据，保持旧 envelope 和 canonical 权限；验收=stale/权限撤销不能采纳且不隐式业务执行；验证=go test ./...；失败仅回修 owner 投影。 | evidence: go test ./internal/inboxjudgment/ → ok（evidence 无问题文本/inline 摘要、stale→judgment_stale_source、撤销→judgment_unauthorized 不点名、shadow 不可采纳、acceptance 只枚举 pinax inbox 原入口命令、handoff.Projection() 复用 domain.Projection 旧 envelope）
- [x] 1.4 owner=Pinax；scope=场景矩阵与边界测试；depends=1.3；lane=verification；复用现有 runner 包装 offline integration，覆盖全部场景与注入/脱敏；验收=非零测试且成功失败均有六类 evidence；验证=go test ./...；失败保留证据并区分本变更与环境问题。 | evidence: go test ./internal/inboxjudgment/ -run 'TestInboxJudgmentScenarioMatrix|TestInboxJudgmentScenarioFailureEvidence' → ok；temp/integration-test-runs/inbox-judgment-{inbox-link,duplicate-warning,vault-isolation,failure-injection}-*/ 六类齐全（summary/command/stdout/stderr/env/artifacts，passed exit 0、失败注入 run 保留非零退出码），artifacts 通过脱敏幂等扫描
- [x] 1.5 owner=Pinax；scope=校准与渐进接入说明；depends=1.4；lane=handoff；固定 baseline/留出集/误报漏报度量及关闭恢复说明，不自动 live；验收=明确 exploratory，保留用户显式启用；验证=openspec validate pinax-inbox-judgment-v1 --strict --no-interactive；失败修 readiness 声明。 | evidence: openspec validate pinax-inbox-judgment-v1 --strict --no-interactive → valid；docs/inbox-judgment.md 固定 exploratory 状态、off→shadow→assist→canary 阶梯、双语校准集/留出集与误报漏报度量、关闭即回 off 的恢复说明
- [x] 2.1 owner=Pinax；scope=实验性开关配置面；depends=1.5；lane=config；新增 experimental judgment 配置（enabled 默认 false + mode off/shadow/assist），off 即完全休眠；验收=默认零调用零装配、显式启用可评估、关闭即恢复原 inbox 流程、非法配置拒绝；验证=go test ./...；失败修配置解析与装配门。 | evidence: go test ./... → ok（68 包全绿，go build + go vet + go test -tags judgment_sdk ./internal/inboxjudgment/ 亦绿；当时 golangci-lint 崩溃归因环境：homebrew go1.27.1 stdlib typecheck，2026-09-22 已修复——Taskfile lint 任务用 go.mod 派生 GOTOOLCHAIN 钉住包加载工具链，vendored go1.27-gated 文件不再喂给 go1.26 构建的 golangci-lint，task lint/check 全绿）；internal/config judgment 节（enabled/mode，默认 false/off，env PINAX_JUDGMENT_ENABLED/PINAX_JUDGMENT_MODE，flag/project/user 合并）+ internal/inboxjudgment/gate.go ExperimentalJudgmentGate；四态测试=默认与 enabled+off 零装配零调用且 AcceptSuggestion 一律 judgment_not_enabled 提示未启用、shadow=shadow_compared 不可采纳、assist=suggested 可采纳只枚举 pinax inbox 原入口；非法组合（未启用 shadow/assist、未知 mode、env 半开残留）config_invalid/judgment_invalid_request fail-fast；关闭 mode 回 off 即恢复原流程；config doctor judgment_status=experimental_off/shadow/assist；docs/inbox-judgment.md 第 3 节实验性开关配置面
