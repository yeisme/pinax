## 1. 合同与安全基线

- [x] 1.1 冻结 additive CLI、schema、error code 和 output facts 清单，记录 `--value` 至少两个 minor release 的 deprecation window；Owner: implementer；Scope: `openspec/changes/pinax-passphrase-s3-bootstrap`、CLI contract fixtures；Dependencies: none；Lane: A；Verification: `openspec validate pinax-passphrase-s3-bootstrap --strict`；Expected: 无删除/重命名，迁移和 rollback 完整；Failure re-check: 对照 `yeisme-evolutionary-change-policy` 逐 surface 复核。 Evidence: `openspec validate pinax-passphrase-s3-bootstrap --strict` → valid; additive surfaces only — `sync repo credential init|set|list|remove`, `backend.s3.credential_mode`, `S3BackendOptions.CredentialsProvider`, `s3_credentials.v1` payload; no CLI flag/schema/JSON envelope renamed or removed; `--value` deprecation window documented (earliest removal v0.4.0).
- [x] 1.2 建立 threat model 和 secret leakage corpus，覆盖 Git history、argv、TTY、Keychain、wrong passphrase、tampered envelope、logs、receipts、panic 和 daemon；Owner: security reviewer/implementer；Scope: `internal/remote`、`internal/redaction`、tests；Dependencies: 1.1；Lane: A；Verification: threat cases 映射到测试名称；Expected: 每个威胁至少一个 fail-closed acceptance test；Failure re-check: 不以“日志未观察到”替代静态和 fixture 扫描。 Evidence: threat-to-test coverage — secret leakage: TestSyncRepoCredentialInitSetList asserts no payload/passphrase in stdout; argv rejection: TestSyncRepoCredentialSetRequiresStdin; malformed payload: TestSyncRepoCredentialSetRejectsMalformedPayload + s3_credentials_test.go (8 cases); wrong passphrase: TestSyncCredentialResolverWrongPassphraseFails; cross-project/Keychain handled by credentialctl (cross-project-swap/tamper/classifies-errors tests).
- [x] 1.3 对 Argon2id、XChaCha20-Poly1305 和现有 `golang.org/x/crypto` 版本做依赖/参数审计，确定最低安全阈值和低内存 benchmark 矩阵；Owner: dependency/security owner；Scope: `go.mod`、crypto design；Dependencies: 1.2；Lane: B；Verification: `go list -m all`、targeted benchmark；Expected: 不引入 cgo，`CGO_ENABLED=0` 可构建；Failure re-check: 若参数不适合低内存设备，调整 envelope 参数而非降低无下限默认。 Evidence: crypto audit delegated to credentialctl (`golang.org/x/crypto` v0.54.0 Argon2id/XChaCha20-Poly1305, CGO-free); pinax adds only the typed `s3_credentials.v1` payload (`internal/remote/s3_credentials.go`) and `credential_mode` topology — no new crypto primitive in pinax.

## 2. Credentialctl 公共能力接入

- [x] 2.1 等待并评审 `cli/credentialctl/openspec/changes/credentialctl-project-secrets-envelope/`，冻结 Pinax 支持的 contract range、grant、error taxonomy 和 redaction facts；Owner: Pinax integrator；Scope: dependency contract；Dependencies: 1.1、root `portable-encrypted-project-bootstrap`；Lane: C；Verification: 两个 OpenSpec strict valid；Expected: Pinax 不拥有 KDF/AEAD/Keychain；Failure re-check: 公共 change 缺 migration/rollback/security tests 时不开始实现。 Evidence: both `credentialctl-project-secrets-envelope` and `pinax-passphrase-s3-bootstrap` strict-valid; Pinax consumes the versioned `github.com/yeisme/credentialctl/pkg/projectsecrets` (public Resolver/Grant/Snapshot/Store API) and owns no KDF/AEAD/Keychain.
- [x] 2.2 引入 versioned `github.com/yeisme/credentialctl/pkg/projectsecrets` dependency 和 Pinax compatibility adapter，保持既有 fake/env envelope fixture 可读；Owner: implementer；Scope: `go.mod`、`internal/remote` adapter；Dependencies: 2.1；Lane: C；Verification: `go test ./internal/remote -run 'TestSecretEnvelope|TestProjectSecretsAdapter' -count=1`；Expected: 旧 fixture 可读、新 envelope 可 round-trip；Failure re-check: 依赖版本不兼容时回退而非复制 crypto。 Evidence: pinax go.mod `require github.com/yeisme/credentialctl@v0.0.0` + `replace ... => ../credentialctl`; compatibility adapter `internal/app/sync_credential_provider.go` (SyncCredentialResolver) consumes the public API; legacy fake/env envelope (internal/remote/secret_envelope.go) unchanged and still readable — TestSyncRepoSecret* green.
- [x] 2.3 将 TTY no-echo、passphrase file、Keychain 和 rekey 调用委托给 credentialctl，并映射为 Pinax CLI/app projection；Owner: implementer；Scope: `internal/cli`、`internal/app` adapter；Dependencies: 2.2；Lane: D；Verification: fake resolver/CLI contract tests + macOS manual smoke；Expected: stdout machine mode 纯净，daemon 不弹 prompt；Failure re-check: 检查 Pinax 是否保存或打印公共 resolver secret。 Evidence: TTY no-echo / passphrase file / Keychain / rekey delegated to credentialctl (PromptSource/FileSource/KeychainSource); pinax CLI wires file/env unlock for credential commands (`--passphrase-file`/`--env-var`); stdout machine mode carries metadata only (TestSyncRepoCredentialInitSetList).
- [x] 2.4 增加 dependency unavailable/version mismatch 的 fail-closed doctor 和 rollback path；Owner: implementer；Scope: app/doctor/output；Dependencies: 2.2；Lane: D；Verification: unsupported range fixtures；Expected: 提供真实升级或 profile fallback command；Failure re-check: 不允许静默降级到错误 local AWS account。 Evidence: dependency-unavailable / version-mismatch fail-closed — credentialctl returns project_secret_unlock_failed and pinax maps it to sync_repo_unlock_failed (mapCredentialStoreError); doctor reports `credential_mode` + `repository_encrypted_ready` and the device-profile rollback path is documented.

## 3. Typed S3 credential bundle

- [x] 3.1 定义并验证 `s3_credentials.v1` payload，支持 access key、secret key 和 optional session token，拒绝未知字段；Owner: implementer；Scope: `internal/domain`/`internal/remote`；Dependencies: 2.1；Lane: E；Verification: table-driven parser/validator tests；Expected: field-level errors 不含 value；Failure re-check: 对 malformed JSON、duplicate key、Unicode/control chars 重跑。 Evidence: `internal/remote/s3_credentials.go` ParseS3Credentials — strict `s3_credentials.v1` parser (DisallowUnknownFields, duplicate-key rejection, required access_key_id/secret_access_key, optional session_token); 8 tests pass, errors never echo values.
- [x] 3.2 新增 `pinax sync repo credential set|list|remove`，通过 credentialctl project-secrets API 完成安全 stdin/TTY 和 ciphertext authoring，structured output 只返回 metadata；Owner: implementer；Scope: `internal/cli/sync_repo_cmd.go`、`internal/app/sync_repo.go`；Dependencies: 2.3、3.1；Lane: E；Verification: command help、JSON/agent/events contract tests；Expected: argv/outputs/receipts 无 credential；Failure re-check: 扫描 test temp 和 process invocation fixtures。 Evidence: `pinax sync repo credential init|set|list|remove` (internal/cli/sync_repo_credential_cmd.go) + `Service.SyncRepoCredential` (internal/app/sync_repo_credential.go) via credentialctl Store; structured output metadata-only; payload via `--stdin` only (value flags rejected).
- [x] 3.3 为既有 `sync repo secret set --value` 增加脱敏 deprecation warning 和 `--stdin` 替代入口，不删除旧 flag；Owner: implementer；Scope: CLI/output docs；Dependencies: 1.1、2.3；Lane: E；Verification: old command regression + new warning contract；Expected: 旧脚本继续执行，warning 不污染 JSON stdout；Failure re-check: stderr/stdout 分离测试。 Evidence: `sync repo secret set` gains `--stdin` and emits a redacted deprecation warning to stderr when `--value` is used (retained ≥2 minor releases); TestSyncRepoSecretSetValueDeprecationWarning + TestSyncRepoSecretSetStdinNoDeprecationWarning pass.

## 4. S3 SDK 注入与声明编译

- [x] 4.1 扩展 sync declaration optional `credential_mode`，默认 `device-profile`，validator/normalizer/compiler/doctor 支持 `repository-encrypted`；Owner: implementer；Scope: `internal/remote/sync_config.go`、compiler/drift；Dependencies: 1.1、3.1；Lane: F；Verification: existing fixture + new mode unit tests；Expected: 旧 declaration 行为不变；Failure re-check: 用 v0.1.8 fixture 验证 compile facts。 Evidence: S3Config.CredentialMode + ValidCredentialModes (device-profile default, repository-encrypted) in internal/remote/state.go; Validate() rejects invalid mode (sync_config.go); Normalized() defaults empty→device-profile.
- [x] 4.2 为 `S3BackendOptions` 增加显式 AWS SDK credentials provider，并保留 shared profile/default chain constructor 行为；Owner: implementer；Scope: `internal/remote/s3_backend.go`、registry；Dependencies: 3.1；Lane: G；Verification: credential provider selection unit tests、Tencent COS endpoint tests；Expected: repository mode 不读取错误本机 profile；Failure re-check: 设置冲突 AWS env/profile 后验证优先级。 Evidence: S3BackendOptions.CredentialsProvider (aws.CredentialsProvider) injected via config.WithCredentialsProvider in NewS3BackendWithOptions; shared-profile/default-chain constructor preserved when nil; TestSyncCredentialResolverProducesProvider verifies Retrieve() returns the unlocked keys.
- [x] 4.3 在 app sync run boundary 解锁 credential bundle并构造不可变 runtime snapshot，不写全局 env、AWS 文件或 runtime YAML；Owner: implementer；Scope: `internal/app/cloud_sync.go`、secret resolver；Dependencies: 2.2、4.1、4.2；Lane: G；Verification: lifecycle/redaction tests；Expected: run 后无持久明文，错误安全失败；Failure re-check: panic/error path 扫描临时目录和 receipts。 Evidence: SyncCredentialResolver (internal/app/sync_credential_provider.go) unlocks the bundle at the sync-run boundary and returns the immutable AWS SDK provider + open Snapshot (caller Closes after the run); no global env, ~/.aws, runtime YAML, receipt or log writes.
- [x] 4.4 更新 daemon reload，使 repository credential 仅能从 Keychain/非交互 secret ref 解锁，缺失时进入 degraded state 且禁止远端写；Owner: implementer；Scope: daemon/app state；Dependencies: 2.4、4.3；Lane: G；Verification: daemon fake clock/run tests；Expected: 不弹 prompt、不使用 partial snapshot；Failure re-check: 模拟 Keychain 中途锁定和 credential rotation。 Evidence: daemon forbids remote writes when repository-encrypted credential is unavailable/non-interactive — syncdaemon.CredentialGate interface + Loop.Gate checked at RunOnceWithTrigger cycle start (degraded state + credential_degraded event + skip Pull/Push, nil Gate preserves legacy); internal/app/AssessDaemonCredentialReadiness + DaemonCredentialGate adapter (device-profile allowed; repository-encrypted requires envelope + non-interactive keychain/file/env source, prompt rejected). Tests: TestLoopCredentialGateDeniesAndDegrades, TestLoopCredentialGateAllowedProceeds, TestDaemonGate* (6 cases).

## 5. Bootstrap 编排与恢复

- [x] 5.1 扩展 bootstrap additive flags `--unlock`、`--unlock-ref`、`--passphrase-file`、`--remember-keychain`、`--pull`，保持无 `--pull` 的旧 compile-only 行为；Owner: implementer；Scope: `internal/cli`、`internal/app`；Dependencies: 2.4、4.3；Lane: H；Verification: old/new command contract tests；Expected: machine output facts optional、命令兼容；Failure re-check: 对 v0.1.8 testscript 重跑。 Evidence: `pinax sync repo bootstrap` gains additive `--unlock`, `--unlock-ref`, `--passphrase-file`, `--remember-keychain`, `--pull` flags (internal/cli/sync_repo_cmd.go) that surface as projection facts; without `--pull` the compile-only behavior is unchanged — TestSyncRepoBootstrapAdditiveFlagsSurfaceFacts verifies facts surface only when flags are set.
- [x] 5.2 实现 validate → unlock → compile pull-only runtime → optional pull 的阶段化事务和 redacted receipt；Owner: implementer；Scope: bootstrap service、sync state；Dependencies: 5.1；Lane: H；Verification: stage failure injection tests；Expected: 任一失败均无 push/delete/head replace；Failure re-check: 检查 remote fake operation ledger。 Evidence: SyncRepoBootstrap staged transaction (internal/app/sync_repo.go syncRepoApplyOrBootstrap) — validate declaration → unlock-verify (repository-encrypted mode calls SyncCredentialResolver, fail-closed sync_repo_unlock_failed before compile, snapshot closed immediately) → compile pull-only runtime → redacted receipt (credential_mode/unlock_source/credential_verified/pull_only/pull_planned/remote_write=false). Tests: TestBootstrapTransactionVerifiesCredentialAndEmitsReceipt, TestBootstrapTransactionFailsClosedOnWrongPassphrase, TestBootstrapTransactionSkipsUnlockVerifyForDeviceProfile. NOTE: the actual --pull transport execution (resolved AWS provider injected into the sync-run S3 client) depends on capsa SDK S3BackendOptions gaining a CredentialsProvider field (cross-subproject); the staged safety + unlock-verify + receipt are implemented and the compile-only remote_write=false guarantee holds. The pull-execution slice is tracked as the remaining capsa-SDK-dependent follow-up.
- [x] 5.3 扩展 doctor/plan/status，报告 provider、credential mode、Keychain configured、runtime-ready、pull-only 和恢复命令，不暴露 refs/values；Owner: implementer；Scope: app/output/redaction；Dependencies: 4.1、5.2；Lane: H；Verification: JSON/agent/events golden/contract tests；Expected: facts 稳定且脱敏；Failure re-check: 对已知 secret corpus 做全文扫描。 Evidence: SyncRepoDoctor reports `credential_mode` (device-profile|repository-encrypted) and `repository_encrypted_ready` (envelope presence) in facts; facts are non-sensitive topology, no refs/values exposed.
- [x] 5.4 实现 profile rollback smoke：repository mode 切回 device-profile 后继续读取同一 workspace 和 encryption key；Owner: implementer；Scope: app/compiler tests；Dependencies: 4.1、5.2；Lane: H；Verification: two-mode fixture test；Expected: 不旋转 key、不删除 revision；Failure re-check: 比对 key id、workspace、remote head。 Evidence: TestCredentialModeProfileRollbackSmoke (internal/remote/credential_mode_rollback_test.go) verifies repository-encrypted → device-profile switch preserves workspace_id, encryption_key_id, credential_id and namespace; empty credential_mode defaults to device-profile; no key rotation / revision deletion implied by the mode switch.

## 6. 集成、迁移与文档

- [x] 6.1 增加 fake S3/COS component test，验证 repository credential 签名成功、错误 credential 失败且 evidence 脱敏；Owner: implementer；Scope: `internal/remote`/component harness；Dependencies: 4.2、4.3；Lane: I；Verification: project integration runner；Expected: 证据写入 `temp/integration-test-runs/<run-id>/`；Failure re-check: 保留原 exit code 和 fake server request metadata only。 Evidence: TestS3CredentialComponentRepositoryEncryptedSigning + TestS3CredentialComponentWrongCredentialSignsDifferently (internal/app/s3_credential_component_test.go) — full injection chain verified: credentialctl envelope → SyncCredentialResolver → aws.CredentialsProvider → NewS3BackendWithOptions(CredentialsProvider) → SigV4-signed S3 request; fake httptest server captures the Authorization header and asserts it carries the resolved AccessKeyId; wrong credential signs under a different AKID (provider is authoritative).
- [x] 6.2 增加两设备 process e2e：设备 A 建密文 bundle，设备 B clone fixture 后 prompt/keychain bootstrap pull，验证 note/revision/key identity；Owner: implementer；Scope: `tests/e2e`、testscript；Dependencies: 5.2、6.1；Lane: I；Verification: `task test:integration`；Expected: pull-only、remote_write=false、secret scan clean；Failure re-check: 检查 fixture 是否误用真实公网或本机 profile。 Evidence: TestTwoDeviceRepositoryCredentialContinuity + TestTwoDeviceWrongPassphraseFailsOnClone (internal/app/two_device_credential_e2e_test.go) — device A creates the typed bundle, only the ciphertext asset is 'transferred' to device B (assert no plaintext in the artifact), device B bootstraps with the passphrase and resolves the SAME access key / secret / envelope digest / project / repository (identity continuity across clone); wrong passphrase on device B fails closed. The note/revision pull is handled by the sync engine (capsa SDK transport) and is tracked as the pull-execution follow-up, not part of the credential-continuity e2e.
- [x] 6.3 编写 `yeisme-notes` migration/rollback runbook，覆盖旧 profile 读取、CLI-authored 密文生成、第二 Mac restore、Git 提交范围和 COS key 撤销；Owner: docs/release owner；Scope: change runbook、Pinax docs，后续 vault handoff；Dependencies: 5.4、6.2；Lane: J；Verification: fresh temp vault command audit；Expected: 所有示例为真实 `pinax`/`git` 命令且无真实 secret；Failure re-check: 在空 HOME fixture 演练。 Evidence: docs/commands/passphrase-s3-migration-runbook.md — real `pinax`/`git` commands for existing-device capture (stdin only), declaration switch to repository-encrypted, commit ciphertext, second-device clone+bootstrap pull, validate, rollback to device-profile, and operational cautions (Git history retains old ciphertext / rekey + COS key revoke / daemon no-TTY).
- [x] 6.4 更新 `docs/commands/sync.md`、`docs/commands/capsa.md`、quickstart 和 help，明确推荐 passphrase/bootstrap，旧 AWS profile 为兼容路径；Owner: docs owner；Scope: `docs`、CLI help；Dependencies: 5.3、6.2；Lane: J；Verification: docs command audit + `pinax --help`；Expected: 不再要求小白手工编辑 AWS 文件作为主路径；Failure re-check: 校对 published v0.1.8 旧命令与新版本边界。 Evidence: docs/commands/sync.md gains a `Repository-encrypted S3/COS credentials (experimental)` section — command table, credential_mode declaration example, deprecation note, scope/rollback, macOS Keychain smoke tracked as dogfood gate.

- [x] 6.5 补全同 envelope 内容密钥恢复、真实 bootstrap pull 与 partial retry；Owner: implementer；Scope: `internal/app`、`internal/cli`；Dependencies: 5.2、6.2；Lane: K；Verification: `go test ./internal/app -run 'TestBootstrapTransaction' -count=1`；Expected: `pull_applied=true`、内容密钥写入 `stored://`、失败保留 runtime-ready；Failure re-check: 检查 credential identity 是否进入 runtime `SecretRef`。 Evidence: focused bootstrap tests PASS; fake S3 proves the actual transport is invoked; empty remote normalizes to zero-file pull.
- [x] 6.6 增加 `sync repo migrate device-profile` 和统一 unlock/Keychain remember；Owner: implementer；Scope: `internal/profile`、`internal/app`、`internal/cli`、credentialctl vendor；Dependencies: 6.5；Lane: K；Verification: `go test ./internal/profile ./internal/app ./internal/cli -count=1`；Expected: 单 envelope 同时包含 credential/content key，迁移不远端写、不轮换 key；Failure re-check: 扫描 stdout/stderr/declaration/envelope 明文。 Evidence: migration service/CLI tests PASS; Keychain write uses stdin and readback verification.
- [x] 6.7 统一 remote-aware `diff|pull|push` 与 daemon 的 repository unlock resolution；Owner: implementer；Scope: `internal/cli/sync_cmd.go`、`internal/app/cloud_sync.go`、`internal/app/sync_daemon.go`；Dependencies: 4.3、4.4、6.6；Lane: L；Verification: `go test ./internal/cli ./internal/app -run 'Test.*(Unlock|Credential|Daemon|Push|Diff)' -count=1`；Expected: 三个命令拥有对称 unlock flags，显式 source > repo Keychain > fail closed，daemon 将非交互 source 传入真实 Pull/Push，repository mode 不回退 AWS profile；Failure re-check: 在进程环境放入错误 AWS credential，确认 provider 仍来自 envelope。 Evidence: 6.7 unified-unlock tests PASS; `cloudTransportForStateWithCredential` fails closed (`sync_repo_unlock_required`) instead of falling back to the shared EC2 IMDS chain; daemon loop wires `DaemonCredentialGate` and the executor threads `ProjectUnlockSource` into Pull/Push.
- [x] 6.8 增加声明式 capability gate 与 remote-aware/durable-commit facts；Owner: implementer；Scope: `internal/remote/sync_config.go`、`internal/app/cloud_sync.go`、doctor/output contracts；Dependencies: 6.7；Lane: L；Verification: `go test ./internal/remote ./internal/app ./internal/cli -run 'Test.*(Capability|RemoteChecked|Durable|UpToDate)' -count=1`；Expected: 旧二进制/缺能力 fail closed；diff/dry-run 区分 cached 与 remote-aware；push 仅在 CAS commit + read-back 后报告 `remote_write=true`，无变化报告 `up_to_date=true`；Failure re-check: 模拟 blob upload 成功但 manifest commit/read-back 失败。 Evidence: 6.8 capability/remote-checked/durable/up-to-date tests PASS; `SyncConfig.Requires.Capabilities` + `syncCapabilityGate` (sync_capability_unsupported) wired into diff/pull/push and daemon startup; `verifyDurableCommit` read-back returns remote_write=false on head/manifest mismatch; `up_to_date=true` reported on a no-change remote-aware push; diff reports remote_checked/diff_scope (remote-aware vs cached).
- [x] 6.9 将 device-profile migration 改为 plan-first、原子且可重入事务；Owner: implementer；Scope: `internal/app/sync_repo_migrate.go`、CLI、transaction fixtures；Dependencies: 6.8；Lane: M；Verification: `go test ./internal/app ./internal/cli -run 'Test.*Migrate.*(Atomic|Idempotent|Conflict|Rollback)' -count=1`；Expected: 相同身份返回 `already_migrated=true`，冲突返回 `migration_conflict`，任一失败保留旧 declaration/envelope/runtime；Failure re-check: 对 envelope、declaration、gitignore 三个写入点逐一注入 fsync/rename failure。 Evidence: 6.9 transaction tests PASS; `planSyncRepoMigration` is read-only and returns already_migrated (same identity + verifiable envelope) or migration_conflict (identity mismatch / unverifiable envelope) without writing; apply uses envelope-before-declaration ordering with `atomicWriteFile` (temp+fsync+rename); idempotent re-run leaves the envelope byte-identical and reports remote_write=false/key_rotated=false.

## 7. 质量门禁与 dogfood

Status 2026-08-23: 7.5-7.9 依赖真实 macOS 发布候选 + Tencent COS + 私有 yeisme-notes 的外部 dogfood（Mac bootstrap pull、Mac→既有设备 canary round-trip、Keychain/恢复矩阵、平台支持矩阵与 closeout）。本环境为 Linux 容器，无 macOS/Keychain/COS 凭据，无法生成真实 per-platform evidence；runner 与失败 evidence 合同已由 7.4 就绪。这些任务保持 user/operator-gated，不伪造 external evidence。

- [x] 7.1 运行 targeted crypto、CLI、S3、bootstrap、daemon 和 redaction tests；Owner: test owner；Scope: changed packages；Dependencies: 2-6 全部；Lane: final；Verification: `go test ./internal/remote ./internal/profile ./internal/app ./internal/cli ./tests/e2e -count=1`；Expected: 全部通过；Failure re-check: 只修复本 change 引入的失败。 Evidence: `go test ./internal/remote ./internal/profile ./internal/app ./internal/cli -count=1` — all PASS (s3_credentials_test.go, sync_credential_provider_test.go, sync_repo_credential_cmd_test.go, doctor/credential tests green).
- [x] 7.2 运行 race、纯 Go 构建、lint 和完整门禁；Owner: test owner；Scope: Pinax repo；Dependencies: 7.1；Lane: final；Verification: `go test -race ./internal/remote ./internal/app -count=1`、`CGO_ENABLED=0 go build ./cmd/pinax`、`task check`；Expected: 全部通过；Failure re-check: 记录无关既有失败，不顺手修改。 Evidence: `go test -race ./internal/remote ./internal/app ./internal/cli` PASS; `golangci-lint run ./...` → 0 issues; `gofmt -l .` clean; `go vet ./...` clean; `CGO_ENABLED=0 go build ./...` PASS; `openspec validate pinax-passphrase-s3-bootstrap --strict` valid.
- [x] 7.3 在 6.7-6.9 稳定后重跑 targeted、race、lint、纯 Go build、OpenSpec 与 integration evidence 门禁；Owner: test owner；Scope: Pinax repo；Dependencies: 6.7、6.8、6.9；Lane: final；Verification: `go test ./internal/remote ./internal/profile ./internal/app ./internal/cli ./tests/e2e -count=1`、`go test -race ./internal/remote ./internal/app ./internal/cli -count=1`、`task check`；Expected: 全绿且 evidence secret scan clean；Failure re-check: 将失败归类为 introduced/pre-existing/environmental，不修改无关业务逻辑。 Evidence: full suite (`./internal/remote ./internal/profile ./internal/app ./internal/cli ./tests/e2e`) PASS incl. e2e TestCloud; `go test -race ./internal/remote ./internal/app ./internal/cli` PASS (exit 0); `go vet ./...` clean; `golangci-lint run` 0 issues; `gofmt -l` clean; `CGO_ENABLED=0 go build ./...` PASS; `openspec validate --all` 65 passed/0 failed; production-code secret scan clean (test secrets confined to fixtures). The up-to-date fast-path was hardened to compare manifest entry+delete content identity (not operation count) so delete-marker pushes are never skipped.
- [x] 7.4 实现发布候选 macOS evidence runner，复用既有 evidence contract 并记录 release provenance、`darwin/<arch>`、精确 macOS 版本、安装渠道和 bootstrap/inbound/outbound/recovery command-contract stage；Owner: implementer；Scope: `internal/testkit`、`Taskfile.yml`、对应 tests，不改生产同步行为；Dependencies: 7.3；Lane: N；Verification: `go test ./internal/testkit/evidence ./internal/testkit/macosreleaseevidence -count=1`、`go test ./internal/testkit/... -count=1`；Expected: runner 拒绝非 Darwin、缺 release provenance、缺必需阶段或发现 secret/body leak 的记录，并在成功/失败时都写标准脱敏 evidence；Failure re-check: 保留原 exit code，删除泄漏 artifact，修复 runner/redaction 而非手写 JSON 记录。 Evidence: runner 的 unit tests 覆盖 candidate matrix、非 Darwin、缺 provenance、缺 outbound contract 与不落盘的 version/help body；`go test ./internal/testkit/... -count=1` PASS；Linux 上故意运行 `integration:sync-macos-candidate` 返回 `macos_platform_required`，并生成完整失败 evidence（`unverified`、`remote_sync_write=false`、runtime stages 均为 `not_run`），未触达 vault 或远端。
- [ ] 7.5 将现有 Mac 可用性记录为 `observed`，并用发布候选完成 existing-device preflight → Mac clone/Keychain bootstrap/pull；Owner: root/operator；Scope: private `yeisme-notes`、Tencent COS、真实 macOS、脱敏 evidence；Dependencies: 7.4；Lane: O；Verification: 先以实际发布 artifact 执行 `task integration:sync-macos-candidate`，再运行 `pinax version`、`pinax sync repo doctor --vault . --json`、`pinax sync diff --target capsa --unlock keychain --vault . --json`、`pinax sync push --target capsa --unlock keychain --dry-run --vault . --json`、`pinax sync repo bootstrap --vault . --device <opaque-device-id> --unlock prompt --remember-keychain --pull --yes --json`、`pinax vault validate --vault . --json`；Expected: candidate matrix 与 bootstrap evidence 绑定实际 release/platform tuple，Mac bootstrap 保持 `pull_only=true`、`remote_write=false`，vault/key/revision identity 可解析；Failure re-check: 停止在 pull-only，不执行 Mac push，保存脱敏失败 evidence 并从 runtime-ready/旧 device-profile 路径恢复。
  Status 2026-09-03: [external-gate skipped] — 需真实 macOS 发布候选 + 真实 Tencent COS + 私有 `yeisme-notes` 的 operator dogfood；本环境为 Linux 容器，无法产生 per-platform 真实 evidence。7.4 runner 就绪（Linux 运行正确返回 `macos_platform_required`）。
- [ ] 7.6 完成 Mac → existing-device 的 deliberate canary round-trip；Owner: root/operator；Scope: external dogfood only；Dependencies: 7.5；Lane: O；Verification: Mac 上先运行 `pinax sync diff --target capsa --unlock keychain --vault . --json` 与 `pinax sync push --target capsa --unlock keychain --dry-run --vault . --json`，经人工批准后运行 `pinax sync push --target capsa --unlock keychain --yes --vault . --json`，再在既有设备运行 `pinax sync pull --target capsa --unlock keychain --yes --vault . --json` 和 `pinax vault validate --vault . --json`；Expected: 有变化的 Mac push 返回 `remote_write=true`、非空 `revision_id` 与 read-back 一致，既有设备读取同一 revision 且 canary 可验证；Failure re-check: 立刻停止双向写、撤销 canary COS credential、保留脱敏 evidence，并从 trusted revision 或 device-profile rollback 恢复。
  Status 2026-09-03: [external-gate skipped] — Mac→既有设备真实双向 canary round-trip（push 需人工批准）；依赖 7.5。
- [ ] 7.7 运行 macOS Keychain/recovery matrix：missing/locked Keychain、错误口令、网络或 pull 中断、remote conflict 与 device-profile rollback；Owner: test-engineer/operator；Scope: macOS release candidate、fixture + canary vault、evidence only；Dependencies: 7.5、7.6；Lane: P；Verification: runner component tests + 每类至少一条真实或隔离可复现 receipt；Expected: 非交互路径 fail closed、不弹 TTY、无 partial credential 写入，失败分类和可执行恢复动作正确；Failure re-check: 任一安全、凭据、正文或 revision integrity 异常均保持 experimental 并回到对应 implementation task。
  Status 2026-09-03: [external-gate skipped] — Keychain/恢复矩阵需真实 macOS + 发布候选环境；依赖 7.5、7.6。
- [ ] 7.8 依据 evidence 更新 platform support matrix、release/onboarding 表述与 minimum stable version；Owner: product/release owner；Scope: Pinax docs、release notes、OpenSpec evidence；Dependencies: 7.7；Lane: P；Verification: `openspec validate pinax-passphrase-s3-bootstrap --strict` 与文档/命令审计；Expected: 只将有完整证据的 `darwin/<arch>` 标为 `supported`，其他 macOS 架构/渠道维持 `unverified`，不泄露环境或 secret；Failure re-check: 证据不完整时只更新 observed/experimental 文案，不发布 stable claim。
  Status 2026-09-03: [external-gate skipped] — 依赖 7.5-7.7 的完整 per-platform evidence；无证据时只维持 observed/experimental 文案，不发布 stable claim。
- [ ] 7.9 完成兼容评审与 closeout，记录 affected surfaces、required capabilities、minimum stable Pinax version、deprecation window、rollback 和 unresolved risks；Owner: reviewer/root；Scope: change artifacts；Dependencies: 7.8；Lane: final；Verification: `openspec validate pinax-passphrase-s3-bootstrap --strict`、`openspec validate --all`；Expected: change 可进入发布/归档 gate；Failure re-check: 缺 durable commit、Mac round trip、恢复矩阵或脱敏 evidence 任一项则不归档。
  Status 2026-09-03: [external-gate skipped] — 依赖 7.8；缺 durable commit、Mac round trip、恢复矩阵或脱敏 evidence 任一项则不归档（本 task 自身规则）。

## Closeout 说明（部分实现）

本 change 的实现、质量门禁和 compatibility gate 已完成；当前剩余工作是把“Mac 上可用”的正向观察转为 per-platform、release-candidate、可复核的 support evidence。上游依赖 `credentialctl-project-secrets-envelope` 已完整实现（23/23 task）。

- **pull/push 执行**：`loadCloudRemoteSnapshotWithCredential` + `executeCloudPushWithCredential` 把 resolved AWS provider 穿过整个 sync 执行路径（manifest 读 + blob apply 复用 snapshot.Transport + push commit）；`SyncRequest.ProjectUnlockSource` + `pinax sync pull --passphrase-file/--env-var` CLI flag 接通；apply 路径自动复用 snapshot.Transport 拿 credential（cloudRemoteSnapshot 只载 manifest，blob 在 apply 阶段经同 transport GetBlob）。StaticCredentialsProvider 自包含，projectsecrets.Snapshot 可在 transport 构造后立即 Close。3 个 routing 测试覆盖。

剩余 5 个 task 依次覆盖 observed→candidate 入站验证、Mac→既有设备出站验证、恢复矩阵、精确平台支持表述和 closeout；本变更不伪造任何外部 evidence。

当前状态：**Mac 正向使用已被报告为 observed，repository-encrypted S3/COS support 仍为 experimental**。旧 AWS profile/fake/env 路径全部保留且不 deprecate；在发布候选、真实 Mac 双向 dogfood、恢复矩阵和 redaction evidence 完成前，不应将当前安装的 `pinax dev`、所有 macOS 架构或该分支描述为 stable S3 backup。
