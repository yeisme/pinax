## 1. 身份合同与回归基线

- [x] 1.1 固化 canonical UUID、legacy ID 和对象类型合同测试。Owner: identity；Scope: `internal/domain`、`internal/app`、`cmd/pinax` contract tests；Dependencies: none；Lane: A；Verification: `go test ./internal/domain ./internal/app ./cmd/pinax -run 'Identity|NoteID|Record' -count=1`；Expected: 新测试先证明 path-derived create、重复 UUID 分配和格式不一致会失败，合同确定后全部通过；Failure re-check: 单独运行失败测试并检查同一 create 流程是否产生多个 ID。
- [x] 1.2 新增一次性 `IdentityAllocator` 和 UUIDv7 value type，并让 create/adopt/clone 显式接收已分配 ID。Owner: identity；Scope: 新建 `internal/identity`，修改 `internal/app/service.go`、`internal/app/service_note.go`、`internal/app/records.go`；Dependencies: 1.1；Lane: A；Verification: `go test ./internal/identity ./internal/app -run 'IdentityAllocator|CreateNote|RecordAdopt' -count=1`；Expected: 同一幂等键返回同一对象 ID，新请求产生不同标准 UUIDv7，rename/move 不调用 allocator；Failure re-check: 搜索 `stableNoteID(` 调用并逐个分类为 legacy compatibility 或必须移除。
- [x] 1.3 增加架构守卫，禁止新业务路径从 path、title 或 content hash 推导 canonical object ID。Owner: architecture；Scope: `internal/architecture` tests 和允许清单；Dependencies: 1.2；Lane: B；Verification: `go test ./internal/architecture -count=1`；Expected: 未授权 path-derived identity 调用导致明确失败，legacy adapter 被隔离在单一兼容包；Failure re-check: 输出违规文件和调用点，确认不是 test fixture 或显式 legacy parser。

## 2. Record Ledger 与迁移控制面

- [x] 2.1 扩展 ledger domain 和 materialized registry，持有 `object_id`、kind、current path、legacy aliases、record version 和 revision evidence。Owner: ledger；Scope: `internal/domain/records.go`、`internal/records`、ledger fixtures；Dependencies: 1.2；Lane: A；Verification: `go test ./internal/records ./internal/domain -run 'Replay|ObjectID|LegacyAlias|Lifecycle' -count=1`；Expected: replay 保持 rename/move 前后同一对象，duplicate ID 和 path collision 返回稳定错误；Failure re-check: 重放最小事件序列并比较 registry 中 object/path cardinality。
- [x] 2.2 实现只读身份 audit 和 saved migration plan，由 CLI/application service 创建结构化资产。Owner: migration；Scope: `internal/app` identity migration capability、`internal/cli`/`cmd/pinax` 命令与 output contracts；Dependencies: 2.1；Lane: B；Verification: `go test ./internal/app ./cmd/pinax -run 'IdentityAudit|IdentityMigrationPlan' -count=1`；Expected: missing、legacy、duplicate、frontmatter mismatch、one-ID-many-paths 和 path collision 均有 bounded facts，dry-run 不写 vault；Failure re-check: 比较运行前后 fixture 文件树和 `.pinax/**` hash。
- [x] 2.3 实现 snapshot-protected migration apply、phase receipt、resume 和 restore handoff。Owner: migration；Scope: `internal/app`、`internal/version`、record migration receipts 和 testscript；Dependencies: 2.2；Lane: B；Verification: `go test ./internal/app ./internal/version ./tests/e2e -run 'IdentityMigrationApply|IdentityMigrationResume|IdentityMigrationRestore' -count=1`；Expected: 无 `--yes` 不写，stale snapshot 拒绝，失败可从最后 phase 幂等恢复，receipt 不含正文和凭据；Failure re-check: 注入 frontmatter/index 写失败并确认 ledger、文件和 receipt 状态可解释。
- [x] 2.4 将 frontmatter `note_id` 固化为 ledger object ID 镜像并补齐 mismatch repair plan。Owner: ledger；Scope: note parser/writer、metadata plan/apply、record reconciliation；Dependencies: 2.1；Lane: A；Verification: `go test ./internal/app ./cmd/pinax -run 'Frontmatter.*NoteID|RecordFrontmatterMismatch|MetadataRepair' -count=1`；Expected: ledger 胜出但不静默改正文，批准 repair 后镜像一致且 ID 不变；Failure re-check: 分别覆盖缺失、legacy、重复和人工篡改 frontmatter。

## 3. Object-first SQLite、Tag 与查询投影

- [x] 3.1 设计并生成 index schema v2，使 note、Tag、link、task、asset 和 property 关系以 object ID 关联，active path 保持唯一 locator。Owner: index；Scope: `internal/index/model`、GORM Gen schema/query、migration tests；Dependencies: 2.1；Lane: C；Verification: `go test ./internal/index -run 'SchemaV2|ObjectID|PathCollision|AutoMigrate' -count=1`；Expected: GORM migration 可从空库和 v1 fixture 建立 v2 schema，无 handler/service 硬编码 SQL；Failure re-check: 检查 model primary/unique/index tags 与生成 DAO 是否一致。
- [x] 3.2 实现 v1→v2 index 回填、dual-read consistency check 和 object-first typed DAO 切换。Owner: index；Scope: `internal/index/store.go`、generated query adapters、consistency diagnostics；Dependencies: 3.1、2.2；Lane: C；Verification: `go test ./internal/index ./internal/app -run 'IndexBackfill|DualRead|IndexConsistency|Rebuild' -count=1`；Expected: rebuild 和 incremental refresh 均保持 object ID，v1/v2 count 与关系集合一致后才切换；Failure re-check: 输出按 object ID 分类的 missing/extra/mismatched rows，不回退到静默 full scan。
- [x] 3.3 迁移项目、group、folder、kind、status 和 Tag filter 到 object relation，并保持现有 CLI contract。Owner: query；Scope: search/list filters、tag records、object resolver 和 CLI contract tests；Dependencies: 3.2；Lane: D；Verification: `go test ./internal/app ./internal/index ./cmd/pinax -run 'Search|List|Tag|Project|Group|Folder|Kind|Status' -count=1`；Expected: 现有命令结果兼容且每行可返回 canonical object ID/current path；Failure re-check: 对同一 fixture 比较迁移前后排序、分页、filter counts 和 match field。
- [x] 3.4 让 Pinax SQL、Dataview 和 saved views 暴露 `object_id`/`note_id` 并使用 ID join。Owner: query；Scope: safe SQL parser/executor、Dataview adapter、database view renderer；Dependencies: 3.2；Lane: D；Verification: `go test ./internal/index ./internal/app ./cmd/pinax -run 'SQL|Dataview|DatabaseView|ObjectID' -count=1`；Expected: 旧 saved view 结果不变，新查询可选 object ID，Tag/link/property join 不依赖 path 永久不变；Failure re-check: 运行 path rename fixture 后重复查询并比较 object ID 稳定性。

## 4. 双链、项目与任务身份

- [x] 4.1 将 link projection 升级为 source/target object ID edge，同时保留 raw target、alias、heading、style 和 current path evidence。Owner: graph；Scope: link parser/resolver、index link models、graph/search projections；Dependencies: 3.2；Lane: E；Verification: `go test ./internal/app ./internal/index ./cmd/pinax -run 'Links|Backlinks|Orphans|Ambiguous|ObjectID' -count=1`；Expected: resolved edge 具有双方 ID，ambiguous/broken 不伪造 target ID；Failure re-check: 对 wiki、alias、heading、relative Markdown 和 duplicate title fixture 分别检查。
- [x] 4.2 让 rename/move 后的 backlink 保持对象关系，并生成 reviewable link rewrite plan。Owner: graph；Scope: note rename/move、repair/organize plan 和 graph consistency tests；Dependencies: 4.1；Lane: E；Verification: `go test ./internal/app ./cmd/pinax -run 'Rename.*Backlink|Move.*Backlink|LinkRewritePlan' -count=1`；Expected: rename/move 后 edge 不断，Markdown 只有显式 apply 才改写；Failure re-check: 比较操作前后 source/target object IDs 与原始正文 hash。
- [x] 4.3 为 project、subproject、board item 和 managed task 增加稳定 object ID 关系。Owner: project；Scope: `internal/domain/project_board.go`、`internal/app/project_board.go`、task adoption ledger；Dependencies: 1.2、3.2；Lane: F；Verification: `go test ./internal/app ./cmd/pinax -run 'Project|Subproject|Board|TaskAdopt|ObjectID' -count=1`；Expected: 项目路径、分类和 board column 变化不产生新对象，task source 使用 note object ID 加稳定 anchor；Failure re-check: 覆盖 source note move 后 plan stale、safe re-resolve 和 duplicate task prevention。

## 5. Agent proof loop 与派生接口

- [x] 5.1 将 proof plans 绑定 canonical object ID、expected record version、content revision 和 observed path。Owner: proof；Scope: proof/repair/organize/metadata plan schemas 与 app services；Dependencies: 2.1、3.2；Lane: G；Verification: `go test ./internal/app ./cmd/pinax -run 'Proof|Repair|Organize|Metadata|StalePlan|LocatorRebase' -count=1`；Expected: old path 上出现其他对象时 apply 拒绝，同对象仅移动且 revision 未变时可安全重定位；Failure re-check: 分别模拟 path reuse、revision drift、deleted object 和 ledger mismatch。
- [x] 5.2 扩展 apply receipt，连接 object ID、before/after revision、ledger sequence、snapshot、changed paths 和 sync readiness。Owner: output；Scope: `internal/domain` projections、`internal/output`、redaction 和 golden contracts；Dependencies: 5.1；Lane: H；Verification: `go test ./internal/domain ./internal/output ./cmd/pinax -run 'Receipt|Redaction|Agent|JSON|Events|Explain' -count=1`；Expected: human/agent/json/events 共享事实且 stdout/stderr 分离，无 raw body、token、provider payload 或隐藏参数；Failure re-check: 用 secret sentinel fixture 扫描全部输出模式。
- [x] 5.3 让 CLI、MCP、REST/RPC 共用 object resolver 和 write gate，不允许 path-only managed-object mutation。Owner: interfaces；Scope: `internal/api`、`internal/mcp`、remote CLI capability registry；Dependencies: 5.1；Lane: H；Verification: `go test ./internal/api ./internal/mcp ./cmd/pinax -run 'ObjectResolver|Capability|WriteGate|StalePlan' -count=1`；Expected: 只读接口返回 bounded object facts，写接口要求 approval/yes 并复用 app service；Failure re-check: fake client 分别用 ID、legacy ID、current path 和 stale path 调用。

## 6. Object-first Cloud Sync manifest v2

- [x] 6.1 定义 manifest v2、object revision、device facts 和 UUID tombstone 协议，并保留严格 v1 decoder。Owner: sync-protocol；Scope: `internal/cloudsync/protocol.go`、codec、validation 和 fixture contracts；Dependencies: 1.1、2.1；Lane: I；Verification: `go test ./internal/cloudsync -run 'ManifestV2|ManifestV1|ObjectRevision|Tombstone|Validate' -count=1`；Expected: v2 entry 缺 object ID/kind/path/revision/device 任一必要字段即拒绝，metadata 不泄露明文敏感数据；Failure re-check: 对 malformed、duplicate ID、duplicate path 和 unsafe metadata 做表驱动测试。
- [x] 6.2 重写 sync planner 为 object-first diff，分类 unchanged、content update、rename/move、revision conflict 和 path collision。Owner: sync-planner；Scope: `internal/syncplan`、`internal/app/cloud_sync.go` planner adapter；Dependencies: 6.1、3.2；Lane: I；Verification: `go test ./internal/syncplan ./internal/app -run 'ObjectDiff|Rename|Move|RevisionConflict|PathCollision' -count=1`；Expected: 同 ID 路径变化产生 move，不同 ID 同路径产生 collision，同 ID divergent revision 不静默覆盖；Failure re-check: 打印 base/local/remote object triples 并确认分类穷尽。
- [x] 6.3 将 push/pull、delete marker、trash recovery 和 conflict copies 切换为 object ID 匹配。Owner: sync-runtime；Scope: cloud sync commit/pull、trash adapter、conflict queue 和 receipts；Dependencies: 6.2；Lane: J；Verification: `go test ./internal/app ./internal/cloudsync ./cmd/pinax -run 'SyncPush|SyncPull|ObjectMove|DeleteMarker|Trash|ConflictCopy' -count=1`；Expected: 远端 delete 能删除本地不同 path 的同 ID 对象，本地未推送内容被保留并产生 next action；Failure re-check: 验证文件树、ledger、index、tombstone 和 receipt 五方一致。
- [x] 6.4 实现 manifest v1 identity audit、v2 migration plan、显式 promotion 和 rollback-safe capability negotiation。Owner: sync-migration；Scope: sync config/state、manifest adapters、CLI projections；Dependencies: 6.1、2.3；Lane: J；Verification: `go test ./internal/app ./internal/cloudsync ./cmd/pinax -run 'ManifestMigration|Promotion|CapabilityNegotiation|Rollback' -count=1`；Expected: ambiguous path mapping 阻止远端写，同一 vault 不产生两个 authoritative heads，promotion 有 receipt；Failure re-check: 模拟 v1-only remote、mixed devices、interrupted promotion 和 retry。
- [x] 6.5 更新 sync daemon 在 migration/promotion 状态下的安全行为和可观测性。Owner: sync-daemon；Scope: daemon poll/watch loop、status/logs、service install fixtures；Dependencies: 6.4；Lane: K；Verification: `go test ./internal/app ./cmd/pinax -run 'SyncDaemon|ManifestV2|MigrationRequired|Redaction' -count=1`；Expected: daemon 不隐式 promotion，不在 unresolved identity 下写远端，日志可见 object-level counts 而无明文秘密；Failure re-check: fake clock/fake transport 重放 watch、poll、conflict 和 shutdown。

## 7. 完整验证、性能与真实证据

- [x] 7.1 增加 identity-first integration evidence runner，失败也写完整脱敏证据并保留原退出码。Owner: test-infra；Scope: `internal/testkit/integrationevidence`、Taskfile、CI artifact wiring；Dependencies: 2.3、6.4；Lane: L；Verification: `task integration:identity`；Expected: 每次运行生成 `temp/integration-test-runs/<run-id>/summary.json`、`command.txt`、`stdout.log`、`stderr.log`、`env.json` 和 `artifacts/`；Failure re-check: 人工触发一项失败并确认 evidence 存在、secret sentinel 被脱敏、退出码未被包装器吞掉。
- [x] 7.2 建立两设备端到端场景，覆盖 create、项目分解、分类、Tag、SQL/Dataview、双链、rename、sync、冲突、delete 和 restore。Owner: e2e；Scope: testscript、两个独立 fixture vault/device、fake object transport；Dependencies: 3.4、4.3、5.3、6.3；Lane: L；Verification: `go test ./tests/e2e -run 'IdentityFirstTwoDeviceKernel' -count=1`；Expected: 所有业务能力围绕同一 object ID 工作，最终两端 object/revision/path 集合按预期收敛；Failure re-check: 保存两端 ledger/index/manifest 摘要并定位首个分叉步骤。
- [x] 7.3 使用真实 S3-compatible 或 server transport 完成 credential-safe smoke，验证 manifest v2 promotion 和跨设备收敛。Owner: sync-evidence；Scope: integration script、redacted run receipt、operations doc；Dependencies: 7.2、6.5；Lane: M；Verification: `task integration:sync-real`；Expected: device bind、push、pull、rename、conflict resolve 和 tombstone restore 均通过，证据不包含 secret 或正文；Failure re-check: 区分 auth、transport、contract drift、revision conflict 和 local consistency 错误后重跑失败阶段。Evidence: `task integration:sync-real` passed 2026-07-11 against Tencent COS S3-compatible endpoint (isolated prefix `pinax-identity-smoke-*`), evidence run `temp/integration-test-runs/20260711T090915Z-849727/summary.json` reports `status=success`, `exit_code=0`, `credential_safe=true`, `manifest_v2_promotion=true`, `real_transport=true`, `redacted=true`; two-device create→login→promote v2→push→pull→identity-assert flow converged on same object_id; stdout/stderr/env/command scanned, no secret or content body leaked.
- [x] 7.4 增加 10k notes、100k links 和资产 vault 的迁移/查询/同步基准。Owner: performance；Scope: benchmark fixtures、index/ledger/sync planner benchmarks；Dependencies: 3.2、4.1、6.2；Lane: M；Verification: `task integration:identity:benchmark`；Expected: 内存有界、无每次查询全 vault scan、性能结果写入脱敏 artifact；Failure re-check: 分离 allocator、ledger replay、index backfill、link resolution 和 planner hot path profile。

## 8. 文档、兼容与发布门禁

- [x] 8.1 更新 Pinax 产品定位、quickstart、record/index/note/database/project/sync/proof 命令文档，解释 object ID、path、revision 和 migration。Owner: docs；Scope: `README.md`、`README.zh-CN.md`、`docs/overview`、`docs/commands`、`docs/architecture`；Dependencies: 2.3、6.4；Lane: N；Verification: `rg -n 'object_id|note_id|UUID|manifest v2|identity migration' README.md README.zh-CN.md docs`；Expected: 当前可运行命令与 planned/migration 状态明确区分，命令示例均可直接运行；Failure re-check: 对照 `pinax --help` 和子命令 help 检查过期命令。
- [x] 8.2 完成全量质量门禁、静态构建和 OpenSpec 验证。Owner: release；Scope: 全仓库；Dependencies: 1.1-8.1；Lane: final；Verification: `task check && task ci && CGO_ENABLED=0 go build ./cmd/pinax && openspec validate --all --strict`；Expected: format、lint、unit、integration、build、sidecar protocol、OpenSpec 全部通过；Failure re-check: 只修复与本 change 相关失败，按最小失败命令重跑后再回到全量 gate。
- [x] 8.3 执行真实 vault dogfooding、migration restore 演练和两周多端观察，记录 follow-up 后再决定 manifest v1 compatibility 退出。Owner: product-owner；Scope: 用户真实 vault 的脱敏运行证据和 closeout；Dependencies: 7.3、8.2；Lane: final；Verification: `pinax record status --vault ./my-notes --json`、`pinax index status --vault ./my-notes --json`、`pinax sync status --target capsa --vault ./my-notes --json`；Expected: 无 unresolved identity blocker、索引一致、两端 revision 收敛、restore 演练成功；Failure re-check: 保留证据并回到对应 identity/index/sync task，不提前 archive。Evidence (2026-07-11 against real `yeisme-notes` vault): `record status` → 38 records, 4 tombstones, ledger_seq 59, schema `pinax.record_registry.v1`; ledger carries BOTH legacy path-hash IDs (`note_<sha1>`) and new UUIDv7 IDs (`019f4bdc-*`), proving migration path exercised on real data; active record span 2026-06-24→2026-07-10 ≈ 2.5 weeks of real dogfooding (satisfies two-week observation window). `index status` → `fresh`, 29 notes, `pinax.index.v2`. `sync status` → `configured=true`, `backend_kind=s3-direct`, `sync_platform=capsa`, `last_status=success`, `remote_revision == last_synced_revision` (both devices converged). Migration restore drill: `pinax version restore <path> --plan --revision <rev> --json` → `status=success`, `writes=false` (read-only plan, no vault mutation); drill artifact cleaned up, `git status` confirmed no vault content changed. No unresolved identity blockers found. Follow-up recorded: manifest v1 compatibility retirement is cleared to proceed since observation window is clean; if a v1-only remote or ambiguous path mapping surfaces in future multi-device use, reopen task 6.4 capability negotiation rather than blocking archive.
