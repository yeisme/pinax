## 0. Execution order locks

- [x] 0.1 Owner: `cli/pinax`; Lane: sequential; Depends on: `pinax-proof-loop-operational-hardening`; Scope: confirm local proof loop has restore apply, shared projection redaction gate, proof-loop run id and widened output contracts before server sync client mutates local vault through remote state; Acceptance: `pinax-proof-loop-operational-hardening` tasks are complete or this task records a reviewed exception with evidence; Validation: `openspec validate pinax-proof-loop-operational-hardening --strict`; Expected result: validation passes and proof-loop hardening is not bypassed; Failure re-check: continue local hardening instead of server sync implementation.

  Evidence: `pinax-proof-loop-operational-hardening` 已存在且 Complete（11/11 tasks）。依赖全部满足：(1) restore apply —— `version restore apply --yes --plan <id>` 通过 `internal/app.VersionRestoreApply` + `gitstore.RestorePathFromCommit` 实现可逆恢复，校验 vault hash 拒绝 stale plan，receipt 写 `local_write=true`/`remote_write=false`（`TestVersionRestoreApplyRevertsBadLocalApply`）；(2) shared projection redaction gate —— `internal/output/projection_redaction.go` 的 `ApplyProjectionRedaction` 在 `RenderWithOptions` 渲染前对 default/json/agent/events/explain 递归脱敏 Authorization/Bearer/token/cookie/webhook/raw prompt（`TestApplyProjectionRedactionScansNestedPayloads`）；(3) proof-loop run id —— `pinax proof loop run` 产出 `proof_loop_run_id` + stage facts（`TestProofLoopRunPreviewEmitsRunIDAndStageFacts`）；(4) widened output contracts —— `TestProofLoopRunContractAcrossModes` 与 `TestVersionRestoreApplyContractAcrossModes` 覆盖四模式。server sync client 在 hardening 完成后验证通过，复用同一同步引擎与 projection 合同，proof-loop hardening 未被绕过。

- [x] 0.2 Owner: `cli/pinax`; Lane: sequential; Depends on: `backend-server/pinax-cloud:pinax-cloud-sync-mlp`; Scope: confirm Pinax Cloud MLP uses the final `vault_id` protocol and exposes revision CAS, encrypted blob boundary, redacted audit and integration evidence before CLI client parity work; Acceptance: CLI contract fixtures name `vault_id` and `/v1/vaults/*` unless a documented compatibility adapter maps old `workspace_id` fields; Validation: `go test ./internal/cloudclient -run 'Contract|Vault|Workspace|MLP' -count=1`; Expected result: tests fail on protocol drift; Failure re-check: reconcile backend and CLI contracts before push/pull orchestration.

  Evidence: `testdata/cloud_contract.json` 升级为 `pinax.cloud.contract.mlp.v1`、`terminology=vault_id`，全部操作用 `/v1/vaults/*`。`TestContractFixtureCoversMinimumCloudAPI` 断言任何 fixture path 不得含 `{workspace_id}` 或 `/workspaces/`。`cloudclient` Config 用 `VaultID`；`--workspace` 在 CLI 配置层保留为兼容字段，MLP REST 边界统一映射到 `vault_id`。`go test ./internal/cloudclient -run 'Contract' -count=1` 通过。

## 1. Contract parity and baseline

- [x] 1.1 Owner: `cli/pinax`; Lane: A; Depends on: `backend-server/pinax-cloud:pinax-cloud-sync-mlp` MLP contract draft; Scope: add or update `internal/cloudclient` contract fixtures for bootstrap/current principal, vault create/link, changes, blob batch-check, upload plan, revision commit success/conflict and audit facts; Acceptance: fixture fields match Pinax Cloud MLP REST envelope and stable error codes; Validation: `go test ./internal/cloudclient -run 'Contract|MLP|Error' -count=1`; Expected result: tests pass or fail only on documented contract mismatch; Failure re-check: update both owner contracts, not only local fake server shape.

  Evidence: `internal/cloudclient` 重写为 MLP REST 合同（`/v1/auth/bootstrap`、`/v1/auth/principal`、`/v1/vaults`、`/v1/vaults/{vault_id}/link|changes|head|blobs:batch-check|blobs:sign-upload|blobs/{blob_id}|revisions`），Config 改用 `VaultID`，新增稳定 error code（`UNAUTHENTICATED`/`DEVICE_REVOKED`/`FORBIDDEN_SCOPE`/`REVISION_CONFLICT`/`VALIDATION_FAILED`/`BLOB_MISSING`/`BACKEND_UNAVAILABLE`）。`testdata/cloud_contract.json` 升级到 `pinax.cloud.contract.mlp.v1`，`terminology=vault_id`，覆盖全部 12 个操作 + 7 个 error code + redaction_rules。新增 `internal/cloudclient/mlptest` 内存 HTTP 服务端忠实实现 MLP 合同。`go test ./internal/cloudclient -count=1` 通过（含 Contract/Bootstrap/Changes/CAS）。

- [x] 1.2 Owner: `cli/pinax`; Lane: A; Depends on: 1.1; Scope: add RED tests proving server transport never reports `remote_write=true` before CAS commit and local sync-state evidence write; Acceptance: dry-run, blob upload only, failed upload, backend unavailable and revision conflict all render `remote_write=false`; Validation: `go test ./cmd/pinax ./internal/app ./internal/cloudclient -run 'RemoteWrite|Server|CAS' -count=1`; Expected result: failing assertions identify missing gate if implementation is incomplete; Failure re-check: do not special-case output without fixing sync orchestration.

  Evidence: `TestServerTransportNeverRemoteWriteBeforeCommit` 证明 CurrentHead/BatchCheck/PutBlob/失败 commit 都不触发 RemoteWrite=true，只有 CAS commit 成功后才 true。`TestServerTransportUnavailablePreservesLocalState` 证明后端不可用时 `TRANSPORT_ERROR` 且 RemoteWrite 保持 false。`TestServerTransportConflictPreservesBothSides` 证明 REVISION_CONFLICT 时 RemoteWrite 不变 true。`cmd/pinax` 的 `TestSyncCloudPlannerCLI` 证明 CLI 端 dry-run `remote_write=false`、--yes 成功后 `remote_write=true`。

## 2. Server auth and backend state

- [x] 2.1 Owner: `cli/pinax`; Lane: B; Depends on: 1.1; Scope: wire server auth/bootstrap/login state into CLI-authored `.pinax/cloud/config.yaml` and sync-state without storing raw bearer token; Acceptance: tests cover `cloud login`, `cloud backend set server`, `cloud doctor` and `sync status` redacted server auth boundary; Validation: `go test ./cmd/pinax ./internal/app -run 'CloudLogin|BackendSetServer|Doctor|SyncStatus' -count=1`; Expected result: output identifies `backend_kind=server`, `server_audit=true` and no raw token; Failure re-check: config/state must store secret refs or token handles, not raw tokens.

  Evidence: `CloudLogin`/`CloudDoctor`/`CloudStatus` 已经存储 `secret_ref`（不是 raw token），`addCloudStateFacts` 投射 `backend_kind=server`、`server_audit=true`、`auth_boundary`，`pinaxcloud.RedactedData` 保证 stdout/json 不含 raw token。`cloudclient.Config.Token` 只在内存传递，配置与 sync-state 只保存 `secret_ref`。MLP `Bootstrap`/`CurrentPrincipal` 返回 `token_ref`/脱敏 principal，不回 raw token。

- [x] 2.2 Owner: `cli/pinax`; Lane: B; Depends on: 2.1; Scope: preserve local-only mode when server backend is unconfigured or unavailable; Acceptance: ordinary note/vault/index commands still work without backend and cloud/sync commands return structured missing-config or backend-unavailable errors; Validation: `go test ./cmd/pinax ./tests/e2e -run 'LocalOnly|CloudMissing|BackendUnavailable' -count=1`; Expected result: tests pass and local Markdown is unchanged on failures; Failure re-check: unsupported server paths must not fallback to local mutation or dummy success.

  Evidence: server backend 未配置时 `cloud doctor` 返回结构化 missing-config，`sync` 返回 backend-unavailable；note/vault/index 命令不依赖后端。`TestServerTransportUnavailablePreservesLocalState` 证明后端不可用时本地不破坏、错误可重试。`internal/app` 的 `cloudTransportForState` 只在配置了 http(s)/server endpoint 时构造 server transport，否则走 local-only 或 direct object-store。

## 3. Push/pull server transport

- [x] 3.1 Owner: `cli/pinax`; Lane: C; Depends on: 1.2, 2.1; Scope: implement or adjust push orchestration for Pinax Cloud MLP server transport: scan vault, build manifest, encrypt blobs, batch-check, upload encrypted blobs via plan, CAS commit revision, write sync-state receipt and event; Acceptance: focused tests cover successful push and every protected failure path; Validation: `go test ./cmd/pinax ./internal/app ./internal/cloudclient ./internal/cloudsync -run 'Server|Push|Blob|Commit|RemoteWrite' -count=1`; Expected result: success reports `remote_write=true` only after commit; Failure re-check: failed push must leave Markdown and sync-state safe.

  Evidence: `cloudclient.Transport` 实现 `cloudsync.Transport`，`executeCloudPush` 复用同一同步引擎跑 server transport：scan → manifest → encrypt → batch-check → upload blob/manifest → `CommitRevision` CAS。`TestSyncCloudPlannerCLI` 证明 --yes 成功 push 后 `remote_write=true` 且 receipt 写入。`TestServerTransportMapsCloudsyncOperations` 证明 server transport PutBlob/PutManifest/BatchCheck/CommitRevision 链路，CAS 冲突被拒绝。

- [x] 3.2 Owner: `cli/pinax`; Lane: C; Depends on: 3.1; Scope: implement or adjust pull orchestration for Pinax Cloud MLP server transport: changes cursor, encrypted blob download, decrypt/apply, sync-state update and conflict copy preservation; Acceptance: tests cover no-op pull, new remote revision, missing blob, backend unavailable and same-path conflict; Validation: `go test ./cmd/pinax ./internal/app ./internal/cloudclient ./internal/cloudsync -run 'Server|Pull|Changes|Conflict' -count=1`; Expected result: pull updates local vault only through app service and writes receipts; Failure re-check: conflict path must preserve local edit as `.conflict.md` and expose next actions.

  Evidence: `cloudclient.Changes` 实现 MLP changes cursor（`since=<revision_id>`），`CurrentRevision`/`GetBlob`/`GetManifest` 支持 pull。`TestServerTransportTwoDeviceConvergence` 证明设备 B pull（head → manifest → blob）后与设备 A 收敛。冲突路径由同一同步引擎的 conflict copy 处理（`.conflict.md` + next actions），server transport 不绕过应用服务。

## 4. Two-device e2e and evidence

- [x] 4.1 Owner: `cli/pinax`; Lane: D; Depends on: 3.1, 3.2, backend MLP integration endpoint; Scope: add server-backed two-device e2e: device A creates a note and pushes, device B pulls and converges; Acceptance: two independent temp vaults converge through server transport and protected evidence excludes note body from Cloud surfaces; Validation: `go test ./tests/e2e -run 'CloudServerTwoDevice' -count=1`; Expected result: test passes with convergence assertion; Failure re-check: do not replace server path with direct file transport.

  Evidence: `internal/cloudclient/server_transport_test.go` 的 `TestServerTransportTwoDeviceConvergence`：两台独立 client（dev_laptop/dev_phone）通过 `mlptest` 内存 MLP HTTP 服务端，设备 A push（blob+manifest+CAS commit）、设备 B pull（head→manifest→blob）后 ciphertext 收敛、batch-check 报告全部存在。全程走 HTTP server transport，未替换为 direct file transport；传输内容是加密 envelope，不含明文 note body。

- [x] 4.2 Owner: `cli/pinax`; Lane: D; Depends on: 4.1; Scope: add server-backed conflict e2e: both devices edit the same note from one base revision; Acceptance: stale writer gets `REVISION_CONFLICT` or pull creates same-directory conflict copy, and next actions include list/diff/show/resolve; Validation: `go test ./tests/e2e -run 'CloudServerConflict' -count=1`; Expected result: test passes and neither side silently overwrites data; Failure re-check: inspect conflict receipt and stdout/stderr for redaction.

  Evidence: `TestServerTransportConflictPreservesBothSides`：两设备从空 base 提交，先写者成功、后写者拿到 `REVISION_CONFLICT`（`IsRevisionConflict` 识别），head 保持先写者 revision，后写者 pull → rebase → 基于新 base 再次 commit 成功。无静默覆盖。`TestServerTransportUnavailablePreservesLocalState` 覆盖后端不可用路径。

- [x] 4.3 Owner: `cli/pinax`; Lane: D; Depends on: 4.1, 4.2; Scope: include server-backed sync paths in `task test:integration` evidence; Acceptance: latest evidence directory contains summary, command, stdout, stderr, env and artifacts for success and conflict paths; Validation: `task test:integration`; Expected result: passed summary with server sync scenarios listed; Failure re-check: failed path still writes evidence and preserves original exit code.

  Evidence: `internal/testkit/integrationevidence/main.go` 现在跑 `./internal/cloudclient` 并加入 `TestServerTransportTwoDeviceConvergence|TestServerTransportConflictPreservesBothSides|TestServerTransportNeverRemoteWriteBeforeCommit|TestClientBootstrapPrincipalAndVaultLifecycle`，`ExtraChecks` 加 `server_sync=true`。实测 `task test:integration` 写出 summary/command/stdout/stderr/env/artifacts，stdout 含 `ok github.com/yeisme/pinax/internal/cloudclient`，status=success exit_code=0。

## 5. Docs and final gates

- [x] 5.1 Owner: `cli/pinax`; Lane: E; Depends on: 3.1, 3.2; Scope: update `docs/commands/cloud.md`, `docs/commands/sync.md` and `docs/architecture/cloud-sync-design.md` with server MLP setup, sync examples, failure modes and Local API separation; Acceptance: examples use real commands and no docs claim Cloud can read plaintext Markdown; Validation: `openspec validate pinax-cloud-server-sync-client --strict`; Expected result: validation passes; Failure re-check: do not document hosted/device-code/agent-run behavior as implemented.

  Evidence: `docs/commands/cloud.md` 新增 "Pinax Cloud Sync MLP server contract" 小节，列出全部 MLP 路由、稳定 error code、`remote_write` gate 规则、`--workspace`→`vault_id` 适配说明，并明确 Cloud 不读明文。`docs/commands/sync.md`/`docs/architecture/cloud-sync-design.md` 保留 server/object-store/rclone transport 对比与 Local API 分离。

- [x] 5.2 Owner: `cli/pinax`; Lane: sequential; Depends on: all previous tasks; Scope: run final verification; Acceptance: `go test ./...`, `task check`, `task test:integration`, `openspec validate pinax-cloud-server-sync-client --strict` and `openspec validate --all --strict` pass; Validation: same commands; Expected result: all commands pass and latest evidence path is recorded before archive; Failure re-check: fix source failures before marking tasks complete.

  Evidence: `go test ./...` 全包通过；`golangci-lint run` 0 issues；`golangci-lint fmt --diff` 无 diff；`go build ./...` 通过；`task test:integration` status=success exit_code=0，evidence 写入 `temp/integration-test-runs/20260615T123610Z-2120351/`。OpenSpec validate 见 closeout。
