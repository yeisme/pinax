## 1. Proof fixture and baseline

- [x] 1.1 Owner: `cli/pinax`; Lane: A; Depends on: none; Scope: create or update a test fixture generator for a realistic proof vault with inbox, journal, normal notes, broken links, orphan notes, incomplete metadata and low-risk repair candidates; Acceptance: focused fixture test proves generated vault has deterministic files and no committed local secrets; Validation: `go test ./tests/e2e -run 'Proof|Fixture' -count=1`; Expected result: test passes and fixture can be used by CLI e2e; Failure re-check: fixture content must not include real user paths, provider payloads, tokens, Authorization headers or hidden prompts. Reopened: fixture assertions must prove concrete seeded issues and expected plan operations, not only command names or envelope keys. Done: fixture testscript asserts concrete issue codes (`broken_link`, `missing_tags`, `orphan_note`), concrete paths (`broken-link-note.md`, `incomplete-metadata.md`, `orphan-root-note.md`), and concrete plan operations (`tags_patch`/automatic, `orphan_review`/manual_review, `link_resolution`, `move`→`notes/`).
- [x] 1.2 Owner: `cli/pinax`; Lane: A; Depends on: 1.1; Scope: add RED e2e/testscript path for `init -> capture -> index -> search -> links -> doctor -> plan -> snapshot -> apply`; Acceptance: test initially fails only at missing proof loop orchestration or missing expected output assertions; Validation: `go test ./tests/e2e -run 'ProofLoop' -count=1`; Expected result: failing assertion names the first missing behavior; Failure re-check: do not weaken assertions to accept raw dumps, fake writes or missing receipts. Reopened: proof loop must assert fresh snapshot-before-each-apply, apply receipt facts, events, and snapshot evidence. Done: proof loop takes a FRESH snapshot before each apply (pre-repair and pre-organize), asserts apply receipt facts (`applied`, `plan_id`), asserts events start/end markers, and asserts durable snapshot evidence files (`vault/.pinax/version/snapshots`, `last_snapshot`, `records/version.json`).

## 2. Agent-safe projection coverage

- [x] 2.1 Owner: `cli/pinax`; Lane: B; Depends on: 1.1; Scope: add contract tests proving proof loop commands render valid `--json` envelopes and stable `--agent` key=value lines from shared projections; Acceptance: JSON parses as one envelope and agent output includes `spec_version`, `mode=agent`, `command`, `status` and bounded facts; Validation: `go test ./cmd/pinax ./internal/output -run 'Proof|Agent|JSON|Projection' -count=1`; Expected result: tests pass without parsing default human output; Failure re-check: if a command hand-builds JSON or agent output, move facts into the shared projection boundary instead. Reopened: JSON checks must recursively reject body leaks and sentinel leaks, not only top-level `body`. Done: `assertNoRecursiveBodyLeak` walks the JSON envelope at every depth, rejects any non-empty `body`/`note_body`/`raw_body` field and any sentinel occurrence; covers default summary, --json, --agent and --events modes.
- [x] 2.2 Owner: `cli/pinax`; Lane: B; Depends on: 2.1; Scope: add redaction tests for proof loop stdout, stderr, events, receipts and fixtures; Acceptance: protected surfaces exclude raw token, Authorization header, provider payload, hidden system prompt, private tool args, full note body and unapproved plaintext paths; Validation: `go test ./cmd/pinax ./tests/e2e -run 'Proof|Redaction' -count=1`; Expected result: redaction tests pass and report the protected surfaces checked; Failure re-check: fix projection/redaction source, not golden files. Reopened: cover default/JSON/agent/events, stderr, saved plans, events, integration evidence, and all forbidden classes. Done: redaction testscript covers 7 surfaces (default summary, --json, --agent, --events, stderr, saved plan files, snapshot evidence) and all forbidden classes (body sentinel, Authorization, Bearer, api_key=, secret=, ANSI).

## 3. Integration evidence runner

- [x] 3.1 Owner: `cli/pinax`; Lane: C; Depends on: 1.2; Scope: add or update the project-owned integration runner so `task test:integration` executes the proof loop and writes `temp/integration-test-runs/<run-id>/summary.json`, `command.txt`, `stdout.log`, `stderr.log`, `env.json` and `artifacts/`; Acceptance: evidence is generated on pass and on failure with original exit code preserved; Validation: `task test:integration`; Expected result: command exits 0 after implementation and latest evidence summary has `schema_version=yeisme.integration_test_evidence.v1`; Failure re-check: summary must be generated by the runner, not hand-written by an agent. Reopened: evidence must redact stdout/stderr/argv/env, avoid absolute path leaks, and include required `project`, `layer`, `finished_at`, and `redaction` fields. Done: `internal/testkit/evidence.Redact` scrubs Authorization/Bearer/secret-KV/CLI-secret-flag/absolute-paths from stdout, stderr, argv and env before writing; summary now carries `project=cli/pinax`, `layer=integration-evidence`, `finished_at`, and a `redaction` object (`applied`, `scanned_surfaces`, `forbidden_classes`, `path_redacted`).
- [x] 3.2 Owner: `cli/pinax`; Lane: C; Depends on: 3.1; Scope: add tests for evidence redaction and failure behavior; Acceptance: a forced failure still writes evidence and no evidence file contains forbidden sensitive strings; Validation: `go test ./tests/e2e -run 'IntegrationEvidence|ProofLoop' -count=1`; Expected result: tests pass and identify the latest run directory; Failure re-check: failed test path must not suppress stderr or overwrite the original command exit code. Reopened: tests must seed forbidden markers into stdout, stderr, argv, and path-like evidence and recursively scan every evidence file. Done: tests seed `EVIDENCE_*` markers into stdout, stderr, argv (`--api-key <value>`) and absolute-path-like values, then `assertEvidenceDirClean` recursively walks every evidence file (summary, command, stdout, stderr, env, artifacts) and rejects markers, forbidden classes and absolute paths.

## 4. Documentation main path

- [x] 4.1 Owner: `cli/pinax`; Lane: D; Depends on: 1.2, 2.1; Scope: update `README.md`, `docs/README.md` and `docs/commands/README.md` to lead with five paths: Capture, Retrieve, Diagnose, Plan, Apply safely; Acceptance: first-screen examples use real commands and do not mention agent-only wrappers; Validation: `go test ./tests -run 'Docs|English|Command' -count=1` if present, otherwise `task check`; Expected result: docs and generated command tests pass; Failure re-check: do not add fake aliases or examples for commands that the binary does not expose.
- [x] 4.2 Owner: `cli/pinax`; Lane: D; Depends on: 4.1; Scope: mark briefing, provider expansion, hosted Cloud and native SDKs as deferred advanced workflows in docs; Acceptance: docs explain that Pinax proof loop is local-first and Cloud/briefing are separate workflows; Validation: `openspec validate pinax-agent-safe-proof-loop --strict`; Expected result: OpenSpec validation passes; Failure re-check: keep subproject docs under `cli/pinax/docs/**`, not root docs.

## 5. Final verification and archive readiness

- [x] 5.1 Owner: `cli/pinax`; Lane: sequential; Depends on: all previous tasks; Scope: run final gates; Acceptance: `go test ./...`, `task check`, `task test:integration`, `openspec validate pinax-agent-safe-proof-loop --strict` and `openspec validate --all --strict` pass; Validation: same commands; Expected result: all commands pass and evidence path is recorded before archive; Failure re-check: fix source failures before marking tasks complete or archiving. Reopened: do not archive until P1 blockers are fixed (`organize apply` requires saved `--plan`; bounded projections stop note body leaks; integration evidence redaction/schema is real) and focused + full gates pass. Done: P1 blockers fixed — organize apply uses saved `--plan` with extracted IDs; `note orphans` no longer leaks body (uses `noteGraphNoteSummary`); `organize plan` freshness check uses `organizeCandidateFacts` so daily notes don't make plans stale; evidence runner does real redaction with required schema fields. Verified: `go test ./...` 29 packages pass, `task check` exit 0, `openspec validate pinax-agent-safe-proof-loop --strict` valid, `openspec validate --all --strict` 37/37, `task test:integration` writes `temp/integration-test-runs/20260615T104528Z-1328421/` with `schema_version=yeisme.integration_test_evidence.v1`, `project=cli/pinax`, `redaction.applied=true`, `env.cwd=[REDACTED_PATH]/`.
