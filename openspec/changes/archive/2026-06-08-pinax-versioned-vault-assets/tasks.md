## 1. 合同测试和基线确认

- [x] 1.1 增加 `version` 命令合同测试，覆盖 `status`、`snapshot`、`backends` 的 default human、`--json`、`--agent` 输出。
- [x] 1.2 增加隐藏兼容 alias 测试，验证 `pinax git snapshot` 可执行但 root help 不展示 `git` 主路径。
- [x] 1.3 增加 `asset` 命令合同测试，覆盖 `add/list/show/verify` 的 JSON envelope、agent key、stdout/stderr 分离和二进制 payload 不泄漏。
- [x] 1.4 增加 `index lookup` 合同测试，覆盖 note、asset、unmanaged Markdown、ambiguous candidates 和 `--scope`/`--kind` filters。
- [x] 1.5 增加 `record adopt <query>` contract tests，覆盖唯一 adoptable 文件、ambiguous 文件、无参数全库 plan 和 no-write 行为。
- [x] 1.6 增加 `note show <stem>`、`metadata plan <query>` resolver contract tests，覆盖只读候选和写入前 ambiguous fail。
- [x] 1.7 增加 `snapshot_required` 输出迁移测试，验证 next action 使用 `pinax version snapshot` 而不是 `pinax git snapshot`。

## 2. Domain 模型和错误码

- [x] 2.1 在 `internal/domain` 增加 `VaultObjectCandidate`、`VaultObjectKind`、`ManagedStatus`、`MatchField`、`ResolverFacts` 结构。
- [x] 2.2 在 `internal/domain` 增加 `VersionStatus`、`VersionCapabilities`、`VersionSnapshot`、`ChangedPath`、`DiffSummary`、`VersionedFile` 结构。
- [x] 2.3 在 `internal/domain` 增加 `Asset`、`AssetManifest`、`AssetLink`、`AssetVerification`、`AssetOperationPlan` 结构。
- [x] 2.4 定义稳定错误码：`vault_object_ref_ambiguous`、`version_read_unavailable`、`version_changed_paths_unavailable`、`asset_not_found`、`asset_ref_ambiguous`、`asset_payload_forbidden`。
- [x] 2.5 为 resolver candidate、version status 和 asset manifest 增加 table-driven unit tests，确认 JSON 字段和默认值稳定。

## 3. Pure Go VersionBackend

- [x] 3.1 新建 `internal/version` 包，定义 pure Go `VersionBackend` 接口和 request/result 类型。
- [x] 3.2 实现 `none` backend，返回无历史读取、无 changed paths、可基础 content evidence 的能力 facts。
- [x] 3.3 实现 `local` backend，基于 vault 文件 facts、record ledger sequence、index epoch 和 content hash 生成 snapshot evidence。
- [x] 3.4 实现 `Status`、`Snapshot`、`ChangedSince`、`ReadFile`、`DiffSummary` capability guard，unsupported 行为返回稳定 CommandError。
- [x] 3.5 增加 fake VersionBackend test helper，用于 search/index/record/repair 测试，不依赖系统 `git`。
- [x] 3.6 增加 streaming hash helper，确认大文件 hashing 不一次性读入内存。
- [x] 3.7 预留 pure Go Git backend adapter 边界和 capability probe，但第一阶段不要求完整历史读取实现。

## 4. Asset manifest 和媒体元数据

- [x] 4.1 新建 `internal/assets` 包，提供 manifest load/save/validate，路径固定为 `.pinax/assets/manifest.json`。
- [x] 4.2 实现 `asset add` use case 的底层能力：vault boundary 校验、copy/register mode、sha256、size、mtime、MIME guess。
- [x] 4.3 实现图片尺寸 pure Go 探测，覆盖 PNG/JPEG/GIF 和 unsupported 格式 fallback。
- [x] 4.4 实现 `asset list/show` 查询逻辑，优先读 index projection，缺失时 fallback 到 manifest。
- [x] 4.5 实现 `asset verify` streaming hash 和 missing/changed/unmanaged/orphan manifest 分类。
- [x] 4.6 实现 asset link extraction，解析 Markdown image/link/embed 引用并生成 `AssetLink` facts，不默认改写 note body。
- [x] 4.7 增加 asset manifest 读写、媒体探测、boundary、payload redaction、large-file hash 的 unit/integration tests。

## 4A. Obsidian-like 附件策略和引用解析

- [x] 4A.1 定义附件 placement policy，保留当前 `attachments/<note-id>/<filename>` 作为默认 `per-note`。
  - Owner: `cli/pinax`
  - Lane: A
  - Depends on: 2.3, 4.1
  - Scope: `internal/assets` 新增 placement 类型和 helper；`internal/app/service.go` 中 `uniqueAttachmentRel` 后续迁移到 assets 包
  - Acceptance: `go test ./internal/assets ./internal/app -run 'AttachmentPlacement|PerNoteAttachment|VaultFolderAttachment|NoteFolderAttachment' -count=1` 通过；默认路径仍兼容现有 `attachments/<note-id>/diagram.png`。
  - Docs/comments: placement helper 需要中文注释说明 vault boundary、note-folder 风险和为什么默认不跟随 note move。

- [x] 4A.2 扩展 `note attach` 请求参数和 app service，支持 `--placement`、`--link-style`、`--embed`、`--mode copy|move|register` 和 `--rename`。
  - Owner: `cli/pinax`
  - Lane: C
  - Depends on: 4A.1
  - Scope: `internal/cli/note_cmd.go`、`internal/app/service.go` 或新增 `internal/app/attachments.go`、`cmd/pinax/main_test.go`
  - Acceptance: `go test ./cmd/pinax ./internal/app -run 'NoteAttachPlacement|NoteAttachLinkStyle|NoteAttachMoveRequiresApproval|NoteAttachRegisterVaultFile' -count=1` 通过；`pinax note attach "认证方案" ./diagram.png --placement note-folder --embed --vault ./my-notes --json` 输出 attachment path、reference、media type 和 next action。
  - Docs/comments: `--mode move` 的 approval/snapshot guard 和 register 只允许 vault 内文件的判断需要中文注释。

- [x] 4A.3 新增共享 attachment reference parser，覆盖 Markdown image/link、Obsidian wiki embed/link、angle target 和 URL decode。
  - Owner: `cli/pinax`
  - Lane: A
  - Depends on: 4A.1
  - Scope: `internal/assets/reference_parser.go`、`internal/assets/reference_parser_test.go`，替换或封装 `internal/app/service.go` 中 `noteAttachmentsFromBody`
  - Acceptance: `go test ./internal/assets ./internal/app -run 'AttachmentReferenceParser|ObsidianWikiEmbed|MarkdownAttachmentLink|AttachmentReferenceBoundary' -count=1` 通过；外部 URL、mailto、data URI、纯 heading 和 `.md` note links 不进入 attachment projection。
  - Docs/comments: parser 状态机和 wiki alias/size 解析必须有中文注释，说明哪些 raw reference 只作为 evidence 保留。

- [x] 4A.4 将 attachment parser 接入 index rebuild/refresh，写入 `assets`、`asset_links` 和 `vault_files` projection。
  - Owner: `cli/pinax`
  - Lane: B
  - Depends on: 4A.3, 5.1
  - Scope: `internal/index/*`、`internal/app/query.go`、`internal/app/linkgraph.go` 中附件关系读取路径
  - Acceptance: `go test ./internal/index ./internal/app -run 'AssetLinksIndex|AttachmentBacklinksIndex|AttachmentOrphansIndex|HasAttachmentSearch' -count=1` 通过；fresh index 时 `note attachments`、`asset backlinks` 和 `search --has-attachment` 不重新扫描全 vault。
  - Docs/comments: index 是可重建 projection 的边界需要中文注释，禁止把 index 当附件真源。

- [x] 4A.5 增加 `asset backlinks`、`asset orphans`、`asset missing` 和 `asset repair --plan` 只读/计划命令。
  - Owner: `cli/pinax`
  - Lane: C
  - Depends on: 4A.4, 7.5
  - Scope: `internal/app/assets.go`、`internal/cli/asset_cmd.go`、`internal/output/render.go`
  - Acceptance: `go test ./cmd/pinax ./internal/app -run 'AssetBacklinks|AssetOrphans|AssetMissing|AssetRepairPlan|OutputContract' -count=1` 通过；缺失附件给 `pinax asset repair --plan --vault <vault> --json` action；orphan 输出 linked count 为 0。
  - Docs/comments: repair plan 不写 Markdown、不删除文件、不改 manifest 的边界需要中文注释。

- [x] 4A.6 增加 asset move/remove plan 的引用重写计划，默认不执行。
  - Owner: `cli/pinax`
  - Lane: B
  - Depends on: 4A.4, 8.3
  - Scope: `internal/assets/plan.go`、`internal/app/assets.go`、`internal/domain/types.go`
  - Acceptance: `go test ./internal/assets ./internal/app -run 'AssetMovePlan|AssetRemovePlan|AttachmentRewritePlan|SharedAssetRemoveRefusesDelete' -count=1` 通过；多个 note 引用同一附件时 remove plan 默认拒绝删除文件，只给 unlink/review actions。
  - Docs/comments: raw reference 精确 patch 和无法唯一定位时 manual review 的逻辑必须有中文注释。

- [x] 4A.7 增加附件补全，复用 resolver/index，避免文件补全污染 asset 命令。
  - Owner: `cli/pinax`
  - Lane: C
  - Depends on: 4A.4, 6.1
  - Scope: `internal/cli/asset_cmd.go`、`internal/cli/root.go` completion helper、`cmd/pinax/main_test.go`
  - Acceptance: `go test ./cmd/pinax -run 'AssetCompletion|AttachmentCompletion|NoFileCompletion' -count=1` 通过；`pinax __complete asset show --vault <vault> ""` 返回 asset 候选和 `ShellCompDirectiveNoFileComp`；`note attach <note> <TAB>` 保留源文件补全。
  - Docs/comments: completion 只读 index/manifest/scan fallback，不 hash 大文件、不刷新 index、不写资产。

- [x] 4A.8 增加附件路径展示层，支持 `vault-relative`、`note-relative`、`absolute`、`markdown` 和 `wiki`。
  - Owner: `cli/pinax`
  - Lane: A
  - Depends on: 4A.4, 6.1
  - Scope: 新增 `internal/assets/path_display.go` 和测试；修改 `internal/app/assets.go`、`internal/app/service.go` 的 `note attachments` projection；修改 `internal/cli/asset_cmd.go`、`internal/cli/note_cmd.go` 增加 `--path-style`、`--context-note`、`--include-paths`
  - Acceptance: `go test ./internal/assets ./internal/app ./cmd/pinax -run 'AssetPathDisplay|AttachmentPathStyle|PathContextRequired|AbsolutePathOptIn|WikiPathDisambiguation|AgentPathFacts' -count=1` 通过；`pinax note attachments "认证方案" --path-style note-relative --vault ./my-notes --json` 输出 canonical `path` 和 requested `display_path`；`pinax asset show diagram.png --path-style note-relative --vault ./my-notes --json` 缺少 `--context-note` 时返回 `path_context_required`。
  - Docs/comments: 绝对路径 opt-in、wiki basename 歧义和 canonical path 不变的规则必须有中文注释。

- [x] 4A.9 增强 rendered note preview，支持内联 Markdown/text 附件并为图片等二进制资产输出占位。
  - Owner: `cli/pinax`
  - Lane: A
  - Depends on: 4A.3, 4A.4, 4A.8
  - Scope: 新增 `internal/assets/preview.go` 和测试；修改 `internal/app/service.go` 的 `ShowNoteProjection` 或拆出 `internal/app/note_preview.go`；修改 `internal/cli/note_cmd.go` 增加 `note preview`、`--embed-attachments`、`--max-embed-depth`、`--max-embed-bytes`、`--max-preview-bytes`
  - Acceptance: `go test ./internal/assets ./internal/app ./cmd/pinax -run 'RenderedNoteEmbedsMarkdownAttachment|RenderedNoteEmbedsTextAttachment|RenderedNoteImagePlaceholder|RenderedNoteEmbedDepth|RenderedNoteEmbedCycle|NotePreviewReadonly' -count=1` 通过；`pinax note show "认证方案" --view rendered --embed-attachments markdown --vault ./my-notes --json` 输出统一 `data.body` 和 `data.embedded_assets`，source view 不内联附件。
  - Docs/comments: embed traversal、循环检测、大小截断和图片占位逻辑必须有中文注释，说明为什么终端中不渲染二进制/图片 payload。

- [x] 4A.10 增加 `asset preview` 单资产预览命令，复用同一附件 preview renderer。
  - Owner: `cli/pinax`
  - Lane: C
  - Depends on: 4A.9, 7.5
  - Scope: `internal/app/assets.go`、`internal/cli/asset_cmd.go`、`internal/output/render.go`、`cmd/pinax/main_test.go`
  - Acceptance: `go test ./cmd/pinax ./internal/app -run 'AssetPreviewMarkdown|AssetPreviewText|AssetPreviewBinaryPlaceholder|AssetPreviewOutputContract' -count=1` 通过；`pinax asset preview spec.md --as markdown --context-note "认证方案" --vault ./my-notes --json` 输出 bounded markdown preview；图片只输出 metadata placeholder。
  - Docs/comments: `asset preview` 不写 render run、不刷新 index、不调用 provider 的边界需要中文注释。

## 5. Index schema 和 lookup projection

- [x] 5.1 扩展 `internal/index` GORM schema，增加 `vault_files`、`assets`、`asset_links`、resolver candidate 相关 projection 表或字段。
- [x] 5.2 实现 schema migration 和 Inspect/Doctor 对新表的 schema/version 检查。
- [x] 5.3 扩展 rebuild/refresh，将 registered notes、unmanaged Markdown、assets 和 asset links 投影到 index。
- [x] 5.4 实现 `Lookup` API，支持 `scope=registered|adoptable|assets|all`、`kind=note|asset|file`、path/filename/stem/title/alias/content match。
- [x] 5.5 实现 lookup ranking，精确 note_id/path/filename/stem/title 优先，content/模糊匹配只用于只读候选。
- [x] 5.6 实现 `index refresh --changed-since` 通过 VersionBackend 获取 changed candidates，不调用系统 Git。
- [x] 5.7 增加 index lookup/refresh/schema migration tests，覆盖 stale、missing、unmanaged exclusion、asset candidates 和 unsupported changed-since。

## 6. Shared Resolver 接入

- [x] 6.1 新建 `internal/app` resolver use case，统一调用 record ledger、index lookup、manifest fallback 和 scan fallback。
- [x] 6.2 实现 resolver scopes：`registered`、`adoptable`、`assets`、`registered_or_adoptable`、`all`。
- [x] 6.3 实现 readonly resolver projection，返回 ranked candidates 和 next actions。
- [x] 6.4 实现 write resolver guard，多个候选时 fail-before-write 并返回 candidates。
- [x] 6.5 将 `note show/read/links/backlinks/orphans` 的 note ref 读取路径接入 resolver。
- [x] 6.6 将 `note rename/move/archive/delete/tag/attach` 写入路径接入 resolver guard。
- [x] 6.7 将 `metadata plan <query>` 接入 registered-or-adoptable resolver。
- [x] 6.8 增加 resolver integration tests，覆盖同名 note/asset/unmanaged 文件冲突和 scoped disambiguation。

## 7. Application services 和命令树

- [x] 7.1 增加 `VersionStatus`、`VersionSnapshot`、`VersionBackends`、`VersionChanged`、`VersionShow`、`VersionRestorePlan` application services。
- [x] 7.2 新建 `internal/cli/version_cmd.go`，接入 `version status/snapshot/history/diff/show/restore/changed/backends`。
- [x] 7.3 调整 `internal/cli/git_cmd.go`，仅保留 hidden `git snapshot` compatibility alias 并路由到 version service。
- [x] 7.4 增加 `AssetAdd/List/Show/Link/Move/Remove/Verify` application services。
- [x] 7.5 新建 `internal/cli/asset_cmd.go`，接入 `asset add/list/show/link/move/remove/verify` 和中文 help/examples。
- [x] 7.6 扩展 `internal/cli/index_cmd.go`，增加 `index lookup`、`index explain`、`index refresh --changed-since` flags。
- [x] 7.7 扩展 `internal/cli/record_cmd.go`，让 `record adopt [query]` 和 `record history <query>` 接受 resolver 输入。
- [x] 7.8 更新 root command grouping，让 `version`、`asset`、`index lookup` 出现在推荐主路径，`git` 不出现在 root help。

## 8. Repair、restore 和写入保护

- [x] 8.1 更新 snapshot-required helper 和所有 repair/organize/remove/restore next actions，统一推荐 `pinax version snapshot`。
- [x] 8.2 扩展 repair plan，识别 asset missing、asset hash changed、orphan manifest entry、dangling asset link、version evidence missing。
- [x] 8.3 实现 asset remove/move 的 `--plan` 输出，列出 linked notes、risk、version snapshot requirement 和 apply command。
- [x] 8.4 实现 `version restore --plan`，只生成 restore operations，不写 Markdown、asset、record、index 或 provider 状态。
- [x] 8.5 为 asset/version repair apply 保留 `--yes` 和 snapshot guard；第一阶段可只实现 fail/plan，不实现高风险 apply。
- [x] 8.6 增加 repair/restore plan tests，验证 no-write、snapshot_required、candidate ambiguity 和 projection-only repair 不需 snapshot。

## 9. Output、文档和兼容迁移

- [x] 9.1 如现有 summary 表格不足，补充 note/asset/version candidate human summary renderer，不影响 JSON/agent/events；成功的默认人类输出不得展示 `状态: 成功` 或 `status=success`，只展示结果事实、风险和下一步。
- [x] 9.2 更新 CLI help 文案、错误 hint、actions、README/docs 示例，移除用户可见 Git wrapper 口径。
- [x] 9.3 更新 output redaction tests，确认 asset payload、raw diff、provider payload、optional media provider args 不进入 stdout/stderr/events/fixtures。
- [x] 9.4 更新 OpenSpec 规格引用和 docs document map，说明 Pinax version backend 与 asset manifest 边界。
- [x] 9.5 增加 migration note：`pinax git snapshot` 是 hidden compatibility alias，推荐迁移到 `pinax version snapshot`。

## 10. E2E、性能和门禁

- [x] 10.1 使用 testscript 增加 version/asset/index lookup e2e，覆盖 fixture vault、fake backend、media fixture 和 process stdout/stderr 分离。
- [x] 10.2 增加 resolver e2e：`record adopt yeisme --plan`、`note show yeisme`、`metadata plan yeisme`、ambiguous candidates。
- [x] 10.3 增加 asset verify benchmark 或聚焦性能测试，记录大文件 streaming hash 的内存和耗时基线。
- [x] 10.4 运行聚焦测试：`go test ./cmd/pinax ./internal/app ./internal/index ./internal/assets ./internal/version -run 'Version|Asset|Lookup|Resolver|Record|Metadata' -count=1`。
- [x] 10.5 运行 e2e：`go test ./tests/e2e -run 'Version|Asset|Lookup|RecordAdopt' -count=1`。
- [x] 10.6 运行 `openspec validate pinax-versioned-vault-assets` 和 `openspec validate --all`。
- [x] 10.7 运行 `task check`，并把验证命令和结果记录到本文件。

## Verification Evidence
- 2026-06-08: `go test ./internal/app -run 'TestVersionApplicationServicesUseInjectedBackend' -count=1` 和 `go test ./internal/app ./internal/version ./internal/version/versiontest ./cmd/pinax -run 'Version|Snapshot|Backend|GitSnapshot|Template' -count=1`、`go test ./internal/app ./internal/index ./cmd/pinax -run 'Version|Snapshot|Backend|ResolveVaultObject|Lookup|IndexRefresh|MetadataPlan|NoteShowStem|NoteReadPaths|NoteWritePaths|RecordAdopt|Asset|Template' -count=1` 通过；7.1 补齐 `VersionChanged`、`VersionShow`、`VersionRestorePlan` application services，统一走注入 `VersionBackend`，status/snapshot 也切到 service backend，restore 只生成 `writes=false`/`requires_snapshot=true` plan，不写 vault 文件。
- 2026-06-08: `go test ./cmd/pinax -run 'TestVersionExtendedCommandsCLI' -count=1` 和 `go test ./cmd/pinax ./internal/cli ./internal/app ./internal/version ./internal/version/versiontest -run 'Version|Snapshot|Backend|GitSnapshot' -count=1` 通过；7.2 将 `version history/diff/show/restore/changed` 接入 Cobra 命令树，`history` 读取 local snapshot evidence，`diff/show/restore/changed` 通过 application service 和 `VersionBackend` capability guard 输出稳定 JSON error，`restore --plan` 默认不写 vault。
- 2026-06-08: `go test ./cmd/pinax ./internal/cli -run 'TestGitSnapshotHiddenCompatibilityAliasCLI|Version|Snapshot|Backend' -count=1` 通过；7.3 确认 `internal/cli/git_cmd.go` 仅保留 hidden `git snapshot` compatibility alias，并直接路由到 `VersionSnapshot` service/root projection，root help 不展示 `git` 主路径。
- 2026-06-08: `go test ./internal/app ./internal/index ./cmd/pinax -run 'Version|Snapshot|Backend|ResolveVaultObject|Lookup|IndexRefresh|MetadataPlan|NoteShowStem|NoteReadPaths|NoteWritePaths|RecordAdopt|Asset|Template' -count=1`、`openspec validate pinax-versioned-vault-assets` 和 `codegraph check` 通过；`codegraph diff-impact` 显示本轮 version CLI/service 改动影响 root command、version app service 和 CLI contract tests，未发现失败规则，仅保留既有 complexity warnings。
- 2026-06-08: `go test ./internal/app -run 'TestAssetLinkPlanIsNoWrite' -count=1` 和 `go test ./internal/app ./internal/assets ./internal/index -run 'Asset(Add|List|Show|Link|Move|Remove|Verify)|Manifest|IndexLookup|AssetLinksAndVaultFiles' -count=1` 通过；7.4 补齐 `AssetLink` application service，与已有 `AssetAdd/List/Show/Move/Remove/Verify` 汇合，`asset.link` 只生成 `writes=false`/`requires_snapshot=true` plan，不写 Markdown、manifest、index 或 asset 文件。
- 2026-06-08: `go test ./cmd/pinax -run 'TestAssetLinkCommandCLI' -count=1` 和 `go test ./cmd/pinax ./internal/cli ./internal/app ./internal/assets ./internal/index -run 'Asset|Manifest|IndexLookup|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentReferenceParser|AttachmentPlacement|AssetLinksAndVaultFiles|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans|Search' -count=1` 通过；7.5 将 `asset link <asset> --note <note>` 接入 `internal/cli/asset_cmd.go`，并确认 `asset add/list/show/link/move/remove/verify` 等主路径仍走 shared projection/JSON envelope，link 命令只生成 no-write plan。
- 2026-06-08: `go test ./cmd/pinax -run 'TestIndexExplainCommandCLI' -count=1` 和 `go test ./cmd/pinax ./internal/cli ./internal/app ./internal/index ./internal/version ./internal/version/versiontest -run 'IndexLookup|IndexExplain|IndexRefresh|RefreshChanged|Lookup|Version|Snapshot|Backend|FakeBackend' -count=1` 通过；7.6 在 `internal/cli/index_cmd.go` 保持 `index lookup` 与 `refresh --changed-since`，新增 `index explain` 子命令复用 `IndexExplain` projection，输出 `index.explain` envelope、index evidence 和可执行下一步。
- 2026-06-08: `go test ./cmd/pinax -run 'TestRecordHistoryUsesResolverInputCLI' -count=1` 和 `go test ./cmd/pinax ./internal/app ./internal/index -run 'RecordAdopt|RecordHistory|Resolver|ResolveVaultObject|IndexLookup|NoteShowStem|MetadataPlan' -count=1` 通过；7.7 保持 `record adopt [query]` resolver/adoptable plan 合同，并将 `record history <query>` 接入 registered note resolver，支持 filename stem/path/title/note_id 查询，ambiguous 时返回 resolver candidates，不再只接受裸 note_id。
- 2026-06-08: `go test ./cmd/pinax -run 'Test(CLITreeHelpSmoke|CLITreePrimaryPathAliases|NotebookCoreOutputContractAndHelp|GitSnapshotHiddenCompatibilityAliasCLI|AssetLinkCommandCLI|IndexExplainCommandCLI)$' -count=1` 通过；7.8 验证 root help 保留 `version`、`asset`、`index` 主路径且隐藏 `git`，index help 展示 `lookup`、`explain`、普通 refresh 和 `refresh --changed-since` 示例。
- 2026-06-08: `go test ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'SnapshotRequired|RepairApplyRequiresApprovalAndSnapshot|OrganizeApplySavedPlanRejectsStaleAndMoves|AssetMoveRemovePlanCommandsCLI|AssetLinkCommandCLI|VersionExtendedCommandsCLI|VersionApplicationServicesUseInjectedBackend' -count=1` 通过；`rg -n "pinax git snapshot" internal cmd` 仅命中两条防回归测试断言。8.1 确认 snapshot_required、repair apply、organize apply、asset move/remove/link 和 version restore next actions 均推荐 `pinax version snapshot`；README/docs 中旧示例保留到 9.2/9.5 统一迁移。
- 2026-06-08: `go test ./internal/app -run 'TestRepairPlanIncludesAssetAndVersionConsistencyOperations' -count=1`、`go test ./internal/app ./internal/assets ./internal/index -run 'RepairPlan|AssetRepair|AssetVerify|AssetBacklinks|AssetOrphans|AssetMissing|Version|Snapshot|IndexRefresh' -count=1` 和 `go test ./cmd/pinax -run 'TestRepairPlanJSONIsReadonlyAndSaveWritesPlanAsset|TestRepairPlanUsesDoctorIssuesAndOrganizePlanSharesSuggestions|TestRepairApplyRequiresApprovalAndSnapshot|TestAssetRelationshipCommandsCLI' -count=1` 通过；8.2 将 root `repair plan` 扩展为识别 `asset_missing`、`asset_hash_changed`、`orphan_manifest_entry`、`dangling_asset_link` 和 `version_evidence_missing`，全部作为 plan/manual_review operation，不写 Markdown、asset、record、index 或 provider 状态。
- 2026-06-08: `go test ./cmd/pinax -run 'TestAssetMoveRemovePlanCommandsCLI' -count=1`、`go test ./internal/app -run 'TestAssetMovePlanAndSharedRemovePlanAreNoWrite' -count=1` 和 `go test ./cmd/pinax ./internal/app ./internal/assets ./internal/index -run 'AssetMoveRemovePlanCommandsCLI|AssetMovePlan|AssetRemovePlan|AttachmentRewritePlan|SharedAssetRemoveRefusesDelete|SharedRemovePlan|AssetRelationshipCommandsCLI' -count=1` 通过；8.3 确认 `asset move/remove --plan` 输出 linked notes、risk、`requires_snapshot=true`、snapshot action 和可运行 apply action，且 plan 阶段不移动、不删除、不重写 Markdown。
- 2026-06-08: `go test ./internal/app -run 'TestVersionRestorePlanUsesResolverInputAndDoesNotWrite|TestVersionApplicationServicesUseInjectedBackend' -count=1`、`go test ./cmd/pinax -run 'TestVersionExtendedCommandsCLI' -count=1` 和 `go test ./cmd/pinax ./internal/app ./internal/index ./internal/version ./internal/version/versiontest -run 'Version|Restore|Resolver|ResolveVaultObject|IndexLookup|Asset|Snapshot|Backend' -count=1` 通过；8.4 将 `version restore --plan` 目标接入 shared resolver，支持标题/路径/asset ref 解析，ambiguous 时 fail-before-write，唯一候选只生成 `version_restore` operation、`writes=false` 和 snapshot action，不写 Markdown、asset、record、index 或 provider 状态。
- 2026-06-08: `go test ./cmd/pinax -run 'TestAssetApplyLikeCommandsRequirePlanCLI' -count=1` 和 `go test ./cmd/pinax ./internal/cli ./internal/app -run 'AssetApplyLikeCommandsRequirePlanCLI|AssetMoveRemovePlanCommandsCLI|AssetRelationshipCommandsCLI|RepairApplyRequiresApprovalAndSnapshot|VersionExtendedCommandsCLI|VersionRestorePlan|SnapshotRequired' -count=1` 通过；8.5 将 asset move/remove/repair 缺少 `--plan` 的 fail-only apply 入口改为稳定 `approval_required` envelope，version restore 继续要求 `--plan`，root repair apply 保留 `--yes` 与 snapshot guard；第一阶段不执行高风险 asset/version 写入。
- 2026-06-08: `go test ./internal/app -run 'TestVersionRestorePlanRejectsAmbiguousCandidatesBeforeBackendRead|TestVersionRestorePlanUsesResolverInputAndDoesNotWrite|TestRepairPlanIncludesAssetAndVersionConsistencyOperations' -count=1`、`go test ./cmd/pinax -run 'TestRepairApplyProjectionOnlyPlanDoesNotRequireSnapshot|TestRepairApplyRequiresApprovalAndSnapshot|TestAssetApplyLikeCommandsRequirePlanCLI|TestAssetMoveRemovePlanCommandsCLI' -count=1` 和 `go test ./cmd/pinax ./internal/cli ./internal/app ./internal/assets ./internal/index ./internal/version ./internal/version/versiontest -run 'Repair|Restore|SnapshotRequired|AssetMove|AssetRemove|AssetRepair|Version|Resolver|ResolveVaultObject|ProjectionOnly|ApplyLike' -count=1` 通过；8.6 补齐 repair/restore plan tests，覆盖 no-write、snapshot_required、restore candidate ambiguity 和 projection-only repair apply 不需 snapshot。
- 2026-06-08: `go test ./internal/output -count=1` 和 `go test ./cmd/pinax -run 'Test(CLITreeHelpSmoke|NotebookCoreOutputContractAndHelp|VaultStatsDashboardAndHelp|NoteGraphCommandOutputContract|VaultInitHelpOutputContract|ThemeConfigAndSummaryRendererCLI|DimensionCommandsCLI|MetadataPlanSummaryCLI|InlineNoteMarkdownRenderingCLI|JournalCommandsCLI)$' -count=1` 通过；9.1 调整 default human summary renderer，成功输出只展示重点、事实、风险和下一步，不再展示执行状态 `状态/成功`，partial/failed 与机器模式 JSON/agent/events 保持原有状态字段。
- 2026-06-08: `rg -n "pinax git snapshot|git snapshot|Git snapshot|pinax git" README.md docs internal/cli cmd/pinax/main_test.go` 仅命中 hidden alias 代码和防回归测试，`go test ./cmd/pinax ./internal/cli -run 'TestApplyHelpDocumentsSafetyFlags|TestGitSnapshotHiddenCompatibilityAliasCLI|Version|Snapshot|Backend|CLITreeHelpSmoke' -count=1` 通过；9.2 更新 README/docs/organize help 示例，用户可见 snapshot workflow 统一推荐 `pinax version snapshot`。
- 2026-06-08: `go test ./cmd/pinax -run 'TestAssetVersionProviderRedactionContractsCLI' -count=1` 和 `go test ./internal/version ./cmd/pinax -run 'VersionBackendCapabilityGuards|Version|Changed|Diff|Redaction|AssetVersionProviderRedactionContractsCLI' -count=1` 通过；9.3 增加 asset/version/provider 输出脱敏 contract，确认 asset add/show 不输出 asset payload，version diff capability guard 不回显 raw revision/diff 片段，Feishu dry-run delivery 不输出 webhook token、secret ref、Authorization 或 Cookie，并将 version backend unsupported hints 改为能力说明而非原样复述 revision/path。
- 2026-06-08: `openspec validate pinax-versioned-vault-assets` 通过，`rg -n "pinax git snapshot|git snapshot|Git snapshot|pinax git" README.md docs openspec/changes/pinax-versioned-vault-assets/specs openspec/changes/pinax-versioned-vault-assets/design.md openspec/changes/pinax-versioned-vault-assets/proposal.md` 仅命中 hidden alias、迁移说明和防旧口径规格；9.4/9.5 更新 docs document map、architecture boundaries、command map 和 delta specs，明确 version backend 只提供证据不拥有 Markdown/asset/ledger/index 真源，asset manifest 是 CLI-authored metadata 不保存 payload，并记录 `pinax git snapshot` 到 `pinax version snapshot` 的迁移说明。
- 2026-06-08: `go test ./tests/e2e -run 'TestVersionAssetLookup' -count=1` 通过；10.1/10.2 新增 testscript 进程 e2e，覆盖 fixture vault、media fixture、asset add redaction、index lookup note/file/asset candidates、`note show yeisme`、`metadata plan yeisme`、`record adopt yeisme --plan`、ambiguous resolver candidates、version snapshot/status、version diff 和 index changed-since unsupported guard 的 stdout/stderr 分离。当前 CLI 无 fake version backend 选择面；fake backend 注入继续由 service/version Go tests 覆盖。
- 2026-06-08: `go test ./internal/assets -run 'Test(HashReaderStreamsWithoutLargeSingleRead|VerifyClassifiesMissingChangedAndUnmanagedAssets)' -count=1` 和 `go test ./internal/assets -run '^$' -bench 'BenchmarkVerifyLargeAssetStreaming' -benchtime=1x -benchmem` 通过；10.3 增加 `BenchmarkVerifyLargeAssetStreaming`，8MiB asset verify 基线为 6360677 ns/op、1318.82 MB/s、63320 B/op、55 allocs/op。
- 2026-06-08: `go test ./cmd/pinax ./internal/app ./internal/index ./internal/assets ./internal/version -run 'Version|Asset|Lookup|Resolver|Record|Metadata' -count=1`、`go test ./tests/e2e -run 'Version|Asset|Lookup|RecordAdopt' -count=1`、`openspec validate pinax-versioned-vault-assets`、`openspec validate --all` 和 `task check` 通过；10.4-10.7 完成聚焦测试、e2e、OpenSpec 单项/全量校验和项目总门禁。`task check` 覆盖 openspec、golangci-lint、go test ./...、golangci-lint fmt --diff 和 go build。
- 2026-06-08: `go test ./internal/app -run 'TestResolveVaultObjectIntegrationDisambiguatesNoteAssetAndUnmanagedConflicts' -count=1` 和 `go test ./internal/app ./internal/index ./cmd/pinax -run 'ResolveVaultObject|Lookup|IndexRefresh|MetadataPlan|NoteShowStem|NoteReadPaths|NoteWritePaths|RecordAdopt|Asset' -count=1` 通过；6.8 新增 resolver integration 覆盖同一 query 命中 registered note、asset 和 unmanaged Markdown file 时 `scope=all` 返回 partial/ambiguous 三候选，并验证 `registered`、`assets`、`adoptable` scope 可稳定解歧，写入 guard 对 registered_or_adoptable 冲突继续 fail-before-write。
- 2026-06-08: `go test ./internal/app -run 'TestMetadataPlanQueryUsesRegisteredOrAdoptableResolver' -count=1` 和 `go test ./internal/app ./internal/index ./cmd/pinax -run 'MetadataPlanQueryUsesRegisteredOrAdoptableResolver|MetadataPlan|NoteShowStem|RecordAdopt|ResolveVaultObject|Lookup|IndexRefresh|Asset' -count=1` 通过；6.7 将 `metadata plan <query>` 改为复用 shared resolver `scope=registered_or_adoptable`，支持 index content/path/stem/title/adoptable candidates，ambiguous 时继续返回 `vault_object_ref_ambiguous` 和 candidates；metadata plan 读取 resolver candidate 后只生成 no-write plan，并补齐 schema_version 缺失的 frontmatter metadata 判定。
- 2026-06-08: `go test ./internal/app -run 'TestNoteWritePathsUseResolverGuardBeforeWrite' -count=1` 和 `go test ./internal/app ./cmd/pinax -run 'NoteWritePathsUseResolverGuardBeforeWrite|NoteUXServiceResolverListCreateAndMutate|NoteAttachment|NoteAttach|NoteShow|RecordAdopt|MetadataPlan|ResolveVaultObject|Asset|SnapshotRequired' -count=1`、`go test ./internal/app ./internal/index ./cmd/pinax -run 'ResolveVaultObject|Lookup|IndexRefresh|NoteWritePathsUseResolverGuardBeforeWrite|NoteReadPathsUseSharedResolverCandidates|NoteShowStem|RecordAdopt|MetadataPlan|Asset|NoteAttach|NoteAttachment' -count=1` 通过；6.6 将 `note rename/move/archive/delete/tag/attach` 写入路径集中接入 `ResolveVaultObjectForWrite` guard，`target` 等多候选查询在任何 Markdown、附件、trash、record ledger 写入前失败，返回 `vault_object_ref_ambiguous` 和 `data.candidates`，正常 note mutation 与 attachment 回归保持通过。
- 2026-06-08: `go test ./internal/app -run 'TestNoteReadPathsUseSharedResolverCandidates' -count=1` 和 `go test ./internal/app ./internal/index ./cmd/pinax -run 'NoteReadPathsUseSharedResolverCandidates|ResolveVaultObject|Lookup|IndexRefresh|NoteShowStem|NoteLinkGraph|NoteLinks|NoteBacklinks|NoteOrphans|RecordAdopt|MetadataPlan|Asset' -count=1`、`go test ./internal/app ./cmd/pinax -run 'NoteShow|NoteRead|ResolveNote|NoteLinkGraph|NoteLinks|NoteBacklinks|NoteOrphans' -count=1` 通过；6.5 将 `ShowNote`/`ShowNoteProjection` 和 `note links/backlinks` 增强读路径接入 shared registered-note resolver，支持 index content/alias/path/stem/title candidates，并保持 note show ambiguous 兼容错误；同时修复 `index.Refresh` 未维护 property schema meta 和 property projection 清理导致 lookup stale/唯一键冲突的问题，resolver `match_field` 现在优先返回最高分匹配字段。
- 2026-06-08: `go test ./internal/app -run 'TestResolveVaultObjectForWriteFailsBeforeWriteOnAmbiguousCandidates' -count=1` 和 `go test ./internal/app -run 'TestResolveVaultObject(ForWriteFailsBeforeWriteOnAmbiguousCandidates|ProjectionReturnsCandidatesAndNextActions|Scopes|UsesIndexManifestAndScanFallback)' -count=1`、`go test ./internal/app ./internal/index ./cmd/pinax -run 'ResolveVaultObject|Lookup|IndexLookupContractsCLI|RecordAdopt|NoteShowStem|MetadataPlan|Asset' -count=1` 通过；6.4 新增 `Service.ResolveVaultObjectForWrite` 写入 guard，复用只读 resolver candidate，多个候选时 fail-before-write 返回稳定 `vault_object_ref_ambiguous`、facts 和 `data.candidates`，测试确认不初始化或写入 record ledger。
- 2026-06-08: `go test ./internal/app -run 'TestResolveVaultObjectProjectionReturnsCandidatesAndNextActions' -count=1` 和 `go test ./internal/app -run 'TestResolveVaultObject(ProjectionReturnsCandidatesAndNextActions|Scopes|UsesIndexManifestAndScanFallback)' -count=1`、`go test ./internal/app ./internal/index ./cmd/pinax -run 'ResolveVaultObject|Lookup|IndexLookupContractsCLI|RecordAdopt|NoteShowStem|MetadataPlan|Asset' -count=1` 通过；6.3 新增 readonly `Service.ResolveVaultObjectProjection`，返回 ranked resolver candidates、facts/data、index/ledger evidence，并按 note/asset/adoptable file 生成 `pinax note show`、`pinax asset show`、`pinax record adopt --plan` 后续动作，不写 vault。
- 2026-06-08: `go test ./internal/app -run 'TestResolveVaultObject(Scopes|UsesIndexManifestAndScanFallback)' -count=1` 和 `go test ./internal/app ./internal/index ./cmd/pinax -run 'ResolveVaultObject|Lookup|IndexLookupContractsCLI|RecordAdopt|NoteShowStem|MetadataPlan|Asset' -count=1` 通过；6.2 为 shared resolver scope 增加行为覆盖，确认 `registered`、`adoptable`、`assets`、`registered_or_adoptable`、`all` 和 `kind=note` filters 的候选边界稳定，并补齐 index missing 时 registered note 的只读 scan fallback。
- 2026-06-08: `codegraph build .`、`codegraph context 'Service.IndexLookup'`、`codegraph context 'Service.RecordAdopt'`、`codegraph impact internal/app/service.go` 和 `codegraph impact internal/app/records.go` 用于定位 resolver 重复逻辑；`go test ./internal/app -run 'TestResolveVaultObjectUsesIndexManifestAndScanFallback' -count=1` 与 `go test ./internal/app ./internal/index ./cmd/pinax -run 'ResolveVaultObject|Lookup|IndexLookupContractsCLI|RecordAdopt|NoteShowStem|MetadataPlan|Asset' -count=1` 通过；6.1 新增 `internal/app.ResolveVaultObject` use case 和 `ResolverRequest/ResolverResult`，优先复用 `internal/index.Lookup`，index missing/stale 或无命中时回退 asset manifest 与 vault scan adoptable Markdown，读取已存在 record ledger version 作为只读 `ledger_seq` evidence，不初始化或写入 ledger。
- 2026-06-08: `go test ./internal/index ./internal/app ./internal/version ./internal/version/versiontest ./cmd/pinax -run 'Diagnose|Schema|Lookup|Refresh|AssetLinksAndVaultFiles|VaultObjectLookupSchema|IndexDoctor|IndexDefaultSummary|Search|Asset|RecordAdopt|NoteShowStem|MetadataPlan|Version|Snapshot|Backend|FakeBackend' -count=1` 通过；5.7 汇总并验证本阶段新增测试覆盖 schema missing/stale、projection table drift、registered/unmanaged Markdown 区分、asset candidates、lookup ranking、refresh changed-since backend 调用、unsupported changed-since 稳定错误以及 ordinary search 排除 unmanaged Markdown。
- 2026-06-08: `go test ./internal/index ./internal/app ./cmd/pinax -run 'RefreshChanged|IndexRefresh|VersionBackendCapabilityGuards|Version|Snapshot|Backend' -count=1` 和 `go test ./internal/index ./internal/app ./internal/version ./internal/version/versiontest ./cmd/pinax -run 'Lookup|Refresh|Index|Asset|Version|Snapshot|Backend|FakeBackend|RecordAdopt|NoteShowStem|MetadataPlan|Search' -count=1` 通过；5.6 新增 `IndexRefreshRequest.ChangedSince`、可注入 `VersionBackend`、`pinax index refresh --changed-since` flag 和 `internal/index.RefreshChanged`，changed-since 路径调用 backend `ChangedSince` 获取 candidates，不 shell out Git，不把过滤后的 notes 传给会删除缺席 projection 的 `Sync`，默认 local/none unsupported 返回稳定 `version_changed_paths_unavailable` JSON error。
- 2026-06-08: `go test ./internal/index -run 'TestLookup(RankingPrefersExactIdentityFieldsOverContent|APIMatchesRegisteredAdoptableAssetsAliasAndContent)' -count=1` 和 `go test ./internal/index ./internal/app ./cmd/pinax -run 'Lookup|IndexLookupContractsCLI|IndexDefaultSummaryAndMachineContractsCLI|RecordAdopt|NoteShowStem|MetadataPlan|Search|Asset' -count=1` 通过；5.5 为 `internal/index.Lookup` 的 scoring/ranking 增加行为覆盖，确认 exact identity fields（note_id/path/filename/stem/title 及 asset identity）优先于 content contains，content match 作为只读 lookup candidate 的低分匹配保留，不改变写入 resolver guard。
- 2026-06-08: `go test ./internal/index -run 'TestLookupAPIMatchesRegisteredAdoptableAssetsAliasAndContent' -count=1` 和 `go test ./internal/index ./internal/app ./cmd/pinax -run 'LookupAPIMatchesRegisteredAdoptableAssetsAliasAndContent|IndexLookupContractsCLI|IndexDefaultSummaryAndMachineContractsCLI|RecordAdopt|NoteShowStem|MetadataPlan|Search|Asset' -count=1` 通过；5.4 新增 `internal/index.Lookup` API，直接消费 SQLite projection，支持 `scope=registered|adoptable|assets|all`、`kind=note|asset|file|all`，从 `note_records`/`note_text_records`/`property_value_records`/`asset_records`/`vault_file_records` 返回稳定 `domain.VaultObjectCandidate`，覆盖 path、filename、stem、title、alias、content、asset_id、sha256 匹配，并将 unmanaged Markdown 映射为 adoptable file candidate。
- 2026-06-08: `go test ./internal/index -run 'TestRebuildAndRefreshProjectRegisteredUnmanagedAndAssets' -count=1` 和 `go test ./internal/index ./internal/app ./cmd/pinax -run 'RebuildAndRefreshProjectRegisteredUnmanagedAndAssets|AssetLinksAndVaultFilesProjectionFromRebuild|IndexDiagnose|IndexDoctorContractsCLI|VaultObjectLookupSchemaHasResolverCandidateFields|IndexLookupContractsCLI|IndexDefaultSummaryAndMachineContractsCLI|Search|Asset|NoteAttachmentCLI' -count=1` 通过；5.3 补齐 rebuild/refresh 的 vault object projection 状态：registered note path 在 `vault_files` 中标记 `object_kind=note`、`managed_status=registered`，同 vault 中未登记 Markdown 保持 `managed_status=unmanaged`，非 Markdown vault 文件继续投影为 asset，refresh 后新增附件文件进入 `vault_files` 和 `assets`，asset links 回归保持 fresh index 查询路径。
- 2026-06-08: `go test ./internal/index -run 'TestIndexDiagnoseClassifiesStatusAndIssues' -count=1` 和 `go test ./internal/index ./internal/app ./cmd/pinax -run 'IndexDiagnose|IndexDoctorContractsCLI|VaultObjectLookupSchemaHasResolverCandidateFields|AssetLinksAndVaultFilesProjectionFromRebuild|IndexLookupContractsCLI|IndexDefaultSummaryAndMachineContractsCLI|Search|Asset|NoteAttachmentCLI' -count=1` 通过；5.2 在 `Diagnose`/`Inspect` 路径的 `AutoMigrate` 前增加 index projection schema drift 检查，缺失新表或 resolver/asset/property 关键字段时返回 `stale` 与 `index_schema_mismatch` evidence（如 `missing_table=asset_link_records`、`missing_column=note_records.filename`），避免 Doctor 静默补表后误报 fresh，同时保留 corrupt SQLite 为 `index_unreadable`。
- 2026-06-08: `go test ./internal/index ./internal/app ./cmd/pinax -run 'VaultObjectLookupSchemaHasResolverCandidateFields|AssetLinksAndVaultFilesProjectionFromRebuild|IndexLookupContractsCLI|IndexDefaultSummaryAndMachineContractsCLI|IndexDoctorContractsCLI|Search|Asset|NoteAttachmentCLI' -count=1` 通过；5.1 扩展 index GORM schema，现有 `assets`、`asset_links`、`vault_files` projection 保持 AutoMigrate，同时 `note_records` 增加 resolver candidate 所需 `filename`、`stem`、`object_kind`、`managed_status` 字段并在 rebuild/update projection 时填充，支持后续统一 lookup/resolver 不扫描 Markdown 真源即可读取候选基础字段。
- 2026-06-08: `go test ./cmd/pinax ./internal/app -run 'AssetPreviewMarkdown|AssetPreviewText|AssetPreviewBinaryPlaceholder|AssetPreviewOutputContract|AssetPreviewOutputContractCLI' -count=1` 和 `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentReferenceParser|AttachmentPlacement|AssetLinksAndVaultFiles|AssetRelationshipCommandsCLI|AssetMoveRemovePlanCommandsCLI|AssetCompletionUsesIndexedAttachmentCandidatesCLI|AttachmentPathStyleCommandsCLI|RenderedNoteAttachmentPreviewCLI|RenderedNoteEmbeds|AssetPreviewOutputContractCLI|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans|Search|Rendered' -count=1` 通过；新增 `asset preview <asset> --as markdown|text` 只读命令，Markdown/text asset 输出 bounded `data.body` 与 `embedded_asset` metadata，图片/二进制只输出 placeholder 和 `asset.show` action，不输出原始 payload、不刷新 index、不写 render run 或 vault 文件。
- 2026-06-08: `go test ./internal/assets ./internal/app ./cmd/pinax -run 'RenderedNoteEmbedsMarkdownAttachment|RenderedNoteEmbedsTextAttachment|RenderedNoteImagePlaceholder|RenderedNoteEmbedDepth|RenderedNoteEmbedCycle|NotePreviewReadonly|RenderedNoteAttachmentPreviewCLI|RenderedNoteEmbeds' -count=1` 和 `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentReferenceParser|AttachmentPlacement|AssetLinksAndVaultFiles|AssetRelationshipCommandsCLI|AssetMoveRemovePlanCommandsCLI|AssetCompletionUsesIndexedAttachmentCandidatesCLI|AttachmentPathStyleCommandsCLI|RenderedNoteAttachmentPreviewCLI|RenderedNoteEmbeds|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans|Search|Rendered' -count=1` 通过；新增 rendered note attachment preview helper，`note show/read --view rendered --embed-attachments markdown|text` 只读内联 Markdown/text 附件，按 `--max-embed-depth` 与 `--max-embed-bytes` 截断和阻止循环，图片/二进制输出 Markdown placeholder 与 `pinax asset show` action，不输出 payload；新增 `note preview` 只读 alias，source view 不内联附件且不写 render run receipt。
- 2026-06-08: `go test ./internal/assets ./internal/app ./cmd/pinax -run 'AssetPathDisplay|AttachmentPathStyle|PathContextRequired|AbsolutePathOptIn|WikiPathDisambiguation|AgentPathFacts|AttachmentPathStyleCommandsCLI' -count=1` 和 `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentReferenceParser|AttachmentPlacement|AssetLinksAndVaultFiles|AssetRelationshipCommandsCLI|AssetMoveRemovePlanCommandsCLI|AssetCompletionUsesIndexedAttachmentCandidatesCLI|AttachmentPathStyleCommandsCLI|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans|Search' -count=1` 通过；新增 `internal/assets` path display helper，支持 `vault-relative`、`note-relative`、`absolute`、`markdown` 和 `wiki`，`note attachments --path-style` 输出 canonical `path` 与 `display_path`，`asset show --path-style` 支持 `--context-note`/`--include-paths`，缺少上下文返回 `path_context_required`，绝对路径只来自 vault 内 asset，wiki 样式使用 vault-relative target 避免 basename 歧义。
- 2026-06-08: `go test ./cmd/pinax ./internal/cli -run 'AssetCompletion|AttachmentCompletion|NoFileCompletion|Asset|Version|Snapshot|Backend' -count=1` 和 `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentReferenceParser|AttachmentPlacement|AssetLinksAndVaultFiles|AssetRelationshipCommandsCLI|AssetMoveRemovePlanCommandsCLI|AssetCompletionUsesIndexedAttachmentCandidatesCLI|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans|Search' -count=1` 通过；新增 asset 专用补全，`asset show/backlinks/move/remove` 从 index assets 读取 path/filename/stem 候选，fallback 到 manifest 和只读 `assets/`/`attachments/` 扫描，描述包含媒体类型与 `linked_notes`，asset 子命令返回 `ShellCompDirectiveNoFileComp`，`note attach` 源文件参数保持文件补全。
- 2026-06-08: `go test ./internal/assets ./internal/app -run 'AssetMovePlan|AssetRemovePlan|AttachmentRewritePlan|SharedAssetRemoveRefusesDelete|SharedRemovePlan' -count=1` 和 `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentReferenceParser|AttachmentPlacement|AssetLinksAndVaultFiles|AssetRelationshipCommandsCLI|AssetMoveRemovePlanCommandsCLI|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans|Search' -count=1` 通过；新增 `asset move <asset> <target> --plan` 与 `asset remove <asset> --plan`，计划包含 `asset_move`、`asset_reference_rewrite` 或 shared remove 的 `asset_reference_review` operations，facts 标记 `writes=false`、`requires_snapshot=true`、linked note/shared/delete_allowed 风险，并确认 plan 阶段不移动、不删除、不重写 Markdown。
- 2026-06-08: `go test ./cmd/pinax -run 'AssetRelationshipCommandsCLI' -count=1` 和 `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentReferenceParser|AttachmentPlacement|AssetLinksAndVaultFiles|AssetRelationshipCommandsCLI|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans|Search' -count=1` 通过；新增 `asset backlinks`、`asset orphans`、`asset missing` 和 `asset repair --plan` 只读/计划命令，输出 shared projection/JSON envelope，repair plan 明确 `writes=false`，缺失引用和孤儿资产只生成 `asset_missing`/`asset_orphan` operations，不写 Markdown、manifest 或 vault 文件。
- 2026-06-08: `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentReferenceParser|AttachmentPlacement|AssetLinksAndVaultFiles|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans|Search' -count=1` 通过；index rebuild/refresh 现在复用 attachment parser 写入 `asset_links`、`vault_files` 和非 Markdown vault asset projection，保留 source path、raw reference、asset path、media type、style/kind、line/status，并保持 note search/attachments 旧路径回归通过。
- 2026-06-08: `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentReferenceParser|AttachmentPlacement|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans' -count=1` 通过；`noteAttachmentsFromBody` 已复用 `internal/assets.ExtractLinks`，共享 parser 覆盖 Markdown image/link、angle target、URL decode、Obsidian wiki embed/link，并过滤外部 URL、mailto/data URI、heading、`.md` note links 和越界引用。
- 2026-06-08: `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentPlacement|NoteAttachmentCLI|NoteAttachPlacement|NoteLinks|NoteBacklinks|NoteOrphans' -count=1` 通过；`note attach` 新增 `--placement`、`--link-style`、`--embed`、`--mode copy|move|register`、`--rename` 和 `--yes`，app service 支持 note-folder copy/embed/rename、move 无 approval no-write、register vault file wiki reference，并输出 placement/link_style/mode/reference/index_status facts。
- 2026-06-08: `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|AttachmentPlacement|NoteAttachmentCLI' -count=1` 通过；新增 `internal/assets` attachment placement policy helper，支持默认 `per-note`、`vault-folder`、`note-folder`、冲突后缀和 boundary 校验，并将 `note attach` 默认路径迁移为复用该 helper，保持 `attachments/<note-id>/<filename>` 兼容。
- 2026-06-08: `go test ./internal/assets ./cmd/pinax -run 'Manifest|AddWithOptions|VerifyClassifies|HashReader|AssetCommandContractsCLI' -count=1` 通过；确认 asset manifest load/save/validate、copy/register boundary、PNG/JPEG/GIF 媒体探测、asset verify 分类、large-file streaming hash 和 CLI payload redaction 合同已有测试覆盖。
- 2026-06-08: `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend|ExtractAssetLinks|RejectsParentTraversal' -count=1` 通过；新增 `internal/assets` asset link extraction，解析 Markdown image/link、wiki embed/link、angle/URL decode 引用为 `AssetLink` facts，保留 raw reference、line、link_style/link_kind，不改写 note body，并过滤外部 URL、data URI、heading、`.md` note link 和越界引用。

- 2026-06-08: `go test ./cmd/pinax ./internal/cli ./internal/app ./internal/version -run 'Version|Snapshot|Backend' -count=1` 通过；新增 `pinax version status/snapshot/backends` projection 合同，覆盖 default human、`--json`、`--agent` 和 local snapshot evidence。
- 2026-06-08: `go test ./cmd/pinax -run 'Test(VersionCommand|VersionWorkflowContractsCLI|GitSnapshotHiddenCompatibilityAliasCLI)$' -count=1` 通过；`pinax git snapshot` hidden alias 路由到 `version.snapshot`，root help 不展示 `git` 主路径。
- 2026-06-08: `go test ./cmd/pinax ./internal/app ./internal/assets -run 'Asset|Version|Snapshot|Backend' -count=1` 通过；新增 `asset add/list/show/verify` 合同测试和最小 manifest 实现，验证 JSON/agent、stdout/stderr 分离以及二进制 payload 不进入输出。
- 2026-06-08: `go test ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'IndexLookup|Asset|Version|Snapshot|Backend' -count=1` 通过；新增 `pinax index lookup <query>` 合同和最小 app lookup，实现 registered note、asset manifest、adoptable Markdown、ambiguous candidates、`--scope`/`--kind` 过滤，普通 search 仍排除 unmanaged Markdown。
- 2026-06-08: `go test ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'RecordAdopt|IndexLookup|Asset|Version|Snapshot|Backend' -count=1` 通过；新增 `record adopt [query] --plan` 合同，覆盖唯一 adoptable 文件、ambiguous fail、无参数全库 plan 和 no-write ledger 行为。
- 2026-06-08: `go test ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'NoteShowStem|MetadataPlan|RecordAdopt|IndexLookup|Asset|Version|Snapshot|Backend' -count=1` 通过；新增 note filename stem resolver facts 和 `metadata plan <query>` resolver 合同，覆盖 adoptable Markdown 只读 plan、ambiguous fail 和 no-write。
- 2026-06-08: `go test ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'SnapshotRequired|RepairApplyRequiresApprovalAndSnapshot|OrganizeApplySavedPlanRejectsStaleAndMoves|NoteShowStem|MetadataPlan|RecordAdopt|IndexLookup|Asset|Version|Snapshot|Backend' -count=1` 通过；先将 snapshot_required 合同断言改为 `pinax version snapshot` 并确认失败，再迁移 repair/organize snapshot hint/action、相关 CLI/MCP 文案，内部输出扫描未命中旧 `pinax git snapshot` 主路径。
- 2026-06-08: `go test ./internal/domain ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'VaultObjectResolverDomainJSON|IndexLookup|RecordAdopt|NoteShowStem|MetadataPlan|Asset|Version|Snapshot|Backend|SnapshotRequired' -count=1` 通过；新增 domain resolver 类型 `VaultObjectCandidate`、`VaultObjectKind`、`ManagedStatus`、`MatchField`、`ResolverFacts` 和 JSON 字段合同，并将 app 层 resolver candidate 收敛为 domain alias。
- 2026-06-08: `go test ./internal/domain ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'VersionDomainModelsJSON|VaultObjectResolverDomainJSON|IndexLookup|RecordAdopt|NoteShowStem|MetadataPlan|Asset|Version|Snapshot|Backend|SnapshotRequired' -count=1` 通过；新增 domain version 类型 `VersionStatus`、`VersionCapabilities`、`VersionSnapshot`、`ChangedPath`、`DiffSummary`、`VersionedFile` 和 JSON 字段合同，并将 `internal/version` 的 status/snapshot/capabilities 收敛为 domain alias。
- 2026-06-08: `go test ./internal/domain ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'AssetDomainModelsJSON|VersionDomainModelsJSON|VaultObjectResolverDomainJSON|IndexLookup|RecordAdopt|NoteShowStem|MetadataPlan|Asset|Version|Snapshot|Backend|SnapshotRequired' -count=1` 通过；新增 domain asset 类型 `Asset`、`AssetManifest`、`AssetLink`、`AssetVerification`、`AssetOperationPlan` 和 JSON 字段合同，并将 `internal/assets` 的 manifest、asset、verification、verify result 收敛为 domain alias。
- 2026-06-08: `go test ./internal/domain ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'StableErrorCodeConstants|AssetDomainModelsJSON|VersionDomainModelsJSON|VaultObjectResolverDomainJSON|IndexLookup|RecordAdopt|NoteShowStem|MetadataPlan|Asset|Version|Snapshot|Backend|SnapshotRequired' -count=1` 通过；新增稳定错误码常量 `vault_object_ref_ambiguous`、`version_read_unavailable`、`version_changed_paths_unavailable`、`asset_not_found`、`asset_ref_ambiguous`、`asset_payload_forbidden`，并替换现有 app 调用点。
- 2026-06-08: `go test ./internal/domain ./cmd/pinax ./internal/app ./internal/assets ./internal/index ./internal/version -run 'DomainModelJSONContractsTableDriven|StableErrorCodeConstants|AssetDomainModelsJSON|VersionDomainModelsJSON|VaultObjectResolverDomainJSON|IndexLookup|RecordAdopt|NoteShowStem|MetadataPlan|Asset|Version|Snapshot|Backend|SnapshotRequired' -count=1` 通过；新增 table-driven domain JSON 合同测试，覆盖 resolver candidate、version status 和 asset manifest 的稳定字段与空值省略。
- 2026-06-08: `go test ./internal/version ./internal/app ./cmd/pinax -run 'TestLocalBackendImplementsVersionBackendContract|Version|Snapshot|Backend' -count=1` 通过；新增 pure Go `VersionBackend` 接口、`StatusRequest` 和携带 vault root 的 `SnapshotRequest`，并将 local backend 与 app version status/snapshot 调用收敛到接口签名。
- 2026-06-08: `go test ./internal/version ./internal/app ./cmd/pinax -run 'Version|Snapshot|Backend|TestNoneBackendReportsNoHistoryCapabilities|TestLocalBackendImplementsVersionBackendContract' -count=1` 通过；新增 `NoneBackend`，通过同一 `VersionBackend` 接口返回 backend=`none`、worktree_state=`unavailable` 和全 false capability facts，不暴露 revision/snapshot 状态。
- 2026-06-08: `go test ./internal/version ./internal/app ./cmd/pinax -run 'Version|Snapshot|Backend|TestLocalBackendSnapshotRecordsLedgerIndexAndFileFacts|TestNoneBackendReportsNoHistoryCapabilities|TestLocalBackendImplementsVersionBackendContract' -count=1` 通过；local backend snapshot evidence 现在包含 vault file facts、ledger_seq、index_epoch 和 content_hash，并把 ledger/index snapshot structured assets 作为 evidence refs。
- 2026-06-08: `go test ./internal/version ./internal/app ./cmd/pinax -run 'Version|Snapshot|Backend|TestVersionBackendCapabilityGuardsReturnStableErrors|TestLocalBackendSnapshotRecordsLedgerIndexAndFileFacts|TestNoneBackendReportsNoHistoryCapabilities|TestLocalBackendImplementsVersionBackendContract' -count=1` 通过；`VersionBackend` 现在声明 `ChangedSince`、`ReadFile` 和 `DiffSummary`，local/none backend 的 unsupported capability guard 返回稳定 `version_changed_paths_unavailable` 或 `version_read_unavailable` CommandError。
- 2026-06-08: `go test ./internal/version ./internal/version/versiontest ./internal/app ./cmd/pinax -run 'Version|Snapshot|Backend|FakeBackend|TestVersionBackendCapabilityGuardsReturnStableErrors|TestLocalBackendSnapshotRecordsLedgerIndexAndFileFacts|TestNoneBackendReportsNoHistoryCapabilities|TestLocalBackendImplementsVersionBackendContract' -count=1` 通过；新增 `internal/version/versiontest.FakeBackend`，可注入 status/snapshot/changed/read/diff 结果与错误，并捕获 request，供 search/index/record/repair 测试复用。
- 2026-06-08: `go test ./internal/version ./internal/version/versiontest ./internal/app ./cmd/pinax -run 'Version|Snapshot|Backend|HashReader|FakeBackend|TestVersionBackendCapabilityGuardsReturnStableErrors|TestLocalBackendSnapshotRecordsLedgerIndexAndFileFacts|TestNoneBackendReportsNoHistoryCapabilities|TestLocalBackendImplementsVersionBackendContract' -count=1` 通过；新增 reader-level `hashReader` streaming helper，`hashFile` 仅负责打开文件并委托 hashing，测试使用 bounded reader 确认不会一次性大块读取。
- 2026-06-08: `go test ./internal/version ./internal/version/versiontest ./internal/app ./cmd/pinax -run 'Version|Snapshot|Backend|GitBackend|HashReader|FakeBackend|TestVersionBackendCapabilityGuardsReturnStableErrors|TestLocalBackendSnapshotRecordsLedgerIndexAndFileFacts|TestNoneBackendReportsNoHistoryCapabilities|TestLocalBackendImplementsVersionBackendContract' -count=1` 通过；新增 pure Go `GitBackend` adapter 边界和 `.git` capability probe，占位实现不调用系统 `git`，第一阶段历史读取、diff、changed-since 和 snapshot 仍返回稳定 unsupported CommandError。
- 2026-06-08: `go test ./internal/assets ./internal/app ./cmd/pinax -run 'Asset|Manifest|Version|Snapshot|Backend' -count=1` 通过；`internal/assets` manifest load/save/validate 固定使用 `.pinax/assets/manifest.json`，缺失 manifest 返回空 schema，保存前和加载后都会拒绝错误 schema、缺失 id/sha256 和越界 asset path。
- 2026-06-08: `go test ./internal/domain ./internal/assets ./internal/app ./cmd/pinax -run 'Asset|Manifest|DomainModelJSON|Version|Snapshot|Backend' -count=1` 通过；`asset add` 底层新增 `AddWithOptions`，支持 copy/register mode、vault boundary 校验、sha256、size、modified_unix 和 MIME guess，现有 CLI 默认 copy 行为保持兼容。
- 2026-06-08: `go test ./internal/domain ./internal/assets ./internal/app ./cmd/pinax -run 'Asset|Manifest|DomainModelJSON|Version|Snapshot|Backend' -count=1` 通过；asset metadata 新增 `width`/`height`，`AddWithOptions` 使用 Go 标准库 PNG/JPEG/GIF decoder 探测图片尺寸，unsupported binary 保持尺寸为空且不阻塞注册。
- 2026-06-08: `go test ./internal/index ./internal/assets ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|Version|Snapshot|Backend' -count=1` 通过；新增 `internal/index` asset projection record/list/find API，`asset list/show` 现在优先读取 fresh index projection，index 缺失、stale、空 projection 或 show 未命中时 fallback 到 manifest，并输出 `engine` 与 `index_status` facts。
- 2026-06-08: `go test ./internal/domain ./internal/assets ./internal/index ./internal/app ./cmd/pinax -run 'Asset|Manifest|IndexLookup|DomainModelJSON|Version|Snapshot|Backend' -count=1` 通过；`asset verify` 现在通过 reader-level streaming hash 校验 manifest assets，分类 verified/missing/changed/unmanaged/orphan/failed，并扫描 `assets/`、`attachments/` 下未登记文件，projection facts 输出 `unmanaged` 和 `orphan`。
