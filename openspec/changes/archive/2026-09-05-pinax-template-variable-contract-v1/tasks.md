## 1. 变量合同

- [x] 1.1 扩展内置 note template 生成器，确定性输出可选 `variables` metadata。
- [x] 1.2 为 `learning.video`、`learning.source`、`source.github` 声明 required `url`。

## 2. 验证与文档

- [x] 2.1 覆盖 metadata 解析、required variables 与 shell completion。
- [x] 2.2 更新模板命令文档。
- [x] 2.3 focused tests、`task check`、`git diff --check` 与严格 OpenSpec 校验均通过。
