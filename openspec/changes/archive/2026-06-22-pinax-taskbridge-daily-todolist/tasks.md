# Tasks

- [x] 1.1 Owner: `cli/pinax`; Lane: spec; Depends on: none; Scope: 定义 TaskBridge daily todolist OpenSpec delta、设计和任务。Acceptance: `openspec validate pinax-taskbridge-daily-todolist --strict` 通过。Failure re-check: 若 spec 重写全量基线或缺少 Mermaid 图，修正为 delta 和设计图。Evidence: `openspec validate pinax-taskbridge-daily-todolist --strict` passed.
- [x] 2.1 Owner: `cli/pinax`; Lane: adapter; Depends on: 1.1; Scope: 新增 TaskBridge daily adapter 和规范化模型，只调用 `taskbridge agent today`，不读取 TaskBridge store 或 Provider token。Acceptance: focused Go tests 覆盖正常 payload、缺命令、坏 schema。Failure re-check: 确认错误码为 `TASKBRIDGE_UNAVAILABLE` 或 `TASKBRIDGE_CONTRACT_UNSUPPORTED`，且没有文件写入。 Evidence: `go test ./internal/app ./cmd/pinax -run 'Plan|Planning|TaskBridge|ManagedBlock|Template|Journal' -count=1` passed; adapter tests cover valid payload, unavailable command, unsupported/invalid JSON safety.
- [x] 2.2 Owner: `cli/pinax`; Lane: planning; Depends on: 2.1; Scope: 扩展 `PlanDaily` 的 `--taskbridge` 路径，生成 `captured_at`、selected commitments、snapshot/decision facts。Acceptance: dry-run tests 证明不写 Markdown 和 `.pinax/planning`。Failure re-check: 检查 projection 的 JSON/agent facts 只做 additive 变更。 Evidence: focused Go tests passed; dry-run asserts no daily note or `.pinax/planning` write and JSON facts are additive.
- [x] 3.1 Owner: `cli/pinax`; Lane: markdown; Depends on: 2.2; Scope: 写入或追加 `planning-daily` managed block，保留用户正文，冲突 fail closed。Acceptance: tests 覆盖新 daily note、既有 note 追加 block、既有 block 替换、重复/未闭合冲突。Failure re-check: 检查只替换 managed block 内部或追加新 block，不猜测其它插入点。 Evidence: focused Go tests passed; coverage includes new daily note, existing note append, duplicate block conflict, and default daily template with both managed blocks.
- [x] 3.2 Owner: `cli/pinax`; Lane: actions; Depends on: 2.2; Scope: 让 `plan actions --from daily --taskbridge` 能使用 TaskBridge daily decision 的 deferred candidates 生成 action draft，仍不执行确认写入。Acceptance: tests 覆盖 saved action draft 的 source snapshot/decision refs 和 next action。Failure re-check: 确认 Pinax 不调用 `taskbridge agent execute --confirm`。 Evidence: focused Go and CLI tests passed for `pinax plan actions --from daily --taskbridge --save --json`; next action remains `taskbridge agent execute --action-file <path> --dry-run`.
- [x] 4.1 Owner: `cli/pinax`; Lane: docs; Depends on: 3.1; Scope: 更新 Pinax command docs，说明 `pinax plan daily --taskbridge --yes` 写入 daily managed block，TaskBridge Markdown 导出延期。Acceptance: docs 示例使用真实命令。Failure re-check: 文档不得要求用户手写 `.pinax` JSON 或 TaskBridge metadata。 Evidence: `docs/commands/plan.md` and `docs/commands/journal.md` updated with real user-runnable commands and no direct `.pinax` metadata editing instructions.
- [x] 5.1 Owner: `cli/pinax`; Lane: verification; Depends on: 2.1,2.2,3.1,3.2,4.1; Scope: 运行 focused tests、integration tests、OpenSpec 和项目质量门禁。Acceptance: `go test ./internal/app ./cmd/pinax -run 'Plan|Planning|TaskBridge|ManagedBlock' -count=1`, `task test:integration`, `task check`, `openspec validate --all` 通过或记录环境阻塞。Failure re-check: 若 integration evidence 失败，保留 `temp/integration-test-runs/<run-id>/` 路径并修复脱敏/证据问题。 Evidence: focused Go tests passed; `task test:integration` passed with evidence `temp/integration-test-runs/20260621T165303Z-1495949`; `task check` passed including lint, fmt-check, `go test ./...`, build, sidecar protocol tests, and `openspec validate --all`.
