# pinax-realtime-sync-daemon Tasks

- [x] 1. Owner: `cli/pinax`; Lane: A; Depends on: none; Scope: 锁定 CLI 命令树和输出合同。新增 `pinax sync daemon run|start|status|stop|logs` help、flag、completion 和 contract tests；命令层只构造 request 并渲染 projection。Acceptance: `go test ./cmd/pinax -run 'TestSyncDaemonCommandHelp|TestSyncDaemonOutputContract|TestSyncDaemonRequiresApprovalForWrites' -count=1` 通过，输出包含 `command=sync.daemon.*` 或 JSON envelope，未泄漏 vault 本地绝对路径之外的敏感内容。Failure re-check: 如果测试需要放宽既有 `sync` envelope 字段，停止并改为只新增字段，不修改旧合同。

- [x] 2. Owner: `cli/pinax`; Lane: B; Depends on: none; Scope: 建立 daemon domain/request/state 类型和 runtime state repository。新增 `internal/app/syncdaemon` 或等价聚合包，定义 `RunRequest`、`StartRequest`、`StatusRequest`、`StopRequest`、`DaemonState`、`DetectionMode`、`SyncDaemonEvent`；通过 service 写 `.pinax/sync-daemon/daemon.json` 和 `events.jsonl`。Acceptance: `go test ./internal/app -run 'TestSyncDaemonStateRepository|TestSyncDaemonStateRedaction|TestSyncDaemonStateIgnoresPinaxRuntimePaths' -count=1` 通过。Failure re-check: 如果任何测试 fixture 或 stdout 包含 raw secret、Authorization、note body、provider payload，先修 redaction 边界再继续。

- [x] 3. Owner: `cli/pinax`; Lane: B; Depends on: 2; Scope: 实现 per-vault daemon lock 和 sync operation lock。锁必须阻止同一 vault 多个 daemon runner；daemon 与显式 `pinax sync push|pull` 不能并发写同一 vault/remote revision。Acceptance: `go test ./internal/sync ./internal/app -run 'TestSyncDaemonSingleRunnerLock|TestSyncOperationLockBlocksConcurrentWrites|TestSyncLockStalePidRecovery' -count=1` 通过。Failure re-check: 如果跨平台 PID 检测不可可靠实现，保留锁文件过期恢复和可诊断 `lock_stale` 状态，不静默覆盖。

- [x] 4. Owner: `cli/pinax`; Lane: C; Depends on: 2; Scope: 实现本地变化检测。使用 `fsnotify` 作为直接依赖，递归监听 vault 内未 hard-deny 目录；实现 debounce、事件 coalescing、watch error 到 scan fallback 的降级路径；`.git/`、`.pinax/`、LanceDB projection、provider cache、临时运行态文件不得触发 sync。Acceptance: `go test ./internal/app -run 'TestSyncDaemonWatcherDebounce|TestSyncDaemonWatcherIgnoresRuntimePaths|TestSyncDaemonScanFallbackOnWatcherError' -count=1` 通过。Failure re-check: 如果平台 watcher 在 CI 不稳定，使用 injectable fake watcher 测行为，真实 watcher 只做窄 smoke test。

- [x] 5. Owner: `cli/pinax`; Lane: C; Depends on: 2; Scope: 实现 remote head poller 和 retry/backoff。通过现有 Cloud Sync transport 读取远端 head；支持 `--poll-interval`、`--sync-timeout`、retry budget、指数退避和 jitter；状态输出展示 `remote_revision`、`last_poll_at`、`last_error_code`、`next_retry_at`。Acceptance: `go test ./internal/app ./internal/cloudsync ./internal/cloudclient -run 'TestSyncDaemonRemotePoll|TestSyncDaemonBackoff|TestSyncDaemonTransportUnavailableStatus' -count=1` 通过。Failure re-check: 如果某 transport 无法廉价读取 head，先返回 `transport_unavailable` 或 `poll_unsupported` partial，不把 poll 失败当作本地已同步。

- [x] 6. Owner: `cli/pinax`; Lane: D; Depends on: 3, 4, 5; Scope: 实现 daemon sync loop。单队列串行处理本地和远端事件；远端较新时先 `SyncPull`，本地 dirty 时再 `SyncPush`；`revision_conflict` 走 pull/retry；pull 产生 conflict copy 时进入 `conflict_required`，停止自动写入直到用户处理。Acceptance: `go test ./internal/app -run 'TestSyncDaemonPullBeforePush|TestSyncDaemonPushesLocalChange|TestSyncDaemonRevisionConflictRetry|TestSyncDaemonPausesOnConflict' -count=1` 通过。Failure re-check: 如果 `remote_write=true` 可在 durable commit 前出现，停止并修复复用路径，daemon 不得伪造该 fact。

- [x] 7. Owner: `cli/pinax`; Lane: E; Depends on: 1, 2, 3, 6; Scope: 实现进程生命周期。`run` 前台运行并处理 SIGINT/SIGTERM；`start` 后台启动 runner；`status` 读取并校验进程状态；`stop` 请求优雅停止并在超时后报告 next action；`logs` 读取 redacted events。Acceptance: `go test ./cmd/pinax ./internal/app -run 'TestSyncDaemonRunForeground|TestSyncDaemonStartStatusStop|TestSyncDaemonLogsRedacted|TestSyncDaemonStopTimeoutNextAction' -count=1` 通过。Failure re-check: 如果 Windows 或非 POSIX 后台 spawn 行为不一致，保留 `run` 作为跨平台稳定入口，并把 `start` 标记为 platform-partial，而不是假装成功。

- [x] 8. Owner: `cli/pinax`; Lane: F; Depends on: 6, 7; Scope: 补齐 process e2e 和集成证据。使用 fixture vault 和 fake/local Cloud Sync transport 启动 daemon，修改 device A 文件，验证 daemon push；device B daemon 或显式 pull 收敛；模拟远端冲突、网络失败和 watcher fallback。测试入口必须写 `temp/integration-test-runs/<run-id>/summary.json`、`command.txt`、`stdout.log`、`stderr.log`、`env.json`、`artifacts/`。Acceptance: `go test ./tests/e2e -run 'TestSyncDaemon' -count=1` 通过且 evidence 目录存在并已脱敏。Failure re-check: 如果 e2e 偶发，先固定 fake clock/fake watcher/fake transport，不用 sleep 猜时间。

- [x] 9. Owner: `cli/pinax`; Lane: G; Depends on: 1, 6, 7; Scope: 更新文档和命令地图。修改 `docs/commands/sync.md`、`docs/commands/cloud.md`、`docs/architecture/cloud-sync-design.md`、`README.md` 和 `README.zh-CN.md` 的 Cloud Sync preview 小节，明确 daemon 是本地进程、远端变化首版靠 poll、Cloud 仍只协调密文。Acceptance: 人工审阅通过，文档只展示真实命令，例如 `pinax sync daemon run --target cloud --vault ./my-notes --yes`，不出现 agent-only wrapper。Validation: `openspec validate pinax-realtime-sync-daemon --strict`。Failure re-check: 如果文档暗示服务端保存明文、远端 push 已实现或无需 `--yes` 自动写入，退回重写。

- [x] 10. Owner: `cli/pinax`; Lane: sequential; Depends on: 1-9; Scope: 汇总质量门禁和 closeout。运行 focused tests、`task check`、`openspec validate pinax-realtime-sync-daemon --strict`、`openspec validate --all --strict`，记录 evidence、已知限制和回滚方式。Acceptance: 所有验证命令通过；若存在 pre-existing failure，必须用 clean baseline 证明非本变更引入。Failure re-check: 任一 contract/redaction/conflict 测试失败时，不以文档说明替代修复。

## 验证记录

- Focused: `go test ./cmd/pinax ./internal/app ./internal/sync ./internal/cloudsync ./internal/cloudclient -run 'TestSyncDaemonCommandHelp|TestSyncDaemonOutputContract|TestSyncDaemonRequiresApprovalForWrites|TestSyncDaemonStateRepository|TestSyncDaemonStateRedaction|TestSyncDaemonStateIgnoresPinaxRuntimePaths|TestSyncDaemonSingleRunnerLock|TestSyncOperationLockBlocksConcurrentWrites|TestSyncLockStalePidRecovery|TestSyncDaemonWatcherDebounce|TestSyncDaemonWatcherIgnoresRuntimePaths|TestSyncDaemonScanFallbackOnWatcherError|TestSyncDaemonRemotePoll|TestSyncDaemonBackoff|TestSyncDaemonTransportUnavailableStatus|TestSyncDaemonPullBeforePush|TestSyncDaemonPushesLocalChange|TestSyncDaemonRevisionConflictRetry|TestSyncDaemonPausesOnConflict|TestSyncDaemonRunForeground|TestSyncDaemonStartStatusStop|TestSyncDaemonLogsRedacted|TestSyncDaemonStopTimeoutNextAction' -count=1` 通过。
- E2E: `go test ./tests/e2e -run 'TestSyncDaemon' -count=1` 通过。
- 文档检查: `rg -n "sync daemon|daemon run|daemon status|remote head|poll|Cloud Sync" docs/commands/sync.md docs/commands/cloud.md docs/architecture/cloud-sync-design.md README.md README.zh-CN.md` 覆盖 daemon 命令、本地进程、remote head poll 和密文边界。
- Integration evidence: `task test:integration` 通过，run id `20260624T093717Z-2771321`，`summary.json` 记录 `sync_daemon: true` 和 `redacted: true`。
- OpenSpec: `openspec validate pinax-realtime-sync-daemon --strict && openspec validate --all --strict` 通过，47 passed, 0 failed。
- Quality gate: `task check` 通过，包含 `go test ./...`、`golangci-lint run`、`golangci-lint fmt --diff`、sidecar protocol tests、`openspec validate --all` 和 `go build -trimpath`。
