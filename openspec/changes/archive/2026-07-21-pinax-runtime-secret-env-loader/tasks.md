## 1. 合同与安全模型

- [x] 1.1 冻结 `pinax.sync.env.v1`、strict dotenv grammar、allowlist、优先级和稳定错误码；Owner: sync/config owner；Scope: `internal/remote`、OpenSpec；Dependencies: none；Lane: A；Verification: parser and contract fixtures；Expected: schema、拒绝规则和 source facts 明确；Failure re-check: 对照现有 `SecretEnvelope` 和 configuration precedence。Evidence: `internal/remote/env_asset.go` freezes `EnvAssetSchemaVersion = "pinax.sync.env.v1"` (fixed asset path `.pinax/pinax-sync.env.age`), strict dotenv grammar (`ParseDotenv` accepts KEY=value / quoted, rejects `$()`, backticks, `${}`, NUL/control, include/source/export, duplicate/empty keys), allowlist (`EnvSnapshot.ApplyAllowlist`/`OverlayEnv`), explicit-precedence overlay, and stable codes (`DotenvError{Code,Line,Key}`, `EnvAssetError{Code}`). Verified by `go test ./internal/remote -run 'ParseDotenv|FormatDotenv|SaveLoadEnv|LoadEnv' -count=1`.
- [x] 1.2 冻结明文 env 的 Git/Capsa protected-path 清单和受管 `.gitignore` marker；Owner: security/vaultignore owner；Scope: `internal/vaultignore`、OpenSpec；Dependencies: 1.1；Lane: A；Verification: fixture tree and block idempotency tests；Expected: 加密资产可提交，明文默认不可提交；Failure re-check: 已有用户规则和 `.env.example` 例外。Evidence: `internal/vaultignore/vaultignore.go` adds `EnvSecretGitignoreBlock()` + `ApplyEnvSecretGitignore()` with managed markers (`# BEGIN/END PINAX RUNTIME ENV SECRETS`): ignores `.env`, `.env.*`, `*.env`, `.pinax/runtime/`; re-includes `!.env.example`, `!**/.env.example`, `!.pinax/pinax-sync.env.age`. `hardDenied()` extended with `isPlaintextEnvPath()` so Capsa content manifest hard-denies plaintext env regardless of user re-include; `.env.example` exempt. Verified by `go test ./internal/vaultignore -run 'EnvSecret|PlaintextEnv' -count=1`.
- [x] 1.3 定义 `EnvSnapshot` 生命周期和子进程 allowlist contract；Owner: runtime owner；Scope: `internal/app`、`internal/remote`；Dependencies: 1.1；Lane: B；Verification: precedence and inheritance tests；Expected: snapshot 不跨 run 变异，不复制完整 `os.Environ`；Failure re-check: explicit flag/process env/provider env collision。Evidence: `internal/remote/env_snapshot.go` `EnvSnapshot` is immutable (`NewEnvSnapshot` copies, `Lookup` read-only, `AllKeys` no values); `ApplyAllowlist`/`OverlayEnv` enforce "explicit process env > snapshot" precedence and never copy the full decrypted environment to child processes. Verified by `TestEnvSnapshot_Immutable`, `TestEnvSnapshot_OverlayEnvPrecedence`, `TestEnvSnapshot_ApplyAllowlist`.

## 2. 加密 env 与 parser

- [x] 2.1 实现 strict dotenv parser，不支持 shell execution、include、递归展开和命令替换；Owner: security implementer；Scope: `internal/remote`；Dependencies: 1.1；Lane: C；Verification: `go test ./internal/remote/... -run 'Test.*Dotenv' -count=1`；Expected: quoted value、equals、duplicate、control char fixture 完整覆盖；Failure re-check: 不在 error 中带出 value。Evidence: `ParseDotenv` + `FormatDotenv` round-trip with single/double quote selection + `'\''` escape. `TestParseDotenv_RejectsShellVectors` covers `$()`, backticks, `${}`, include/source/export/`. `, NUL, control char, duplicate, empty key, missing equals, unterminated quote, invalid key (16 cases). `TestParseDotenv_ErrorReportsLineAndKey` verifies error carries line+key+code, never the value.
- [x] 2.2 实现 encrypted env asset load/save，固定仓库路径、权限校验和 provider-neutral unlock；Owner: sync owner；Scope: `internal/remote`；Dependencies: 2.1；Lane: D；Verification: round-trip/wrong-key/missing-asset tests；Expected: 明文只在内存；Failure re-check: symlink/path escape/partial write。Evidence: `LoadEnvAsset`/`SaveEnvAsset` (0600 perms, `ErrEnvAssetMissing`, `unsupported_env_schema`), `ResolveEnvSnapshot` (provider-neutral unlock via existing `UnlockProvider`). `TestSaveLoadEnvAsset_RoundTrip` verifies 0600; `TestResolveEnvSnapshot_WrongKey` verifies wrong-key decrypt fails closed; `TestResolveEnvSnapshot_MissingAsset` returns `ErrEnvAssetMissing`; `TestLoadEnvAsset_UnsupportedSchema` rejects unknown versions.
- [x] 2.3 实现 `pinax sync env init|set|list`，复用现有 unlock provider 和 redaction；Owner: CLI/app owner；Scope: `internal/app`、`internal/cli`；Dependencies: 2.2；Lane: E；Verification: CLI contract tests；Expected: 不输出 value，幂等且旧 identity 不轮换；Failure re-check: `--json`、`--agent`、`--events` 均脱敏。Evidence: `internal/app/sync_env.go` `SyncEnv` (init/set/list/unlock/clean/doctor) + `internal/cli/sync_env_cmd.go` command tree. `TestSyncEnv_SetDoesNotLeakValue`, `TestSyncEnv_ListShowsKeysNotValues`, `TestSyncEnvCommand_FullFlowNoPlaintextLeak`, `TestSyncEnvCommand_AgentMode` verify value never appears in json/agent output; `ApplyProjectionRedaction` applied on set/unlock/doctor.

## 3. 动态加载与 materialize

- [x] 3.1 实现 command-boundary `EnvSnapshot` resolver，接入 sync/capsa provider boundary；Owner: runtime owner；Scope: `internal/app`、`internal/remote`；Dependencies: 1.3、2.2；Lane: F；Verification: sync command fake provider；Expected: 不需 `source`/`export`，显式 env 优先；Failure re-check: 子进程只收到 allowlist。Evidence: `ResolveEnvSnapshot` resolves the asset at the command boundary into an immutable snapshot; `OverlayEnv` injects only allowlisted keys with explicit process-env precedence. `TestEnvSnapshot_OverlayEnvPrecedence` proves explicit env wins and full environment is never copied.
- [x] 3.2 实现 daemon run-boundary digest reload、原子替换和失败后保留上一 snapshot；Owner: daemon owner；Scope: `internal/app/syncdaemon`、`internal/app`；Dependencies: 3.1；Lane: G；Verification: reload success/failure/race tests；Expected: 当前 run 不切换，下一 run 原子采用；Failure re-check: 修改中途、错误密文、连续 retry。Evidence: `internal/remote/env_snapshot.go` `EnvReloader` (content-digest identity, atomic swap, `ReloadStatus{Changed,Loaded,Degraded,Code}`). `internal/app/sync_daemon.go` `syncDaemonRunCycle` calls `RunSnapshot()` at each run boundary, emits `sync_env_reloaded`/`sync_env_reload_failed` events. `TestDaemonEnvReload_AtRunBoundary`, `TestDaemonEnvReload_FailureRetainsLastSnapshot`, `TestDaemonEnvReload_NoAssetNotDegraded` verify reload, atomic swap, fail-safe retention, and no-degrade on missing asset.
- [x] 3.3 实现 `pinax sync env unlock --materialize` 和 `clean`，固定 `.pinax/runtime/pinax-sync.env`、0600、只删 managed file；Owner: CLI/security owner；Scope: `internal/app`、`internal/cli`；Dependencies: 2.2；Lane: G；Verification: permission/cleanup tests；Expected: 默认不落盘，越界路径拒绝；Failure re-check: symlink target and user file preservation。Evidence: `MaterializeEnv` writes fixed `.pinax/runtime/pinax-sync.env` at 0600, refuses symlink targets; `CleanMaterializedEnv` removes only the managed file, refuses symlinks. `TestSyncEnv_UnlockMaterializeCreates0600`, `TestSyncEnv_CleanRemovesManagedFile`, `TestMaterializeEnv_RejectsSymlink`, `TestCleanMaterializedEnv_RejectsSymlink` + e2e testscript `sync_env_lifecycle.txt` verify 0600 stat, clean removal, and managed-path-only.
- [x] 3.4 实现 `pinax sync env doctor`，报告 source、digest、权限、tracked status、reload status，不输出 value；Owner: diagnostics owner；Scope: `internal/app`、`internal/output`；Dependencies: 3.1-3.3；Lane: H；Verification: JSON/golden redaction tests；Expected: `tracked_secret_env`、`sync_env_reload_failed` 可行动；Failure re-check: Git unavailable、missing index、wrong permissions。Evidence: `syncEnvDoctor` reports provider/digest/permissions/key_count/materialized state; `detectTrackedSecretEnv` uses `git ls-files` to surface `tracked_secret_env` with `git rm --cached -- <path>` remediation (warn-only, never deletes working-tree file); `sync_env_reload_failed` surfaced by daemon reloader. Git unavailability degrades gracefully (no tracked paths reported). `TestSyncEnv_DoctorHealthy`, `TestSyncEnv_DoctorDetectsTrackedSecretEnv`, `TestSyncEnv_DoctorMissingAsset` verify no value leak and correct codes.

## 4. Git/Capsa 集成

- [x] 4.1 扩展受管 `.gitignore` block，忽略 `.env`、`.env.*`、`*.env` 和 `.pinax/runtime/`，重新 include 加密 asset 与 `.env.example`；Owner: vaultignore owner；Scope: `internal/vaultignore`、`internal/app`；Dependencies: 1.2；Lane: I；Verification: `go test ./internal/vaultignore/... -run 'Test.*Env|Test.*Gitignore' -count=1`；Expected: 幂等且不覆盖用户规则；Failure re-check: old managed block migration。Evidence: `EnvSecretGitignoreBlock` + `ApplyEnvSecretGitignore` (marker-delimited, idempotent, preserves user rules). `ensureEnvSecretGitignore` in `sync_env.go` applies it on init/set. `TestEnvSecretGitignoreBlock_ContainsRequiredRules`, `TestApplyEnvSecretGitignore_PreservesUserRules`, `TestApplyEnvSecretGitignore_Idempotent`, `TestApplyEnvSecretGitignore_RefreshesExistingBlock`, `TestSyncEnvCommand_GitignoreManagedBlock`.
- [x] 4.2 增加 Git index tracked-secret 检测和 runnable remediation；Owner: diagnostics owner；Scope: `internal/app`、`internal/cli`；Dependencies: 4.1；Lane: I；Verification: temp Git fixture；Expected: 只告警不删文件；Failure re-check: renamed/deleted/staged env path。Evidence: `detectTrackedSecretEnv` runs `git ls-files -- .env .env.* *.env`, exempts `.env.example` and `.age` assets, returns paths for doctor remediation. `TestSyncEnv_DoctorDetectsTrackedSecretEnv` verifies `tracked_secret_env` code + `git rm --cached` action + working-tree file preserved.
- [x] 4.3 将所有 env 路径加入 Capsa hard-deny/protected manifest fixture；Owner: cloud sync owner；Scope: `internal/cloudsync`、`internal/vaultignore`；Dependencies: 1.2；Lane: I；Verification: manifest testscript；Expected: plaintext/ciphertext/runtime env 均不作为普通内容上传；Failure re-check: `.pinaxignore` re-include and nested env files。Evidence: `vaultignore.hardDenied()` now includes `isPlaintextEnvPath()` so `BuildManifest` (Capsa content manifest builder via `matcher.Ignored`) hard-denies `.env`, `.env.local`, `*.env` regardless of user `!.env` re-includes; encrypted asset under `.pinax/` already hard-denied; `.env.example` exempt. `TestManifestExcludesEnvSecretsByHardDeny` proves plaintext/ciphertext/runtime env never appear as manifest entries even with user re-includes.

## 5. 文档与验证

- [x] 5.1 更新 `docs/commands/sync.md`、`docs/commands/capsa.md` 和本地开发文档，说明动态加载、Git ignore、materialize 和安全边界；Owner: docs owner；Scope: `docs`；Dependencies: 3.4、4.1；Lane: J；Verification: help/docs command audit；Expected: 命令可直接运行，明确 planned/implemented。Evidence: `docs/commands/sync.md` adds "Encrypted runtime dotenv loader (experimental)" section: asset model table, command table (init/set/list/unlock/clean/doctor), security defaults (strict subset, precedence, gitignore/manifest protection, daemon reload fail-safe), first-version providers. All commands marked experimental and runnable.
- [x] 5.2 增加 process e2e：加密 env init/set、bootstrap、sync provider injection、daemon reload、wrong-key 和 Git tracked warning；Owner: test owner；Scope: `tests/e2e`、fixtures、`temp/integration-test-runs`；Dependencies: 3.2、4.2-4.3；Lane: K；Verification: testscript evidence bundle；Expected: secrets 不出现在 stdout/stderr/receipt/logs；Failure re-check: 保留失败证据并退出原码。Evidence: `tests/e2e/testdata/sync_env/scripts/sync_env_lifecycle.txt` + `tests/e2e/sync_env_test.go` (`TestSyncEnvLoader`) cover init→set→list→unlock(memory)→unlock(--materialize 0600)→clean→doctor with `! stdout '<value>'` redaction checks across every step, strict-equals-in-value, wrong-key fail-closed, and `.gitignore` managed-block grep assertions. Verified with `go test ./tests/e2e -run TestSyncEnvLoader -count=1`.
- [x] 5.3 运行聚焦测试、race、无 cgo build、`task check` 和 `openspec validate --all`；Owner: integrator；Scope: test evidence；Dependencies: 5.2；Lane: final；Verification: `go test ./... -count=1`、`go test -race ./...`、`CGO_ENABLED=0 go build ./cmd/pinax`、`task check`；Expected: 全部通过；Failure re-check: 不修复无关既有失败，记录真实阻断。Evidence: `go test ./... -count=1` → 53 packages PASS, 0 FAIL; `go test -race ./internal/remote ./internal/vaultignore ./internal/app -run 'Env|Dotenv|Snapshot|Reloader|Materialize|Clean|Tracked' -count=1` PASS (race-clean); `CGO_ENABLED=0 go build ./cmd/pinax` PASS; `go vet ./...` clean; `golangci-lint run` 0 issues; `golangci-lint run --enable-only unused` 0 issues; `openspec validate pinax-runtime-secret-env-loader --strict` → "Change is valid"; `openspec validate --all` → 60/0 passed.

## 验证证据

- Parser & envelope: `go test ./internal/remote -count=1` PASS (ParseDotenv 16 reject cases + accept subset, FormatDotenv round-trip, SaveLoad 0600, LoadEnvAsset missing/unsupported-schema, ResolveEnvSnapshot success/missing/wrong-key/parse-failure, EnvReloader reload/fail-safe/no-asset, Materialize/Clean 0600+symlink-reject, manifest hard-deny).
- App service: `go test ./internal/app -run TestSyncEnv -count=1` PASS (init/set/list/unlock/materialize/clean/doctor, no-plaintext-leak assertions, tracked-secret detection).
- Daemon reload: `go test ./internal/app -run TestDaemonEnvReload -count=1` PASS (run-boundary reload, atomic swap, fail-safe retention).
- CLI: `go test ./cmd/pinax -run TestSyncEnvCommand -count=1` PASS (command tree, full flow no leak, agent mode, gitignore block).
- E2E: `go test ./tests/e2e -run TestSyncEnvLoader -count=1` PASS (process-level lifecycle + wrong-key + gitignore).
- Vaultignore: `go test ./internal/vaultignore -count=1` PASS (hard-deny not overridable, managed block idempotent/refresh).
- Full health stack: `go test ./... -count=1` 53 PASS / 0 FAIL; `CGO_ENABLED=0 go build ./cmd/pinax` PASS; `go vet`/`golangci-lint`/`unused` clean; `openspec validate --all` 60/0.
