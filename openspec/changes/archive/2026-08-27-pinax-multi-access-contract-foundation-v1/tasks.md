## 0. 前置门禁与兼容基线

- [x] 0.1 Owner: change integrator；Scope: 当前 active OpenSpec、`git status --short`、共享 `internal/app`/`internal/api`/`internal/mcpserver`/`internal/cli`；Dependencies: none；Lane: gate；确认 `pinax-agent-memory-runtime`、`pinax-agent-continuity-iteration-adoption-signal` 与其他并行工作已 closeout，或为本 change 建立不重叠路径租约；Acceptance: 不覆盖用户未提交改动，所有共享文件 owner 明确；Validation: `openspec list && git status --short`；Expected: 可安全进入实现或明确哪些 lane 只能创建新文件；Failure re-check: 暂停重叠写任务，只执行 catalog/schema 新包与只读基线。Evidence: active changes remain open, but relevant remote/API/MCP files are clean; current writer lease is limited to `internal/transportcatalog/**` and this change directory until each shared path is rechecked.
- [x] 0.2 Owner: compatibility；Scope: `internal/app`、`internal/api`、`internal/remoteapi`、`internal/mcpserver`、`internal/cli`、`cmd/pinax`；Dependencies: 0.1；Lane: A；冻结当前 routes/capabilities/OpenAPI paths/MCP tools、CLI remote mode、auth 与 token alias 的 machine-readable baseline fixtures；Acceptance: fixture 能检测新增、删除、重命名、method/tool 漂移；Validation: `go test ./internal/app ./internal/api ./internal/remoteapi ./internal/mcpserver ./internal/cli ./cmd/pinax -run 'Remote|API|Auth|MCP|Route|Schema|Token' -count=1`；Expected: baseline tests 通过并记录现有合同；Failure re-check: 先分类 pre-existing/concurrent/environmental，不修改无关业务以清全局失败。Evidence: all six packages passed on 2026-08-24; existing contract tests remain the compatibility baseline.
- [x] 0.3 Owner: contract；Scope: 本 change proposal/design/specs 与现有 `openspec/specs/pinax-cli-remote-api-mode`、`pinax-web-client-contracts`、`pinax-agent-brain-layer`；Dependencies: 0.2；Lane: A；确认 new/additive、legacy/deprecated、split-owner、retained-next 分类和两个 mutation canary 未被后续实现扩大；Acceptance: capability ledger 无 silent removal，`surfaces` 与 `--token-file` 的迁移窗口明确；Validation: `rg -n 'fit|split-owner|reject-now|additive|deprecated|两个 minor|rollback|retained-next' openspec/changes/pinax-multi-access-contract-foundation-v1`；Expected: owner、兼容、回滚均可追溯；Failure re-check: 更新规格后重新 strict validate。Evidence: ledger, two-minor compatibility windows, split owners and rollback are present; the remote-mode table was corrected to retain the legacy default instead of making an optional field immediately required.

## 1. Transport catalog 与 authoritative manifest

- [x] 1.1 Owner: transport-contract；Scope: 新建 `internal/transportcatalog` 类型、validation 和 tests；Dependencies: 0.3；Lane: B；实现 `CapabilityDefinition`、`TransportBinding`、`TransportManifest`、availability/readiness enums 和 stable schema refs；Acceptance: 类型不依赖 HTTP/MCP framing，未知 enum fail-safe；Validation: `go test ./internal/transportcatalog -run 'Definition|Binding|Manifest|Validation' -count=1`；Expected: valid/invalid/unknown table tests 通过；Failure re-check: 检查 enum coercion、duplicate id 和 empty backing ref。Evidence: `go test ./internal/transportcatalog -count=1` passed on 2026-08-24; tests cover deterministic sorting, input immutability, duplicate/unknown identities, fail-safe enums, available backing refs, and planned/blocked/future-owner surface exclusion.
- [x] 1.2 Owner: application-contract；Scope: `internal/app/remote.go` 与新增独立 capability definition files；Dependencies: 1.1；Lane: B；把 domain capability metadata 与 legacy `surfaces` 声明抽离，保持 `RemoteCapabilities()` 输出兼容；Acceptance: legacy fixture byte/semantic contract不丢字段，新 definition 可被 catalog compiler 使用；Validation: `go test ./internal/app -run 'RemoteCapabilities|Legacy|Manifest' -count=1`；Expected: 旧 projection 通过，新 definition 无 duplicate id；Failure re-check: 恢复 additive facade，禁止直接重解释旧 `surfaces`。Evidence: additive `internal/app/remote_catalog.go` projects every legacy field and copies `surfaces` into declarations without mutating `RemoteCapabilities()`; focused validation passed on 2026-08-24.
- [x] 1.3 Owner: api-binding；Scope: `internal/api` REST routes、RPC dispatcher 与 catalog registration；Dependencies: 1.1,1.2；Lane: C；为每个公开 REST route/RPC method 注册真实 binding id、method/path、readonly/write gate 和 handler backing；Acceptance: 每个公开 route/method 双向可解析，无 planned capability 伪装 binding；Validation: `go test ./internal/api ./internal/app -run 'Route|RPC|Binding|Manifest' -count=1`；Expected: REST/RPC registry 与 manifest 一致；Failure re-check: 输出缺失/孤儿 binding ids，不靠 allowlist 隐藏。Evidence: manifest bindings are compiled directly from the existing `RemoteRoutes()` registry with transport-specific backing refs and capability schemas; binding count/identity/parity tests and the existing per-route REST/RPC dispatcher coverage passed on 2026-08-24.
- [x] 1.4 Owner: mcp-binding；Scope: `internal/mcpserver` registry 与 catalog registration；Dependencies: 1.1,1.2；Lane: D；为真实 MCP tools/resources 注册 binding，排除未实现 mutation 和 planned inventory；Acceptance: MCP available bindings 精确等于 runtime discovery；Validation: `go test ./internal/mcpserver -run 'Discovery|Binding|Manifest|Readonly' -count=1`；Expected: 只读/plan tools 有 backing，folder direct mutation 不出现；Failure re-check: 检查 legacy surfaces 是否误入 authoritative manifest。Evidence: the single registry now backs all 20 discovered tools and all six advertised resources; `resources/read` resolves every template instance through application services, emits bounded `pinax.mcp.resource.v1` content, strips vault paths/body/excerpts/snippets/evidence, and rejects missing/unknown URIs. Full package tests and focused race validation passed on 2026-08-25; direct folder/inbox/memory mutations remain absent.
- [x] 1.5 Owner: remote-cli-binding；Scope: `internal/remoteapi`、`internal/cli` remote command mapping；Dependencies: 1.1,1.2；Lane: E；为每个 remote CLI command 建立 RPC/REST binding ref 和 local-only rejection reason；Acceptance: unsupported command 不 fallback local，manifest 可解释 remote support；Validation: `go test ./internal/remoteapi ./internal/cli -run 'RemoteMode|Unsupported|Binding|VaultConflict' -count=1`；Expected: supported/unsupported/control-command matrix 通过；Failure re-check: 检查 config-derived remote mode 与 explicit flag 差异。Evidence: catalog bindings derive from the existing single remote dispatch registry and resolve every entry to a registered RPC capability/schema; coverage reports retain explicit `local_only`/`unsupported` reasons, and focused CLI/remoteapi tests passed on 2026-08-24.
- [x] 1.6 Owner: manifest-surface；Scope: `internal/app`、`internal/api`、`internal/cli`、`cmd/pinax`；Dependencies: 1.3,1.4,1.5；Lane: F；实现 `pinax api manifest`、`GET /v1/manifest`、`Pinax.Transport.Manifest` 和 `pinax.transport_manifest.v1` projection；Acceptance: CLI/REST/RPC 返回同一排序、digest、schema version 和 bindings；Validation: `go test ./internal/app ./internal/api ./internal/cli ./cmd/pinax -run 'TransportManifest|APIManifest' -count=1`；Expected: 三入口 semantic parity；Failure re-check: 固定 deterministic sort 和 canonical digest input。Evidence: deterministic `sha256:` digest is computed from the canonical schema/capability payload; the real CLI, injected REST server and RPC dispatcher return deep-equal manifest data. Focused package tests passed on 2026-08-25, and the stdio MCP manifest resource reported the same digest as `pinax api manifest --json`.
- [x] 1.7 Owner: conformance；Scope: transport catalog、所有 public adapter registries；Dependencies: 1.6；Lane: F；实现 manifest→adapter 与 adapter→manifest 双向 contract tests；Acceptance: missing binding、orphan route/tool/resource、duplicate operationId、false available 均 deterministic fail；Validation: `go test ./internal/transportcatalog ./internal/app ./internal/api ./internal/mcpserver ./internal/remoteapi ./internal/cli -run 'Conformance|Parity|Orphan|Duplicate' -count=1`；Expected: 正向通过、故障 fixture 精确失败；Failure re-check: 禁止用宽泛 snapshot 掩盖具体 orphan identity。Evidence: `transportcatalog.ValidateConformance` checks both directions for selected transports and rejects duplicate runtime identities, orphan adapters, false available bindings and metadata drift. REST/RPC, MCP tool/resource and remote CLI registries each build their operation inventory from the owning runtime registry; focused tests passed on 2026-08-25.

## 2. Typed OpenAPI 3.1

- [x] 2.1 Owner: schema-registry；Scope: 新建 `internal/transportcatalog/schema` 或等价独立包；Dependencies: 1.1；Lane: G；定义 versioned request/projection/error/readiness/manifest/operation JSON Schema registry；Acceptance: deterministic、closed object policy 明确、无 dangling ref；Validation: `go test ./internal/transportcatalog/... -run 'Schema|Deterministic|Closed' -count=1`；Expected: 多次生成 byte-stable 或 semantic-stable；Failure re-check: 排除 map iteration 和 Go field-order 非确定性。Evidence: `internal/transportcatalog/schema` now owns a draft-2020-12 registry for shared projection/error/readiness/manifest/operation schemas, the transport-manifest request, the generic MCP result envelope and all 20 MCP request contracts. Registry construction injects stable `$id`, rejects duplicate/invalid IDs, implicit object openness, unsupported/dangling refs and returns defensive clones; repeated JSON snapshots are byte-stable. The exact focused validation, full related packages and race tests passed on 2026-08-25.
- [x] 2.2 Owner: api-contract；Scope: REST route descriptors 与 typed request metadata；Dependencies: 1.3,2.1；Lane: G；为现有 REST bindings补齐 path/query/header/body schema、success/error response refs 和 body exposure；Acceptance: 每个 route 可生成完整 operation，不用空 object 代替未知输入；Validation: `go test ./internal/api ./internal/app -run 'RouteSchema|RequestSchema|ResponseSchema' -count=1`；Expected: route schema completeness matrix 通过；Failure re-check: 对暂未 typed 的 route 标记 schema blocked，禁止伪造完成。Evidence: all 46 current REST routes have a typed descriptor with exact method/path identity, path/query parameter schemas, aliases, request-body metadata where implemented, projection success/error envelopes, capability payload refs, body exposure, write gate and stability. Path placeholders are checked bidirectionally, request schemas with parameters cannot be empty, and the memory-capture body contract matches the real 1 MiB decoder limit. Exact validation, full related packages and race tests passed on 2026-08-25.
- [x] 2.3 Owner: openapi-export；Scope: `internal/app` OpenAPI exporter；Dependencies: 1.6,2.2；Lane: H；从真实 REST binding 生成 OpenAPI 3.1 components、securitySchemes、operation metadata 和 typed responses；Acceptance: RPC/MCP/CLI-only/planned capability 不生成 path；Validation: `go test ./internal/app -run 'APISchemaExport|OpenAPI|RegisteredRESTMethods|NoFabricatedPath' -count=1`；Expected: paths/methods 与 REST registry 一致；Failure re-check: 检查 route id、method、path template 和 schema ref join。Evidence: `BuildOpenAPI` now emits OpenAPI 3.1 from the 46 real REST contracts only, rewrites shared registry refs into `components.schemas`, publishes `BearerAuth`, typed parameters/requestBody/success/error responses and additive capability/readiness/stability/body-exposure extensions while retaining legacy extension keys. Existing tests continue to prove that non-REST brain and other local-only capabilities do not fabricate paths; exact and full validation passed on 2026-08-25.
- [x] 2.4 Owner: openapi-verification；Scope: OpenAPI semantic/golden tests 与 CLI export；Dependencies: 2.3；Lane: H；加入 OpenAPI 3.1 semantic validator、duplicate operationId、security ref、schema round-trip tests；Acceptance: 导出可被 typed client generator/validator消费，existing top-level fields保留；Validation: `go test ./internal/app ./cmd/pinax -run 'OpenAPI|SchemaExport|Semantic|Golden' -count=1`；Expected: validator通过且 legacy CLI output contract不回归；Failure re-check: 输出最小 invalid path/ref，不更新 golden 掩盖错误。Evidence: the in-process semantic validator rejects duplicate operation IDs, dangling/unsupported schema refs, unknown security schemes, invalid path parameters and incomplete response contracts. JSON round-trip validation passes, CLI export revalidates the additive readiness, operation routes, inbox capture recovery headers/parameters, and folder rename expected-revision/operation recovery parameters and reports its deterministic `sha256:298b6b47a7c2a922bf520d93fb0773ec88fc8d0664ed08647962ff23b3090e54` golden digest. Exact, full-package and race validation passed on 2026-08-25.

## 3. Local stdio MCP 合同加固

- [x] 3.1 Owner: mcp-protocol；Scope: `internal/mcpserver` current/legacy protocol lifecycle；Dependencies: 1.4；Lane: I；实现 current `2026-07-28` per-request metadata、mandatory `server/discover`、legacy initialize allowlist、真实 capabilities 与 unsupported-version error；Acceptance: current request stateless version gate 生效，legacy inventory 在 initialize 前失败，process 保持运行至 stdin EOF/signal；Validation: `go test ./internal/mcpserver -run 'Initialize|Protocol|Lifecycle|Modern' -count=1`；Expected: supported/unsupported versions 与 current/legacy paths 稳定；Failure re-check: 检查 response id、`-32022`/`-32602`、metadata 和 shutdown cleanup。Evidence: official MCP versioning was rechecked on 2026-08-25; current `2026-07-28` discovery and request metadata, four legacy initialize versions, unsupported-version details, pre-initialize rejection and EOF lifecycle are covered. A real `pinax mcp serve` transcript returned current discovery, 20 tools, three resource templates and the authoritative manifest digest without non-JSON stdout.
- [x] 3.2 Owner: mcp-schema；Scope: tool registry、input/output schema 与 validation；Dependencies: 2.1,3.1；Lane: I；为每个 available tool 增加 non-empty closed input schema 和 negotiated output schema/structured content ref；Acceptance: unknown params 在 service 前失败，旧 tool name 与 bounded result兼容；Validation: `go test ./internal/mcpserver -run 'ToolSchema|InvalidParams|InvalidToolArguments|StructuredContent|OutputSchema' -count=1`；Expected: 所有 discovered tools 通过 schema completeness；Failure re-check: 列出空 schema/tool，不用通用任意 object。Evidence: all 20 tools now load their draft-2020-12 closed input schemas and `pinax.mcp.tool_result.v1` output schema from the shared registry; a direct contract test rejects discovery drift from those registered documents. Unknown/missing/type-invalid arguments fail before service execution, while successful calls retain legacy fields and add versioned structured content. Focused, full-package and race validation passed on 2026-08-25.
- [x] 3.3 Owner: mcp-resource；Scope: resources registry、`resources/list`、`resources/templates/list`、`resources/read`；Dependencies: 1.6,3.1；Lane: J；实现 manifest 等 advertised resources 的 bounded read；Acceptance: 每个 advertised URI/template 可读，planned resource 不出现；Validation: `go test ./internal/mcpserver -run 'ResourcesList|ResourcesRead|ResourceTemplate' -count=1`；Expected: list/read parity 和 path-free projection；Failure re-check: 检查 URI identity、scope 和 body exposure。Evidence: eight registered resources are readable; current discovery separates five concrete resources from three URI templates, while legacy discovery remains compatible. `pinax://manifest` and additive `pinax://readiness` return validated bounded contracts; note/search/project resources remain 64 KiB bounded and strip vault paths, bodies, excerpts, snippets and evidence. Focused and full MCP package tests passed on 2026-08-25.
- [x] 3.4 Owner: mcp-io；Scope: MCP stdout/stderr、panic/signal cleanup；Dependencies: 3.1；Lane: J；确保 stdout 只有 JSON-RPC，diagnostics/deprecation/shutdown 只到 stderr；Acceptance: malformed request、internal error、panic recovery 与 signal cleanup 不污染 stdout；Validation: `go test ./internal/mcpserver ./internal/cli ./cmd/pinax -run 'Stdout|Stderr|JSONRPC|Signal|Panic' -count=1`；Expected: transcript 每行/帧可解析；Failure re-check: 搜索 fmt/log 默认 writer 和 Cobra human rendering。Evidence: request panics are recovered into a generic JSON-RPC internal error; diagnostics contain method/type only and never panic payloads. Malformed input still yields parseable JSON-RPC frames, Cobra passes stderr explicitly, and SIGINT/SIGTERM cancellation returns without stdout text. Focused tests passed on 2026-08-25.
- [x] 3.5 Owner: mcp-e2e；Scope: `tests/e2e` 与 `tools/testkit/mcpevidence`；Dependencies: 3.2,3.3,3.4；Lane: K；新增 current `server/discover -> tools/list -> tools/call -> resources/list/templates/list -> resources/read -> close` 与 legacy `initialize -> initialized -> tools/list -> resources/read -> close` process e2e；Acceptance: discovery、manifest、call result 三方一致，证据脱敏；Validation: `go test ./tests/e2e -run 'MCPProtocolLifecycle|MCPTransportParity' -count=1 && go run ./tools/testkit/mcpevidence`；Expected: fixture vault 全流程通过并写标准 evidence；Failure re-check: 保存最小 stdout/stderr excerpt，不记录正文或私有参数。Evidence: compiled-binary e2e verifies both protocol eras, EOF shutdown, 20 tool/8 resource manifest parity, registry-backed structured content and identical MCP/API manifest digest. The readiness addition passed the lifecycle/parity process tests on 2026-08-25; the prior standard redacted evidence remains at `temp/integration-test-runs/20260825T051226Z-826857/`.

## 4. Public client SDK、connection 与 credential 合同

- [x] 4.1 Owner: client-sdk；Scope: 新建 `pkg/pinaxclient` DTO、config、typed error 和 tests；Dependencies: 1.6,2.3；Lane: L；实现 manifest/readiness/operation 基础 client 与有界 HTTP transport；Acceptance: public package不 import `internal/**`，DTO/version/error可供 sibling module 使用；Validation: `go test ./pkg/pinaxclient -run 'Client|Manifest|Readiness|Error' -count=1`；Expected: fake owner server typed calls 通过；Failure re-check: 检查 public API 泄漏 internal types/path。Evidence: the new public package exposes versioned projection/manifest/readiness/operation DTOs, structured client errors and typed `Manifest`, `Readiness`, `Operation` and controlled `CallRPC` methods with bounded request/response handling. It validates projection/schema/manifest/operation identities and marks malformed operation results reconcile-required. Fake-owner tests, full package tests, race tests and static dependency checks prove the package imports no Pinax `internal/**` code; exact validation passed on 2026-08-25.
- [x] 4.2 Owner: client-security；Scope: `pkg/pinaxclient` URL/redirect/size/timeout policy；Dependencies: 4.1；Lane: L；实现 absolute URL、HTTP-loopback-only、no userinfo/query/fragment、redirect rejection 和 response size limits；Acceptance: credential 在所有拒绝场景前不外发；Validation: `go test ./pkg/pinaxclient -run 'BaseURL|TLS|Loopback|Redirect|ResponseLimit|Timeout' -count=1`；Expected: 安全矩阵通过；Failure re-check: 检查 IPv4/IPv6/localhost normalization 和 redirect method preservation。Evidence: client construction rejects relative/unsupported URLs, non-loopback HTTP, userinfo/query/fragment, encoded traversal, invalid size limits and unbounded/overlong timeouts; IPv4 loopback, IPv6 `::1`, normalized localhost and remote HTTPS are covered. Redirects are never followed, the target receives no credential, bearer tokens are header-only, malformed/oversized responses fail closed, and all errors omit request payloads and secrets. Exact security, full package and race validation passed on 2026-08-25.
- [x] 4.3 Owner: credential-security；Scope: `pkg/pinaxclient` secure token file、`internal/redaction`；Dependencies: 4.1；Lane: M；实现 absolute regular owner-only no-symlink token file、descriptor/inode recheck 与 bounded read；Acceptance: secret只进入 Authorization header，不进入 error/evidence；Validation: `go test ./pkg/pinaxclient ./internal/redaction -run 'TokenFile|Symlink|Owner|Permission|Redact' -count=1`；Expected: unsafe files fail closed，leak scan通过；Failure re-check: 按平台区分 owner-only实现，不用 skip 伪装支持。Evidence: `pkg/pinaxclient` now accepts an additive `Config.TokenFile` and exposes `LoadTokenFile`; absolute clean paths, regular files, non-symlink ancestors, owner-only access, bounded 8 KiB reads, pre/open/post file identity and descriptor checks all fail closed with path- and secret-free typed errors. Unix enforces current effective UID plus exact `0600`; Windows enforces owner SID, restricted DACL and no reparse points; other platforms explicitly reject the feature instead of pretending support. Exact focused tests, full package tests, race tests and Linux/macOS/Windows compile validation passed on 2026-08-25.
- [x] 4.4 Owner: remote-cli-migration；Scope: `internal/remoteapi`、`internal/cli` remote path；Dependencies: 4.1,4.2,4.3；Lane: N；让 remote CLI 逐步复用 `pkg/pinaxclient`，保留 `internal/remoteapi` compatibility facade；Acceptance: supported commands输出 parity，unsupported不 fallback local；Validation: `go test ./internal/remoteapi ./internal/cli ./cmd/pinax -run 'RemoteMode|ClientParity|Unsupported|VaultConflict' -count=1`；Expected: legacy and new path semantic match；Failure re-check: 按 capability切换，不一次删除旧 facade。Evidence: remote command request construction and execution now use `pkg/pinaxclient` directly, including the strict token-file loader, while `internal/remoteapi` remains as a source-compatible facade over the public SDK. The facade preserves `NewClient`, `Config`, `RPCRequest`, `Ping`, `Capabilities`, `Call` and legacy domain projection/error semantics; an explicit parity test covers data/facts identity. Unsupported commands and explicit vault conflicts still fail before transport with no local fallback. Exact focused tests, full remote/CLI/cmd/client tests and race validation passed on 2026-08-25.
- [x] 4.5 Owner: connection-runtime；Scope: `internal/config`、`internal/cli`、新增 connection resolver/DTO；Dependencies: 4.1；Lane: M；实现 `remote.mode`、`--connection-mode`、mode/transport separation、legacy-default migration、`connection inspect|doctor`；Acceptance: non-loopback未声明mode按兼容默认继续运行但readiness degraded，doctor只读且提示显式迁移；Validation: `go test ./internal/config ./internal/cli ./cmd/pinax -run 'Connection|RemoteMode|LegacyDefault|Doctor|Inspect|Precedence' -count=1`；Expected: flag/env/project/user/local precedence与两minor兼容矩阵通过；Failure re-check: 确认 `pinax profile` 不参与 API resolution且HTTPS policy未放宽。Evidence: additive `remote.mode`, `PINAX_CONNECTION_MODE` and `--connection-mode` now resolve after the existing endpoint precedence without repurposing storage profiles. The new `pinax.connection_descriptor.v1` keeps owner mode separate from embedded/loopback-http/https transport, sanitizes endpoint output, reports credential source type only, preserves the non-loopback `remote-service + legacy_default` compatibility path as degraded, and blocks TLS/mode/credential conflicts before remote execution. `connection inspect` is non-probing; `connection doctor` performs only local manifest compilation or remote GET manifest/readiness probes and distinguishes reachable transport from rejected auth. Exact focused, full package, race and diff validation passed on 2026-08-25.
- [x] 4.6 Owner: auth-cli；Scope: `internal/cli/api_cmd.go`、`internal/api` auth config、help/tests；Dependencies: 0.2；Lane: N；新增 canonical `api serve --token-store`，保留服务端 `--token-file` compatibility alias和脱敏迁移 warning；Acceptance: 两 flag互斥，client `--api-token-file`语义不变，machine stdout不污染；Validation: `go test ./internal/api ./internal/cli ./cmd/pinax -run 'TokenStore|TokenFile|AuthMode|OutputContract' -count=1`；Expected: temp/store/no-auth/alias矩阵通过；Failure re-check: 区分 hashed store 与 plaintext secret fixture。Evidence: `pinax api serve --token-store` is now the canonical hashed registry flag and `api.ServerOptions.TokenStore`/`AuthModeTokenStore` are additive public-internal names. Deprecated `--token-file`, `ServerOptions.TokenFile` and `AuthModeTokenFile` remain behavior-compatible aliases; canonical and legacy stores authenticate the same token records. Both store flags conflict with each other and with `--no-auth` before server startup. Human alias use emits one path-free migration warning, while JSON/agent conflicts stay stdout-only and events remain parseable NDJSON with empty stderr. Client `--api-token-file` remains the plaintext secure bearer source from 4.3. Exact focused, full API/CLI/cmd and race validation passed on 2026-08-25.
- [x] 4.7 Owner: readiness；Scope: connection readiness DTO、API/CLI/MCP projection；Dependencies: 1.6,4.5；Lane: O；实现六层 readiness、稳定 status/maturity、blocker/next-action/evidence refs；Acceptance: local fixture不合成 production ready，unknown status fail-safe；Validation: `go test ./internal/app ./internal/api ./internal/mcpserver ./internal/cli -run 'Readiness|Maturity|UnknownStatus|Production' -count=1`；Expected: local/remote/auth-blocked/recovery-blocked矩阵通过；Failure re-check: 检查 overall summary是否越级推断。Evidence: `pinax.connection_readiness.v1` now reports exactly `contract`, `transport`, `auth`, `owner`, `mutation_recovery`, and `production`, each with stable status/maturity plus blocker, next-action, and evidence-ref arrays. CLI `connection readiness`, REST `GET /v1/readiness`, RPC `Pinax.Connection.Readiness`, public SDK validation, and MCP `pinax://readiness` share the same projection contract. Local embedded/stdio fixtures never synthesize production readiness; loopback remote mutation recovery remains blocked until the operation ledger exists; unknown future statuses are preserved but treated as non-ready. Focused, full package, process e2e, race, OpenAPI and diff validation passed on 2026-08-25.

## 5. Remote mutation operation 与 reconcile canary

- [x] 5.1 Owner: operation-store；Scope: 新建 `internal/operation` GORM models/repository/migration；Dependencies: 0.1；Lane: P；实现 operation/idempotency records、indexes、bounded result/receipt和 additive migration；Acceptance: application service以外无raw SQL，migration失败不执行domain write；Validation: `go test ./internal/operation -run 'Migration|Repository|Idempotency|Index' -count=1`；Expected: disposable SQLite create/reopen/query通过；Failure re-check: 检查 unique key、transaction和旧 vault无损。Evidence: `internal/operation` now owns additive GORM models and migration for `.pinax/api/operations.sqlite`, with owner-local directory/file modes, indexed operation state/scope, a unique principal+scope+idempotency binding, and a unique operation identity. Accepted operation plus idempotency binding are created in one transaction; same binding returns the original record, conflicting operation/capability/binding/digest fails closed, and concurrent callers create exactly one logical record. Result and receipt JSON are validated and bounded before any state update; unsafe absolute path refs are rejected rather than truncated. Create/reopen, legacy vault preservation, migration failure, index, repeated concurrency, full package and race validation passed on 2026-08-25; the package contains no raw SQL execution.
- [x] 5.2 Owner: operation-domain；Scope: `internal/operation` state machine/reconcile policy；Dependencies: 5.1；Lane: P；实现 accepted/applying/succeeded/failed/reconcile_required、canonical digest、same-key replay与conflict；Acceptance: illegal transition fail closed，复杂状态和crash window有中文注释；Validation: `go test ./internal/operation -run 'State|Transition|Digest|Conflict|Reconcile' -count=1`；Expected: table-driven state matrix通过；Failure re-check: 检查 clock、canonicalization、principal/scope binding。Evidence: the operation package now implements the stable accepted/applying/succeeded/failed/reconcile_required transition matrix, terminal-state protection, retry only when both retryable and replay-safe, canonical recursively sorted JSON request digests, and length-delimited principal/scope digests. Reconcile decisions use only receipt/resource/revision proof: contradictory evidence fails, insufficient evidence remains reconcile_required with a no-replay action, and proven outcomes finalize the same operation identity. Complex crash-window and no-blind-replay rules are documented in Chinese comments. Exact state/digest/conflict/reconcile, full package and race validation passed on 2026-08-25.
- [x] 5.3 Owner: operation-surface；Scope: `internal/app`、`internal/api`、`internal/cli`、`pkg/pinaxclient`；Dependencies: 4.1,5.2；Lane: Q；实现 operation show/reconcile 的 CLI、REST/RPC、SDK contract与scope authorization；Acceptance: mismatch scope不泄漏存在性，malformed 2xx触发 reconcile required；Validation: `go test ./internal/app ./internal/api ./internal/cli ./pkg/pinaxclient -run 'OperationShow|OperationReconcile|Scope|Malformed' -count=1`；Expected: 四入口projection parity；Failure re-check: 检查 operation identity和opaque error。Evidence: additive `pinax operation show|reconcile`, `GET /v1/operations/{operation_id}`, `POST /v1/operations/{operation_id}:reconcile`, `Pinax.Operation.Get|Reconcile`, and public SDK `Operation|ReconcileOperation` now share `pinax.operation.v1`. The projection includes binding, retry/replay/reconcile facts, bounded result/receipt, resource/revision refs, timestamps and redacted error while omitting principal/scope/idempotency secrets and absolute vault paths. API middleware binds records to a hashed token principal and hashed owner-vault scope; a mismatched token receives the same operation_not_found shape as a missing ID. Local owner CLI uses an explicit owner-local access path. REST/RPC semantic parity, remote CLI RPC mapping, malformed 2xx reconcile-required handling, exact focused, full related package, OpenAPI, diff and race validation passed on 2026-08-25.
- [x] 5.4 Owner: inbox-canary；Scope: inbox capture application/API/RPC/remote CLI path；Dependencies: 5.2,5.3；Lane: R；为 `inbox.capture` 接入 client-generated operation/idempotency binding；Acceptance: duplicate submit只创建一条note，dry-run不进入apply状态；Validation: `go test ./internal/app ./internal/api ./internal/remoteapi ./tests/e2e -run 'InboxCapture.*Idempot|InboxCapture.*Reconcile|InboxCapture.*DryRun' -count=1`；Expected: timeout/duplicate/success fixture通过；Failure re-check: 对比canonical note ref、receipt和operation record。Evidence: `inbox.capture` now accepts an additive operation/idempotency pair through REST headers, RPC params, and remote CLI flags; the CLI generates a cryptographically random pair when both flags are omitted and preserves explicit pairs for retry. Legacy REST/RPC requests that send neither field retain the direct compatibility path, while partial identity fails before ledger creation. Dry-run writes neither domain state nor operation ledger. Recovery-enabled execution persists bounded planned path/note identity before applying, binds the append-only record event to the operation, stores no request body or absolute vault path, and returns the original operation on duplicate submit without creating a second note. Reconcile proves success from the record event or matching frontmatter object identity and never replays the mutation. REST/RPC bindings are marked first-support ready with evidence refs; exact validation, full related package/e2e tests, OpenAPI semantic/golden validation, diff checks, and focused race tests passed on 2026-08-25.
- [x] 5.5 Owner: folder-canary；Scope: folder rename application/API/RPC/remote CLI path；Dependencies: 5.2,5.3；Lane: R；为 `folder.rename` 增加 expected revision、snapshot/restore proof和operation recovery；Acceptance: stale revision零写入，成功只rename一次并记录before/after；Validation: `go test ./internal/app ./internal/api ./internal/remoteapi ./tests/e2e -run 'FolderRename.*Revision|FolderRename.*Idempot|FolderRename.*Reconcile' -count=1`；Expected: conflict/success/crash-after-write fixture通过；Failure re-check: 检查resolver canonical path和snapshot gate。Evidence: REST/RPC/remote CLI now carry additive `expected_revision`, operation ID and idempotency key fields; remote CLI performs a dry-run revision preflight and reuses one generated identity. The owner rejects stale revisions with zero folder writes, requires the latest snapshot to match current vault content before creating a new operation, persists source/target/snapshot/revision preparation evidence, records revision before/after and bounded receipt refs, returns the original terminal operation on replay, and reconciles ambiguous completion only from filesystem plus folder-registry proof without replaying the rename. Legacy callers with neither operation identity retain the direct route behavior. Focused, full related-package and race validation passed on 2026-08-25.
- [x] 5.6 Owner: client-recovery；Scope: `pkg/pinaxclient` mutation flow与remote CLI；Dependencies: 5.4,5.5；Lane: S；实现 ambiguous outcome后先status/reconcile、仅terminal retryable+replay_safe才同binding重试；Acceptance: client不生成第二operation/key，不自动fallback local；Validation: `go test ./pkg/pinaxclient ./internal/remoteapi -run 'Ambiguous|NoBlindRetry|ReplaySafe|Reconcile' -count=1`；Expected: timeout/reset/5xx/malformed2xx矩阵通过；Failure re-check: 用request counter证明没有额外mutation POST。Evidence: `pkg/pinaxclient.CallMutationRPC` validates the retained operation/idempotency pair, sends one mutation request, classifies timeout/reset/response-read failure/5xx/malformed-or-oversized-2xx as ambiguous, then queries the same operation and reconciles non-terminal state before any retry. A second mutation POST is permitted only for terminal `failed && retryable && replay_safe`, reuses the exact original request and identity, and is capped at one; succeeded, unsafe, non-terminal, and operation-not-visible outcomes never blind-retry and return durable recovery facts or `mutation_outcome_unknown`. Remote CLI now uses this path for the two canary mutations without local fallback, while `internal/remoteapi.CallMutation` preserves an additive compatibility facade and redacted error projection. Request-counter fixtures cover timeout, connection reset, 5xx, malformed 2xx, missing operation, unsafe failure and exact-binding replay. Focused, full related-package, race and diff validation passed on 2026-08-25.
- [x] 5.7 Owner: recovery-e2e；Scope: disposable API process、fixture vault、operation ledger evidence；Dependencies: 5.6；Lane: T；新增inbox response-loss与folder finalize-crash process e2e；Acceptance: owner state只变化一次，status/reconcile恢复同一operation，evidence脱敏；Validation: `go test ./tests/e2e -run 'RemoteMutationRecovery|OperationReconcile' -count=1`；Expected: 每次run写标准evidence且原exit code保留；Failure re-check: 保存operation状态、revision/receipt refs和最小失败excerpt。Evidence: a real `pinax api serve --allow-write --no-auth --events` child process is exercised through a loopback response-loss proxy that drops the completed inbox response only after the owner has committed. `pkg/pinaxclient` recovers the same succeeded operation with one forwarded mutation, one operation-bound record event, a present note resource, and durable receipt/resource refs. The folder fixture persists an applying operation, performs exactly one child-process rename, then a real API reconcile proves the outcome from filesystem plus folder registry and finalizes the same revision/receipt without replay. The additive `operation-recovery` integration evidence profile and `task integration:operation-recovery` entrypoint run the client/facade/CLI/process matrix under the standard evidence writer. Passed evidence is stored at `temp/integration-test-runs/20260825T085637Z-474552/` with summary/command/stdout/stderr/env/artifacts, exit code 0, empty stderr, and a clean scan for token, request body and absolute-path markers.

## 6. 输出、文档与 split-owner handoff

- [x] 6.1 Owner: output-contract；Scope: `internal/output`、`internal/redaction`、CLI JSON/agent/events/explain；Dependencies: 1.6,4.7,5.3；Lane: U；定义manifest/readiness/operation的default/`--json`/`--agent`/`--events`/`--explain`投影与stdout/stderr分离；Acceptance: machine fields additive、English stable keys、无secret/body/path泄漏；Validation: `go test ./internal/output ./internal/redaction ./cmd/pinax -run 'Manifest|Readiness|Operation|OutputContract|Leak' -count=1`；Expected: golden/contract tests通过；Failure re-check: 不把human warning写machine stdout。Evidence: manifest, six-layer readiness and operation projections now have stable human labels, a dedicated readiness layer table, existing JSON envelopes, additive `fact.*` agent keys, two-frame NDJSON events, and redacted explain summaries. A command-specific remote-contract output gate normalizes struct data before rendering and redacts accidental body/content/request payloads, principal/scope/idempotency fields, vault paths, credentials, provider payloads and machine absolute paths without changing legitimate note body commands. CLI contract tests exercise manifest/readiness/operation in all five modes, assert stdout/stderr separation, stable English machine keys, operation/revision refs, and no seeded secret/body/path leakage. Focused, race and diff validation passed on 2026-08-25.
- [x] 6.2 Owner: docs；Scope: `docs/commands/api.md`、`docs/commands/mcp.md`、`docs/interfaces/remote-api-contract.md`、`docs/interfaces/auth-contract.md`、`docs/interfaces/client-cli-parity-and-sync.md`、`docs/README.md`；Dependencies: 4.6,6.1；Lane: U；更新真实命令、三种mode、manifest、token-store/token-file区别、MCP lifecycle和operation recovery；Acceptance: 所有示例可直接运行，不再把同一文件同时用于server store/client secret；Validation: `rg -n -- '--token-store|--api-token-file|connection doctor|api manifest|operation reconcile' docs README.md`；Expected: canonical示例齐全且legacy迁移明确；Failure re-check: 与CLI help和contract tests逐条对照。Evidence: the API, MCP, auth, remote contract, parity and documentation index now describe the three owner modes separately from embedded/loopback-http/https/stdio transports, authoritative manifest/readiness entrypoints, the current `2026-07-28` per-request MCP lifecycle plus all four retained initialize versions, and operation show/reconcile recovery without changing legacy tool/resource/capability names. Canonical examples use server `--token-store` for the hashed registry and a distinct owner-only client `--api-token-file`; the deprecated server `--token-file` is documented only as the same hashed-store alias. The stale token command and full usage examples were corrected as well. Required documentation search, exact CLI help inspection, focused CLI/cmd contract tests and `git diff --check` passed on 2026-08-25.
- [x] 6.3 Owner: evidence；Scope: `tools/testkit/integrationevidence`、Taskfile/test scripts；Dependencies: 3.5,5.7；Lane: V；为MCP protocol、SDK security、remote mutation recovery生成标准evidence目录并执行redaction scan；Acceptance: summary/command/stdout/stderr/env/artifacts齐全，失败保留原exit code；Validation: `go test ./tools/testkit/integrationevidence -count=1 && find temp/integration-test-runs -maxdepth 2 -type f | sort | tail -n 30`；Expected: fixture evidence完整且无secret/raw prompt；Failure re-check: 检查runner exit propagation和redaction allowlist。Evidence: the unified integration evidence runner now exposes `mcp-protocol`, `sdk-security`, and `operation-recovery` profiles through additive Taskfile targets. The shared writer applies deterministic bearer/secret/path and body/prompt/provider sentinel redaction, performs a post-write idempotence scan over the actual bundle, records `redaction_scan_passed`/issue count, and changes a successful command to exit 1 if the scan fails while preserving an already failing command's original exit code. Writer/profile tests passed. Real evidence runs passed at `temp/integration-test-runs/20260825T092209Z-1192774/` (MCP e2e), `temp/integration-test-runs/20260825T092231Z-1198351/` (SDK security component), and `temp/integration-test-runs/20260825T092249Z-1200805/` (operation recovery e2e); every bundle contains summary/command/stdout/stderr/env/artifacts, has exit code 0, empty stderr, `scan_passed=true`, zero scan issues, and no seeded body/prompt/provider, Authorization, token or absolute-path residue.
- [x] 6.4 Owner: root-handoff-author；Scope: 本 change `handoffs/` 与根级后续候选，不修改 foreign runtime code；Dependencies: 1.6,4.1,4.7；Lane: W；冻结 future `mcp/pinax` 的SDK methods、tool/resource taxonomy、scope/readiness、operation refs、禁止事项和验收命令；Acceptance: handoff可供根级另立OpenSpec，明确stdiod first与gateway owner；Validation: `rg -n 'mcp/pinax|pkg/pinaxclient|resources/read|Streamable HTTP|不得' openspec/changes/pinax-multi-access-contract-foundation-v1/handoffs`；Expected: owner/input/output/AC/rollback完整；Failure re-check: 若SDK contract未稳定，保持handoff blocked而不创建subproject。Evidence: the sibling MCP handoff now separates currently available public SDK methods from blocking typed note/search/context additions, keeps the handoff `retained-next / blocked`, adopts stdio first with Streamable HTTP owned by `mcp/gateway`, aligns retained tool/resource names with the local registry, and freezes scope/readiness discovery, opaque operation/resource refs, mutation gates, forbidden imports/filesystem/CLI/retry duplication, process e2e, static guards and rollback. No foreign runtime, repository, submodule or deployment was created. Required handoff search and diff validation passed on 2026-08-25.
- [x] 6.5 Owner: root-admission-author；Scope: 本 change hosted-service handoff；Dependencies: 4.7；Lane: W；形成future hosted service的产品/owner决策问题、数据真源、单用户/多人、identity/RBAC/backup/ops门禁；Acceptance: 候选名不被描述为已批准项目，`cli/pinax`不被改写为云后端；Validation: `rg -n 'candidate|owner-fit|数据真源|多用户|不批准|rollback' openspec/changes/pinax-multi-access-contract-foundation-v1/handoffs`；Expected: root可据此做fit/split-owner/reject-now；Failure re-check: 未决问题保留open，不推断授权。Evidence: the hosted-service packet remains `candidate / not approved` and classifies current action as `split-owner candidate / reject-now for implementation`. It records the unresolved product/JTBD, source-of-truth, single-user/multi-tenant, identity/RBAC, backup/restore, operations, pricing/adoption and rollback decisions; maps fit/split-owner/reject-now outcomes and owner boundaries; sequences dependencies before a private canary; and requires independent six-layer readiness without rewriting `cli/pinax` into a cloud backend. Required admission search and diff validation passed on 2026-08-25.

## 7. 最终验证与 closeout

- [x] 7.1 Owner: focused-verification；Scope: touched packages；Dependencies: 1.7,2.4,3.5,4.7,5.7,6.3；Lane: verify；运行focused unit/integration/component tests；Acceptance: 所有新合同与故障fixture通过；Validation: `go test ./internal/transportcatalog/... ./internal/api ./internal/mcpserver ./internal/remoteapi ./internal/operation ./pkg/pinaxclient ./internal/cli ./cmd/pinax ./tests/e2e -run 'Manifest|OpenAPI|MCP|Connection|Token|Operation|RemoteMutation|Parity|Reconcile' -count=1`；Expected: exit 0；Failure re-check: deterministic failure回到原owner，禁止修无关代码。Evidence: the exact focused command passed across catalog/schema, API, MCP, compatibility facade, GORM operation ledger, public SDK, CLI, binary command contracts and process e2e on 2026-08-25.
- [x] 7.2 Owner: race-verification；Scope: catalog/API/MCP/client/operation concurrency；Dependencies: 7.1；Lane: verify；运行race detector覆盖registry compile、server lifecycle、idempotency和reconcile；Acceptance: 无data race、double apply或session leak；Validation: `go test -race ./internal/transportcatalog/... ./internal/api ./internal/mcpserver ./internal/operation ./pkg/pinaxclient -count=1`；Expected: exit 0；Failure re-check: 保存race stack，定位共享状态owner。Evidence: the exact race command passed on 2026-08-25 with catalog/schema, API, current/legacy MCP lifecycle, operation state transitions/reconcile and SDK recovery all clean.
- [x] 7.3 Owner: full-quality；Scope: Pinax subproject stable diff；Dependencies: 7.2；Lane: verify；在代码、focused tests和文档稳定后运行最终质量门；Acceptance: fmt/lint/test/build/OpenSpec全通过，失败已归因；Validation: `task check`；Expected: exit 0；Failure re-check: 区分introduced/pre-existing/concurrent/environmental/ambiguous，不回滚用户改动。Evidence: the first final run identified four introduced static-check findings in the new MCP/catalog paths (two unchecked test pipe closes, one redundant embedded selector and one case-insensitive comparison). Minimal mechanical fixes were applied and focused lint/tests passed. The second full `task check` passed on 2026-08-25: golangci lint/fmt reported zero issues, all Go packages passed, the binary built, Web TypeScript/Vitest passed, and all 73 OpenSpec items validated.
- [x] 7.4 Owner: contract-closeout；Scope: OpenSpec change与稳定spec sync候选；Dependencies: 7.3；Lane: verify；校验任务证据、capability ledger、deprecation/rollback和下游handoff，准备archive而不自动归档；Acceptance: 无unchecked required task，无production overclaim；Validation: `openspec validate pinax-multi-access-contract-foundation-v1 --strict && openspec status --change pinax-multi-access-contract-foundation-v1`；Expected: valid且tasks完成；Failure re-check: 修正spec/evidence引用，不以勾选替代验证。Evidence: every required task now has concrete validation evidence; legacy routes/RPC methods/MCP names/initialize behavior/capability fields remain additive and retained; server token-file rollback compatibility, SDK facade compatibility, operation recovery rollback and both split-owner handoffs are documented without production overclaim. Strict validation reported the change valid and status reported all four artifacts complete on 2026-08-25. The change is prepared for archive review but was not archived automatically.
