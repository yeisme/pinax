# 任务

- [x] 1. Owner: `cli/pinax`; Lane: docs; Depends on: none; Scope: 客户端覆盖和同步边界文档。Acceptance: 新增 `docs/interfaces/client-cli-parity-and-sync.md`，并从 `docs/README.md`、`docs/README.zh-CN.md`、`docs/commands/api.md`、`docs/commands/sync.md` 可达；失败时检查链接和命令示例是否是真实 `pinax` 命令。
  - Evidence: 已新增客户端 CLI 覆盖和实时同步说明，并接入文档地图与命令页。
- [x] 2. Owner: `cli/pinax`; Lane: A; Depends on: 1; Scope: 生成 CLI tree 与 `RemoteCapabilities()` 覆盖矩阵。Acceptance: 新增测试或命令可列出每个 root command 的 `remote_supported|local_only|unsupported` 状态；`go test ./internal/app ./internal/cli -run 'Remote|Capability|CommandParity' -count=1` 通过；失败时先检查 alias、隐藏兼容命令和 local-only 命令分类。
  - Evidence: 2026-06-26 运行 `go test ./internal/app ./internal/cli -run 'Remote|Capability|CommandParity' -count=1`，退出码 0。当前 `RemoteCommandCoverage` 覆盖矩阵按 `remote_supported`、`local_only`、`unsupported` 分类可见 runnable commands；remote-supported 项绑定 capability/RPC method，local-only/unsupported 项保留原因说明。
- [x] 3. Owner: `cli/pinax`; Lane: A; Depends on: 2; Scope: Phase 1 只读能力补齐。Acceptance: note/search/kb/index/query/dataview/database/view 的只读或 view 管理命令进入 capability registry、RPC dispatcher 和 remote CLI mapper；`go test ./internal/api ./internal/cli ./cmd/pinax -run 'Remote|API|Schema|Route' -count=1` 通过；失败时检查 projection envelope 和 registry/OpenAPI 派生。
  - Evidence: 2026-06-26 运行 `go test ./internal/api ./internal/cli ./cmd/pinax -run 'Remote|API|Schema|Route' -count=1`，退出码 0。API route/RPC registry、CLI remote mapper 和 schema/OpenAPI 派生测试覆盖只读与 view 管理能力的 projection envelope。
- [x] 4. Owner: `cli/pinax`; Lane: B; Depends on: 2; Scope: Phase 2 内容与资产能力补齐。Acceptance: template/asset/prompt/collection/graph/import/export 的客户端能力支持 read、plan、dry-run 或 gated write；所有写路径经过 application service；`go test ./internal/api ./internal/cli ./cmd/pinax -run 'Remote|Template|Asset|Prompt|Import|Export' -count=1` 通过。
  - Evidence: 2026-06-26 运行 `go test ./internal/api ./internal/cli ./cmd/pinax -run 'Remote|Template|Asset|Prompt|Import|Export' -count=1`，退出码 0。现有 registry/route/remote command tests 覆盖 template、asset、prompt、collection、graph、import/export 相关 read、plan、dry-run 或 gated write 客户端能力。
- [x] 5. Owner: `cli/pinax`; Lane: C; Depends on: 2; Scope: Phase 3 维护闭环能力补齐。Acceptance: repair/metadata/organize/proof/version 的 plan/snapshot/apply/restore 客户端路径保持 approval、snapshot、receipt 和 redaction；`go test ./internal/api ./internal/cli ./cmd/pinax -run 'Remote|Repair|Organize|Proof|Version' -count=1` 通过。
  - Evidence: 2026-06-26 运行 `go test ./internal/api ./internal/cli ./cmd/pinax -run 'Remote|Repair|Organize|Proof|Version' -count=1`，退出码 0。维护闭环能力经 registry、API/CLI remote tests 覆盖，保留 approval、snapshot、receipt 和 redaction 边界。
- [x] 6. Owner: `cli/pinax`; Lane: C; Depends on: 2; Scope: Phase 4 集成能力补齐。Acceptance: publish/plugin/mcp/backend/storage/cloud 的客户端能力可发现，危险操作默认 plan/dry-run 或明确 local-only；`go test ./internal/api ./internal/cli ./cmd/pinax -run 'Remote|Publish|Plugin|Backend|Storage|Cloud' -count=1` 通过。
  - Evidence: 2026-06-26 运行 `go test ./internal/api ./internal/cli ./cmd/pinax -run 'Remote|Publish|Plugin|Backend|Storage|Cloud' -count=1`，退出码 0。publish、plugin、mcp、backend、storage、cloud 集成面可通过 capability/route/remote mapping 审计；危险操作保持 plan/dry-run 或 local-only 边界。
- [x] 7. Owner: `cli/pinax`; Lane: sequential; Depends on: 3,4,5,6; Scope: 最终全量覆盖审计和文档收口。Acceptance: `pinax api routes --vault ./my-notes --json` 能解释所有可客户端化命令；local-only 命令有稳定 unsupported/local-only 说明；`task check` 或等价命令通过，若 lint 有既有失败需记录无关证据和聚焦测试结果。
  - Evidence: 2026-06-26 使用临时 vault 运行 `go run ./cmd/pinax api routes --vault <temp>/my-notes --json`，输出 `command=api.routes`、`facts.routes=62`，evidence/data 包含 REST/RPC capabilities，例如 `project.subproject.show`、`database.view.render`、`graph.summary`、`sync.push`、`sync.pull`。随后运行 `task check`，退出码 0，覆盖 `golangci-lint fmt --diff`、`golangci-lint run`、`go test ./...`、sidecar protocol tests、`openspec validate --all` 和 `go build -trimpath -ldflags="-s -w" -o dist/pinax ./cmd/pinax`。
