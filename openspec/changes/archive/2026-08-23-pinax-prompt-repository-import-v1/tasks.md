# 1. Contract and bridge

- [x] 1.1 Pin public immutable `github.com/yeisme/promptrepo` version without local replace；验收：anonymous module resolution；验证：`GOWORK=off go mod download github.com/yeisme/promptrepo@v0.3.0`。 Evidence: annotated tag `v0.3.0` published to github.com/yeisme/promptrepo (release-prep commit `d113f86` after the v0.3.0 gate: mod verify/test/vet, CGO-off test/build, strict OpenSpec, secret scan all clean); anonymous `GOWORK=off go list -m github.com/yeisme/promptrepo@v0.3.0` resolves through proxy.golang.org; pinax go.mod pins `github.com/yeisme/promptrepo v0.3.0` with no replace, vendored, full build + prompt-catalog suites PASS against the public tag.
- [x] 1.2 定义 Pinax bridge/application ports、consumer capabilities 和 error mapping；验收：不 import Registry internal、不直接读写 promptrepo state；验证：architecture/source guard tests。 Evidence: `internal/promptbridge` defines Bridge ports (OpenShared engine factory, optional Inspector/Validator/Previewer/Renderer/ContractResolver resolution, consumer descriptor with `pinax` consumer + capability keys, stable `prompt_catalog_*`/`prompt_repository_*`/`prompt_install_*` error mapping, panic containment as `prompt_catalog_provider_panic`); `internal/architecture/promptbridge_guard_test.go` locks engine/source imports behind the bridge and Registry internals out; PASS.
- [x] 1.3 接入 effective RepositorySet/PolicyDecision optional interfaces；验收：user/org/project/session precedence 和 deny-wins；验证：table tests。 Evidence: `internal/promptbridge/scope.go` ScopeBinding/SessionScope/RepositorySetSource optional interfaces + EffectiveRepositorySet (most-specific pin wins, deny at any scope wins, PolicyDecision per repository); `scope_test.go` table tests cover user-only/org pins/project-over-org/session-over-project/deny-wins/session-deny-not-overridable/unknown-pin cases; session `--repository`/`--deny` wired into catalog search/resolve in `service_prompt_catalog.go`; PASS.

# 2. Repository and catalog commands

- [x] 2.1 Additive 注册 `pinax prompt repository add|list|show|remove|enable|disable|sync|doctor`；验收：现有 prompt commands manifest 不变；验证：Cobra help/completion/contract tests。 Evidence: `internal/cli/prompt_catalog_cmd.go` registers the additive `prompt repository` group; `cmd/pinax/prompt_catalog_command_test.go` TestPromptCatalogCommandTreeIsAdditive asserts old commands (create/import/search/show/resolve/lifecycle/feedback) and new groups coexist; existing `prompt_command_test.go` golden tests pass unchanged.
- [x] 2.2 注册 `pinax prompt catalog search|show|resolve|inspect|validate|preview`；验收：`prompt search` 仍只搜索 local vault；验证：testscript command tree and semantic separation。 Evidence: `prompt catalog` group registered; `tests/e2e/prompt_catalog_test.go` asserts federated search returns 6 while local `prompt search` over the same query returns 0 in a fresh vault (semantic separation), and `--repository/--deny` session scoping filters federated results; PASS.
- [x] 2.3 统一 `operation_id`、human/agent/JSON/events projection；验收：machine keys stable、reason/readiness parity；验证：golden output tests。 Evidence: every repository/catalog projection emits `operation_id=promptrepo.*.v1` facts with stable machine keys (`provider_calls`/`durable_writes`/`ready`/`missing_inputs`/`rights`/`snapshot_digest`); `cmd/pinax` TestPromptCatalogOutputModeParity asserts the same projection feeds default/agent/JSON renderers (`command=prompt.repository.list`, `fact.results=1`, `fact.operation_id=...`, `fact.repository.1.id=parity`); e2e asserts readiness parity (inspect `next_action=supply_inputs`/`missing_inputs=subject`, validate ready true/false, preview rendered digest).
- [x] 2.4 增加 provider panic、body/input/credential sentinel；验收：inspect/validate/preview provider_calls=0、durable_writes=0；验证：focused regression tests。 Evidence: `internal/promptbridge/bridge_test.go` TestBridgeContainsProviderPanic (panic → `prompt_catalog_provider_panic`, never escapes) + capability degradation tests; `tests/e2e/prompt_catalog_test.go` asserts inspect/validate/preview `provider_calls=0`/`durable_writes=0` and scans outputs for body (`SENTINEL-PROMPT-BODY-…`), input value (`SENTINEL-INPUT-VALUE`), and credential ref (`keychain://SENTINEL-CREDENTIAL-REF`) sentinels; `sanitizeRepositoryView` strips credential refs and URI userinfo from every repository projection; PASS.

# 3. Rights-gated import

- [x] 3.1 实现 exact resolve、contract/body digest、rights/copy permission 和 conflict plan；验收：preview-only/no-copy fail closed；验证：permission matrix tests。 Evidence: `PromptCatalogInstall` gates: exact resolve → blocked/prohibited rights → verified companion contract (missing → fail closed) → import permission (`local_import`/`local_copy`/`copy`/`install`) → digest-bound body read (mismatch → `prompt_install_body_unverifiable`) → conflict plan (reject default, `--fork` side-by-side, idempotent already_installed). e2e covers preview-only (`prompt_install_rights_blocked`), blocked rights, no-contract, conflict/fork matrix, MIT license → public permission.
- [x] 3.2 通过 Pinax application service/GORM repository 创建 PromptAsset `draft`、version 和 source refs；验收：不手写 rows、不自动 tested/accepted/promoted；验证：repository/service tests。 Evidence: install goes through `promptasset.OpenVaultRepository().Create` (existing GORM transaction writing asset + version + source refs); plan mode without `--yes` writes nothing (`durable_writes=0`); e2e asserts installed asset lifecycle stays `draft` via `prompt show`, `--yes` is required for the durable write, and the conflict path leaves the existing asset unchanged.
- [x] 3.3 增加 local `pinax://prompt/<id>` 与 external `promptrepo://...` provenance round-trip；验收：show/resolve 不输出 body/path；验证：URI and projection tests。 Evidence: installs store source refs `{exact ref + snapshot/template digests, promptrepo://contract/<digest>, promptrepo://stage/<receipt-id>}`; deterministic local ID `catalog_<package>_<solution>` and `pinax://prompt/<id>` next-action; e2e asserts installed draft resolves locally, survives repository removal, and no projection contains the body sentinel (catalog show/resolve never read the body at all).

# 4. Verification and docs

- [x] 4.1 增加 file/Git/GitHub/S3、offline cache、quarantine 和 scope/policy fixtures；integration/e2e evidence 写入 `temp/integration-test-runs/<run-id>/`。 Evidence: `internal/promptbridge/testfixture` builds canonical file fixtures; `tests/e2e/prompt_catalog_test.go` (file) and `prompt_catalog_sources_test.go` (git+file real clone w/ full install, fake-S3 httptest endpoint, offline preview from cached bare clone after origin removal, tampered-catalog quarantine keeping last good snapshot, scope pin/deny policy) PASS; `task integration:prompt-catalog` writes redacted evidence to `temp/integration-test-runs/20260823T161830Z-3573228/` (status success, exit 0). GitHub remotes share the git adapter path (`git+https`); no live-network fixture is used.
- [x] 4.2 更新 Pinax 本地 docs/command reference，不回填 root Pinax 文档；验证：real command examples and body/secret scan。 Evidence: `docs/commands/prompt.md` gains the federated surface (two-surface separation, full subcommand table with writes column, real workflows, rights-gate rules, identity/provenance, scope policy, boundaries); `docs/README.md` gains the experimental additive summary; no root docs touched; all examples are real `pinax` commands and contain no bodies or secrets.
- [x] 4.3 focused verification：`go test ./internal/cli ./internal/app ./internal/promptasset`。 Evidence: `go test ./internal/cli ./internal/app ./internal/promptasset -count=1` → all ok; plus `./internal/promptbridge/... ./internal/architecture ./cmd/pinax ./tests/e2e` prompt-catalog suites PASS; `golangci-lint run` 0 issues; `gofmt -l` clean.
- [x] 4.4 final gate：`task check` 和 `openspec validate pinax-prompt-repository-import-v1 --strict --no-interactive`。 Evidence: `task check` PASS (exit 0, full suite) after pinning public `github.com/yeisme/promptrepo v0.3.0` without replace; `openspec validate pinax-prompt-repository-import-v1 --strict --no-interactive` → valid; `openspec validate --all` → 72 passed/0 failed; `golangci-lint run` → 0 issues; `CGO_ENABLED=0 go build ./cmd/pinax` PASS; `task integration:prompt-catalog` evidence run recorded at `temp/integration-test-runs/20260823T161830Z-3573228/` (status success).
