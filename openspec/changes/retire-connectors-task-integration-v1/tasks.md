# 任务

- [x] 1.1 清理 Pinax 规划 service、domain、CLI wiring、模板和 Connectors/TaskBridge 专属测试；Owner: root；Scope: `internal/app/**`、`internal/domain/**`、`internal/cli/**`、`cmd/pinax/**`；Verification: `go test ./internal/app/... ./internal/cli/... ./cmd/pinax/... -count=1`；Failure re-check: 只修复本次退役引入的编译或断言失败。
- [x] 1.2 更新 Pinax 活跃命令文档、AGENTS、架构文档和 canonical OpenSpec；Owner: root；Scope: `docs/**`、`AGENTS.md`、`README.md`、`openspec/specs/**`；Verification: `grep -RInE 'connectors task|connectors-task|TASKBRIDGE|taskbridge|--taskbridge'`（排除 archive/history）；Failure re-check: 区分历史归档与当前合同。证据：archive/history 与本变更自身文档之外零匹配；`docs/commands/plan.md`、`docs/commands/task.md`、`openspec/specs/planning-workflows/spec.md`、`openspec/specs/project-board-workspace/spec.md` 均已表述本地化规划（禁止外部任务运行时）；`import.md` 与 `general-agent-memory-prd.md` 中的 "connectors" 属未来导入源/记忆源连接器，非已退役 Connectors 任务运行时。
- [x] 1.3 验证子项目质量门禁与 OpenSpec；Owner: root；Scope: Pinax worktree；Verification: `gofmt -w`、`go test ...`、`task check`、`openspec validate --all --strict`、`git diff --check`；Failure re-check: 归因 dirty worktree 中的预存失败，不修改无关变更。证据（2026-08-08）：`gofmt -l internal/ cmd/` 零输出；`git diff --check` 零输出；`openspec validate --all --strict` 66/66 通过；`go vet ./...` 零输出；`go build ./...` 通过；planning 范围聚焦测试（`TestPlanningActionDraft*`、`TestPlanDailyTaskReview*`、`TestPlanWeekly*`、project-board/task-adopt 全集）全绿；`tests/e2e` 全绿。本变更引入失败数 = 0。dirty worktree 全量 `go test ./...` 有 7 个失败，全部位于 `cmd/pinax/sync_cloud_backend_plan_command_test.go`（sync/briefing），分类为 **concurrent**：`pinax-personal-local-cli-simplification` task 4.2 证据（2026-07-30）记录当时全绿，HEAD `1199521`（2026-08-02）为 KB 提交，此后未提交的 sync 工作（`pinax-sync-output-ux` 5/7、`pinax-passphrase-s3-bootstrap` 30/32）修改了该测试文件（-101/+7 行）与 sync 实现，归 `pinax-sync-output-ux` task 7（未完成）所有；本变更未触碰 sync 代码，按 1.3 指令不修改无关变更。
