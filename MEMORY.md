- 2026-09-10: 官方 SDK candidate runtime 落地（pinax-mcp-official-sdk-v1 0/9→7/9）：`internal/mcpserver/sdkruntime` 基于官方 Go SDK v1.7.0（go.mod+vendor 已接入），注册目录/dispatch 全部经 `mcpserver.Server.Handle` 单点复用，`PINAX_MCP_RUNTIME=official-sdk` 显式开关、默认 runtime 不变。验证：进程级 e2e + 组件测试（并发/取消/preview→apply→status 恢复/race）、官方 TS SDK 1.30.0 strict client 双模式 protocol_errors=0、Inspector v2 三方法（evidence profile `mcp-official-sdk`/`mcp-inspector` 已归档）。冻结差异：unknown tool=-32602（legacy -32601）、无 server/discover、无顶层缓存字段、stdin EOF 即拆会话（EOF 归一化零退出）。另修复基线红：inputrequests 裸 sqlite.Open 迁移 sqlitedsn 统一 DSN + errcheck/goimports，task check 全绿，mcp-collaboration-v1 5/5 收口（quality evidence 20260910T184839Z-2036941）。遗留：4.2 真实四客户端会话验收、4.4 默认切换为外部门；本轮全部改动与前次 MCP wave 同树未提交（go.mod/vendor 与新文件不可拆分）。
# Pinax Project Memory
- 2026-09-01: Completed OpenSpec change `pinax-knowledge-source-adapter-v1` (6/6, not archived). Shipped `pinax knowledge export-projection`: default-empty allowlist yields an empty provider-neutral package with `empty_reason=allowlist_empty` and `read_note_bodies=0`; a note is included only when both the path allowlist and `knowledge_export: allow` marker match; entries carry refs/digest/permission/citation/freshness; incremental `--from-package` digest-diff emits tombstones for deleted allowlisted notes and omits unchanged digests; export writes only `--output` outside the vault. Focused CLI tests in `cmd/pinax/knowledge_command_test.go`; integration evidence `temp/integration-test-runs/20260901T165448Z-1132885/`; `go test ./... && go vet ./... && go build ./...` and `openspec validate pinax-knowledge-source-adapter-v1 --strict --no-interactive` passed.
- 2026-06-11: Clarified Pinax remote architecture: `pinax api serve` is centralized local REST/RPC access to one server-side vault, while Pinax Cloud Sync is intended as distributed local-first multi-device sync where every device keeps its own local vault and the Cloud backend coordinates encrypted blobs, manifest revisions, CAS commits, and conflicts.
- 2026-06-12: Cloud Sync server and direct transports are now wired: server uses `internal/cloudclient.Transport`, S3/file/rclone direct transports share `internal/cloudsync.ObjectStoreTransport`, and `remote_write=true` is valid after durable revision commit. Older docs/spec text that says server or rclone remain guarded/unwired is stale and should be corrected before release messaging.
- 2026-06-12: Fixed review findings under OpenSpec change `openspec/changes/pinax-project-review-fixes/`: Cloud Sync docs/specs now reflect wired server/file/S3/rclone transports; RPC `Pinax.Note.Read` defaults to bounded `display=card`; REST uses Projection-aware status mapping; server commit requests carry revision/blob/device metadata; unknown direct-store conditional capability now uses lock fallback; dead `internal/sync/executor.go` was removed. Verified with focused tests, CodeGraph, and `task check`.
- 2026-06-11: Added `internal/cloudclient` as the first CLI-side Cloud Sync contract slice. It defines Config/Client, health/current revision/blob batch-check/upload/download/revision commit methods, stable redacted `cloudclient.Error`, and `testdata/cloud_contract.json`. Verified with `go test ./internal/cloudclient -count=1` and `task check`.
- 2026-06-11: Expanded Cloud Sync design: Cloud Sync is now defined as a protocol with pluggable transports, not only a remote Pinax Cloud Server. Supported target design includes server transport, S3-compatible direct transport, rclone/OneDrive direct transport, and embedded Go API/local RPC, all sharing one sync engine and `remote_write=true` only after durable revision commit.
- 2026-06-11: Implemented Pinax Cloud Sync CLI-side code slice: `internal/cloudsync` protocol/transport abstractions, object-store direct transport over `remote.BlobStore`, server transport adapter over `internal/cloudclient`, `cloud backend set s3`, `cloud backend set rclone` configuration, direct file/S3 push and pull with encrypted envelopes, sync-state receipts, conflict copy preservation, and local RPC `Pinax.Sync.Push/Pull`. Verified with focused cloud tests and `task check`.
- 2026-06-11: Configured and smoke-tested local MinIO direct S3 sync. Bucket `pinax-cloud-ec-test` was created on endpoint `http://10.10.1.102:9010`; Pinax S3 backend now persists custom endpoint/region/path-style query options, pushed a note with `remote_write=true`, and pulled it into a second vault successfully. Do not store or echo the raw MinIO secret in docs/final output.
- 2026-06-11: Fixed Pinax Cloud Sync UX for configured MinIO/S3 direct vaults: `pinax sync init --vault <vault>` now reuses existing `.pinax/cloud/config.yaml` or legacy `.pinax/cloud/config.json` instead of requiring endpoint/workspace/device again; `--target` completion returns cloud/s3/git; `--vault` completion includes registered vault aliases plus local directories. Verified live with the configured MinIO vault and `task check`.
- 2026-06-11: Fixed Cloud Sync commands being hijacked by remote API mode. `cloud` and `sync` now stay local even when `remote.api_url` config or `PINAX_API_URL` is set, so configured S3 direct sync no longer returns `remote_command_unsupported` before reaching local sync logic. Verified with regression tests, installed `pinax`, and `task check`.
- 2026-06-11: Reconfigured current Pinax vault MinIO/S3 direct sync to use AWS SDK profile `ec` matching the user's `mc alias ec`. Current vault Cloud Sync config now lives at `.pinax/cloud/config.yaml` with `secret_ref=profile://ec`; S3 list verification succeeded with 4 objects under the prefix. Never echo the raw profile secret.
- 2026-06-11: Cloud Sync config is now YAML-primary. New writes use `.pinax/cloud/config.yaml`; S3 direct config is stored as structured `s3.bucket`, `s3.prefix`, `s3.endpoint`, `s3.region`, `s3.profile`, and `s3.path_style` fields instead of a URL-escaped endpoint string. Legacy `.pinax/cloud/config.json` is read-compatible only and removed on new cloud backend writes. Current vault is migrated to YAML using profile `ec`.
- 2026-06-12: Implemented rclone direct Cloud Sync transport slice in `internal/remote` and `internal/cloudsync`: `rclone://remote/prefix` now uses external `rclone` CLI (`cat`, `copyto`, `lsf`, `deletefile`) as a real `remote.BlobStore`, maps missing objects, redacts provider stderr, supports context cancellation, declares conditional writes unsupported, and relies on object-store lock fallback for durable commits. Focused verification: `go test ./internal/remote -count=1`, `go test ./internal/cloudsync -count=1`, and `go test ./internal/app -run 'RcloneDirectSync|Cloud|Sync' -count=1`.
- 2026-06-12: Completed and archived OpenSpec change `pinax-cloud-distributed-sync` as `openspec/changes/archive/2026-06-12-pinax-cloud-distributed-sync/`. Cloud Sync now has server, embedded/file, S3-compatible direct, and rclone direct transports sharing the same sync engine; server transport uses `internal/cloudclient.Transport`; no-op `dummyStore` paths were removed; sync conflicts/logs/status now use app-service projections; `pinax.sync_run.v1` receipts, current-only `.pinax/sync-state.json`, safe sync summary events, CAS/lock fallback, redaction gates, docs/specs, and backend Pinax Cloud HTTP handlers/tests are complete. Final verification observed: Pinax `task check`, backend `/workspaces/yeisme-agent/backend-server/pinax-cloud` `task check`, and `openspec validate --all` all passed.
- 2026-06-15: Completed OpenSpec change `pinax-agent-safe-proof-loop` (9/9 tasks). The proof loop connects existing capabilities into a reproducible agent-safe loop: Capture → Retrieve → Diagnose → Plan → Apply safely. Added `tests/e2e/testdata/proof_loop/scripts/{proof_loop,proof_loop_fixture,proof_loop_redaction}.txt`, `cmd/pinax/proof_loop_contract_test.go` (json/agent/events projection contracts with recursive body-leak scan), refactored integration evidence runner into `internal/testkit/evidence` package with `tests/e2e/integration_evidence_test.go` redaction/failure tests. Docs (`README.md`, `docs/README.md`, `docs/commands/README.md`) now lead with the five core workflows. The proof loop exposed two real bugs, both fixed: (1) `ensureOrganizePlanFresh` used unfiltered `scanNoteFacts` while `buildOrganizePlan` used `organizeCandidateFacts`, making every organize plan with daily notes stale — fixed by applying `organizeCandidateFacts` in the freshness check; (2) `note orphans` projected full `domain.Note` (including Body) instead of bounded summaries — fixed by routing through `noteGraphNoteSummary` in both `QueryOrphans` and the fallback. Plan→apply must be separate cycles because applying one plan changes vault facts for the next.
- 2026-06-15: Hardened `pinax-agent-safe-proof-loop` after review reopened tasks for shallow assertions. Fixture testscript now asserts concrete issue codes (`broken_link`/`missing_tags`/`orphan_note`) and concrete plan operations (`tags_patch`/`orphan_review`/`link_resolution`/`move`). Proof loop takes a FRESH snapshot before each apply, asserts apply receipt facts (`applied`/`plan_id`), events start/end, and durable snapshot evidence files. Contract test recursively walks JSON at every depth to reject `body`/`note_body`/`raw_body` fields and sentinel leaks across default/json/agent/events modes. Redaction testscript covers 7 surfaces (default summary, json, agent, events, stderr, saved plan files, snapshot evidence) and all forbidden classes. `internal/testkit/evidence.Redact` scrubs Authorization/Bearer/secret-KV/CLI-secret-flag/absolute-paths from stdout/stderr/argv/env; summary now carries `project=cli/pinax`, `layer=integration-evidence`, `finished_at`, and a `redaction` object. Evidence tests seed `EVIDENCE_*` markers into stdout/stderr/argv/path-like values and recursively scan every evidence file. Verified: `go test ./...` 29 packages, `task check` exit 0, `openspec validate --all --strict` 37/37, `task test:integration` with real redaction (`env.cwd=[REDACTED_PATH]/`).
- 2026-06-15: Completed OpenSpec change `pinax-gorm-gen-database-access` (9/9 tasks incl. review-added reconciliation). Migrated `internal/index` from direct GORM business queries to GORM Gen typed DAO. Architecture: 14 GORM model structs moved to `internal/index/model/records.go` (hand-written canonical source); generator `internal/index/gormgen/main.go` (package main, `go run ./internal/index/gormgen` or `task gen:index`) opens a temp SQLite, AutoMigrates models, and `ApplyBasic`-generates `internal/index/query` (imports `model` only — no import cycle). `internal/index` keeps public API stable via type aliases (`type NoteRecord = model.NoteRecord`), so the 7 external refs in `internal/app` are unchanged. All business queries (Diagnose/Search/Lookup/Rebuild/Refresh/Sync/UpdateNote/DeleteNote/projection rebuilds) now use `query.Use(db).X.WithContext(ctx)`; only `schema.go`'s `indexSchemaReadError` retains PRAGMA (allowlisted). `guard_test.go` (`TestNoDirectGormBusinessQueries` + `TestGuardRegexDetectsViolations`) bans `database/sql`, hardcoded SQL verbs, and `db./tx.`-prefixed GORM chains in business files. Had to upgrade `gorm.io/plugin/dbresolver` to v1.6.2 for gorm v1.31 compat (`gorm.Stmt` removed). gen field `Group`→`Group_` (reserved-word rename), local var `query` in Search renamed to `queryText` to avoid shadowing the import.
- 2026-06-15: Completed OpenSpec change `pinax-cloud-server-sync-client` (13/13 tasks incl. review-added order locks). Rewrote `internal/cloudclient` for the Pinax Cloud Sync MLP REST contract: `Config.VaultID` (was `WorkspaceID`), `/v1/vaults/{vault_id}/*` paths, new methods `Bootstrap`/`CurrentPrincipal`/`CreateVault`/`LinkVault`/`Changes`/`SignUpload`, uppercase stable error codes (`UNAUTHENTICATED`/`DEVICE_REVOKED`/`FORBIDDEN_SCOPE`/`REVISION_CONFLICT`/`VALIDATION_FAILED`/`BLOB_MISSING`/`BACKEND_UNAVAILABLE`), `IsRevisionConflict` helper. Added `internal/cloudclient/mlptest` — a faithful in-memory MLP HTTP server (bootstrap, vault create/link, changes cursor, batch-check, sign-upload, blob upload/download, CAS commit) used by all cloud tests. `Transport` adapter dropped the vaultID arg (`NewTransport(client)` uses `client.VaultID()`). Contract fixture upgraded to `pinax.cloud.contract.mlp.v1`/`terminology=vault_id`. `server_transport_test.go` proves two-device convergence + conflict (REVISION_CONFLICT, pull→rebase→commit) + backend-unavailable through real HTTP server transport, not direct file. Backend MLP (`backend-server/pinax-cloud`) is still 0/14; the CLI side is MLP-ready against the documented contract via the fake server. `cmd/pinax/main_test.go` fake server updated from `/revision`,`/blobs:batchCheck`,`/revisions:commit` to `/head`,`/blobs:batch-check`,`/revisions`. `--workspace` stays for CLI config compat but maps to `vault_id` at the REST boundary. `task test:integration` now runs `./internal/cloudclient` convergence/conflict/bootstrap tests with `server_sync=true`.
- 2026-06-15: Completed OpenSpec change `pinax-proof-loop-operational-hardening` (11/11 tasks). Three deliverables: (1) **Restore apply path** — `domain.RestorePlan` (pinax.restore_plan.v1) + `internal/app.VersionRestoreApply` + `pinax version restore apply --yes --plan <id>`. Version backends (local/git) don't implement `ReadFile`, so restore uses git checkout: `VersionRestorePlan` records `gitstore.HeadCommit` + recursive `versionVaultHash`; `VersionRestoreApply` verifies vault hash (refuses stale plans via `restore_plan_stale`) then `gitstore.RestorePathFromCommit` reverts the file, writes `.pinax/receipts/restore-*.json` with `local_write=true`/`remote_write=false`. ReadFile is best-effort now; plan generation fails only when neither git history nor ReadFile is available (`version_read_unavailable`). (2) **Shared projection redaction gate** — `internal/output/projection_redaction.go` `ApplyProjectionRedaction` runs in `RenderWithOptions` before ALL modes (default/json/agent/events/explain). Redacts Authorization/Bearer/KV-secrets/webhooks/raw_prompts via regex + `sensitiveFieldNames` (api_key/authorization/cookie/raw_prompt/etc.) full-value replacement. IMPORTANT: does NOT clear body/content fields globally — note preview/show legitimately render content; body-leak is controlled by bounded projections + the existing `assertNoRecursiveBodyLeak` contract test. (3) **`pinax proof loop run`** — `internal/app/proof_loop.go` `ProofLoopRun` orchestrates VaultStats/VaultDoctor/PlanRepair/buildOrganizePlan/GitSnapshot/ApplyRepair/ApplyOrganize (no shell-out), emits one projection with `proof_loop_run_id` + stage facts; preview default, `--apply --yes` takes fresh snapshot then applies (manual-review ops auto-skip). Registered in `internal/cli/proof_cmd.go`. All four OpenSpec changes now Complete; `task check` exit 0, `openspec validate --all --strict` 39/39.
- 2026-06-16: Archived all 4 completed OpenSpec changes to `openspec/changes/archive/2026-06-16-*` and synced delta specs into main specs. Before archiving: fixed stale `pinax-cloud-server-sync-client` task 0.1 evidence (had claimed `pinax-proof-loop-operational-hardening` did not exist; updated to reflect it is Complete with restore apply / redaction gate / proof-loop run id / output contracts verified), and restructured that change's delta spec (`pinax-cloud-sync`) so the new "Server sync client SHALL wait for local proof-loop safety gates" requirement is under `## ADDED Requirements` instead of `## MODIFIED` (it had no matching main-spec header, which blocked the archive). Archive order: pinax-agent-safe-proof-loop → pinax-proof-loop-operational-hardening → pinax-gorm-gen-database-access → pinax-cloud-server-sync-client. After archiving: `openspec list` shows no active changes, `openspec validate --all --strict` 36/0, `task check` exit 0. Main specs now carry the GORM Gen, proof-loop hardening, and cloud-sync MLP client requirements.





- 2026-06-16: Reviewed completed Pinax OpenSpec state. Current active completed changes are `pinax-agent-safe-proof-loop`, `pinax-proof-loop-operational-hardening`, `pinax-gorm-gen-database-access`, and `pinax-cloud-server-sync-client`; `openspec validate --all --strict`, `task check`, and `task test:integration` passed with integration evidence `temp/integration-test-runs/20260616T012249Z-2985583/`. Review note: `pinax-cloud-server-sync-client` task 0.1 still records a now-stale exception saying `pinax-proof-loop-operational-hardening` did not exist; update that evidence before archiving.

- 2026-06-16: Created active OpenSpec design `pinax-release-packaging-distribution` for multi-channel Pinax releases. Scope: keep `pinax/vX.Y.Z` tags, extend GoReleaser for source archives, SHA-256 checksums, SBOMs, Homebrew cask (`yeisme/homebrew-tap`), Scoop manifest (`yeisme/scoop-bucket`), nFPM `.deb/.rpm/.apk` assets, guarded Chocolatey generation, least-privilege release publisher tokens, local package validation, and post-release install smoke. Validation observed: `openspec validate pinax-release-packaging-distribution --strict` and `openspec validate --all --strict` passed in `cli/pinax`.

- 2026-07-06: Refactored `internal/app` to decompose the 11462-line `service.go` god file into 12 cohesive topic files (`requests.go`, `service_vault.go`, `service_note.go`, `service_backend.go`, `service_index.go`, `service_asset.go`, `service_version.go`, `service_template.go`, `service_plan.go`, `service_sync_briefing.go`, `service_prompt.go`, `service_journal.go`). `service.go` now 4573 lines (60% reduction). Same-package file split — zero API/call-site/test changes. Fixed a real publish doc migration guard bug: `validatePublishDocPackageForProfile` (renderer_mismatch) was checked before the migration guard, so markdown-file→native-docx switch never returned `publish_object_type_migration_required`; extracted `publishDocMigrationGuard` and moved it before package validation and dry-run. `task check`, `openspec validate --all` (55/55), `golangci-lint unused` (0 issues) all green. Known pre-existing flaky test `TestObjectStoreTransportLockFallbackRejectsConcurrentFirstHeadCreation` in `internal/cloudsync` (race-clean, fails ~5% under load; unrelated to app refactoring).

- 2026-07-09: Reverted `stableNoteID` from UUID v7 back to deterministic SHA1(path). The UUID v7 change (commit 6573eef) broke idempotency: `stableNoteID` is called multiple times per note lifecycle (frontmatter, record event, projection, daily index) and each call produced a different random ID, causing record ledger duplication, checklist task ID mismatch, and `task adopt` failures. `checklistTaskID` in `project_board.go` was also fixed to use its own deterministic SHA1 hash instead of delegating to `stableNoteID`. Rename-safety is already handled by the `note_id` field in frontmatter (immutable across renames). Also archived both active OpenSpec changes: `pinax-cloud-sync-auto-merge` (6/6 tasks, three-way sync + LOCAL_UNPUSHED_CHANGES + soft delete markers) and `pinax-cli-sync-unification` (8/8 tasks, removed cloud/git snapshot/storage Hidden/backend sync commands, unified to sync+capsa+storage+backend blob). Removed dead code: `backendPlanCommand` helper and `backendDryRun` field (orphaned after backend push/pull/diff removal). Verification: `task check` all green, `openspec validate --all --strict` 54/54 passed, zero active changes remaining.

- 2026-07-18: Completed OpenSpec change `pinax-runtime-secret-env-loader` (16/16 tasks). Implemented the encrypted runtime dotenv loader: `internal/remote/env_asset.go` (strict `ParseDotenv`/`FormatDotenv`, `EnvAsset` envelope `pinax.sync.env.v1` at fixed `.pinax/pinax-sync.env.age`, `EnvDocumentDigest`), `internal/remote/env_snapshot.go` (immutable `EnvSnapshot`, `EnvReloader` with content-digest identity + atomic swap + fail-safe retention, `MaterializeEnv`/`CleanMaterializedEnv` at fixed `.pinax/runtime/pinax-sync.env` 0600 + symlink rejection), `internal/app/sync_env.go` (SyncEnv init/set/list/unlock/clean/doctor with `detectTrackedSecretEnv` git ls-files warning), `internal/cli/sync_env_cmd.go` (additive `pinax sync env` tree). `internal/vaultignore/vaultignore.go` extended: `hardDenied()` now hard-denies plaintext env paths (`isPlaintextEnvPath`) so Capsa content manifest never uploads `.env`/`*.env` regardless of user re-include; added `EnvSecretGitignoreBlock`/`ApplyEnvSecretGitignore` managed marker block (refactored `ApplyMetadataOnlyGitignore` to shared `applyManagedBlock`). Daemon `syncDaemonRunCycle` calls `EnvReloader.RunSnapshot()` at each run boundary, emitting `sync_env_reloaded`/`sync_env_reload_failed`. Strict parser accepts KEY=value/quoted only, rejects `$()`, backticks, `${}`, include/source/export, NUL/control, duplicate/empty keys; errors carry line+key+code, never the value. Precedence: explicit flags > explicit process env > snapshot > config > defaults; child processes only receive allowlisted keys. Verification: `go test ./...` 53 PASS/0 FAIL, race-clean, `CGO_ENABLED=0 go build` PASS, `go vet`/`golangci-lint`/`unused` clean, `task check` 60/0, `openspec validate --all` 60/0. E2e testscript `tests/e2e/testdata/sync_env/scripts/sync_env_lifecycle.txt` covers full lifecycle with redaction checks + wrong-key fail-closed + gitignore grep. The other two active changes (`pinax-agent-memory-runtime` 41/43, `pinax-agent-continuity-experience` 32/39) are blocked on real multi-week dogfood/CEO product-decision evidence that cannot be fabricated; their code-complete portions are done and capability stays experimental.

- 2026-08-21: Review pass + `pinax-dsh-pane-contract` OpenSpec change created for the DSH Pinax Pane owner slice (`internal/app/pane.go`, tracked by root `dsh-pane-plugin-ecosystem-v1` + harness-plugins `dsh-pinax-pane-v1`). Review fixes landed: (1) `AssemblePaneSnapshot` status mapping now includes `failed` — `NewErrorProjection` reports Status `failed` (not `error`), so failed note.list projections previously produced `ready`+empty entities; regression test added. (2) Envelope `occurredAt`/`observedAt` now real UTC time (fixed placeholder `2026-08-21T00:00:00Z`); harness-side `snapshot.ts` still uses the placeholder for Host tests — do not copy it back. (3) ineffassign + gofmt fixed in pane/testkit. (4) Crash-safe note overwrites: `cloud_sync.go` pull materialization + conflict copies, `project_board.go` patchProjectItemNote, `service_note.go` import markdown, `service_vault.go` note frontmatter patch helper, `identity_migration.go` note_id rewrite, `brain_maintenance.go` plan save all switched from `os.WriteFile` to `atomicWriteFile` (temp+fsync+rename). New-note creation paths (`CreateNote`, `ProjectItemAdd`) still use plain writes — lower risk (no prior content destroyed), unify when touching them next. `paneUnsafe` is a denylist (token/authorization/cookie + path shapes), does not cover secret/password/api_key; `paneNotes` only accepts in-process `[]domain.Note` — a JSON round-tripped projection silently yields empty entities. Docs: `docs/interfaces/dsh-pane.md` + both README maps.

- 2026-08-22: Review follow-up executed. (1) `pinax-dsh-pane-contract` change created (pane.go owner slice; failed-projection→offline mapping fix, real timestamps, allowlist+denylist ref red line, fail-closed notes shape; task 7 archive review pending harness consumption). (2) All note write paths now `atomicWriteFile`. (3) Real-S3 dual-device regression `TestSyncOutputRealDualDeviceRegression` added (env-gated via `PINAX_SYNC_REAL_ENDPOINT`, run through `syncrealevidence`); `pinax.sync.output.v1` golden tests in `sync_output_view_test.go`; `cloud_direct_two_device.txt` carries sync_view assertions. (4) Discovered+fixed manifest v2 conflict-copy identity bug under `pinax-manifest-v2-conflict-copy-identity`: conflict copies duplicate canonical note_id and permanently blocked v2 pushes; now local-only (`remote.IsConflictCopyPath`). sync-output-ux task 6 complete (S3 evidence `temp/integration-test-runs/20260822T094645Z-1210789`); COS virtual-hosted addressing untested. MinIO test bucket deleted after evidence; creds live only in `~/.config/pinax-e2e/s3env` (0600).

- 2026-08-23: Archived `pinax-manifest-v2-conflict-copy-identity` and `pinax-sync-output-ux` (task 6/7 closed with real-S3 evidence + compat review; specs synced). Created and fully implemented `pinax-sync-pull-fast-forward`: planner marks blob/revision-proven untouched-local downloads with `FastForward`+`LocalBlobID` (additive op fields); apply unifies preserveConflict with the move-branch rule plus a content-hash TOCTOU guard; converged pull now reports `result=up_to_date` (facts/sync_view aligned with push). Sequential edits across devices no longer leave noise `*.conflict.md` copies; both-diverged still yields exactly one preserved copy. Key planner facts: v2 object-keyed branch (`diffObjectManifests`) classifies both-diverged as `revision_conflict`; v1 path-keyed branch (`diffManifests`) was already blob-precise — the noise came from apply ignoring op semantics. Real-S3 evidence `temp/integration-test-runs/20260823T135145Z-1082619`. Also scaffolded `pinax-dsh-pane-projections-v2` (backlink/graph/history owner surfaces, 0/7, harness-gated archive). Remaining user-gated: memory/continuity dogfood, passphrase 7.4 Mac dogfood; COS virtual-hosted addressing evidence.

- 2026-08-23 (b): Archived `pinax-sync-pull-fast-forward` (+2 specs synced). Implemented `pinax-dsh-pane-projections-v2` tasks 1-6: three bounded owner surfaces in `internal/app/pane_projections.go` — `AssemblePaneBacklinks` (≤50 source entities from note.backlinks projection, title ≤160, ok/broken), `AssemblePaneGraphSummary` (nodes/edges/components totals via union-find + deterministic top-20 degree entities, no paths/titles), `AssemblePaneHistory` (≤100 record-ledger events into `payload.timeline` with op/ref/revision/time). All reuse `paneUnsafe`, share the envelope (now with timeline slot), expose `dropped_unsafe`+`truncated`; truncation scans the full input for unsafe drops instead of breaking early (bug caught by test). Round-tripped shapes fail closed. Evidence `temp/integration-test-runs/20260823T150153Z-2265383`; docs/interfaces/dsh-pane.md updated. Task 7 (harness cross-check) remains the archive gate.

- 2026-08-23 (c): Closed the pane archive gates — harness `dsh-pinax-pane-v1` archived/landed same day; cross-check passed (its `packages/client/ui-pane-domain/src/pinax.ts` is a faithful TS port: identical allowlist regex + 7 denylist substrings, failed/error/offline→offline, same gated actions/owner commands, handwritten-metadata fail closed; harness 66/66 green, evidence dsh-pinax-pane-v1-20260823T081658Z-3785522). Archived `pinax-dsh-pane-contract` + `pinax-dsh-pane-projections-v2`. Committed my review/session work as three commits on develop: 4d12a33 fix(sync) (atomic writes + v2 conflict copies local-only + pull fast-forward + golden/real-S3 evidence + 3 archived sync changes), e0f05ea feat(pane) (4 owner surfaces + evidence profile + docs + 2 archived pane changes), 0f0c297 chore(openspec) (staged sync-output-ux source-tree removal left by the archive move). Concurrent session's in-flight files (skills/agentadapter/agentmemory/prompt-import/osv) deliberately NOT committed. Remaining active: prompt-repository-import (other session), osv (other session, complete), memory/passphrase/continuity (user-gated dogfood). Not pushed.
- 2026-09-03: 四个 active change 门控如实标注（无归档、无伪造）：`pinax-trusted-continuity-dogfood-v1` 8.2-8.4、`pinax-agent-memory-runtime` 8.5/8.6、`pinax-passphrase-s3-bootstrap` 7.5-7.9、`pinax-agent-continuity-iteration-adoption-signal` 4.1-4.4 全部标注 `[external-gate skipped]`（真实 dogfood/日历 2026-09-05/真实 macOS+COS/发布决策门控）。只读取证：真实 yeisme-notes vault `continue report --since 6w --json` total_runs=1（want ≥30）、runtime 仅 codex、class 仅 release_operations、go_ready=false、binding `bdabffe4d9b88a90` 仍 ready；canary 窗口 2026-08-23→09-05 week1（已结束）与 week2 captures=0、recorder self-test PASS；`agent status --json`/`agent memory proposals --json`（count=0）只读正常。四 change `openspec validate --strict --no-interactive` 全过。注意：任务简报中 dogfood-v1 "3.2 rollback smoke" 已过时——其 3.2 是 Resume Card 渲染（早已勾选）；rollback smoke 是 adoption-signal 3.2，2026-08-23 已在隔离 profile 完成勾选，无本地遗留。
- 2026-09-06: Completed OpenSpec change `pinax-share-explore-v1` (6/6 tasks) on branch `wt/share-explore` (base develop a62af0b, 4 commits 427199b..bd5dfa4, not pushed, not archived). Shipped `pinax share start --view explore` (`--scope vault-readonly` gated, `--view` default published = zero-change) with `/explore` self-contained HTML page (go:embed template in `internal/app/explore_page.html`, double-JSON-escaped inline data, zero external src=/href= enforced by `ScanExploreExternalRefs` which strips script/style BODIES but keeps their open-tag attrs so `<script src=//cdn>` is still caught), `/explore/data.json` (`pinax.explore_bundle.v1`: nodes≤5000/edges≤20000/title≤160/summary≤240/tags≤8, truncated flag, broken edges to bounded raw target), and `/explore/note/<id>` bounded preview (ApplyProjectionRedaction + 2048-byte cap; summary only from frontmatter summary/description, never body-derived). Browser navigation auth: shareAuthHandler now also accepts `?token=` query param (page JS converts it to Bearer header); token never printed in web_url/facts. Trust/fresh derived locally from flattened frontmatter (`parseExploreOKFSignals` in `internal/app/explore.go`, comment marks merge with okf-trust-discovery domain helper). Gotchas: (1) e2e publish scripts need `bun install` in `web/pinax-web-renderer` inside fresh worktrees (node_modules gitignored, `PINAX_WEB_RENDERER_DIR` points at repoRoot copy); (2) `TestPromptCatalogRepositoryImportE2E` was deterministically red at develop HEAD because e7b9eb0 changed DefaultLocale zh-CN→en without updating the assertion — realigned to expect `en` (commit 172c674); (3) force layout in page is capped: >600 visible nodes fall back to deterministic ring layout, layout cached per filter signature so selection doesn't recompute; (4) empty-vault explore `--once` fails closed by design ("explore smoke skipped note preview"). Verified: per-task validations, `task test:integration` exit 0, `task check` exit 0, `openspec validate pinax-share-explore-v1 --strict` clean, inline page JS bun syntax-checked.
- 2026-09-06 (b): 三个 OKF 对标 change 由并行 subagent 全量实现并合并入 develop（提交 1c1c62a/da4e791/09af570/89971e1，未推送）：`pinax-okf-trust-discovery-v1` 12/12（trust.go YAML 节点级 patch/note verify 幂等/identity 配置键/facets/search show/browse/brain 信任标注）、`pinax-share-explore-v1` 6/6（explore bundle + go:embed 自包含页面/零外联扫描器双正则拆分/环形布局兜底/`?token=` 查询参数认证）、`pinax-pipeline-unified-ux-v1` 8/8（pinax.plan.v1 读模型/pipeline status+show/四管道 freshness 守卫+--allow-stale/pipeline_stage tracker；metadata 新增 --save/--plan 与 pinax.metadata_plan.v1）。合并缝两处主会话修复：saved-plan 的 trust_fields 操作消费（PlanOperation.Target 携带 stale_after，TestMetadataSavedPlanAppliesTrustFields）与 explore 信任派出去重到 domain.TrustTierOf/FreshnessOf。三 agent 独立发现基线预存红灯 TestPromptCatalogRepositoryImportE2E（en locale 断言漂移）并各自修复，合并取其一。`task check` 87/87 + `task test:integration` 全绿（证据 20260906T112009Z-504980）。worktree 三分支已清理。注意：新 worktree 需在 web/pinax-web-renderer 跑 bun install 否则 publish e2e 环境性失败；Go 测试 t.Setenv 与 t.Parallel 不能同用。
- 2026-09-06 (c): 五 change 归档（OKF 三件套 + en-prompt + promptrepo v0.5，promptrepo delta MODIFIED→ADDED 改写后归档，88ee72e）+ `pinax-workbench-continuity-projection-v1` 3/3 完成（9dd6ab0）：typed facade `pinax continue workbench <projectRef>`（envelope 合同 digest/exact-by-id projectRef/有界 resume card/evidence-observed TTL refresh-only）+ `--packet` provider packet；binding_id 只在 `continue bind --json` 的 data 载荷（facts 只有 16 位 digest）；留 Complete 未归档待根仓消费 verdict。Go 结构体 omitempty 不省零值结构——指针化才 omit。task check 88/88 + integration 20260906T142011Z。并行会话同窗合入 summary-list-bound-v1。
- 2026-09-06 (e): action-capture canary 负结果收口（9e31c04 已推 origin/develop 6784658..9e31c04，共 31 提交）：窗口 08-23→09-05 结束、两周期 captures=0（recorder 在 ~/.hermes/profiles/personal/skills/productivity/pinax-action-capture/scripts/，状态 ~/.hermes/cache/pinax-action-canary/state.json），按 4.4 预登记规则出唯一 NOT GO receipt（decision-receipt-2026-09-06.md：保持 experimental/不建接口固化/停止扩张）；adoption-signal 13/15（4.1/4.2 永久未勾作诚实负记录）、dogfood 28/30（8.3 需 ≥30 真实 loops 至 10-10、8.4 决策依 8.3）。passphrase 7.5-7.9（真 macOS+COS）与 memory-runtime 8.5/8.6（四周双端 dogfood）确证无法本地推进。workbench continuity-desktop（10/29，1h 前活跃）归并行会话；根程序 1.3 属授权决策道待用户。
