# pinax-web-renderer

Static publish renderer for Pinax export bundles. This package generates public static HTML output for `pinax publish build`; it is not an internal workbench or standalone app page.

```bash
bun install
bun run test
bun run build
bun run render:static
```
